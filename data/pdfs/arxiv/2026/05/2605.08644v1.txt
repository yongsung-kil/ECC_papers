arXiv:2605.08644v1 [cs.IT] 9 May 2026

On Codes with Support-Constrained Parity Checks

Barron Han Computing and Mathematical Sciences
California Institute of Technology Pasadena, CA, USA bshan@caltech.edu

Hikmet Yildiz Independent Researcher
Pasadena, CA, USA hhikmetyildiz@gmail.com

Babak Hassibi Electrical Engineering California Institute of Technology Pasadena, CA, USA hassibi@caltech.edu

Abstract
We study linear codes that maximize minimum distance subject to arbitrary support constraints on the parity-check matrix. Such constraints arise naturally in the design of LDPC codes, locally repairable codes, and hardware-constrained systems where each parity check must involve only a limited number of code symbols. They are also essential in quantum error correction, where sparse stabilizers reduce measurement noise and respect the connectivity constraints of physical qubit architectures. We derive the optimal minimum distance possible given support constraints on the parity-check matrix and show it is achievable over sufficiently large fields. When this maximum distance coincides with the Singleton bound for unconstrained parity check matrices, the dual GM-MDS construction yields generalized Reed–Solomon codes obeying the mask. In the generator-matrix setting, the GM-MDS theorem guarantees that the optimal distance can always be achieved by a subcode of a generalized Reed–Solomon code while satisfying arbitrary support constraints. We show that this is not true for the parity-check setting. We exhibit a set of support constraints, derived from the vertex-edge incidence of K6,6, for which the optimal minimum distance cannot be realized by any subcode of a generalized Reed– Solomon code over any field. We also analyze structured constraint families—regular, balanced, and cyclic masks—through numerical optimization, providing design guidance for practical code constructions.
1 Introduction
The design of linear codes with large minimum distance under sparsity constraints is a well-studied problem. Support constraints on the generator matrix are particularly relevant for distributed storage systems and multiple access networks [1,2], as they restrict code symbols to linear combinations of message symbols from accessible nodes only. This leads to a fundamental question: given a prescribed set of support constraints, what is the maximum achievable minimum distance? Furthermore, can this optimum be achieved using structured codes over smaller fields? The GM-MDS theorem, conjectured in [3] and proven in [4, 5], addresses this by establishing that for any support constraint on the generator matrix of a length n code, the optimal minimum distance, d, is achievable by a subcode of a generalized Reed–Solomon code with field size q ≥ 2n − d.

1

This paper studies the dual problem of optimal minimum distance codes under sparsity constraints on the parity check matrix, which effectively limit each parity check equation to a linear combination of a prescribed set of code symbols. This framework is driven by two primary motivations. First, it models scenarios where parity checks must “sparsely” measure code symbols, such as limiting the frequency with which a specific symbol is accessed (column weight) or restricting the maximum number of terms per check equation to w (row weight). Second, arbitrary sparsity patterns can be used to enforce topological design constraints. For instance, in physical circuit implementations, restricting bit interactions to local “neighbors” eliminates the need for additional long-range connections, thereby reducing hardware complexity.
In the classical setting, parity check sparsity is foundational to the theory of Low-Density ParityCheck (LDPC) codes [6]. The sparse structure of the parity check matrix enables iterative messagepassing decoders whose block complexity is linear in the number of edges per iteration, in contrast to the exponential complexity of maximum-likelihood decoding for general linear codes. The capacityapproaching performance of irregular LDPC codes [7], combined with their hardware-friendly decoding, has made them the channel codes of choice in modern communication standards: the 5G New Radio specification [8] mandates quasi-cyclic LDPC codes with structured row and column weight constraints that are dictated by decoder architecture. More broadly, the study of codes with parity check sparsity constraints—including expander codes [9] and spatially coupled LDPC codes—reveals a fundamental tension between sparsity, minimum distance, and decoding complexity. Sparse parity checks reduce hardware cost and decoding latency, but a row weight of w limits the minimum distance of any single check equation, and careful global design is needed to ensure that the code’s minimum distance remains large.
A complementary motivation arises from distributed storage. In locally repairable codes (LRCs) [10], each code symbol must be recoverable from a small local group, which amounts to requiring low-weight parity checks supported within prescribed locality sets. Partial MDS codes [11] further partition checks into local and global groups, imposing a coupled block diagonal sparsity pattern on the parity check matrix. In each of these settings, the sparsity pattern is dictated by the system architecture and cannot be freely chosen. The question we ask—what is the optimal minimum distance achievable under a given parity check support constraint? —therefore subsumes these families as special cases.
These motivations extend naturally to quantum error correction. In the Calderbank-Shor-Steane (CSS) construction [12, 13], the nonzero entries of the classical parity check matrices become stabilizer measurements on qubits. Dense parity check matrices therefore require high-weight stabilizers that couple many qubits, exacerbating noise and violating the connectivity constraints of physical architectures [14, 15]. Sparse parity check matrices are thus essential for practical fault-tolerant quantum computing, as they enable low-weight, local stabilizer measurements.
While quantum analogs of LDPC [16], Reed–Solomon [17], and Tamo–Barg [18, 19] codes can achieve linearly scaling distance and dimension, implementing them often requires long-range connections or dense parity checks. In practice, topological c√odes such as surface codes [20, 21] use only local connections but achieve distance scaling only as n. Designing codes with near-optimal distance, efficient decoding, and hardware-compatible sparse parity checks remains an important open problem towards fault tolerant quantum computation.
1.1 Outline
In Section 2, we formalize the sparsity-constrained parity-check setting, derive the optimal minimum distance from the support structure of the mask, and show that this distance is achievable over sufficiently large fields. Section 3 treats the MDS regime: when the derived bound coincides with the
2

Singleton bound, the dual GM-MDS construction yields a generalized Reed–Solomon code obeying the mask. Section 4 introduces a factorization of a Vandermonde matrix that certifies when a sparse parity-check code can be realized as a subcode of a generalized Reed–Solomon code. Section 5 demonstrates the limits of this approach: we exhibit sparsity constraints arising from the vertex-edge incidence of the complete bipartite graph K6,6 for which no subcode of a generalized Reed–Solomon code achieves the optimal distance over any field. Finally, Section 6 studies structured constraint families—regular, balanced, and cyclic—and presents numerical optimization results.

1.2 Notation
For n ≥ 0, [n] := {1, . . . , n}. Given sets S1, . . . , Sn and R ⊂ [n], we write S(R) := j∈R Sj. An [n, k, d]q code is a linear code over Fq with length n, dimension k, and minimum distance d.

2 Sparse Parity-Check Masks and Exact Dimension

Let Fq be a finite field. We use m parity-check equations; the intended dimension is n − m, achieved when rank H = m. A linear code C ⊆ Fnq can be specified as the kernel of a parity-check matrix H ∈ Fmq ×n. We adopt the convention dmin({0}) = n + 1. We impose sparsity via a binary mask

M ∈ {0, 1}m×n,

Mij = 0 =⇒ Hij = 0,

(1)

Mij = 1 =⇒ Hij is free.

For each column j ∈ [n], define Sj := {i ∈ [m] : Mij = 1}, and for R ⊆ [n], define S(R) := j∈R Sj .
Definition 1 (Structural row rank).

ρ(M ) := max{rank(H) : Hij = 0 whenever Mij = 0}.

(2)

Let GM be the bipartite graph with left vertices the rows, right vertices the columns, and edges at the free positions of M . By Hall’s theorem [22], ρ(M ) = ν(GM ), the maximum matching size in GM . In particular, ρ(M ) = m if and only if |N (U )| ≥ |U | for every U ⊆ [m], where

N (U ) := {j ∈ [n] : ∃ i ∈ U, Mij = 1}.

(3)

Definition 2 (Structural Kruskal rank).

τ (M ) = max{s ∈ {0, 1, . . . , n} : ∀R ⊆ [n], (4)
|R| ≤ s =⇒ |S(R)| ≥ |R|},

The case ρ(M ) = m is the full-structural-row-rank regime: the mask can support m independent parity-check equations, so a filling with rank m defines a code of the intended dimension n − m.
While the structural row rank determines whether the mask can support a code of the intended dimension n − m, the structural Kruskal rank controls the best achievable minimum distance. Together, these quantities characterize the achievable parameters:

Theorem 1 (Minimum distance of sparse parity check). Let M ∈ {0, 1}m×n with column supports

Sj. Define

dm⋆ in(M ) := τ (M ) + 1,

n

∆(M ) := ρ(M ) + τ (M )

.

τ (M )

3

Then every filling H obeying M over any field satisfies kr(H) ≤ τ (M ), hence dmin(ker H) ≤ dm⋆ in(M ). Moreover, for every prime power q > ∆(M ), there exists a filling H ∈ Fqm×n obeying M with rank(H) = ρ(M ) and dmin(ker H) = dm⋆ in(M ).
In particular, if ρ(M ) = m < n, then ker H is an [n, n−m, d⋆min(M )]q code obeying M .

Proof. Write τ = τ (M ) and ρ = ρ(M ). We use the identity dmin(ker H) = 1 + kr H, where kr H is

the largest r such that every set of at most r columns of H is linearly independent.

Upper bound. If τ < n, maximality of τ in (4) gives R0 ⊆ [n] with |R0| = τ +1 and |S(R0)| < |R0|. For any H obeying M , the columns H:,R0 are supported on S(R0), so rank(H:,R0) ≤ |S(R0)| < |R0| and thus kr H ≤ τ . If τ = n, the bound dmin(ker H) ≤ n + 1 is immediate from the convention

dmin({0}) = n + 1. Achievability. The cases τ = 0 and τ = n are immediate. For 0 < τ < n, let X be the symbolic

matrix with an independent variable xij at each allowed position.

For each R ⊆ [n] with |R| = τ , the expansion hypothesis gives |S(J)| ≥ |J| for all J ⊆ R, so Hall’s

theorem yields a matching of R into S(R). Let IR be the matched rows and set pR := det XIR,R. The matching contributes a monomial j∈R xµ(j),j that no other permutation can produce (the variables are indexed by distinct positions), so pR is nonzero with deg pR = τ .
Similarly, since ρ(M ) = ν(GM ), choose a maximum matching of size ρ in GM . The corresponding

minor p0 := det XI0,J0 is nonzero with deg p0 = ρ.

Form P := p0

|R|=τ pR.

This

is

nonzero

with

deg P

= ρ+τ

n τ

= ∆(M ). Evaluate the free

entries of X independently and uniformly over Fq. By the Schwartz–Zippel lemma,

∆(M )

Pr[P (H) = 0] ≤

<1

q

for q > ∆(M ).

Thus some evaluation H has P (H) ≠ 0.
Since p0(H) ̸= 0, we get rank(H) ≥ ρ; by definition of ρ(M ), equality holds. Since pR(H) ≠ 0 for every τ -subset R, every such subset is independent; since any smaller subset extends to a τ -subset
and a subset of an independent set is independent, kr H ≥ τ . The upper bound gives kr H = τ . Therefore dmin(ker H) = τ + 1 = d⋆min(M ) and dim ker H = n − ρ(M ), which equals n − m when ρ(M ) = m.

Theorem 1 is a generic existence result: it identifies the best distance permitted by the support pattern alone but does not provide an efficient decoder or a small-field algebraic construction. The remaining question is whether this generic optimum can be realized as a subcode of a structured parent family—especially a generalized Reed–Solomon code—over smaller fields.

3 The MDS Regime and the Dual GM-MDS Construction

For the existence of an MDS code, a necessary condition is that the expansion condition of Theorem 1 holds through size m:

|R| ≤ m =⇒ |S(R)| ≥ |R| for every R ⊆ [n].

(5)

Proposition 2 (Sparse parity-check MDS construction). Let M ∈ {0, 1}m×n satisfy (5). Then, for every prime power q ≥ n + m − 1, there exists a generalized Reed–Solomon [n, n−m]q code whose parity-check matrix obeys M .

4

Proof. Regard M as a generator support mask for an [n, m] code: the column-expansion condition |S(R)| ≥ |R| for all R ⊆ [n] with |R| ≤ m is exactly the GM-MDS zero-pattern condition on the m × n generator matrix, since the allowed zero positions in each column of the generator are the complement of Sj. The GM-MDS theorem [4, 5] then yields a generalized Reed–Solomon [n, m, n−m+1]q code with generator obeying M for q ≥ n + m − 1. Its dual is again generalized Reed–Solomon, with parity-check matrix obeying M .
When (5) holds, the GM-MDS theorem provides a recipe: construct a generalized Reed–Solomon generator obeying M and take its dual. However, when dm⋆ in(M ) < m + 1, the GM-MDS theorem does not directly yield generalized Reed–Solomon constructions satisfying the parity-check constraints. A natural question is whether the sparse code can still be realized as a subcode of a generalized Reed–Solomon code; the next section formalizes this.
4 Vandermonde Certificates
Theorem 1 guarantees that a filling of the mask achieving the optimal minimum distance exists over sufficiently large fields, but the proof is non-constructive and relies on an exponentially large in n field size. In the generator-matrix setting, the GM-MDS theorem [4, 5] shows that the optimal distance can be achieved by subcodes of generalized Reed–Solomon codes under support constraints. Subcodes of a well-understood parent family are desirable because they inherit both the minimum distance guarantee and the efficient decoding algorithms of the parent code. This motivates the following question:
Can a sparse parity-check code always be realized as a subcode of a generalized Reed–Solomon code?
Definition 3 (Full-rank Vandermonde certificate). Fix a target distance d and set r = d − 1. A mask M admits a full-rank generalized Vandermonde certificate of distance d over Fq if there exist matrices H ∈ Fmq ×n and A ∈ Fqr×m such that H obeys M , rank(H) = m, rank(A) = r, and AH = V for some Vandermonde matrix V ∈ Frq×n with distinct evaluation points.
Remark 1. A generalized Reed–Solomon parity-check matrix has nonzero column multipliers; these can be absorbed into H by rescaling columns without changing the support pattern, so V may be taken to be a plain Vandermonde matrix without loss of generality.
Lemma 3 (Certificate equivalence). Let H ∈ Fm×n have full row rank and let V ∈ Fr×n. Then ker(H) ⊆ ker(V ) if and only if there exists A ∈ Fr×m with V = AH. If rank(V ) = r, then rank(A) = r.
Proof. If V = AH and Hc = 0, then V c = A(Hc) = 0. Conversely, ker(H) ⊆ ker(V ) implies rowsp(V ) ⊆ rowsp(H) by nullspace–rowspace duality, so V = AH for some A.
5 A Full-Rank Mask with No Vandermonde Certificate
We answer the question posed in Section 4 in the negative. We exhibit a mask that admits no generalized Vandermonde certificate (Definition 3) of the optimal distance given by Theorem 1 over any field.
Let m = 12, n = 36, r = 5, d = 6. Index the rows by L1, . . . , L6, R1, . . . , R6 and the columns by ordered pairs cij for 1 ≤ i, j ≤ 6. Define column cij to be allowed exactly in rows Li and Rj:
Sij = {Li, Rj}.
5

Equivalently, M is the vertex-edge incidence matrix of K6,6, the complete bipartite graph with 6 left and right nodes. With columns grouped by left vertex,

 1⊤6 0 · · ·

 0 16⊤ · · ·



MK6,6

=

 

...

... . . .

0

0

...

 , 

 

0

0

···

1⊤6

 

I6 I6 · · · I6

where 16 is the all-one column vector and I6 is the 6 × 6 identity matrix. The first six rows are L1, . . . , L6, and the last six rows are R1, . . . , R6.
Proposition 4 (Column expansion). dm⋆ in(MK6,6) = 6.
Proof. Any edge set of size ≤ 5 in K6,6 spans at least as many vertices: a bipartite graph on v ≤ 4 vertices has at most ⌊v2/4⌋ ≤ v edges. The six edges {cij : i ∈ {1, 2}, j ∈ {1, 2, 3}} span only five vertices.

Proposition 5 (Full structural row rank). ρ(MK6,6) = 12.
Proof. Match Li → cii and Rj → cj+1,j (indices mod 6). These twelve columns are distinct and each is incident to its matched row.

Together, Propositions 4 and 5 show that τ (M ) = 5 and ρ(M ) = m = 12. By Theorem 1, there
exists a [36, 24, 6]q code obeying MK6,6 over any sufficiently large field. We now show that no such code can be realized as a subcode of a generalized Reed–Solomon code.

Theorem 6. MK6,6 admits no full-rank Vandermonde certificate of distance 6 over any field.

Proof sketch. Assume for contradiction that a full-rank Vandermonde certificate exists. It suffices to

rule out certificates over an algebraically closed field. After absorbing the nonzero column multipliers

into H, write

ν(t) = (1, t, t2, t3, t4)T , ν(tij) = xijai + yijbj,

where the 36 values tij are distinct and ai, bj ∈ F5 are the columns of A indexed by the left and
right vertices of K6,6. This would imply that the vectors ν(tij) for distinct evaluation points lie in
the span of ai and bj. Fix i ≠ k and project away from span(ai, ak) by choosing W ∈ F3×5 with kernel span(ai, ak)
and set q(t) = W ν(t). Then q(tij) and q(tkj) are proportional for all j. The key algebraic lemma, proved in Appendix A, says that these six proportionalities force the existence of fractional-linear involutions τik ∈ Mob(F) ∼= PGL2(F) such that

tkj = τik(tij), j = 1, . . . , 6.

Analyzing this family of involutions demonstrates that it eventually forces too many of the vectors ν(tij) to lie in a low-dimensional subspace. This violates the fact that any four such vectors with distinct evaluation points must remain linearly independent. The full algebraic details are given in Appendix A.

The mask MK6,6 satisfies ρ(M ) = 12 and dm⋆ in(M ) = 6. Over large fields, generic methods produce an exact [36, 24, 6] code obeying the mask. Yet no such code can be realized as a subcode
of a generalized Reed–Solomon code via a Vandermonde factorization.

6

6 Structured Mask Optimization
While the general condition of Theorem 1 is difficult to analyze, structured mask families admit sharper results.
Definition 4 (Regular). A mask M ∈ {0, 1}m×n is w-regular if each row has exactly w ones.
Definition 5 (Balanced). A w-regular mask is balanced if each column has ⌊wm/n⌋ or ⌈wm/n⌉ ones.
Definition 6 (Cyclic). A mask is cyclic if each row is a cyclic shift of the first row.
A cyclic mask is always w-regular. Regular masks simplify hardware since each parity check uses exactly w terms. Balanced masks distribute measurements evenly—important in quantum systems where repeated measurement introduces noise [14, 15].
Proposition 7. For any w ≥ n−m+1 and every prime power q ≥ n+m−1, there exist generalized [n, n−m]q Reed–Solomon codes satisfying a w-regular, balanced, and cyclic mask.
Proof. The dual codes of the sparse Reed–Solomon constructions in [23] satisfy these constraints by Proposition 2.

6.1 Numerical Results

Figures 1–3 display the distance

Dfr(m, n, w) := max{dm⋆ in(M ) : M ∈ Mreg(w),

(6)

ρ(M ) = m},

computed by exhaustive search over all mask patterns for n = 9 under regular and cyclic constraints, and for n = 16 under balanced-cyclic constraints. Each mask defines an [n, n−m] code over sufficiently large fields by Theorem 1.

Figure 1: Full-rank w-regular mask distance Dfr(m, 9, w). 7

Figure 2: Full-rank cyclic mask distance for n = 9. Comparing with Figure 1, the cyclic optimum at (m, w) = (5, 3) is only 3 versus 4.

6.2 Observations
Remark 2 (Insufficiency of cyclic masks). Let dm⋆ in(m, n, w) and dc⋆yclic(m, n, w) denote the maximum expansion-bound distance over all w-regular masks and all cyclic masks, respectively. There exist parameters for which dc⋆yclic(m, n, w) < d⋆min(m, n, w). For instance, Figures 1 and 2 show d⋆cyclic(5, 9, 3) = 3 while dm⋆ in(5, 9, 3) = 4. Thus cyclic structure alone is not sufficient to maximize the expansion-bound distance.
Remark 3 (Non-monotonicity of balanced-cyclic masks). The expansion-bound distance is not monotone in the row weight w. Figure 3 exhibits this for balanced-cyclic masks at n = 16: dm⋆ in(9, 16, 6) = 9 but d⋆min(9, 16, 7) = 8.
Definition 7 (Graph incidence expansion). For a multigraph G = (V, E) and R ⊆ V , let

E(R) := {e ∈ E : e ∩ R ̸= ∅}

be the set of edges incident to at least one vertex of R, counted with multiplicity. The incidence expansion of G, denoted η(G), is

η(G) = max { s ∈ Z≥0 : ∀R ⊆ V,

(7)

1 ≤ |R| ≤ s =⇒ |E(R)| ≥ |R|} .

Proposition 8 (2-regular masks are limited to repetition-code distance). Let m < n. A 2-regular mask M with m rows and n columns satisfies

dm⋆ in(M ) ≤

n n−m

.

(8)

Equality is achieved by a cyclic mask (Def. 6). In particular, 2-regular parity-check constraints can never exceed the minimum distance of the [n, n−m] block repetition code.
Proof. A 2-regular mask corresponds to a multigraph G = (V, E) with |V | = n and |E| = m, where columns are vertices and rows are edges. Under this correspondence, τ (M ) = η(G). Any

8

Figure 3: Full-rank balanced-cyclic mask distance for n = 16. The m = 9 row exhibits nonmonotonicity: dm⋆ in(9, 16, 6) = 9 but d⋆min(9, 16, 7) = 8. such graph has at least n−m acyclic connected components (each cycle closes at most one excess edge). Let V1, . . . , Vn−m be the vertex sets of these components. By pigeonhole, some V ∗ satisfies |V ∗| ≤ ⌊n/(n−m)⌋. Since V ∗ is an entire acyclic component, its incident edge set equals its internal edge set, so |E(V ∗)| = |V ∗| − 1 < |V ∗|. Hence η(G) ≤ ⌊n/(n−m)⌋ − 1 and dm⋆ in(M ) ≤ ⌊n/(n−m)⌋.
For tightness, delete n−m nearly evenly spaced edges from the n-cycle, leaving n−m disjoint paths with balanced numbers of vertices. For any R with |R| ≤ ⌊n/(n−m)⌋−1, partition Ri = R∩Pi; since Ri ⊊ Pi, each piece satisfies |E(Ri)| ≥ |Ri|, so |E(R)| ≥ |R|. Thus d⋆min(M ) = ⌊n/(n−m)⌋. This mask is cyclic and has ρ(M ) = m (every forest has a matching saturating all edges). Remark 4 (Conditions for d⋆min ≤ 2). For w-regular masks: (1) dm⋆ in(m, n, w) = 1 if and only if m < n/w, since the mw ones cannot cover all n columns; (2) dm⋆ in(m, n, w) = 2 if and only if n/w ≤ m < 2n/(w+1), since a pigeonhole argument forces two weight-1 columns to share a row.
9

7 Conclusion
We derived a tight expansion-based bound on the minimum distance of sparse parity-check codes and connected the MDS regime to the dual GM-MDS construction. We introduced generalized Vandermonde certificates for realizing sparse codes as subcodes of generalized Reed–Solomon codes and showed via the K6,6 counterexample that, unlike the generator-matrix setting, such certificates do not always exist.
Several directions remain open. Determining tight necessary and sufficient conditions on the mask for the existence of a Vandermonde certificate is a natural next step; we have obtained explicit generalized Reed–Solomon subcode constructions under certain mask restrictions, which will appear in subsequent work. The certificate framework itself generalizes to other structured code families. Finally, the CSS construction [12, 13] converts pairs of dual-contained classical codes into quantum codes; extending the sparse parity-check framework to this setting is a promising direction for quantum codes with structured stabilizers.
10

A Proof of Theorem 6

A.1 Algebraic reduction
We prove Theorem 6. It suffices to rule out certificates over an algebraically closed field F: if a certificate exists over a field F , then after extending scalars to F the same equations hold, ranks are preserved, and the Vandermonde evaluation points remain distinct.
Assume, for contradiction, that such a certificate exists. Write the columns of A corresponding to rows Li as ai, and those corresponding to rows Rj as bj. After absorbing the nonzero column multipliers into H, the Vandermonde columns have the form
ν(t) = (1, t, t2, t3, t4)T .

Since column (i, j) of H is supported only on rows Li and Rj, there are scalars xij, yij ∈ F, not

both zero, such that

ν(tij) = xijai + yijbj, 1 ≤ i, j ≤ 6,

(9)

where the 36 evaluation points tij are pairwise distinct.

A.2 Algebraic preliminaries
Lemma 9 (Moment-curve independence). If t1, . . . , ts ∈ F are distinct and s ≤ 5, then
ν(t1), . . . , ν(ts) ∈ F5
are linearly independent. Consequently:
(a) two distinct vectors of the form ν(t) are not proportional;
(b) if p1, p2, p3, p4 are four distinct vectors of the form ν(t), then
span(p1, p2) ∩ span(p3, p4) = {0}.
Proof. The s × s submatrix obtained from the first s rows is a Vandermonde matrix with nonzero determinant
(tb − ta).
1≤a<b≤s
Thus the s columns are linearly independent. The two consequences follow immediately.
Lemma 10 (Nonzero and pairwise independent left vectors). In any hypothetical certificate, the six left-row vectors a1, . . . , a6 ∈ F5 are nonzero and pairwise linearly independent.
Proof. Use the notation of (9). First suppose ai = 0 for some i. Then each ν(tij) is a nonzero scalar multiple of bj. If also ak = 0 for some k ≠ i, then ν(tij) and ν(tkj) are proportional for every j, contradicting Lemma 9. Hence choose k ≠ i with ak ≠ 0.
For each j, the two independent vectors ν(tij) and ν(tkj) lie in the same two-dimensional space span(ak, bj); if span(ak, bj) were one-dimensional, they would be proportional. Thus they form a basis of span(ak, bj), and in particular
ak ∈ span(ν(tij), ν(tkj))
for all six values of j. Taking two different values of j gives two spans of four distinct vectors of the form ν(t) meeting in the nonzero vector ak, contradicting Lemma 9. Therefore every ai is nonzero.

11

Now suppose ai and ak are proportional for some i ̸= k, and let a be a common nonzero vector spanning them. For each j, the two independent vectors ν(tij) and ν(tkj) lie in span(a, bj). As above, they form a basis of this space, so

a ∈ span(ν(tij), ν(tkj))
for all j. Again two different values of j contradict Lemma 9. Hence ai and ak are linearly independent.

Lemma 11 (Algebraic normal form for irreducible ternary quadratics). Let Q(X, Y, Z) ∈ F[X, Y, Z]

be an irreducible homogeneous polynomial of degree 2. Then there are independent linear forms

L1, L2, L3 in X, Y, Z such that

Q = L22 − L1L3.

In characteristic two, this is the same as Q = L22 + L1L3.

Proof. First choose a nonzero vector p ∈ F3 with Q(p) = 0. This is elementary: restrict Q to

any two-dimensional subspace. If the restriction is the zero polynomial, any nonzero vector in that

subspace works. Otherwise the restriction is a nonzero binary homogeneous quadratic, which factors

into linear forms over the algebraically closed field F and hence has a nonzero zero. Extend p to a basis p, u, v of F3. In the corresponding coordinates, replace Q by

Q(X, Y, Z) := Q(Xp + Y u + Zv).

Since Q(1, 0, 0) = Q(p) = 0, there is no X2 term. Thus

Q = Xℓ(Y, Z) + q(Y, Z),

where ℓ is linear and q is homogeneous quadratic in Y, Z.

If ℓ = 0, then Q = q(Y, Z) is a binary homogeneous quadratic, so it factors into linear forms

over F, contradicting irreducibility. Hence ℓ ̸= 0. After an invertible linear change in the variables

Y, Z, assume ℓ = Z. Then

Q = XZ + aY 2 + bY Z + cZ2.

If a = 0, then

Q = Z(X + bY + cZ),

again reducible. Hence a ≠ 0. Since F is algebraically closed, rescale Y so that a = 1. Replacing X
by X − bY − cZ gives Q = Y 2 + XZ.

Equivalently, after replacing X by −X when the characteristic is not two,

Q = Y 2 − XZ.

Undoing the change of variables gives Q = L22 − L1L3 for independent linear forms L1, L2, L3.

Lemma 12 (Polynomial triples satisfying F22 = F1F3). Let F1, F2, F3 ∈ F[t] be linearly independent polynomials with

gcd(F1, F2, F3) = 1,

F22 = F1F3.

Then there are coprime polynomials R, S ∈ F[t] and nonzero scalars λ, µ, η ∈ F× such that

F1 = λR2, F2 = ηRS, F3 = µS2, η2 = λµ.

If M0 = maxi deg Fi and h = max(deg R, deg S), then

2h ≤ M0, h ̸= 0.

12

Proof. The identity F22 = F1F3 and the condition gcd(F1, F2, F3) = 1 imply gcd(F1, F3) = 1. Indeed, any common divisor of F1 and F3 divides F22, hence also divides F2, contradicting the gcd condition.
Because F1F3 is a square and F1, F3 are coprime, unique factorization in F[t] gives
F1 = λR2, F3 = µS2

for coprime R, S and nonzero λ, µ ∈ F×. Then

F22 = λµR2S2.
Since F is algebraically closed, choose η ∈ F× with η2 = λµ; then F2 = ±ηRS, and the sign may be absorbed into η.
The degree inequality follows because F1 and F3 have degrees 2 deg R and 2 deg S. If h = 0, then R, S are constants, so all three Fi are constant multiples of one another, contradicting their linear independence. Hence h ≠ 0.

For the next lemma, write

F := F ∪ {∞}.

A fractional linear transformation is a function on F of the form

αt + β

t −→

,

γt + δ

αδ − βγ ≠ 0,

with the usual conventions at the pole and at ∞. Let Mob(F) denote this group; equivalently, it is the action of PGL2(F) on F.

Lemma 13 (Quadratic quotients have a residual fractional-linear involution). Let R, S ∈ F[t] be coprime with
max(deg R, deg S) = 2.
Set N (T, U ) := R(T )S(U ) − R(U )S(T ).

Assume there exist distinct t, u ∈ F with N (t, u) = 0. Then there is a nonidentity involution τ ∈ Mob(F) such that, whenever p ̸= q and N (p, q) = 0, one has q = τ (p).

Proof. Since N (T, T ) = 0, the polynomial T − U divides N (T, U ). Write

N (T, U ) = (T − U )B(T, U ).

Because R and S have degree at most 2, the quotient B has degree at most 1 in each variable. Also N (U, T ) = −N (T, U ) and U − T = −(T − U ), so B(U, T ) = B(T, U ). Hence

B(T, U ) = aT U + b(T + U ) + d

for some a, b, d ∈ F. We claim b2 − ad ̸= 0. If b2 − ad = 0, then the symmetric bilinear polynomial B(T, U ) has
rank at most one. If B is constant, then N (T, U ) is a scalar multiple of T − U , which forces the rational function R/S to have degree at most one; this contradicts max(deg R, deg S) = 2 with R, S coprime. Otherwise, over the algebraically closed field F, the polynomial B(T, U ) is a nonzero scalar

13

multiple of ℓ(T )ℓ(U ) for some nonconstant linear polynomial ℓ. Choose t0 with ℓ(t0) = 0. Then B(t0, U ) = 0 for every U , so N (t0, U ) = 0 for every U . Thus

R(t0)S(U ) − R(U )S(t0) = 0

for every U . Since R and S have no common root, (R(t0), S(t0)) ̸= (0, 0); the last identity forces R and S to be scalar multiples, contrary to max(deg R, deg S) = 2 and coprimality. Therefore b2 − ad ̸= 0.

Define

bt + d

τ (t) := −

.

at + b

The determinant condition b2 − ad ̸= 0 says that this is a fractional linear transformation. For

p ̸= q, the equation N (p, q) = 0 is equivalent to B(p, q) = 0, and

B(p, q) = 0 ⇐⇒ (ap + b)q + (bp + d) = 0 ⇐⇒ q = τ (p),

with the usual interpretation when ap + b = 0. Finally B(T, U ) = B(U, T ) implies

q = τ (p) ⇐⇒ p = τ (q),

so τ 2 = id. The assumed existence of a distinct pair t ̸= u with N (t, u) = 0 implies u = τ (t) ̸= t, so τ ̸= id.
Lemma 14 (Characteristic-two translation invariant quadratics). Assume char F = 2 and a ∈ F×. Let R, S ∈ F[t] be coprime polynomials of degree at most 2 satisfying

R(t + a)S(t) − R(t)S(t + a) ≡ 0.

Then R and S both lie in the two-dimensional space span{1, t2 + at}.

If R, S are linearly independent, then span{R2, RS, S2} = span{1, t2 + at, (t2 + at)2}.

Proof. The displayed identity gives

R(t + a)S(t) = R(t)S(t + a).

Since gcd(R(t), S(t)) = 1, the polynomial R(t) divides R(t + a). The two polynomials have the same degree, so R(t + a) = cR(t) for some c ∈ F×. Similarly S(t + a) = cS(t) with the same scalar c. Applying the shift twice gives c2 = 1. In characteristic two, this forces c = 1. Hence R and S are individually invariant under t → t + a.
Let f (t) = pt2 + qt + r be any polynomial of degree at most 2. In characteristic two,
f (t + a) − f (t) = pa2 + qa.

Thus f (t + a) = f (t) if and only if q = pa, i.e. f (t) = r + p(t2 + at).

14

Therefore every invariant polynomial of degree at most 2 lies in span{1, t2 + at}. If R, S are linearly independent, they form a basis of this two-dimensional invariant space.
Taking pairwise products gives the full symmetric-square space
span{1, t2 + at, (t2 + at)2}.

Lemma 15 (Three-polynomial involution lemma). Assume a certificate exists. For every pair i ̸= k, there is a nonidentity involution
τik ∈ Mob(F)

such that

tkj = τik(tij) for every j = 1, . . . , 6.

Moreover, in characteristic two, if τik(t) = t + a with a ̸= 0, then for every full-row-rank matrix W ∈ F3×5 with right kernel span(ai, ak), the three coordinate polynomials of W ν(t) span

span{1, t2 + at, (t2 + at)2}.

Proof. Fix i ̸= k. By Lemma 10,

U := span(ai, ak)

is a two-dimensional subspace of F5. Choose a full-row-rank matrix

W ∈ F3×5

with right kernel U , and define q(t) = W ν(t) = (q1(t), q2(t), q3(t))T ∈ F[t]3.

The polynomials q1, q2, q3 are linearly independent. Indeed, a relation c1q1 + c2q2 + c3q3 ≡ 0 would give (cT W )ν(t) ≡ 0, hence cT W = 0 because 1, t, t2, t3, t4 are linearly independent. Since W has
row rank 3, this gives c = 0.
Applying W to (9) gives

q(tij) = yijW bj, q(tkj) = ykjW bj.
Thus q(tij) and q(tkj) are proportional for every j, allowing the zero vector for now. Let D(t) = gcd(q1, q2, q3), q(t) = D(t)q(t),
e := deg D.
We first prove e ≤ 2. The three qℓ span a 3-dimensional subspace of F[t]≤4. Since all are divisible by D, this subspace lies inside
D(t)F[t]≤4−e,
whose dimension is 5 − e. Therefore 3 ≤ 5 − e, so e ≤ 2. The components of q have no common factor. Since F is algebraically closed, they have no
common root. Hence q(t0) = 0 if and only if D(t0) = 0. Call the pair {tij, tkj} ruined if one of its two values is a root of D. The six pairs are disjoint because all 36 evaluation points are distinct.

15

Since D has at most e distinct roots, at most e pairs are ruined. Hence at least 6 − e pairs are intact. Choose exactly 5 − e intact pairs.
The vector space of homogeneous quadratic forms in three variables has dimension 6. The conditions
Q(q(tij)) = 0
for the chosen 5 − e intact pairs are homogeneous linear conditions on the coefficients of Q. Hence there is a nonzero homogeneous quadratic Q satisfying these conditions. For each chosen intact pair, the two nonzero vectors q(tij) and q(tkj) are proportional; since Q is homogeneous, Q vanishes at both values of the pair. Therefore
P (t) := Q(q(t))
has at least 2(5 − e) = 10 − 2e distinct roots. But each coordinate of q has degree at most 4 − e, so

deg P ≤ 2(4 − e) = 8 − 2e.

Thus P ≡ 0. The quadratic Q is irreducible. If Q = L1L2 were a product of linear forms, then

L1(q(t))L2(q(t)) ≡ 0.

Since F[t] is an integral domain, one factor, say L1(q(t)), is identically zero. Multiplying by D gives

a nontrivial linear relation among q1, q2, q3, impossible.

By Lemma 11, after an invertible linear change of the three coordinates of q, we obtain polyno-

mials F1, F2, F3 such that

F2(t)2 = F1(t)F3(t).

The polynomials F1, F2, F3 remain linearly independent, have no common factor, and have maximum degree at most 4 − e. Lemma 12 gives coprime polynomials R, S with

F1 = λR2, F2 = ηRS, F3 = µS2

for nonzero constants, and

2h ≤ 4 − e, h := max(deg R, deg S), h ̸= 0.

For every chosen intact pair, the triples

(F1(tij), F2(tij), F3(tij)) and (F1(tkj), F2(tkj), F3(tkj))

are proportional. Using the displayed factorization, this implies that the two pairs

(R(tij), S(tij)) and (R(tkj), S(tkj))
are proportional. Indeed, for nonzero pairs (r, s) and (r′, s′), proportionality of (r2, rs, s2) and (r′2, r′s′, s′2) gives (rs′ − r′s)2 = 0, hence rs′ = r′s in the field F. Equivalently,

R(tij)S(tkj) − R(tkj)S(tij) = 0.

If h = 1, the equality above forces tij = tkj for every chosen intact pair: two coprime linear polynomials separate distinct scalar values. This contradicts distinctness of the Vandermonde evaluation points. Hence h = 2. The inequality 2h ≤ 4 − e now forces e = 0.

16

Thus D is constant, so no evaluation point is ruined. The same coprime quadratic pair R, S satisfies
R(tij)S(tkj) − R(tkj)S(tij) = 0
for all six values of j. Since tij ≠ tkj, Lemma 13 gives a nonidentity involution τik ∈ Mob(F) with

tkj = τik(tij), j = 1, . . . , 6. For the final assertion in characteristic two, suppose τik(t) = t + a, a ̸= 0. The same R, S satisfy

R(t + a)S(t) − R(t)S(t + a) ≡ 0.

By Lemma 14,

span{R2, RS, S2} = span{1, t2 + at, (t2 + at)2}.

The coordinate change from q to (F1, F2, F3) was invertible and D is constant, so the coordinate polynomials of q(t) = W ν(t) span the same space. This proves the characteristic-two assertion.

A.3 Fractional-linear involutions

Lemma 16 (Pairwise quotient involutions). Let g1, . . . , gs ∈ Mob(F) be distinct fractional linear transformations such that gkgi−1 is an involution for every i ≠ k.
(i) If char F ̸= 2, then s ≤ 4.

(ii) If char F = 2, then after one fractional linear change of variable, all gi are translations

gi(t) = t + αi,

with distinct αi ∈ F.
Proof. Replace each gi by gig1−1. Then g1 = id, and each gi for i > 1 is an involution. For i, k > 1, the quotient condition says gkgi−1 = gkgi is also an involution. Hence
(gkgi)2 = id .
Since gi2 = gk2 = id, this implies gkgi = gigk. Thus the gi commute. Assume first char F ̸= 2. Choose a nonidentity involution among the gi and change variable so
that it is t → −t. Any fractional linear transformation commuting with t → −t must preserve the set {0, ∞}. Hence it has one of the forms

t → λt, t → λ/t.

Among maps t → λt, being an involution forces λ = ±1. If two maps t → λ/t and t → µ/t both

occur in a commuting elementary two-group, their product is t → (λ/µ)t, so λ/µ = ±1. Thus there

are at most four such transformations. Therefore s ≤ 4.

Now assume char F = 2. A nonidentity fractional-linear involution has a unique fixed point in F: its representing 2 × 2 matrix has a repeated eigenvalue, and if it had two independent eigenvectors

it would be scalar. Since the involutions commute, they share this fixed point. Indeed, if a has

unique fixed point P and b commutes with a, then b(P ) is also fixed by a, so b(P ) = P .

Make one fractional linear change of variable sending the common fixed point to ∞. Then every

gi is affine:

gi(t) = λit + βi.

The equation gi2 = id gives λ2i = 1, hence λi = 1 in characteristic two. Thus gi(t) = t + αi. The αi are distinct because the gi are distinct.

17

A.4 Completion of proof
Assume, for contradiction, that a full-rank generalized Vandermonde certificate of distance 6 exists. Work over an algebraically closed field as above. We have distinct scalars tij and vectors ai, bj ∈ F5 satisfying
ν(tij) = xijai + yijbj.
For each i ≠ k, Lemma 15 gives a nonidentity involution τik ∈ Mob(F) such that
tkj = τik(tij), j = 1, . . . , 6.

Fix the first left row and define

g1 = id, gi = τ1i (i = 2, . . . , 6).

Then

tij = gi(t1j) for all i, j.

For i ≠ k,

gk(t1j) = tkj = τik(tij) = τikgi(t1j)

for j = 1, . . . , 6. The six values t11, . . . , t16 are distinct. A fractional linear transformation is determined by its values on three distinct elements of F. Therefore

gk = τikgi,

so

τik = gkgi−1.

Thus every pairwise quotient gkgi−1 is an involution. The gi are distinct, because gi = gk would imply tij = tkj for all j, contradicting distinctness of the 36 Vandermonde evaluation points.
If char F ≠ 2, Lemma 16 gives 6 ≤ 4, a contradiction. It remains to handle characteristic two.
Assume char F = 2. By Lemma 16, after a fractional linear change of the parameter t,

gi(t) = t + αi, i = 1, . . . , 6,

with the αi distinct. The common fixed point of these translations is ∞. No value t1j is this fixed point; otherwise all six values gi(t1j) would be equal, contradicting distinctness. Hence, after this change of variable, all relevant evaluation points remain finite. The induced change of basis on the five-dimensional space spanned by 1, t, t2, t3, t4 lets us continue to write
ν(t) = (1, t, t2, t3, t4)T .

For i ≠ k, the involution

τik = gkgi−1

is the translation

t −→ t + aik, aik := αi + αk ̸= 0.

Choose any full-row-rank matrix Wik ∈ F3×5 with right kernel span(ai, ak). By the characteristictwo part of Lemma 15, the coordinate polynomials of Wikν(t) span

span{1, t2 + aikt, (t2 + aikt)2}.

Since

(t2 + aikt)2 = t4 + ai2kt2

18

in characteristic two, every polynomial in this span has zero coefficient of t3. Therefore every row of Wik has zero fourth coordinate. Equivalently,
Q0 := (0, 0, 0, 1, 0)T ∈ ker Wik = span(ai, ak)
for every pair i ̸= k. Choose p such that ap is not proportional to Q0. Such a p exists because the ai are pairwise
linearly independent. For every k ̸= p,
Q0 ∈ span(ap, ak).
Since ap and Q0 are linearly independent, this forces
ak ∈ span(ap, Q0).
Therefore all six vectors a1, . . . , a6 lie in one fixed two-dimensional subspace P := span(ap, Q0) ⊂ F5.
Fix any right index j. From (9), for every i,
ν(tij) = xijai + yijbj ∈ P + span(bj).
The space P + span(bj) has dimension at most 3. Hence the six distinct vectors
ν(t1j), . . . , ν(t6j)
all lie in a three-dimensional vector space. This contradicts Lemma 9, since any four distinct vectors of the form ν(t) are linearly independent.
This contradiction rules out characteristic two. Therefore no full-rank generalized Vandermonde certificate of distance 6 exists over any field, proving Theorem 6.
References
[1] S. H. Dau, W. Song, and C. Yuen, “On simple multiple access networks,” IEEE J.Sel. A. Commun., vol. 33, p. 236–249, Feb. 2015.
[2] W. Halbawi, T. Ho, H. Yao, and I. Duursma, “Distributed Reed–Solomon codes for simple multiple access networks,” in 2014 IEEE International Symposium on Information Theory, pp. 651–655, June 2014.
[3] S. H. Dau, W. Song, and C. Yuen, “On the existence of MDS codes over small fields with constrained generator matrices,” in 2014 IEEE International Symposium on Information Theory, pp. 1787–1791, June 2014.
[4] H. Yildiz and B. Hassibi, “Optimum linear codes with support-constrained generator matrices over small fields,” IEEE Transactions on Information Theory, vol. 65, pp. 7868–7875, Dec. 2019.
[5] S. Lovett, “MDS matrices over small fields: A proof of the GM-MDS conjecture,” in 2018 IEEE 59th Annual Symposium on Foundations of Computer Science (FOCS), pp. 194–199, Oct. 2018.
19

[6] R. Gallager, “Low-density parity-check codes,” IRE Transactions on Information Theory, vol. 8, pp. 21–28, Jan. 1962.
[7] T. Richardson, M. Shokrollahi, and R. Urbanke, “Design of capacity-approaching irregular lowdensity parity-check codes,” IEEE Transactions on Information Theory, vol. 47, pp. 619–637, Feb. 2001.
[8] 3rd Generation Partnership Project, “NR; multiplexing and channel coding (3GPP TS 38.212),” 2018. Release 15, v15.2.0.
[9] M. Sipser and D. Spielman, “Expander codes,” in Proceedings of the Twenty-Sixth Annual ACM Symposium on Theory of Computing, pp. 261–271, May 1996.
[10] I. Tamo and A. Barg, “A family of optimal locally recoverable codes,” IEEE Transactions on Information Theory, vol. 60, pp. 4661–4676, Aug. 2014.
[11] M. Blaum, J. L. Hafner, and S. Hetzler, “Partial-MDS codes and their application to RAID type of architectures,” IEEE Transactions on Information Theory, vol. 59, pp. 4510–4519, July 2013.
[12] A. R. Calderbank and P. W. Shor, “Good quantum error-correcting codes exist,” Physical Review A, vol. 54, p. 1098–1105, Aug. 1996.
[13] A. Steane, “Multiple particle interference and quantum error correction,” Proceedings of the Royal Society A: Mathematical, Physical and Engineering Sciences, vol. 452, Nov. 1996.
[14] D. Ruiz, J. Guillaud, A. Leverrier, M. Mirrahimi, and C. Vuillot, “LDPC-cat codes for lowoverhead quantum computing in 2d,” Nature Communications, vol. 16, Jan. 2025.
[15] E. Sabo, L. G. Gunderman, B. Ide, M. Vasmer, and G. Dauphinais, “Weight-reduced stabilizer codes with lower overhead,” PRX Quantum, vol. 5, Oct. 2024.
[16] D. Gottesman, “Fault-tolerant quantum computation with constant overhead,” Quantum Info. Comput., vol. 14, p. 1338–1372, Nov. 2014.
[17] M. Grassl, W. Geiselmann, and T. Beth, Quantum Reed–Solomon Codes, p. 231–244. Springer Berlin Heidelberg, 1999.
[18] L. Golowich and V. Guruswami, “Quantum locally recoverable codes,” in Proceedings of the 2025 Annual ACM-SIAM Symposium on Discrete Algorithms (SODA), (Philadelphia, PA), pp. 5512–5522, Society for Industrial and Applied Mathematics (SIAM), Jan. 2025.
[19] S. Sharma, V. Ramkumar, and I. Tamo, “Quantum locally recoverable codes via good polynomials,” IEEE Journal on Selected Areas in Information Theory, vol. 6, pp. 100–110, May 2025.
[20] S. B. Bravyi and A. Y. Kitaev, “Quantum codes on a lattice with boundary,” 1998.
[21] A. G. Fowler, M. Mariantoni, J. M. Martinis, and A. N. Cleland, “Surface codes: Towards practical large-scale quantum computation,” Physical Review A, vol. 86, Sept. 2012.
[22] P. Hall, “On representatives of subsets,” Journal of the London Mathematical Society, vol. s1-10, pp. 26–30, Jan. 1935.
20

[23] W. Halbawi, Z. Liu, I. M. Duursma, H. Dau, and B. Hassibi, “Sparse and balanced Reed– Solomon and Tamo–Barg Codes,” IEEE Transactions on Information Theory, vol. 65, pp. 118– 130, Jan. 2019.
21

