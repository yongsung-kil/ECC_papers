Mutual Information-Maximizing Quantized Belief
Propagation Decoding of Regular LDPC Codes
Xuan He†, Kui Cai‡, Zhen Mei∗, Peng Kang‡, and Xiaohu Tang† †Southwest Jiaotong University, Chengdu 611756, China
‡Singapore University of Technology and Design, Singapore 487372, Singapore ∗Nanjing University of Science and Technology, Nanjing 210094, China
Email: xhe@swjtu.edu.cn, cai kui@sutd.edu.sg, meizhen@njust.edu.cn, peng kang@sutd.edu.sg, xhutang@swjtu.edu.cn

arXiv:1904.06666v5 [cs.IT] 16 Dec 2022

Abstract—In this paper, we propose a class of finite alphabet iterative decoder (FAID), called mutual information-maximizing quantized belief propagation (MIM-QBP) decoder, for decoding regular low-density parity-check (LDPC) codes. Our decoder follows the reconstruction-calculation-quantization (RCQ) decoding architecture that is widely used in FAIDs. We present the first complete and systematic design framework for the RCQ parameters, and prove that our design with sufficient precision at node update is able to maximize the mutual information between coded bits and exchanged messages. Simulation results show that the MIM-QBP decoder can always considerably outperform the state-of-the-art mutual information-maximizing FAIDs that adopt two-input single-output lookup tables for decoding. Furthermore, with only 3 bits being used for each exchanged message, the MIM-QBP decoder can outperform the floating-point belief propagation decoder at the high signal-to-noise ratio regions when testing on high-rate LDPC codes with a maximum of 10 and 30 iterations.
Index Terms—Finite alphabet iterative decoder (FAID), lookup table (LUT), low-density parity-check (LDPC) code, mutual information (MI), quantized belief propagation (QBP).
I. INTRODUCTION
Low-density parity-check (LDPC) codes [1] have been widely applied to communication and data storage systems due to their capacity approaching performance. Many of these systems, such as the NAND flash memory, have strict requirements on the memory consumption and implementation complexity of LDPC decoders [2], [3]. For the sake of simple hardware implementation, many efforts have been devoted to efficiently represent messages for LDPC decoding [4]–[11]. For example, in [4] and [5], the messages exchanged within the LDPC decoders are represented by log-likelihood ratios (LLRs) in low resolutions, which have generally 5 to 7 bits. The works in [6]–[11] develop the finite alphabet iterative decoder (FAID) for LDPC codes, which make use of messages represented by symbols from finite alphabets rather than by LLRs for decoding. These FAIDs draw much attention due to their excellent error rate performance and low decoding complexity. More specifically, they adopt the quantized messages of only 3 or 4 bits to approach and even surpass the performance of the floating-point belief propagation (BP) decoder [12]. In this paper, we focus on the class of FAIDs.
The early FAIDs investigated in [6]–[8] consider simple mappings, additions, and non-uniform quantization for decoding, leading to a reconstruction-calculation-quantization

(RCQ) decoding architecture. However, the design of these FAIDs (RCQ parameters) focuses on the specific class of (3, 6) (variable node (VN) degree 3 and check node (CN) degree 6) LDPC codes and requires large amount of manual optimizations. Thus, one can hardly generalize the decoder design to different scenarios. Recently, the mutual informationmaximizing FAIDs (MIM-FAIDs) [9]–[11] are designed by density evolution (DE) [6] for various bit width settings of the decoder, with the aim of maximizing the mutual information (MI) between the coded bits and the exchanged messages within the decoder. These MIM-FAIDs are also referred to as MIM lookup table (MIM-LUT) decoders, since they adopt a series of cascaded two-input single-output LUTs for decoding. However, concatenating LUTs causes the degradation of both mutual information and error rate performance.
To overcome the drawbacks of the FAIDs in [6]–[11], we have presented a mutual information-maximizing quantized belief propagation (MIM-QBP) decoder in [13]. The MIMQBP decoder adopts the RCQ decoding architecture at both the CN and VN update similarly to the FAIDs in [6]–[8]. The main contribution of [13] is to establish a high-level principle for designing good RCQ parameters, aiming at maximizing the MI between the coded bits and the exchanged messages, for decoding any regular LDPC codes. The principle has enabled the development of many follow-up MIM-FAIDs [14]–[19]. These MIM-FAIDs’ VN update follows the RCQ decoding architecture where the RCQ parameters are designed based on the principle established in [13]; Their CN update employs the min operation (like the min-sum decoder [4]) to simplify the RCQ decoding architecture used in the CN update of the MIM-QBP decoder, at the cost of slightly degrading error rate performance.
This paper, as the extended version of [13], presents the first complete and systematic design framework for the RCQ parameters of the MIM-QBP decoder. More specifically, Section III of this paper includes the design principle originally proposed in [13] and gives more explanations. The most important extension over [13] is that, this paper contains a new section (Section IV) to illustrate the optimality of a class of reconstruction functions (RFs) which can make the corresponding MIM-QBP decoder maximize the MI between the coded bits and the exchanged messages; Meanwhile, it presents a near-optimal design of practical RFs based on

DMC

Quantizer

Fig. 1. Quantization of a discrete memoryless channel (DMC).

scaling the optimal RFs. Therefore, this paper essentially establishes a fundamental theory for supporting the validity of the aforementioned MIM-FAIDs [14]–[19]. Moreover, we believe that our design framework will facilitate the development of more follow-up MIM-FAIDs. The main contributions of this paper are summarized as follows:
• This paper presents, for the first time, a complete and systematic design framework of the RCQ parameters for decoding any regular LDPC codes.
• We prove that with sufficient precision for node update, our design for the RCQ parameters is able to maximize the MI between the coded bits and the exchanged messages.
• We investigate the error rate performance of the proposed MIM-QBP decoder based on extensive simulations. We observe that the MIM-QBP decoders can always considerably outperform the MIM-LUT decoders, and can sometimes even outperform the floating-point BP decoder with only 3 bits for each exchanged message.
The remainder of this paper is organized as follows. Section II first introduces the optimal quantization method for the binary-input discrete memoryless channel (DMC), and then gives a review of the MIM-LUT decoding and also highlights the linkage between the two topics. Section III illustrates the general design principle for designing the RCQ parameters for decoding regular LDPC codes. Section IV develops an efficient design of the RFs of the MIM-QBP decoder. Section V presents the simulation results. Finally, Section VI concludes this paper.

where PX,Z (x, z) = PX (x) y∈Y PY |X (y|x)PZ|Y (z|y) and PZ (z) = x∈X PX,Z (x, z).
A deterministic quantizer (DQ) Q : Y → Z means that
for each y ∈ Y, there exists a unique z ∈ Z such that
PZ|Y (z|y) = 1 and PZ|Y (z |y) = 0 for z = z ∈ Z. Let Q−1(z) ⊂ Y denote the preimage of z ∈ Z. We
name Q a sequential deterministic quantizer (SDQ) [21] if
it can be equivalently described by an integer set Λ =
{λ0, λ1, . . . , λM−1, λM } with λ0 = 0 < λ1 < · · · < λM−1 < λM = N in the way given below

 Q−1(1) 

= {y1, y2, . . . , yλ1 },

  

Q−1(2)

= {yλ1+1, yλ1+2, . . . , yλ2 },



...



 

Q−1(M ) = {yλM−1+1, yλM−1+2, . . . , yλM }.

We thus also name Λ an SDQ.
According to [20], there always at least exists a deterministic Q∗ for (1) that can maximize I(X; Z). Moreover, Q∗ is an optimal SDQ if PY |X further satisfies

PY |X (y1|0) ≥ PY |X (y2|0) ≥ · · · ≥ PY |X (yN |0) , (2)

PY |X (y1|1) PY |X (y2|1)

PY |X (yN |1)

where largest

if PY |X (·|1) = 0, we regard value. (However, Q∗ may not

= PY |X (·|0)
PY |X (·|1)
be an SDQ

∞ as when

the (2)

does not hold, in which case Q∗ may not be simply described

by the set Λ.) Note that after merging any two elements y, y ∈

Y with PY |X (y|0)/PY |X (y|1) = PY |X (y |0)/PY |X (y |1), the

resulting optimal quantizer is as optimal as the original one

[20], [21]. A general framework has been developed in [21] for

applying dynamic programming (DP) [22, Section 15.3] to find

an optimal SDQ among all SDQs to maximize I(X; Z). (The

condition of (2) is not required, and if it holds, the optimal

SDQ is an optimal DQ which can maximize I(X; Z) among

all quantizers from Y to Z.)

II. PRELIMINARIES

A. MIM Quantization of Binary-Input DMC

Due to the strong linkage between the MIM based channel quantization and the MIM based LDPC decoding message quantization, we first review the quantization of a binaryinput DMC. As shown by Fig. 1, the channel input X takes values from X = {0, 1} with probability PX (0) and PX (1), respectively. The channel output Y takes values from Y = {y1, y2, . . . , yN } with channel transition probability given by PY |X (yj|x) = P r(Y = yj|X = x), where x = 0, 1 and j = 1, 2, . . . , N . The channel output Y is quantized to Z which takes values from Z = {1, 2, . . . , M }. A well-known criterion for channel quantization [20], [21] is to design a quantizer Q∗ : Y → Z to maximize the MI between X and Z, i.e.

Q∗ = arg max I(X; Z)
Q

=

arg

max
Q

x∈X

,z∈Z

PX,Z

(x,

z)

log

PX,Z (x, z) PX (x)PZ (z)

,

(1)

B. MIM-LUT Decoder Design for Regular LDPC Codes
Consider a binary-input DMC. Denote the channel input by X which takes values from X = {0, 1} with equal probability, i.e., PX (0) = PX (1) = 1/2. Denote L as the DMC output which takes values from L = {0, 1, . . . , |L| − 1} with channel transition probability PL|X . Consider the design of a quantized message passing (MP) decoder for a regular (dv, dc) LDPC code, where dv and dc are the degrees of VNs and CNs, respectively. Denote R = {0, 1, . . . , |R| − 1} and S = {0, 1, . . . , |S| − 1} as the alphabets of variable-to-check (V2C) and check-to-variable (C2V) messages, respectively. Note that L, R, S and their related functions may or may not vary with iterations. We use these notations without specifying the associated iterations since after specifying the decoder design for one iteration, the design is clear for all the other iterations.
For the V2C message R ∈ R (resp. C2V message S ∈ S), we use PR|X (resp. PS|X ) to denote the probability mass function (pmf) of R (resp. S) conditioned on the channel input bit X. If the code graph is cycle-free, R (resp. S)

(a)

(b)

Fig. 2. Node update for mutual information-maximizing lookup table (MIMLUT) decoding, where the circle and square represent a variable and check node, respectively. (a) Check node update. (b) Variable node update.

DMC

Quantizer

DMC

Quantizer

Fig. 4. Quantization of a discrete memoryless channel (DMC), where the quantizer works exactly the same as the variable node update function Qv for the mutual information-maximizing lookup table (MIM-LUT) decoding shown by Fig. 2(b).

Fig. 3. Quantization of a discrete memoryless channel (DMC), where the quantizer works exactly the same as the check node update function Qc for the mutual information-maximizing lookup table (MIM-LUT) decoding shown by Fig. 2(a).

conditioned on X is independent and identically distributed (i.i.d.) with respect to different edges for a given iteration. In the following, we introduce the design of the MIM-LUT decoder [9]–[11] based on density evolution (DE) [6]. In particular, DE is carried out to construct the LUTs of the message mappings for decoding by tracking the pmfs PR|X and PS|X at each iteration. Note that although a cycle-free code graph is assumed in the design of the MIM-LUT decoder, it still works well on code graphs containing cycles as shown in [9]–[11].

For each iteration, we first design the update function (UF)

Qc : Rdc−1 → S

(3)

for the CN update, which is shown by Fig. 2(a). The MIMLUT decoding methods design Qc to maximize I(X; S). For easy understanding, we can equivalently convert it to the problem of DMC quantization, as shown by Fig. 3.

We assume PR|X is known, since for the first iteration, PR|X can be solely derived from the channel transition probability PL|X , and for the other iteration, PR|X is known after the design at VN is completed. The joint distribution PR|X of the incoming message R ∈ Rdc−1 conditioned on the channel
input bit X (i.e., the channel transition probability PR|X of

the DMC shown by Fig. 3) is given by [9]

PR|X (r|x) =

1 dim(r)−1

dim(r)

2

PR|X (ri|xi), (4)

x:⊕x=x i=1

where r = (r1, r2, . . . , rdc−1) ∈ Rdc−1 is a realization of R, dim(r) = dc − 1 is the dimension of r, x ∈ X is a realization of X, x = (x1, x2, . . . , xdc−1) ∈ X dc−1 consists of channel input bits corresponding to the VNs associated with incoming
edges, and ⊕x = x1 ⊕ x2 ⊕ · · · ⊕ xdc−1 with ⊕ denoting the addition in GF (2). Based on (4), we have



 

PR|X (r|0) ± PR|X (r|1) =

1 2

dim(r)−1 ×

 

dim(r) i=1

(PR|X

(ri

|0)

±

PR|X

(ri

|1)),

(5)

 PX|R(0|r) ± PX|R(1|r) =

  

dim(r) i=1

(PX

|R(0|ri

)

±

PX

|R(1|ri

)).

Given PR|X , the design of Qc is equivalent to the design of Q∗ in (1) by setting Y = Rdc−1 and Z = S. We can solve this
design problem by using the DP method proposed in [20], after
listing r in descending order based on PR|X (r|0)/PR|X (r|1) (see (2)). After designing Qc, the output message S is passed to the CN’s neighbour VNs, with PS|X being given by

PS|X (s|x) =

PR|X (r|x).

(6)

r∈Q− c 1(s)

We then proceed to design the UF

Qv : L × Sdv−1 → R

(7)

for the VN update, which is shown by Fig. 2(b). The MIMLUT decoding methods also design Qv to maximize I(X; R). For easy understanding, we can equivalently convert it to the problem of DMC quantization, as shown by Fig. 4.
The joint distribution PL,S|X of incoming message (L, S) ∈ L × Sdv−1 conditioned on the channel input bit X (i.e., the channel transition probability PL,S|X of the DMC shown by

Fig. 4) is given by [9]

dim(s)

PL,S|X (l, s|x) = PL|X (l|x)

PS|X (si|x), (8)

i=1

where l ∈ L is a realization of L, s = (s1, s2, . . . , sdv−1) ∈ Sdv−1 is a realization of S, dim(s) = dv − 1 is the dimension of s, and x ∈ X is a realization of X.
Given PL,S|X , the design of Qv is equivalent to the design of Q∗ in (1) by setting Y = L × Sdv−1 and Z = R.
We can solve this design problem by using the DP method proposed in [20], after listing (l, s) in descending order based on PL,S|X (l, s|0)/PL,S|X (l, s|1) (see (2)). After designing Qv, the output message R is passed to the VN’s neighbour CNs,
with PR|X given by

PR|X (r|x) =

PL,S|X (l, s|x).

(9)

(l,s)∈Qv−1 (r)

For each iteration, we can design the estimation function

Qe : L × Sdv → X

(10)

to estimate the channel input bit corresponding to each VN. The design of Qe can be carried out similarly to that of Qv. The main differences involved in the design lie in the aspect that i) the incoming message alphabet L×Sdv−1 is changed to L × Sdv ; and ii) the outgoing message alphabet R is changed to X . We thus ignore the details.
After completing the design of Qc, Qv, and Qe for all iterations, the design of the MIM-LUT decoder is completed. In general, |L| = |R| = |S| = 8 (resp. 16) is used for all iterations, leading to a 3-bit (resp. 4-bit) decoder. Given |L|, |R|, |S|, and the maximum allowed decoding iterations Imax, the design of the MIM-LUT decoder is determined by PL|X . As shown in the literature [9]–[11], PL|X depends on the choice of the noise standard derivation σd for designing the decoder parameters for an AWGN channel. We define the design threshold of an MIM-LUT decoder as

σ∗ = sup σd : I(Imax)(X; R) > 1 − ξ ,

(11)

where I(Imax)(X; R) is the value of I(X; R) computed after Imax iterations, and ξ is a preset small number (e.g. 10−2). Based on extensive simulations, by selecting σd = σ∗, the MIM-LUT decoder can achieve very good error rate performance across a wide range of signal-to-noise ratios (SNRs). However, the underlying reason remains an open problem.
Note that Qc, Qv, and Qe are stored as LUTs when implementing the MIM-LUT decoding. The sizes of the tables for Qc, Qv, and Qe are |R|dc−1, |L| · |S|dv−1, and |L| · |S|dv , respectively. Thus, a huge memory requirement may arise when the sizes of the tables are large in practice. To solve this problem, current MIM-LUT decoding methods [9]–[11] need to decompose Qc, Qv, and Qe into a series of two-input singleoutput LUTs. The decomposition can significantly reduce the cost of storage. However, it will degrade the performance of Qc, Qv, and Qe compared to the case without decomposition.

Fig. 5. Check node update for mutual information-maximizing quantized belief propagation (MIM-QBP) decoding. The part enclosed by the dash square corresponds to the update operation in the CN of Fig. 2(a).

III. MIM-QBP DECODING FOR REGULAR LDPC CODES
In this section, we propose a general design framework for the MIM-QBP decoder. This decoder follows the RCQ decoding architecture and can be implemented based only on simple mappings and additions. Moreover, it can handle all incoming messages at a given node (CN or VN) simultaneously without causing any storage problem. As a result, the MIM-QBP decoder can greatly reduce the memory consumption and avoid the error rate performance loss due to table decomposition compared to the MIM-LUT decoder. In the following, we specify the design principles of the RCQ parameters at CN and VN, respectively.

A. CN Update for MIM-QBP Decoding

As shown by Fig. 5, the CN update for the MIM-QBP decoding proceeds the following three steps (reconstruction, calculation, and quantization): First, an RF φc is used to map each V2C message symbol to a number of larger bit width which is called V2C computational message; second, we use a function Φc to combine all V2C computational messages together as defined by (14) to form the C2V computational message; third, we use an SDQ Γc to quantize the C2V computational message into the C2V message symbol. In this way, the UF Qc at the CN is fully determined by φc, Φc, and Γc. In the rest of this subsection, we show the principles of designing φc, Φc, and Γc so as to obtain a Qc that can maximize I(X; S).
1) Design principle of φc: We use an RF

φc : R → D

(12)

to map each V2C message realization r ∈ R to a computational message φc(r) in the computational domain D, where we set D = R (real number set) or D = Z (integer set) for different considerations. Let sgn(α) be the sign of α ∈ R, and
 −1 α < 0,  sgn(α) = 0 α = 0,
1 α > 0.

For r ∈ R, let

LLR(r) = log PX|R(0|r)/PX|R(1|r) .

A good choice for φc(r) based on empirical observation is

sgn(φc(r)) = sgn(LLR(r)),

|φc(r)|

∝

1 |LLR(r)|

.

(13)

Based on (13), we associate φc(r) to the channel input bit X in the following way: we predict X to be 0 if sgn(φc(r)) > 0 and to be 1 if sgn(φc(r)) < 0, while |φc(r)| indicates the unreliability of the prediction result (larger |φc(r)| means less reliability). Note that the intuition behind (13) comes from [1] and [4]. In [1] and [4], the function f (θ) = log((eθ +1)/(eθ − 1)) is adopted by the CN update for any positive LLR value θ, where f (θ) decreases monotonically with θ.

2) Design principle of Φc: For each V2C message realization r ∈ Rdc−1, we combine all the V2C computational
messages to form the C2V computational message as follows:

dim(r)

 dim(r)

Φc(r) = 

sgn(φc(ri))

|φc(ri)|. (14)

i=1

i=1

Similar to the CN update employed in [1] and [4], we predict

X to be 0 if sgn(Φc(r)) =

dim(r) i=1

sgn(φc(ri))

>

0,

and

to

be 1 if sgn(Φc(r)) < 0. Note that |Φc(r)| =

dim(r) i=1

|φc(ri

)|

indicates the unreliability of the prediction result. Prediction in

this way is consistent with the true situation shown by Fig. 3:

X is the binary summation of the channel input bits associated

with r, which is determined by sgn(φc(ri)), i = 1, 2, . . . , dc −

1. More incoming messages lead to more unreliability (i.e.,

larger dim(r) leads to larger |Φc(r)|. This is the reason why

we use |φc(r)| as the unreliability.). The calculation step by (14) results in a set of distinct values of Φc(r), ∀ r ∈ Rdc−1,

which we denote by

A = {a1, a2, . . . , a|A|}.

(15)

The elements in A are labelled to satisfy

a1 a2 · · · a|A|,

(16)

where is a binary relation on R defined by

α β ⇐⇒ sgn(α) > sgn(β) or (sgn(α) = sgn(β) and α < β)

to maximize I(X; S) among all SDQs. Here

λ1, λ2, . . . , λ|S|−1 are corresponding to the indices of |S| − 1 elements in A. Based on Λc, instead of using (6), we

can compute PS|X for the C2V message S in a simpler way

given by

λs+1

PS|X (s|x) =

PA|X (ai|x).

(19)

i=λs +1

Since the indices in Λc cannot be directly applied to decoding, we convert the index λi ∈ Λc (1 ≤ i ≤ |S| − 1) to the λi-th element aλi ∈ A, which leads to the threshold set (TS) Γc given by

Γc = {γi = aλi : 1 ≤ i ≤ |S| − 1}.

(20)

At this point, the UF Qc : Rdc−1 → S is fully determined by φc, Φc, and Γc in the following way as

 0 
Qc(r) = i

Φc(r) γ1, γi Φc(r) γi+1, 1 ≤ i ≤ |S| − 2,

|S| − 1 γ|S|−1 Φc(r), (21)

where is a binary relation on R defined by

α β ⇐⇒ α β or α = β, for α, β ∈ R.

Note that the storage complexity of Qc given by (21) is O(|R| + |S|) (O(|R|) for storing φc and O(|S|) for storing Γc), which is negligible since each element of φc and Γc is a small integer in practice. On the other hand, implementing the CN update shown by Fig. 5 for one outgoing message has complexity O(dc+ log2(|S|) ). In particular, computing Φc(r) has complexity O(dc) (binary operations mainly including additions), which allows a binary tree-like parallel implementation; meanwhile, mapping Φc(r) to S based on Γc has complexity O( log2(|S|) ) (binary comparison operations). Note that the simple implementation for mapping Φc(r) to S indeed benefits from the use of SDQ rather than the optimal DQ. Because if an optimal DQ is used to map Φc(r) ∈ A to S in (18), it requires an additional table of size |A| to store this optimal
DQ. This is the essential reason why we choose SDQs in the
quantization step.

for α, β ∈ R. For example, we have 1 2 0 −2 −1. Assuming Φc(r) = ai, we know from (16) that it is more likely to predict X to be 0 for smaller i and to be 1 for larger i. Thus, the listing order of (16) has a similar feature as that of (2). Let A be a random variable taking values from A. With (14), the pmf of A conditioned on the channel input bit X is

PA|X (ai|x) =

PR|X (r|x),

r∈Rdc −1 ,Φc (r)=ai

(17)

where 1 ≤ i ≤ |A|, and PR|X (r|x) is given by (4).

3) Design principle of Γc: Based on A and PA|X , we adopt the general DP method proposed in [21] to find an optimal SDQ

Λc = {λ0 = 0, λ1, . . . , λ|S|−1, λ|S| = |A|} : A → S (18)

B. VN Update for MIM-QBP Decoding
As shown by Fig. 6, the VN update for MIM-QBP decoding consists of the following three steps (reconstruction, calculation, and quantization): First, we use RF φv (resp. φch) to map each C2V (resp. channel) message symbol to a number of larger bit width which is called C2V (channelto-variable) computational message; second, we use a function Φv to combine all computational messages together as defined by (25) to form the V2C computational message; third, we use an SDQ Γv to quantize the V2C computational message into the V2C message symbol. In this way, the UF Qv at the VN is fully determined by φv, φch, Φv, and Γv. In the rest of this subsection, we show the principles of designing φv, φch, Φv, and Γv so as to obtain a Qv that can maximize I(X; R).

listing order of (27) has a similar feature as the listing order of (2). Let B be a random variable taking values from B. With (25), the pmf of B conditioned on the channel input bit X is

PB|X (bi|x) =

PL,S|X (l, s|x), (28)

(l,s)∈L×Sdv −1,Φv (l,s)=bi

where 1 ≤ i ≤ |B| and PL,S|X (l, s|x) is given by (8). 3) Design principle of Γv: Based on B and PB|X , we apply
the general DP method in [21] to find an optimal SDQ

Fig. 6. Variable node update for mutual information-maximizing quantized belief propagation (MIM-QBP) decoding. The part enclosed by the dash square corresponds to the update operation in the VN of Fig. 2(b).

1) Design principle of φv and φch: We use an RF

φv : S → D

(22)

to map each C2V message realization s ∈ S to φv(s) ∈ D, and use another RF

φch : L → D

(23)

to map the channel output message realization l ∈ L to φch(l) ∈ D. For s ∈ S, let

LLR(s) = log PX|S(0|s)/PX|S(1|s) .

For l ∈ L, let

LLR(l) = log PX|L(0|l)/PX|L(1|l) .

A good choice for φv(s) and φch(l) based on empirical observation is

φv(s) ∝ LLR(s), φch(l) ∝ LLR(l).

(24)

Based on (24), we associate φv(s) and φch(l) to the channel input bit X in the following way: X is more likely to be 0 (resp. 1) for larger (resp. smaller) φv(s) and φch(l).
2) Design principle of Φv: For each incoming message realization (l, s) ∈ L × Sdv−1, we combine the corresponding
incoming computational messages to form the V2C computa-
tional message by

dim(s)

Φv(l, s) = φch(l) +

φv (si ).

(25)

i=1

The channel input bit X is more likely to be 0 (resp. 1) for
smaller (resp. larger) Φv(l, s). The calculation step by (25) results in a set of distinct values of Φv(l, s), ∀ (l, s) ∈ L × Sdv−1, which we denote by

B = {b1, b2, . . . , b|B|}.

(26)

The elements in B are labelled to satisfy

b1 > b2 > · · · > b|B|.

(27)

Assuming Φv(l, s) = bi, we know from (27) that X is more likely be 0 (resp. 1) for smaller (resp. larger) i. Thus, the

Λv = {λ0 = 0, λ1, . . . , λ|R|−1, λ|R| = |B|} : B → R (29)

to maximize I(X; R) among all SDQs. Here λ1, λ2, . . . , λ|R|−1 are corresponding to the indices of |R| − 1 elements in B. Based on Λv, instead of using (9), we can compute PR|X for the outgoing message R in a simpler way given by

λr+1

PR|X (r|x) =

PB|X (bi|x).

i=λr +1

(30)

Since the indices in Λv cannot be directly applied to decoding, we convert the index λi ∈ Λv (1 ≤ i ≤ |R| − 1) to the λi-th element bλi ∈ B, which leads to the threshold set (TS) Γv given by

Γv = {γi = bλi : 1 ≤ i ≤ |R| − 1}.

(31)

At this point, the UF Qv : L × Sdv−1 → R is fully determined by φv, φch, Φv, and Γv in the following way as

 0 
Qv(l, s) = i

Φv(l, s) ≥ γ1, γi > Φv(l, s) ≥ γi+1, 1 ≤ i ≤ |R| − 2,

|R| − 1 Φv(l, s) < γ|R|−1. (32)

Note that the storage complexity of Qv given by (32) is O(|S| + |L| + |R|) (O(|S|) for storing φv, O(|L|) for storing φch, and O(|R|) for storing Γv), which is negligible since each element of φv, φch, and Γv is a small integer in

practice. On the other hand, implementing the VN update

shown by Fig. 6 for one outgoing message has complexity

O(dv + log2(|R|) ). In particular, computing Φv(l, s) has complexity O(dv), which allows a binary tree-like parallel im-
plementation; meanwhile, mapping Φv(l, s) to R based on Γv
has complexity O( log2(|R|) ). The simple implementation for mapping Φv(l, s) to R also benefits from the use of SDQ

rather than the optimal DQ. Because if an optimal DQ is used

instead, it in general requires an additional table of size |B| to

store this optimal DQ.

C. Remarks
For each decoding iteration, the design of Qe : L × Sdv → X for the MIM-QBP decoding is similar to that of Qv introduced in Section III-B. In particular, the same RFs φv and φch can be used for the design of Qe and Qv for a given decoding iteration. We thus ignore the details.
Assume |L|, |R|, |S|, Imax, and σd are given. According to the design framework proposed in this section, it is clear that

the calculation functions Φc and Φv are fixed, and the TSs Γc and Γv are fully determined after choosing RFs φc, φv, and φch. This results in a great convenience for designing an MIM-QBP decoder, as we only need to find good and even optimal RFs. Furthermore, we propose an efficient design for RFs in the next section.

IV. AN EFFICIENT DESIGN OF RECONSTRUCTION FUNCTIONS FOR MIM-QBP DECODER
We illustrated the general framework of the MIM-QBP decoding in Section III. The resulting MIM-QBP decoder works similarly to the quantized BP decoders in [6]–[8]. In fact, we borrow the terms “reconstruction function”, “computational domain”, “unreliability”, and “threshold set” from [6]–[8]. However, the works of [6]–[8] require manual optimization to design the decoder parameters. On the contrary, we propose an efficient way to systematically design practical MIM-QBP decoders in this section under the general framework of Section III. More specifically, we use DE to track the message pmfs, which are given by (17) and (19) for the CN update, and (28) and (30) for the VN update. Based on the evolved message pmfs at each iteration, we design the RFs according to the principles discussed in Section III. We further determine the TSs by the general DP method [21] to obtain the UFs of the MIM-QBP decoder, which aims to maximize I(X; S) for the CN update and I(X; R) for the VN update, respectively. The detailed design of the UFs is illustrated as follows.

A. Design of Qc at CN
The design of Qc for the CN update consists of designing the RF φc, the computation of A and PA|X , and determining the TS Γc, which corresponds to the three subsections under Section III-A, respectively.
1) Design of φc: We first derive the closed-form of the optimal RF φc∗ by setting D = R. Let g(r) = PX|R(0|r) − PX|R(1|r) for r ∈ R and g(r) = PX|R(0|r) − PX|R(1|r) for r ∈ Rdc−1. Note that PX|R can be easily derived from PR|X by Bayes rule with PX (0) = PX (1) = 1/2. For r ∈ R, let

φ∗c (r) =

sgn(g(r)) −sgn(g(r)) log(|g(r)|)

|g(r)| = 1, otherwise,

(33)

where satisfies
0 < dc < min | log(|g(r)|) − log(|g(r )|)| : r, r ∈ Rdc−1,
g(r) = g(r ), sgn(g(r)) = sgn(g(r )) = 0 . (34)
Here is a small number close to 0, which ensures the condition of (13) to be valid for the case of φc = φc∗. Meanwhile, is necessary for the following theorem.
Theorem 1: Consider the computational domain D = R and let φc = φc∗. Qc defined by (21) can maximize I(X; S) among all quantizers from Rdc−1 to S.
Proof: See Appendix A. Theorem 1 indicates that φ∗c is an optimal choice for φc in terms of maximizing I(X; S). Compared to the function

f (θ) = log((eθ + 1)/(eθ − 1)) used in [1] and [4] for the CN update, φc∗ is closely related to f (θ) in the sense that we have f (|LLR(r)|) = − log(|g(r)|) and sgn(LLR(r)) = sgn(g(r)) for r ∈ R. This indicates a close connection between the CN
update of the BP decoding and that of the MIM-QBP decoding by using the RF φc = φ∗c .
Corollary 1: Consider the computational domain D = R and φc = ηφc∗, where η is an arbitrary positive real number. Qc defined by (21) can maximize I(X; S) among all quantizers from Rdc−1 to S.

Proof: Corollary 1 can be proved similarly to the proof of Theorem 1.

Corollary 1 indicates that for D = R, the scaled version of φ∗c is also optimal for Qc in the sense of maximizing I(X; S). This provides a near-optimal solution for designing φc by scaling φ∗c for D = Z. In the following, we consider D = Z. Denote the maximum allowed absolute value of φc
by |φc|max. Suppose that there are at most qc bits allowed for
computing the additions by Φc, where one bit is needed for
representing the sign of each outgoing message. We have

|φc|max = (2qc−1 − 1)/dc

such that

dc i=1

φc(ri)

does

not

overflow.

Let

|φc∗|max = max{|φ∗c (r)| : r ∈ R, g(r) = 0},

where |φc∗|max > 0 holds for a general case. Inspired by Corollary 1, we scale φc∗ approximately by a factor around ηc = |φc|max/|φc∗|max to obtain φc : R → Z given by

φc(r) =

sgn(g(r)) max{1, round(ηc|φc∗(r)|)} |φc|max

g(r) = 0, g(r) = 0,

(35)

where round() returns the closest integer of a floating number.

In this case, we have |φc(r)| ≤ |φc|max, ∀ r ∈ R. Note that

for g(r) = 0, we make |φc(r)| ≥ 1 to ensure sgn(φc(r)) =

sgn(g(r)). In particular, for |g(r)| = 1, we have |φc(r)| = 1

since is close to 0. Moreover, for g(r) = 0, r has the most

unreliability since PX|R(0|r) = PX|R(1|r) = 1/2. In this

case, we use φc(r) = |φc|max according to the design principle

(13).

2) Computation of A and PA|X : By using the RF φc given by (35), the function Φc defined by (14) is capable of mapping Rdc−1 to Z and obtaining the C2V computational messages represented by qc-bit integers. Meanwhile, the integer set A and the message pmf PA|X need to be computed for the decoder design in the quantization step. Note that directly
adopting the computation in the ways of (15) and (17) can be a prohibitive task when |R|dc−1 is large. In the following, we propose a fast method to compute A and PA|X . The essential idea is using DP [22, Section 15.3] to handle the dc − 1 V2C message pmfs one by one, and carry out the computation with
the previous intermediate results and the current V2C message
pmf.

Starting from (4), for k ≥ 1 and R ∈ Rk, we have

PR|X (r|x) =

1 dim(r)−1

dim(r)

2

PR|X (ri|xi),

x:⊕x=x i=1

which denotes the joint distribution of k V2C messages
conditioned on the channel input bit. Denote the set of distinct values of Φc(r), ∀ r ∈ Rk by

Ak = {ak,1, ak,2, . . . , ak,|Ak|}.

Let Ak be a random variable which takes values from Ak. Motivated by (5), we define δk+(·) and δk−(·) as

δk±(ak,i)

=

(PR|X (r|0) ± PR|X (r|1))

r∈Rk ,Φc (r)=ak,i

=PAk|X (ak,i|0) ± PAk|X (ak,i|1).

Based on Φc given by (14), for α, β ∈ R, we define as a binary operator such that

α β = sgn(α)sgn(β)(|α| + |β|).

Then we have the following Proposition 1.

Proposition 1: For k = 1, we have

Ak = {φc(r) : r ∈ R}, and

δk±(ak,i) =

(PR|X (r|0) ± PR|X (r|1)). (36)

r∈R,φc (r)=ak,i

For k > 1, we have

Ak = {φc(r) ak−1,j : r ∈ R, ak−1,j ∈ Ak−1}, and

δk±(ak,i) =

1 2

(PR|X

(r|0)

±

PR|X

(r|1))δk±−1

(ak−1,j

).

r∈R,ak−1,j ∈Ak−1,

φc(r) ak−1,j =ak,i

(37)

meanwhile, we have

δk±(ak,i)

=

(PR|X (r|0) ± PR|X (r|1))

r∈Rk ,Φc (r)=ak,i

=
r∈Rk ,Φc (r)=ak,i

1 k−1 k

2

(PR|X (ru|0) ± PR|X (ru|1))

u=1

1

=

2 (PR|X (rk|0) ± PR|X (rk|1))×

rk ∈R

r∈Rk−1 ,

1 k−2 k−1

2

(PR|X (ru|0) ± PR|X (ru|1))

u=1

φc(r) Φc(r)=ak,i

1

=

2 (PR|X (rk|0) ± PR|X (rk|1))×

rk ∈R

(PR|X (r|0) ± PR|X (r|1))

r∈Rk−1 ,

1

φc(r) Φc(r)=ak,i

=

2 (PR|X (r|0) ± PR|X (r|1))

δk±−1(ak−1,j ).

r∈R

ak−1,j ∈Ak−1,

φc(r) ak−1,j =ak,i

This completes the proof.
Proposition 1 sets up the recursive computation rules, where for k > 1, Ak−1 and δk±−1 are the previous intermediate computation results, and they are used to compute Ak and δk± together with the k-th V2C message R and its pmf PR|X . According to Proposition 1, we can compute A1, δ1±, A2, δ2±, . . ., Adc−1, δd±c−1 sequentially, and obtain the message pmf PAk|X by
PAk|X (ak,i|0) = (δk+(ak,i) + δk−(ak,i))/2, PAk|X (ak,i|1) = (δk+(ak,i) − δk−(ak,i))/2.
Then, the integer set A and the message pmf PA|X are equal to Adc−1 and PAdc−1|X , respectively. We summarize the corresponding computation by Algorithm 1. Since |Ak−1| in line 7 of Algorithm 1 is upper-bounded by 2qc , the complexity of Algorithm 1 is O(dc2qc |R|).
3) Determining Γc: Based on A and PA|X , we can compute the optimal SDQ Λc by (18). Then we can obtain the pmf PS|X and the TS Γc given by (19) and (20), respectively. The UF Qc operates as (21) to compute the C2V messages, where we only
need to store φc and Γc for the CN update. The computational complexity is O(dc + dc log2(|S|) ) for one CN per iteration (including integer comparisons and additions with bit width
qc).

B. Design of Qv at VN

Proof: For k = 1, (36) holds obviously. For k > 1, we have
Ak = {Φc(r) : r ∈ Rk} = {φc(r) Φc(r) : r ∈ R, r ∈ Rk−1} = {φc(r) ak−1,j : r ∈ R, ak−1,j ∈ Ak−1};

The design of Qv for the VN update contains designing the RFs φv and φch, the computation of B and PB|X , and determining the TS Γv, which corresponds to the three subsections under Section III-B, respectively.
1) Design of φv and φch: We first derive the closed-form of the optimal RFs φ∗v and φ∗ch by setting D = R. For s ∈ S

Algorithm 1 Computation of A and PA|X Input: PR|X , φc, dc.

Output: A and PA|X . 1: Set Ak = ∅ and δk±(·) = 0 for k = 1, 2, . . . , dc − 1. 2: for r ∈ R do

3: A1 = A1 ∪ {φc(r)}. //See (36) 4: δ1±(φc(r)) += PR|X (r|0) ± PR|X (r|1). 5: end for

6: for k = 2, 3, . . . , dc − 1 do

7: for r ∈ R, ak−1,j ∈ Ak−1 do

8:

ak,i = φc(r) ak−1,j .

9:

Ak = Ak ∪ {ak,i}. //See (37)

10:

δk±

(ak,i

)+=

1 2

(PR|X

(r|0)

± PR|X

(r|1))δk±−1

(ak−1,j

).

11: end for

12: end for

13: for k = 1, 2, . . . , dc − 1 do

14: for ak,i ∈ Ak do

15:

PAk|X (ak,i|0) = (δk+(ak,i) + δk−(ak,i))/2.

16:

PAk|X (ak,i|1) = (δk+(ak,i) − δk−(ak,i))/2.

17: end for

18: end for

19: A = Adc−1. 20: PA|X = PAdc−1|X . 21: return A and PA|X .

and l ∈ L, let

φ∗v(s) = log(PS|X (s|0)/PS|X (s|1)), φc∗h(l) = log(PL|X (l|0)/PL|X (l|1)).

(38)

We can easily verify that the condition of (24) holds for φv = φ∗v and φch = φ∗ch. Moreover, (φ∗v, φ∗ch) is proved to be an optimal choice for (φc, φch) in terms of maximizing I(X; R)
by the following theorem.
Theorem 2: Consider the computational domain D = R. Let φv = φv∗ and φch = φ∗ch. Qv defined by (32) can maximize I(X; R) among all quantizers from L × Sdv−1 to R.

Proof: See Appendix B. Note that we have φv∗(s) = LLR(s) + log(PX (1)/PX (0)) and φ∗ch(l) = LLR(l) + log(PX (1)/PX (0)). With PX (1) = PX (0) = 1/2, φv∗ and φc∗h implies a close relation between the VN update of the BP decoding and that of the MIMQBP decoding by using φv = φ∗v and φch = φ∗ch. To realize fixed-point implementation, we further consider the following
corollary. Corollary 2: Consider D = R. Let φv = ηφv∗ and φch =
ηφ∗ch, where η is an arbitrary positive real number. Qv defined

by (32) can maximize I(X; R) among all quantizers from L× Sdv−1 to R.
Proof: Corollary 2 can be proved in a way similar to the
proof of Theorem 2.
Corollary 2 indicates that for D = R, the scaled versions of φ∗v and φ∗ch are also optimal for Qv with respect to maximizing I(X; R). This provides a near-optimal solution for designing φv and φch by scaling φ∗v and φ∗ch, respectively, for D = Z. In the following, we consider D = Z. Denote the maximum allowed absolute value of φv(·) and φch(·) by |φv,ch|max. Suppose that there are at most qv bits allowed for computing the additions by Φv. Then, |φv,ch|max can be taken as

|φv,ch|max = (2qv−1 − 1)/(dv + 1)

such that φch(l) +

dv i=1

φv (si )

does

not

overflow.

Let

|φv∗,ch|max = max({|φv∗(s)| : s ∈ S} ∪ {|φ∗ch(l)| : l ∈ L}).

Note that |φv∗,ch|max > 0 holds for a general case. Then, inspired by Corollary 2, we respectively scale φv∗ and φc∗h by a factor around ηv = |φv,ch|max/|φ∗v,ch|max to obtain φv : S → Z and φch : L → Z as follows

φv(s) = sgn(φv∗(s))round(ηv|φ∗v(s)|), φch(l) = sgn(φ∗ch(l))round(ηv|φc∗h(l)|).

(39)

In this case, we have |φv(s)| ≤ |φv,ch|max and |φch(l)| ≤ |φv,ch|max.
2) Computation of B and PB|X : By using the RFs given by (39), the function Φv defined by (25) is able to map L×Sdv−1 to Z and obtain the V2C computational messages represented by qv-bit integers. Similar to the case of the CN update, the integer set B and the message pmf PB|X need to be computed accordingly. However, directly computing B and PB|X by (26) and (28) requires large complexity if |L| × |S|dv−1 is large. In the following, we propose a fast method to compute B and PB|X . Similar to the case of calculating A and PA|X , the essential idea is adopting DP [22, Section 15.3] to handle the dv message pmfs (one channel-to-variable message pmf and dv − 1 C2V message pmfs) one by one, and carry out the computation with the previous intermediate results and the
incoming current message pmf.
Beginning with (8), for k ≥ 0, L ∈ L, and S ∈ Sk, we
have

dim(s)

PL,S|X (l, s|x) = PL|X (l|x)

PS|X (si|x),

i=1

which denotes the joint distribution of incoming message (L, S) ∈ L × Sk conditioned on the channel input bit X at a
VN. Denote the set of distinct values of Φv(l, s), ∀ l ∈ L, ∀ s ∈ Sk by
Bk = {bk,1, bk,2, . . . , bk,|Bk|},

In particular, for k = 0, we have

PL,S|X (l, s|x) = PL|X (l|x), and Φv(l, s) = φch(l).

Let Bk be a random variable which takes values from Bk.

Then we have the following Proposition 2. Proposition 2: For k = 0, we have

Bk = {φch(l) : l ∈ L}, and

PBk|X (bk,i|x) =

PL|X (l|x). (40)

l∈L,φch (l)=bk,i

For k > 0, we have

Bk = {φv(s) + bk−1,j : s ∈ S, bk−1,j ∈ Bk−1}, and

PBk|X (bk,i|x) =

PS|X (s|x)PBk−1|X (bk−1,j |x).

s∈S,bk−1,j ∈Bk−1, φv (s)+bk−1,j =bk,i

(41)

Proof: For k = 0, (40) holds obviously. For k > 0, we have
Bk = {Φv(l, s) : l ∈ L, s ∈ Sk} = {φv(s) + Φv(l, s) : s ∈ S, l ∈ L, s ∈ Sk−1} = {φv(s) + bk−1,j : s ∈ S, bk−1,j ∈ Bk−1};

meanwhile, we have

PBk|X (bk,i|x)

=

PL,S|X (l, s|x)

l∈L,s∈Sk,Φv (l,s)=bk,i k

=

PL|X (l|x) PS|X (si|x)

l∈L,s∈Sk,Φv (l,s)=bk,i

i=1 k−1

= PS|X (sk|x)

PL|X (l|x) PS|X (si|x)

sk ∈S

l∈L,s∈S k−1 , φv (sk)+Φv (l,s)=bk,i

i=1

= PS|X (sk|x)

PL,S|X (l, s|x)

sk ∈S

l∈L,s∈S k−1 , φv (sk)+Φv (l,s)=bk,i

= PS|X (s|x)

PBk−1|X (bk−1,j |x).

s∈S

bk−1,j ∈Bk−1, φv (s)+bk−1,j =bk,i

This completes the proof.
Proposition 2 sets up the recursive computation rules, where
we use Bk−1 and bk−1 as the previous intermediate computation results for k > 0 to compute Bk and bk together with the k-th incoming message S and its pmf PS|X . According to Proposition 2, we can compute B0, PB0|X , B1, PB1|X , . . ., Bdv−1, PBdv−1|X sequentially. Then, the integer set B and the message pmf PB|X are equal to Bdv−1 and PBdv−1|X , respectively. We summarize the corresponding computation
by Algorithm 2. Since |Bk−1| in line 7 of Algorithm 2 is upper-bounded by 2qv , the complexity of Algorithm 2 is O(dv2qv |S|).
3) Determining Γv: Based on B and PB|X , we can compute the optimal SDQ Λv by (29). Then we obtain the pmf PR|X and the TS Γv given by (30) and (31), respectively. The UF Qv
operates as (32) to compute the messages passed from VN to
CN, where we need to store φv, φch, and Γv for the VN update. The computational complexity is O(dv + dv log2(|R|) ) for one VN per iteration (including integer comparisons and
additions with bit width qv).

Algorithm 2 Computation of B and PB|X Input: φv, φch, PS|X , PL|X , dv.

Output: B and PB|X .

1: Set Bk = ∅ and PBk|X (·|x) = 0 for k = 0, 1, . . . , dv − 1 and for x = 0, 1.

2: for l ∈ L do

3: B0 = B0 ∪ {φch(l)}. //See (40)

4: PB0|X (φch(l)|x) += PL|X (l|x) for x = 0, 1. 5: end for

6: for k = 1, 2, . . . , dv − 1 do

7: for s ∈ S, bk−1,j ∈ Bk−1 do

8:

bk,i = φv(s) + bk−1,j .

9:

Bk = Bk ∪ {bk,i}. //See (41)

10:

PBk|X (bk,i|x) += PS|X (s|x)PBk−1|X (bk−1,j |x)) for

x = 0, 1.

11: end for

12: end for

13: PB|X = PBdv−1|X . 14: return B and PB|X .

C. Remarks
As illustrated by Section III-C, the design of Qe is quite similar to that of Qv. In particular, the same RFs φv and φch can be used for the design of Qe and Qv for a given decoding iteration, and the design of Qe has a complexity of O(dv2qv |S| + 22qv |X |) (for one decoding iteration). After the design of Qe, implementing Qe for one VN for one iteration during decoding has complexity O(dv), which is equal to the complexity of addition operations with qv bit widths.
After setting desirable maximum allowed bit widths qc for the CN update and qv for the VN update, we can obtain the near-optimal practical RFs φc by (35) and (φv, φch) by (39), which are scaled versions of their optimal counterparts φc∗, φv∗, and φc∗h, respectively. We remark that if qc and qv are sufficiently large (e.g., infinite precision is allowed), φc, φv, and φch coincide with their optimal counterparts, which can make the MIM-QBP decoder always maximize the MI between the outgoing message and the coded bit according to Corollaries 1 and 2. In this case, the MIM-QBP decoder works the same as the MIM-LUT decoder without table decomposition. On the other hand, for practical implementations with low precision, our design of the RFs based on (35) and (39) may not be optimal anymore. This is because the scaling becomes less accurate with the decrease of qc and qv. However, our design can still achieve good error rate performance according to extensive simulation results. Furthermore, we believe that the only way to obtain the optimal RFs with low precision is to

conduct the exhaustive search for φc, φv, and φch, which will be tedious and time-consuming.
At this point, it is clear that setting proper (|L|, |R|, |S|, Imax, qc, qv, σd) is sufficient for our systematic design of the RCQ parameters of an MIM-QBP decoder. Moreover, we can set (|L|, |R|, |S|, Imax, qc, qv) according to practical considerations, e.g., complexity. On the other hand, similar to the MIM-LUT decoder, the performance of the MIM-QBP decoder also depends greatly on the choice of σd. We also refer to σ∗ given by (11) as the design threshold of the MIM-QBP decoder. Our simulation results show that the MIM-QBP decoder designed at σd = σ∗ can achieve desirable error rate performance across a wide SNR range.
V. SIMULATION RESULTS
In this section, we evaluate the error rate performance of the proposed MIM-QBP decoders via Monte-Carlo simulations. Assume binary phase-shift keying (BPSK) transmission over the AWGN channel. We design the MIM-QBP decoder by fixing |L| = |R| = |S| = 8/16 (3-/4-bit decoder) for all iterations. We specify qc (bit width used for CN update), qv (bit width used for VN update), and σd (design noise standard deviation) for each specific example. At least 100 frame errors are collected for each simulated SNR.
Example 1: Consider the regular (6, 32) LDPC code taken from [23]. This code has length 2048 and rate 0.84. We use (qc, qv) = (10, 8)/(12, 10) to design the 3-/4-bit MIM-QBP decoder at σd = 0.5343/0.5417, respectively. The bit error rate (BER) and frame error rate (FER) of different decoders are illustrated by Fig. 7. We observe that our proposed 4bit MIM-QBP decoder outperforms both the 4-bit MIM-LUT decoder [11] and the floating-point BP decoder, with 10/30 iterations, respectively. Moreover, the 3-bit MIM-QBP decoder outperforms the 3-bit MIM-LUT decoder by about 0.15 dB, and even surpasses the floating-point BP decoder at the high SNR region. Note that the degradation of the performance of the floating-point BP decoder is mainly caused by the (8, 8) trapping sets in the code graph [24]. In contrast, our proposed MIM-QBP decoders are designed based on maximizing the mutual information, which may improve the ability to overcome certain trapping sets during the decoding process [9].
Example 2: Consider the regular (3, 6) LDPC code with identifier 8000.4000.3.483 taken from [25]. This code has length 8000 and rate 0.5. We use (qc, qv) = (9, 8)/(10, 10) to design the 3-/4-bit MIM-QBP decoder at σd = 0.8479/0.8660, respectively. The BER performance of different decoders is presented by Fig. 8. Note that the 3-/4-bit non-uniform QBP decoder [7] requires (qc, qv) = (9, 8)/(12, 10), respectively. In addition, the design of the corresponding decoders involves much manual optimization, while our proposed MIM-QBP decoders are designed systematically.
From Fig. 8, we observe that the 3-bit MIM-QBP decoder outperforms the 3-bit MIM-LUT decoder [11] by about 0.2 dB, and also performs better than the 3-bit non-uniform QBP decoder [7]. Moreover, the 4-bit MIM-QBP decoder shows almost the same performance as the 4-bit non-uniform decoder

Solid line: BER; dash line: FER

BER/FER

10-2

10-4

10-6

Floating-point BP 3-bit MIM-LUT

4-bit MIM-LUT

3-bit MIM-QBP

4-bit MIM-QBP 10-8

3.5 3.6 3.7 3.8 3.9 4 Eb/N0 (dB)

4.1 4.2 4.3

(a) A maximum of 10 iterations. Solid line: BER; dash line: FER

10-2 10-4

BER/FER

10-6 10-8

Floating-point BP 3-bit MIM-LUT 4-bit MIM-LUT 3-bit MIM-QBP 4-bit MIM-QBP

3.5 3.6 3.7 3.8 3.9 4 Eb/N0 (dB)

4.1 4.2 4.3

(b) A maximum of 30 iterations.

Fig. 7. BER and FER simulation results for the (6, 32) code [23] of length 2048 and rate 0.84. We set (qc, qv) = (10, 8) and (qc, qv) = (12, 10) for the 3-bit and 4-bit MIM-QBP decoders, respectively.

by using 2 less bits for the CN update. In addition, the 4-bit MIM-QBP decoder achieves better performance than the 4-bit MIM-LUT decoder [11], and approaches the performance of the floating-point BP decoder within 0.05 dB at the BER of 10−6.
VI. CONCLUSION
In this paper, we have proposed the MIM-QBP decoding for regular LDPC codes, which follows the RCQ decoding architecture. Notably, we have established the first complete and systematic design framework for the RCQ parameters with respect to that, setting proper (|L|, |R|, |S|, Imax, qc, qv, σd) is sufficient for the design of the optimal/near-optimal RCQ parameters which are able to maximize I(X; S) and I(X; R)

10-2

BER

10-4

Floating-point BP

3-bit non-uniform QBP

10-6

4-bit non-uniform QBP

3-bit MIM-LUT

4-bit MIM-LUT

3-bit MIM-QBP

4-bit MIM-QBP

10-8

1

1.2

1.4

1.6

1.8

2

2.2

Eb/N0 (dB)

Fig. 8. BER simulation results for the (3, 6) code (with identifier 8000.4000.3.483 in [25]) of length 8000 and rate 0.5. A maximum of 50 iterations is used. We set (qc, qv) = (9, 8) and (qc, qv) = (10, 10) for the 3-bit and 4-bit MIM-QBP decoders, respectively.

for sufficiently large qc and qv, respectively. In terms of error rate performance, simulation results showed that the MIMQBP decoder can always considerably outperform the state-ofthe-art MIM-LUT decoder [9]–[11]. Moreover, the MIM-QBP decoder has advantages over the floating-point BP decoder when
• the maximum allowed number of decoding iterations is small (generally less than 30), and/or
• the code rate is high, and/or • the operating SNR is high.

Proof of (42): We start the proof of (42) by showing some properties of Φc. For r ∈ Rdc−1, we have

dim(r)

g(r) =

g(ri)

i=1

according to (5). Let

h(r) = |{ri : 1 ≤ i ≤ dim(r), |g(ri)| = 1}|.

Then, we have

dim(r)

|Φc(r)| =

|φ∗c (ri)|

i=1 dim(r)

=−

log (|g(ri)|) + h(r)

i=1

dim(r)



= − log  |g(ri)| + h(r)

i=1

= − log (|g(r)|) + h(r) .

(43)

Meanwhile, we have

dim(r)

sgn(Φc(r)) =

sgn(φc(ri))

i=1 dim(r)

=

sgn(g(ri)) = sgn (g(r)) . (44)

i=1

We continue to prove (42). We have

PR|X (r|0)/PR|X (r|1) > PR|X (r |0)/PR|X (r |1) ⇒PX|R(0|r)/PX|R(1|r) > PX|R(0|r )/PX|R(1|r ) ⇒g(r) > g(r )

If sgn(g(r)) = sgn(g(r )), we have

APPENDIX A PROOF OF THEOREM 1

Note that Qc realizes the quantization from Rdc−1 to S with two steps: First, convert Rdc−1 to A based on Φc given by (14), and then quantize A to S based on the quantizer Λc given by (18). Therefore, to prove Theorem 1 (i.e., Qc can maximize I(X; S) among all quantizers from Rdc−1 to S), it
suffices to prove that the following two statements hold:

• The optimal quantizer from A to S can achieve the same maximum I(X; S) as the optimal quantizer from Rdc−1 to S.
• Λc is an optimal quantizer from A to S which can maximize I(X; S).

Let φc = φ∗c . For r, r ∈ Rdc−1, assume Φc(r) = ai ∈ A and Φc(r ) = ai ∈ A, where Φc and A are given by (14) and
(15), respectively. We first prove

PR|X (r|0) > PR|X (r |0) ⇒ i < i ,

(42)

PR|X (r|1) PR|X (r |1)

which is sufficient for proving the above two statements.

PR|X (r|0)/PR|X (r|1) > PR|X (r |0)/PR|X (r |1) ⇒sgn(g(r)) > sgn(g(r )) (⇒a)sgn(Φc(r)) sgn(Φc(r )) ⇒Φc(r) Φc(r ) ⇒(b)i < i ,
where (a) and (b) are based on (44) and (16). Otherwise, we have sgn(g(r)) = sgn(g(r )) = 0, leading to
PR|X (r|0)/PR|X (r|1) > PR|X (r |0)/PR|X (r |1) ⇒sgn(g(r))|g(r)| > sgn(g(r))|g(r )| ⇒ − sgn(g(r)) log(|g(r)|) < −sgn(g(r)) log(|g(r )|) ⇒(c)sgn(g(r))(− log(|g(r)|) + h(r) ) <
sgn(g(r))(− log(|g(r )|) + h(r ) ) (⇒d)Φc(r) < Φc(r ) ⇒Φc(r) Φc(r ) (⇒e)i < i ,
where (c), (d) and (e) are based on (34), (43), and (16), respectively. At this point, the proof of (42) is completed.
We are ready to utilize (42) to prove the aforementioned

two statements.
Proof of the first statement: Let N = |R|dc−1. We list the elements of Rdc−1 as {r1, r2, . . . , rN } such that for any 1 ≤ j < j ≤ N , assuming Φc(rj) = ai ∈ A and Φc(rj ) = ai ∈ A, it holds that i ≤ i . Based on (42), we have

PR|X (r1|0) ≥ PR|X (r2|0) ≥ · · · ≥ PR|X (rN |0) , (45)

PR|X (r1|1) PR|X (r2|1)

PR|X (rN |1)

and for any Φc(rj) = Φc(rj ) = ai ∈ A, we have

PR|X (rj |0) = PR|X (rj |0) = PA|X (ai|0) . (46) PR|X (rj |1) PR|X (rj |1) PA|X (ai|1)

The above (46) indicates that some elements in Rdc−1 with the same LLR value are merged into a single element in A. However, the optimal quantizer from A to S can achieve the same maximum I(X; S) as the optimal quantizer from Rdc−1 to S [20].
Proof of the second statement: On the other hand, with (42) and (45), we have

PA|X (a1|0) ≥ PA|X (a2|0) ≥ · · · ≥ PA|X (a|A||0) . (47)

PA|X (a1|1) PA|X (a2|1)

PA|X (a|A||1)

As a result, the optimal SDQ Λc : A → S given by (18) can maximize I(X; S) among all quantizers from A to S, as explained in Section II-A. The proof of Theorem 1 is completed.

APPENDIX B PROOF OF THEOREM 2

Note that Qv realizes the quantization from L × Sdv−1 to R with two steps: First, convert L × Sdv−1 to B based on
Φv given by (25), and then quantize B to R based on Λv given by (29). Therefore, to prove Theorem 2 (i.e., Qv can maximize I(X; R) among all quantizers from L × Sdv−1 to
R), it suffices to prove that the following two statements hold:

• The optimal quantizer from B to R can achieve the same maximum I(X; R) as the optimal quantizer from L × Sdv−1 to R.
• Λv is an optimal quantizer from B to R which can maximize I(X; R).

Let φv = φ∗v and φch = φ∗ch. For (l, s), (l , s ) ∈ L×Sdv−1, assume Φv(l, s) = bi ∈ B and Φv(l , s ) = bi ∈ B, where Φc
and A are given by (25) and (26), respectively. We first prove

PL,S|X (l, s|0) > PL,S|X (l , s |0) ⇒ i < i . (48) PL,S|X (l, s|1) PL,S|X (l , s |1)

which is sufficient for proving the above two statements.

Proof of (48): For (l, s) ∈ L × Sdv−1, according to (25),

we have

Φv (l,

s)

=

log


PL|X (l|0) 
PL|X (l|1)

dim(s) i=1

PS|X (si|0) PS|X (si|1)

 

.

(49)

Then, we have
PL,S|X (l, s|0) > PL,S|X (l , s |0) PL,S|X (l, s|1) PL,S|X (l , s |1)
(⇒f) PL|X (l|0) dim(s) PS|X (si|0) > PL|X (l|1) i=1 PS|X (si|1) PL|X (l |0) dim(s ) PS|X (si|0) PL|X (l |1) i=1 PS|X (si|1)
(g)
⇒Φv(l, s) > Φv(l , s ) (⇒h)i < i ,

where (f ), (g) and (h) hold because of (8), (49), and (27), respectively. At this point, the proof of (48) is completed.
We are ready to utilize (48) to prove the aforementioned two statements.
Proof of the first statement: Let N = |L| · |S|dv−1. We list the elements of L × Sdv−1 as {y1, y2, . . . , yN } such that for any 1 ≤ j < j ≤ N , assuming Φv(yj) = bi ∈ B and Φv(yj ) = bi ∈ B, it holds that i ≤ i . Based on (48), we have

PL,S|X (y1|0) ≥ PL,S|X (y2|0) ≥ · · · ≥ PL,S|X (yN |0) ,

PL,S|X (y1|1) PL,S|X (y2|1)

PL,S|X (yN |1)

(50)

and for any Φv(yj) = Φv(yj ) = bi ∈ B, we have

PL,S|X (yj |0) = PL,S|X (yj |0) = PB|X (bi|0) .

(51)

PL,S|X (yj |1) PL,S|X (yj |1) PB|X (bi|1)

The above (51) indicates that some elements in L × Sdv−1 with the same LLR value are merged into a single element in B. However, the optimal quantizer from B to R can achieve the same maximum I(X; R) as the optimal quantizer from L × Sdv−1 to R [20].
Proof of the second statement: On the other hand, with (48) and (50), we have

PB|X (b1|0) ≥ PB|X (b2|0) ≥ · · · ≥ PB|X (b|B||0) . (52)

PB|X (b1|1) PB|X (b2|1)

PB|X (b|B||1)

As a result, the optimal SDQ Λv : B → R given by (29) can maximize I(X; R) among all quantizers from B to R, as explained in Section II-A. The proof of Theorem 2 is completed.

ACKNOWLEDGMENT
This work was supported by National Natural Science Foundation of China (Grant Nos. 62101462 and 62201258), by Natural Science Foundation of Sichuan (Grant No. 2022NSFSC0952), by Singapore Ministry of Education Academic Research Fund Tier 2 T2EP50221-0036 and MOE2019-T22-123.

REFERENCES
[1] R. G. Gallager, “Low-density parity-check codes,” IRE Trans. Inf. Theory, vol. 8, no. 1, pp. 21–28, Jan. 1962.

[2] P. Chen, K. Cai, and S. Zheng, “Rate-adaptive protograph LDPC codes for multi-level-cell NAND flash memory,” IEEE Commun. Lett., vol. 22, no. 6, pp. 1112–1115, Jun. 2018.
[3] C. A. Aslam, Y. L. Guan, and K. Cai, “Edge-based dynamic scheduling for belief-propagation decoding of LDPC and RS codes,” IEEE Trans. Commun., vol. 65, no. 2, pp. 525–535, Feb. 2017.
[4] J. Chen, A. Dholakia, E. Eleftheriou, M. P. Fossorier, and X.-Y. Hu, “Reduced-complexity decoding of LDPC codes,” IEEE Trans. Commun., vol. 53, no. 8, pp. 1288–1299, Aug. 2005.
[5] Z. Zhang, L. Dolecek, B. Nikolic, V. Anantharam, and M. J. Wainwright, “Design of LDPC decoders for improved low error rate performance: quantization and algorithm choices,” IEEE Trans. Commun., vol. 57, no. 11, pp. 3258–3268, Nov. 2009.
[6] T. J. Richardson and R. L. Urbanke, “The capacity of low-density paritycheck codes under message-passing decoding,” IEEE Trans. Inf. Theory, vol. 47, no. 2, pp. 599–618, Feb. 2001.
[7] J.-S. Lee and J. Thorpe, “Memory-efficient decoding of LDPC codes,” in Proc. IEEE Int. Symp. Inf. Theory, Sep. 2005, pp. 459–463.
[8] J. Thorpe, “Low-complexity approximations to belief propagation for LDPC codes,” Oct. 2002. [Online]. Available: http://www.systems. caltech.edu/∼jeremy/research/papers/low-complexity.pdf
[9] F. J. C. Romero and B. M. Kurkoski, “LDPC decoding mappings that maximize mutual information,” IEEE J. Sel. Areas Commun., vol. 34, no. 9, pp. 2391–2401, Sep. 2016.
[10] J. Lewandowsky and G. Bauch, “Information-optimum LDPC decoders based on the information bottleneck method,” IEEE Access, vol. 6, pp. 4054–4071, Jan. 2018.
[11] M. Meidlinger, G. Matz, and A. Burg, “Design and decoding of irregular LDPC codes based on discrete message passing,” IEEE Trans. Commun., vol. 68, no. 3, pp. 1329–1343, Mar. 2020.
[12] D. J. C. MacKay, “Good error-correcting codes based on very sparse matrices,” IEEE Trans. Inf. Theory, vol. 45, no. 2, pp. 399–431, Mar. 1999.
[13] X. He, K. Cai, and Z. Mei, “On mutual information-maximizing quantized belief propagation decoding of LDPC codes,” in Proc. IEEE Global Commun. Conf., Dec. 2019, pp. 1–6.
[14] L. Wang, C. Terrill, M. Stark, Z. Li, S. Chen, C. Hulse, C. Kuo, R. D. Wesel, G. Bauch, and R. Pitchumani, “Reconstruction-computationquantization (RCQ): A paradigm for low bit width LDPC decoding,” IEEE Trans. Commun., vol. 70, no. 4, pp. 2213–2226, Apr. 2022.
[15] P. Mohr, G. Bauch, F. Yu, and M. Li, “Coarsely quantized layered decoding using the information bottleneck method,” in Proc. IEEE Int. Conf. Commun., Jun. 2021, pp. 1–6.
[16] P. Kang, K. Cai, X. He, and J. Yuan, “Memory efficient mutual information-maximizing quantized min-sum decoding for ratecompatible LDPC codes,” IEEE Commun. Lett., vol. 26, no. 4, pp. 733– 737, Apr. 2022.
[17] P. Kang, K. Cai, X. He, S. Li, and J. Yuan, “Generalized mutual information-maximizing quantized decoding of LDPC codes with layered scheduling,” IEEE Trans. Veh. Tech., vol. 71, no. 7, pp. 7258–7273, Jul. 2022.
[18] P. Kang, K. Cai, and X. He, “Design of mutual-information-maximizing quantized shuffled min-sum decoder for rate-compatible quasi-cyclic LDPC codes,” Electronics, vol. 11, no. 19, Oct. 2022.
[19] C. Lv, X. He, P. Kang, K. Cai, J. Xing, and X. Tang, “Mutual information-maximizing quantized layered min-sum decoding of QCLDPC codes,” in Proc. IEEE Global Commun. Conf., Dec. 2022, pp. 1–6.
[20] B. M. Kurkoski and H. Yagi, “Quantization of binary-input discrete memoryless channels,” IEEE Trans. Inf. Theory, vol. 60, no. 8, pp. 4544– 4552, Aug. 2014.
[21] X. He, K. Cai, W. Song, and Z. Mei, “Dynamic programming for sequential deterministic quantization of discrete memoryless channel,” IEEE Trans. Commun., vol. 69, no. 6, pp. 3638––3651, Jun. 2021.
[22] T. H. Cormen, C. E. Leiserson, R. L. Rivest, and C. Stein, Introduction to Algorithms: 2nd Edition. Cambridge, MA, USA: MIT Press, 2001.
[23] IEEE standard for information technology—telecommunications and information exchange between systems—local and metropolitan area networks-specific requirements part 3: Carrier sense multiple access with collision detection (CSMA/CD) access method and physical layer specifications, IEEE Std. 802.3an, Sep. 2006.
[24] W. Ryan and S. Lin, Channel codes: classical and modern. Cambridge University Press, 2009.

[25] D. J. C. MacKay, “Encyclopedia of sparse graph codes.” [Online]. Available: http://www.inference.phy.cam.ac.uk/mackay/codes/data.html

