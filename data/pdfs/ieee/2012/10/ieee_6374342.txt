A Spatially-Coupled Communication System for the Correlated Erasure Channel

Reza A. Ashrafi Department of Electrical and
Electronics Engineering Bog˘azic¸i University Istanbul, Turkey
Email: reza.ashrafi@boun.edu.tr

Ali E. Pusane Department of Electrical and
Electronics Engineering Bog˘azic¸i University Istanbul, Turkey
Email: ali.pusane@boun.edu.tr

Abstract—Low implementation complexity, low-delay, and close-to-optimal performance of spatially-coupled LDPC codes over a wide variety of channels make them a very good candidate for upcoming wireless communication standards. However, due to the nature of the sliding window decoding architecture that is used to decode these codes, the associated error performance is considerably degraded over channels with memory, such as the burst erasure channel. In this case, using a block interleaver to break up the effects of the channel memory is not a viable option, since a block interleaver introduces a large amount of delay to the communication system and therefore takes back many of the advantages of using a sliding window decoder.
In this paper, a reduced-delay communication system employing a convolutional interleaver is proposed. This scheme benefits from the inherent convolutional nature of the spatially-coupled codes and matches their structure with a low-delay convolutional interleaver. Thus, the resulting communication system exhibits very low overall delay. The performance of the proposed communication system is analyzed using the density evolution technique. Specifically, single-memory (minimum decoding delay), asymptotically (3,6)-regular spatially-coupled LDPC code ensembles are considered in the presence of burst erasures and the performance improvement of using a convolutional interleaver is demonstrated as a function of added interleaving delay in terms of iterative decoding thresholds.
I. INTRODUCTION
Spatially-coupled (SC) low-density parity-check (LDPC) coding is the newest member of the LDPC coding family that has gained considerable research interest during the recent couple of years. SC codes are in fact terminated versions of LDPC convolutional codes, which were first introduced in [1]. As a special case of LDPC convolutional codes, SC codes inherit very good characteristics, including simple code construction property, encoding and decoding simplicity, and superior performance compared to the underlying block LDPC codes. The terminated sections of the associated graph representations further give them a domino effect in the decoding performance.
Just like any type of LDPC codes, SC codes can be fully defined by their sparse parity-check matrix H representations. Their convolutional structure causes a ribbon-shaped form in their parity-check matrix. This ribbon-shaped structure allows us to perform sliding window decoding, which results in lowering the decoding delay when compared to block decoding,

Variable Nodes
Check Nodes

1

2

3

A

B

Copy

1

2

3

1

2

3

1

2

3

A

B

AB

A

B

Permute the edges

1

2

3

1

2

3

1

2

3

A

B

AB

A

B

Fig. 1. Copy-and-permute-operation.

since the window decoder does not need to wait for the entire codeword to arrive at the receiver before starting the decoding process. Furthermore, it has been proven that these code ensembles exhibit a “threshold saturation” phenomenon, which results in maximum-likelihood (ML) performance over the binary erasure channel (BEC) [2]. In this paper, we examine the performance of SC codes over correlated erasure channels as a first step in order to design communication systems that are capable of achieving high performance over practical wireless channels. We begin, in Section II, by reviewing the basic code construction technique for SC codes that will be employed throughout the paper. Sections III and IV cover the decoding problem and the density evolution technique, which is an analytical tool that is used to estimate the error performance in the asymptotic case. The main contribution of the paper is the proposed low-delay communication system, which is presented in Section V. Section VI consists of the error performance analysis of the proposed communication system and Section VII concludes the paper.
II. PROTOGRAPH BASED SC CODES
A protograph is a small bipartite graph from which a larger graph can be obtained by a simple copy-and-permute procedure [3]. First, the protograph is copied M times, and then the edges of the individual copies are permuted to obtain a single, large bipartite graph, referred to as the derived graph. The parameter M is called the expansion factor. If a protograph has NP variable nodes and MP check nodes, then the resulting graph consists of n = NP × M variable nodes and m = MP × M check nodes. Fig. 1 demonstrates

Authorized licensed use limited to: Yons9e7i 8U-n1iv-.4D6o7w3nl-o4a4de5d5o-n5/J1u2ne/$2341,2.00206 a©t 0290:3152:3I3EUETEC from IEEE Xplore. Restrictions apply.

this copy-and-permute operation for a simple example with NP = 3, MP = 2, and M = 3.
A protograph can be represented by an MP × NP biadjacency matrix, B, which is called the base matrix of a protograph. Each entry in the base matrix, Bi,j, i = 1, . . . , MP , j = 1, . . . , NP , is the number of edges between ci and vj. This is a non-negative integer and since parallel edges in protographs are allowed, it can be greater than one. SC codes can be constructed using protographs with base matrices in the form
L

 B0

 B1 B0

  

...

B1 . . .

B[L]

=

 Bms

...

...

  

Bms . . .







B0

 

,

(1)

B1

 

...

  

Bms

where ms is the code memory, each entry is a J × K submatrix, and for an asymptotically (J, K)-regular SC code family, the parameters J and K are obtained using

J = J/ gcd(J, K),

(2)

K = K/ gcd(J, K).

(3)

The corresponding protograph has NP = LK variable nodes and MP = (L + ms)J check nodes. Thus, the rate of the associated SC code can be defined as

RL

=

1

−

L

+ ms L

J K

= 1 − (1 + ms )(1 − R), L

(4)

where R = 1 − (J /K ) is the rate of unterminated LDPC convolutional code [4].
An example for the base matrix with B0 = [2 2] and B1 = [1 1] can be given as

2 2



1 1 2 2



 

112 2

 



B[L]

=

 







... ...



 

.

(5)













1 1 2 2

11

Another example with B0 = [1 2] and B1 = [2 1] is

1 2



2 1 1 2



 

211 2

 



B[L]

=

 







... ...



 

.

(6)







 

2 1 1 2

21

For a base matrix in this form, the copy-and-permute operation is equivalent to replacing each entry Bi,j, i = 1, . . . , MP , j =

Fig. 2. An illustration of window decoding at the second time instant.
1, . . . , NP , in the base matrix with a sum of Bi,j permutation matrices of size M × M . As a result, in (5) and (6), each of the entries 1 and 2 is replaced by an M × M matrix such that each column and row contains only 1 and 2 ones, respectively. The resulting matrix after this procedure represents the paritycheck matrix H of the code, with size (L + ms)M × 2LM .
Note that, due to the special structure presented in (5) and (6), the first and last ms rows of the base matrix, or, equivalently, the first and last M ms check nodes of the constructed SC code have lower weights than the rest of the graph. Therefore, we are not able to call these codes (J, K)regular anymore, even though a big portion of the parity-check matrix satisfies the regularity condition. Instead, SC codes with parity-check matrices that differ slightly from a regular parity-check matrix are referred to as asymptotically (J, K)regular, meaning that if we neglect the first and last portions of irregularities in H (corresponding to the case with L → ∞), the remaining matrix can be called (J, K)-regular. This slight irregularity of SC codes resulting from the termination of the underlying LDPC convolutional code has been shown to be the reason for SC codes having better BP decoding thresholds compared to corresponding block code ensembles [2].
III. WINDOW DECODING OF SC CODES
The ribbon-shaped parity-check structure of SC codes allows the use of a sliding window BP-based decoder. The window decoder breaks the BP decoding scheme down into a series of decoding steps. Specifically, the window decoder with a window size of W performs BP decoding over a portion of the codeword where the window consists of W × M rows and all connected columns. The symbols to be decoded within a window are referred to as the targeted symbols and the window slides over the next portion of the codeword when the targeted symbols are correctly decoded or a maximum number of iterations is reached [5]. The decoding process is successful when all codeword symbols are decoded correctly. Fig. 2 illustrates the concept of window decoding for a window size of 4 at the second decoding instant for an SC code with ms = 1, B0 = [2 2], B1 = [1 1], and L = 9.
Since the window decoder starts decoding as soon as it receives the codeword portion corresponding to the first window position, the decoding delay is considerably less than that of the standard block decoder that could also be used

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:35:33 UTC from IEEE Xplore. Restrictions apply.

V. A LOW-DELAY CONVOLUTIONAL-INTERLEAVER-BASED
COMMUNICATION SYSTEM

Fig. 3. Block diagram of a low-delay spatially-coupled communication system for correlated erasure channels.

to decode SC codes. This is indeed a significant reduction in decoding delay, since a block decoder would have to wait for the entire codeword to be received before starting the decoding algorithm. Note that the reduction in the decoding delay is further improved for more practical codeword lengths corresponding to larger L values, such as L ≥ 100.
The low-decoding delay of SC codes makes them suitable candidates in communication scenarios where low latency is the primary design criterion, such as real-time audio/video communication systems.

IV. DENSITY EVOLUTION ANALYSIS

In this section we briefly summarize the density evolution technique over the BEC. There is an analytic derivation of the density evolution technique over the BEC based on the beliefpropagation (BP) algorithm, which can be performed over a protograph [6].
The set of check nodes connected to variable node j and the set of variable nodes connected to check node i are denoted by C(j) and V (i), respectively. qi(→ l) j is the probability that a message from check node i to variable node j is an erasure at the lth iteration. Similarly, pj(l→) i represents the erasure probability of a message from variable node j to check node i at the lth iteration. The variable-to-check messages are initialized using
pj(0→) i = ,

where represents the channel erasure rate. The probabilities are then iteratively updated using

qi(→ l) j = 1 −

(1 − p(jl−→1i)),

(7)

j ∈V (i)\j

pj(l→) i = ·

(qi(l→ ) j ),

(8)

i ∈C(j)\i

until all-zero messages are achieved, or a maximum number of iterations lmax is reached.
The iterative decoding threshold ∗ is the largest value of such that decoding is successful. Since the BP decoding operation are split into portions for a window decoder, success is defined as reducing the erasure probability of the targeted symbols below some pre-determined threshold δ. In this paper, we set δ = 10−6.

As discussed in the previous sections, the ribbon-shaped structure of SC codes brings along several advantages in terms of code construction, encoding and decoding complexity, and low-delay decoding. However, it is the same structure that hurts the performance of SC codes over channels with memory, e.g., correlated erasure channels. In many practical applications, randomly constructed LDPC block codes allow the system designer to avoid using block interleavers, since the random connections of an LDPC graph guarantees that even consecutive erasures are spread over the entire codeword length. However, since the ribbon-shaped structure has a limited width in the SC code parity-check matrices, bursts of erasures severely degrade the error performance.
In the literature, this problem has been tackled in the way of designing a special code structure and choosing the code parameters in a way to disperse the effects of the erasure bursts [7]. However, this solution imposes several constraints in the code construction step that could otherwise be relaxed to optimize iterative decoding thresholds, code properties, etc. In this paper, we propose to separate the code design and erasure burst dispersion tasks and design a spatially-coupled communication system that has high error performance and design flexibility. The proposed communication employs a convolutional interleaver, which is used to disperse the erasure burst over a window that is wider than W so that the window decoder can observe the consecutive erasures of the channel as random. The corresponding block diagram is presented in Fig. 3.
The information sequence is first input to an SC encoder and the corresponding codeword is interleaved using a convolutional interleaver, whose structure will be described shortly. The output of the interleaver is then fed to the channel and is degraded by a burst of erasures starting at a random position. At the receiver side, the received sequence is deinterleaved and decoded using the window decoder. It is clear that the interleaving and deinterleaving steps cannot be achieved without causing some delay, but we will show next that this delay can be kept to a minimum while achieving a great improvement in error performance.
The block diagram of a convolutional interleaver that is composed of Λ delay lines is given in Fig. 4, where the λth delay line consists of 1 + (λ − 1) ∗ Ψ delay elements, λ = 1, 2, . . . , Λ [8]. The first delay element in each delay line is initialized with data, whereas the remaining ones are assumed to be initialized with zeros. The parameters of the convolutional interleaver depicted in Fig. 4 are used throughout the paper.
There are two practical issues faced when a convolutional interleaver is used to inerleave a finite-length sequence. The first one is the zeros present within the delay elements when the interleaver is initialized. The second issue relates to the end of the data sequence. Since there are unequal number of

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:35:33 UTC from IEEE Xplore. Restrictions apply.

delay elements on each delay line, some portion of data still remain in the delay elements, even after the last code symbol has been input to the interleaver. In order to flush this portion out of the interleaver, several dummy symbols (zeros) must be padded at the end of each input symbol stream. As a result, there will be several deterministic zero symbols among the output symbols that need to be filtered out.
As an example, assume that a codeword v = (v1, v2, . . . , v10) of length of 10 is the input to the convolutional interleaver given in Fig. 4. The output of the interleaver is then determined as
π(v) = (v1, 0, 0, v4, 0, 0, v7, v2, 0, v10, v5, 0, 0, v8, v3, 0, 0, v6, 0, 0, v9, 0, 0, . . .).
In order not to waste transmission resources by transmitting non-information-carrying symbols, zeros are punctured before transmitting the data over the channel. Therefore, the actual data to be transmitted becomes

Fig. 4. Schematic of the convolutional interleaver with Λ = 3 and Ψ = 2 at the first time instant. Initial values are set to zero. Output of this time instant will be v1, 0, 0.
TABLE I DELAY PROFILE OF PROPOSED CONVOLUTIONAL INTERLEAVER
clk v v T 1 v1 v1 0 2 v2 v4 2 5 v3 v7 2 8 v4 v2 0 9 v5 v10 1 10 v6 v5 0 10 v7 v8 0 10 v8 v3 0 10 v9 v6 0 10 v10 v9 0

v = π(v) = (v1, v2, v3, . . . , v10)
= (v1, v4, v7, v2, v10, v5, v8, v3, v6, v9).
We then observe that the deinterleaver to be used to recover v from v is
v = π−1(v ) = (v1, v4, v7, v2, v10, v5, v8, v3, v6, v9)
i.e. π = π−1 and the same interleaver can be used as a deinterleaver to recover the original codeword. This gives the system designer an advantage in the form of reduced memory requirements to store the interleaver and deinterleaver and also allows the reuse of the same hardware block to interleave and deinterlave data for two-way communications.
Table I gives the delay analysis of this interleaver. v and v denote original and interleaved symbol sequences, respectively. clk shows the current time instant and T is the delay introduced in current time instant. At the first time instant (clk = 1), the interleaver assigns the first input symbol to the first output symbol without any delay. At the second time instant (clk = 2), the fourth input symbol is assigned to the second output symbol, but since the second and third symbols of the input have not arrived yet, the interleaver waits for two more instants in order to access the fourth input symbol, which causes T = 2 units of delay and make the clock clk = 4. At the next time instant (clk = 5), the interleaver waits for two more instants to output the seventh input symbol. Once clk = 10 is reached, there is no additional delay.
In this simple example, the total delay (Ttotal) equals to 5 time instants and is actually upper bounded by the number of zero elements in the delay elements at the time of initialization, which is 6. For a larger code of length 80 (L = 40, M = 1, and K = 2) the total delay is indeed verified to be equal to 6. This helps us in demonstrating the delay introduced by the interleaver (and deinterlaver) is almost negligible when compared to the block length of the code.

VI. ERROR PERFORMANCE ANALYSIS OF THE PROPOSED COMMUNICATION SYSTEM

In this section, we first evaluate the asymptotic performance

of the protograph-based SC code ensemble with B0 = [2 2] and B1 = [1 1] under window decoding by the density evolution technique over the BEC. In our analysis, L is set

to 40 and therefore, the base matrix is of size 41 × 80 and the

maximum number of iterations is 1000. The window size is

chosen to be W = 4 and the target erasure probability for the target symbols is δ = 10−6.

The window decoding threshold of this code ensemble over

the standard BEC is calculated as

∗ wd

=

0.4843.

However,

we

are interested in calculating the window decoding threshold of

this code ensemble over the correlated erasure channel, where

we assume a single burst of erasures occurs at consecutive

intervals and the rest of the codeword is degraded by the

channel’s standard erasure rate. We consider erasure bursts

of length 4 with a burst erasure rate of B = 0.61. We sweep this burst along the codewords of length Np = 80, i.e., 4tuples (vi, vi+1, vi+2, vi+3), i = 1, . . . , 77 are erased with probability B = 0.6 and the remaining symbols are erased

with probability . Using the density evolution technique, we

look for the largest erasure rate that allows for successful

decoding.

The results are presented in Fig. 5, where the iterative

decoding threshold of the corresponding code ensemble is

demonstrated to be severely reduced due to the presence of

consecutive high probability erasures. The top plot in this

figure shows the iterative decoding threshold of the same code

ensemble when no bursts are present. It can be seen that

whereas this code ensemble can overcome channel erasures

with probability

∗ wd

=

0.4843

without

any

erasure

bursts,

1The burst length of 4 is defined on the protograph. When the expansion factor is chosen to be nontrivial (M > 1) for a practical code, this analysis corresponds to having a single burst of 4M high probability erasures.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:35:33 UTC from IEEE Xplore. Restrictions apply.

Fig. 5. Density evolution results for non-interleaved and interleaved data transmission over the correlated erasure channel. Burst and codeword lengths are chosen to be 4 and 80, respectively. Also provided is the window decoding threshold of the code ensemble when erasures occur randomly.

the threshold values fall in the region 0.2182 ≤ ≤ 0.2790. This is a large loss of performance due to the correlated nature of erasures. As can be seen from the figure, iterative decoding thresholds are dependent on whether a burst starts at an odd or even position of the codeword. A burst that starts at an odd position (e.g. first burst that happens on symbols 1 − 4), affects three check nodes connected to these variable nodes. Two of these check nodes are partially affected and the third one is completely erased (assuming that the erasures within a burst are highly probable). This will decrease the threshold since there is a loss of connectivity in the graph. This makes threshold to be is at its minimum. For the even start positions (e.g. second burst that happens on symbols 2 − 5), four check nodes are affected, but none of the check nodes are completely erased and this causes a slightly higher iterative decoding threshold compared to that of an odd start position. This behavior describes the alternating nature of the plot. Note that the burst covering the last symbols of a codeword does not severely affect the targeted symbols and that is why pseudo-high thresholds are observed on those burst locations. Furthermore, since it is more likely that a burst of erasures hits the middle regions of a codeword (for large L values), their threshold improvement is more concerning.
Fig. 5 also shows the iterative decoding thresholds of the same code ensemble when the proposed convolutional interlaver is employed. It is clear from the obtained iterative decoding thresholds that by employing a convolutional interleaver, the error performance of the communication system increases drastically. Although this gain comes at cost of introducing an interleaving delay, this delay is almost negligible when the codeword is long enough (corresponding to large L values).

VII. CONCLUSION
SC codes have excellent error performance over the BEC. Furthermore, they can be equipped with a low-complexity and low-delay sliding window decoding architecture, which makes them an interesting candidate for upcoming communication standards. On the other hand, their error performance shows a major degradation over the correlated erasure channel where bursts of erasures corrupt the transmitted codeword. We studied the asymptotic performance of such SC code families over the correlated erasure channel. A communication system employing a convolutional interleaver was proposed to mitigate the effects of erasure bursts as an alternative to using a block interleaver which would introduce a large amount of delay. Although employing a convolutional interleaver also introduces some delay, this delay is negligible for practical applications where codeword length is large enough. Through asymptotic performance analysis, we showed that this system, consisting of a sliding window decoder and a convolutional interleaver, provides a considerable amount of improvement in decoding performance while maintaining reduced latency communications.
ACKNOWLEDGMENT
This work was supported by the Scientific and Technological Research Council of Turkey (TUBITAK) under Grant No. 111E276.
REFERENCES
[1] A. Jime´nez-Feltstro¨m and K. Sh. Zigangirov, “Time-varying periodic convolutional codes with low-density parity-check matrix,” IEEE Trans. Inform. Theory, vol. IT-45, pp. 2181–2191, Sep. 1999.
[2] S. Kudekar, T. J. Richardson, and R. L. Urbanke, “Threshold saturation via spatial coupling: why convolutional LDPC ensembles perform so well over the BEC,” IEEE Trans. Inform. Theory, vol. 57, no. 2, pp. 803–834, Feb. 2011.
[3] J. Thorpe, “Low-density parity-check (LDPC) codes constructed from protographs,” JPL INP Progress Report, vol. 42-154, Aug. 2003.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:35:33 UTC from IEEE Xplore. Restrictions apply.

[4] A.R. Iyengar, M. Papaleo, P.H. Siegel, J.K. Wolf, A. Vanelli-Coralli, and G.E. Corazza, “Windowed decoding of protograph-based LDPC convolutional codes over erasure channels,” IEEE Trans. Inform. Theory, vol. 58, no. 4, pp. 2303 –2320, Apr. 2012.
[5] G. E. Corazza, A. R. Iyengar, M. Papaleo, P. H. Siegel, A. VanelliCoralli, and J. K. Wolf, “Latency constrained protograph-based LDPC convolutional codes,” in Proc. IEEE Intl. Symposium on Turbo Codes and Iterative Information Processing, Brest, France, 2010, pp. 6–10.
[6] M. Lentmaier and G. P. Fettweis, “On the thresholds of generalized LDPC convolutional codes based on protographs,” in Proc. IEEE Intl. Symposium on Inform. Theory, Austin, TX, USA, June 2010, pp. 709– 713.
[7] A. R. Iyengar, M. Papaleo, G. Liva, P. H. Siegel, J. K. Wolf, and G. E. Corazza, “Protograph-based LDPC convolutional codes for correlated erasure channels,” in Proc. IEEE Intl. Conference on Commun., Cape Town, South Africa, May 2010, pp. 1–6.
[8] S. Vafi and T. Wysocki, “Weight distribution of turbo codes with convolutional interleavers,” IET Communications, vol. 1, no. 1, pp. 71 –78, Feb. 2007.
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:35:33 UTC from IEEE Xplore. Restrictions apply.

