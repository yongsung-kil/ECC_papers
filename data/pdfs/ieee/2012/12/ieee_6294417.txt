3556

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 60, NO. 12, DECEMBER 2012

Performance Analysis of Iterative Decoding Algorithms with Memory over Memoryless Channels
Emil Janulewicz and Amir H. Banihashemi, Senior Member, IEEE

Abstract—Density evolution is often used to determine the performance of an ensemble of low-density parity-check (LDPC) codes under iterative message-passing algorithms. Conventional density evolution techniques over memoryless channels are based on the assumption that messages at iteration are only a function of the messages at iteration −1 and possibly the channel output. This assumption is valid for many algorithms such as standard belief propagation (BP) and min-sum (MS) algorithms. However, there are other important iterative algorithms such as successive relaxation (SR) versions of BP and MS, and differential decoding with binary message passing (DD-BMP) algorithm of Mobini et al., for which this assumption is not valid. The reason is the introduction of memory in these algorithms. In this work, we propose a model for iterative decoding algorithms with memory which covers SR and DD-BMP algorithms as special cases. Based on this model, we derive a Bayesian network for iterative algorithms with memory over memoryless channels and use this representation to analyze the performance of the algorithms using density evolution. The density evolution technique is developed based on truncating the memory of the decoding process and approximating it with a finite order Markov process, and can be implemented efficiently. As an example, we apply our technique to analyze the performance of DD-BMP on regular LDPC code ensembles, and make a number of interesting observations with regard to the performance/complexity tradeoff of DD-BMP in comparison with BP and MS algorithms.
The model presented in this paper is based on certain simplifying assumptions about the memory structure of iterative algorithms such as the existence of memory only at the output of variable nodes in the code’s Tanner graph rather than at both outputs of variable and check nodes. The Bayesian network framework introduced here however, can still be used to analyze the more general scenarios.
Index Terms—Iterative coding schemes, low-density paritycheck (LDPC) codes, iterative decoding algorithms with memory, memoryless channels, density evolution, Bayesian networks, belief propagation (BP), min-sum (MS), differential decoding with binary message passing (DD-BMP).
I. INTRODUCTION
L OW-DENSITY parity-check (LDPC) codes [3] are known to have good performance when decoded with iterative decoding algorithms, also known as message-passing
Paper approved by A. Graell i Amat, the Editor for Concatenated Codes and Iterative Decoding of the IEEE Communications Society. Manuscript received December 9, 2011; revised April 2 and May 26, 2012.
E. Janulewicz is with Ericsson Canada Inc., Montreal, Canada (e-mail: ejanulewicz@gmail.com).
A. H. Banihashemi is with the Systems and Computer Engineering Dept., Carleton University, Ottawa, Canada (e-mail: ahashemi@sce.carleton.ca).
A preliminary version of this paper was presented at the 2010 IEEE Information Theory Workshop, Cairo, Egypt, Jan. 6–8, 2010. The first author was with the SCE Department, Carleton University, at the time this work was performed.
Digital Object Identifier 10.1109/TCOMM.2012.082812.110838

algorithms [12]. An analytical tool called density evolution can be used to find the threshold of a particular code ensemble under a given iterative decoding algorithm [12]. The threshold is an asymptotic measure of performance and is defined as the worst channel parameter (e.g., largest noise variance) for which the probability of error still converges to zero as the number of iterations tends to infinity. Density evolution is also a powerful technique for constructing irregular LDPC codes through the optimization of the degree distributions [11]. In general, there is a large body of literature devoted to the derivation and applications of density evolution for a variety of iterative decoding algorithms, most notably for belief propagation (BP) and min-sum (MS) algorithms and their variants. To the best of our knowledge however, all the message-passing algorithms analyzed by density evolution in the literature are memoryless, i.e., the output message of a variable node (check node) at iteration is only a function of the input messages to that node at iteration ( − 1) and also of the initial message of the channel in the case of variable nodes. There exist however a number of iterative decoding algorithms, such as successive relaxation (SR) variants of BP and MS [4], [15], and DD-BMP (differential decoding with binary message-passing) [9], that have memory. In fact, the presence of memory in these algorithms improves the performance but makes the density evolution analysis much more complex. So far, the analysis of such algorithms has been limited to Monte Carlo simulations.
In performing density evolution and in the calculation of thresholds, one assumes a cycle-free (tree-like) graphical representation of the ensemble [12], and tracks the probability distribution of messages under this assumption. In the density evolution analysis of memoryless algorithms over memoryless channels, the cycle-free assumption along with the extrinsic nature [12] of message passing leads to the independence between all messages being processed at variable and check nodes. This, together with the memoryless nature of the algorithm, significantly simplifies the analysis, as one can derive the distribution of messages at iteration as a deterministic function of the distribution at iteration − 1. The complexity of density evolution in each iteration is thus independent of the iteration number , resulting in the total complexity of density evolution to scale only linearly with .
In this paper, we develop the framework for the density evolution analysis of iterative extrinsic message-passing algorithms with memory over memoryless channels. We first present a model for iterative decoding algorithms with memory which includes DD-BMP and SR algorithms as special cases.

0090-6778/12$31.00 c 2012 IEEE Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

JANULEWICZ and BANIHASHEMI: PERFORMANCE ANALYSIS OF ITERATIVE DECODING ALGORITHMS WITH MEMORY OVER MEMORYLESS . . .

3557

Based on this model, we employ the Bayesian network representation, via a directed acyclic graph (DAG), to capture the dependencies among different messages and memory contents

C1

M1( )

C2 M2( )

V

MV(

) →C

C

in a space with two dimensions: iteration number and the depth of the decoding tree d. The model presented here is based on certain assumptions about the memory structure of the decoding algorithms. Such assumptions however, are made to simplify the Bayesian network representation and consequently the analysis. In particular, the Bayesian network framework can still be used to analyze the more complex scenarios where such assumptions are relaxed.
Using the Bayesian network, we show that for algorithms with memory, although the incoming messages to a node along different edges are still independent (as a consequence of treelike decoding neighborhood and extrinsic message-passing), there exist dependencies between a message passed along a

Md(v)−1 Cdv−1
M0

(a) Memoryless

C1

M1( )

B( −1) D

C2 M2( )
Md(v)−1 Cdv−1

V X( )

ΨB

B( )

M0

(b) With Memory

MV(

) →C

C

ΨM

given edge at iteration and all the messages passed along that Fig. 1. Message passing between variable and check nodes in iterative edge at previous iterations < . Such dependencies cause the algorithms a)without and b)with memory.

complexity of density evolution to grow at least exponentially

with . We also derive the density evolution equations and use techniques to make them tractable. In particular, we introduce an approximation by truncating the memory of the decoding

noise (BIAWGN) channel, including ensemble thresholds. In Section VII, some concluding remarks are provided.

process and estimating it with a Markov process of finite order n (not an increasing function of ). This reduces the II. ITERATIVE DECODING ALGORITHMS WITH MEMORY

complexity of density evolution to linear in . As an example, we apply the proposed density evolution technique to DDBMP for a number of regular LDPC code ensembles and obtain the thresholds. By comparing these threshold values to those of BP and MS, a number of interesting observations with regard to the performance/complexity tradeoff of DDBMP in comparison with BP and MS algorithms are made. In addition to the above results, one of the main contributions of this work is to introduce the general framework for presentation and analysis of iterative decoding algorithms with memory. This can be used to design irregular codes for the existing algorithms or to devise new algorithms with improved performance/complexity tradeoffs.
A preliminary version of this work has appeared in [6]. Compared to [6], this paper contains much more details on density evolution, memory truncation and simulation results. In particular, while [6] only contains the general steps for the density evolution analysis and memory truncation, this paper presents the detailed derivation of the equations for such an analysis. Moreover, compared to [6], more simulation results on the convergence behavior of the density evolution analysis with truncated memory, as well as, more results on the comparison between the threshold values of DD-BMP and BP algorithms are provided here.
This paper is organized as follows: In Section II, we present the general model for iterative algorithms with memory and show that both DD-BMP and SR algorithms are special cases. Section III is devoted to the Bayesian network representation of iterative algorithms with memory and discussions on (conditional) dependencies among different random variables. In Section IV, the equations for density evolution are derived and the complexity of the analysis is discussed. In Section V, mem-

A. General Model

Fig. 1(a) shows a snapshot of the Tanner graph [14] of

an LDPC code at iteration for a memoryless decoding

algorithm, where variable and check nodes are represented

by circles and squares, respectively. Under cycle-free assump-

tion over memoryless channels and based on the principle

of extrinsic message passing [12], all incoming messages

M1( ), . . . , Md(v)−1 to node V are independent of each other and of the channel mesage M0 (dv is the variable node

degree). Moreover, all the incoming messages are identically

distributed.

The

outgoing

message

MV(

) →C

of node V

to node

C at iteration is thus a function of dv − 1 independent

identically distributed (i.i.d.) random variables and the channel

message. Similarly, the outgoing message of a check node is a

function of dc −1 i.i.d. variables corresponding to the extrinsic incoming messages, where dc is the check node degree. The distribution of a function of independent random variables

is relatively easy to find since the joint distribution of these

variables is the product of their marginal distributions. In this

case, one can recursively derive the distribution of messages

at iteration as a deterministic function of the distribution at

iteration − 1 with a complexity that is independent of [12].

In this work, we consider the existence of memory on

the links from variable nodes to check nodes. This can

be generalized to the case where memory exists in both

directions.1 Our model is shown in Fig. 1(b) on a snapshot

of the Tanner graph at iteration . Similar to Fig. 1(a), we

have a set of i.i.d. extrinsic incoming messages to a variable node V . The outgoing message from V is denoted by X( ).

The straight pass through line between V and C in Fig. 1(a) is now replaced by a memory unit whose content B( ) is

ory truncation is explained. Section VI is used to report and discuss the numerical results of DD-BMP applied to regular LDPC codes over the binary input additive white Gaussian

1In this case, the Bayesian network framework is still applicable for the analysis. The analysis however is more complex due to the larger number of random variables and connections (dependencies) in the network.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

3558

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 60, NO. 12, DECEMBER 2012

updated by ΨB(X( ), B( −1)), where ΨB is a deterministic function of X( ) and the content of the memory at iteration
− 1. In a more general case, B( ) can be a function of all previous memory contents B(0), . . . , B( −1).2 For the sake of

good performance of soft-decision algorithms, where the soft information is stored in edge- or node-based memories. In the former case, studied in this paper, the variable node map, following the model of Fig. 1(b), is defined by:

simplicity however, we limit ourselves in this work to the

case

described

in

Fig.

1(b).

The

incoming

message

MV(

) →C

to

node C from V is obtained by ΨM (B( )) as a deterministic

function of the memory content B( ). It is important to note

that while the message X( ) is a function of independent

random

variables,

the

outgoing

message,

MV(

) →C

,

is

a

function

of dependent random variables X( ) and B( −1). For the check

nodes however, since no memory exists at the output links to

the variable nodes, we just need to find the distribution of a

function of dc − 1 i.i.d. extrinsic incoming messages. This is

similar to the case for memoryless algorithms. In the following

therefore, our focus will be on the link from variable nodes

to check nodes and on finding the distribution of B( ) and

MV(

) →C

.

B( ) = ΨB B( −1), X ( ) = B( −1) + X ( ) ,

(2)

MV(

) →C

=

ΨM

B( )

= sgnr(B( )) ,

where sgnr(x) = 1 for x > 0, and = −1 for x < 0. For x = 0,

sgnr(x) takes +1 or -1 randomly with equal probability. In

(2), X( ) = ΨV (M1( ), · · · , Md(v)−1), which for the BIAWGN

channel reduces to X( ) =

dv −1 i=1

Mi(

).

The

check

node

operation, performed on extrinsic binary messages from the

alphabet space {+1, −1}, is simply a product.

Both the variable and the check node operations (particu-

larly the latter) are simpler for DD-BMP compared to BP and

MS algorithms. In addition, and probably more importantly,

the binary message-passing of DD-BMP compared to the soft

message-passing of BP and MS is advantageous. In a VLSI

B. SR and DD-BMP Algorithms as Special Cases

implementation of the decoder, this advantage translates to the

We consider the application of a binary LDPC code over a binary-input output-symmetric memoryless channel, where coded bits 0 and 1 are mapped to channel input symbols +1 and −1, respectively. We assume that the LDPC code has block length N and is described by a full-rank M × N partiy check matrix H. (Parameters M and N represent the number of check nodes and variable nodes in the corresponding Tanner graph, respectively.) For the sake of simplicity, we only consider regular (dv, dc) codes in this paper, where dv and dc are the variable and check node degrees, respectively.
1) SR Algorithms: Any standard memoryless iterative algorithm, such as BP or MS, can be turned into an SR algorithm by proper introduction of memory [4], [15]. SR algorithms can be performed in different message domains. In this work, we assume log-likelihood ratio (LLR) domain for messages (SRLLR). Under this assumption, SR algorithms are symmetric [12]. Consider a memoryless iterative algorithm with variable and check node maps denoted by ΨV and ΨC, respectively. Based on the model of Fig. 1(b), the SR version is defined by

number of interconnection wires for DD-BMP to be smaller by a factor of q compared to a q-bit quantized BP or MS decoder. This in turn simplifies the routing of the wires and reduces
the chip area and the power consumption of the decoder significantly.
In both SR-LLR and DD-BMP, the memory content B(0) is initialized by a function of the channel (initial) message M0. In particular, for SR-MS-LLR and DD-BMP over BIAWGN channel, the memory of the edge connecting the variable node vi and check node cj is initialized by Bv(0i,)cj = yi, for i = 1, ..., N , and j = 1, ..., M , such that Hji = 1, where yi is the channel output corresponding to vi.
In a practical implementation of iterative algorithms, the
size of the memory is finite and quantization is used to represent the messages with a finite number of bits. In this
case, fixed-point arithmetic is used to implement different mappings and functions. Simple modifications in (1) and (2)
can reflect such constraints [5].

the following variable node map (the check node map remains intact):

B( ) = ΨB B( −1), X ( ) = (1 − β) B( −1) + βX ( ) ,

MV(

) →C

=

ΨM

B( )

= B( ) ,

(1) where X( ) = ΨV (M0, M1( ), · · · , Md(v)−1). In (1), β is called the relaxation factor, and can be optimized for the best

performance. The optimal value of β is usually in the interval

(0, 1) [15]. For the effect of β on the performance of SR

C. Symmetry of the Decoder and Error Probability
The analysis of iterative decoders is greatly simplified assuming that both the channel and the decoder are symmetric. The decoder symmetry conditions for variable and check nodes are defined in [12]. All the decoders considered in this work are assumed to be symmetric. In particular, the variable node symmetry condition has some implications on the choices of the mappings ΨB and ΨM : ΨB should be sign inversion invariant [12], and ΨM (−x) = −ΨM (x). As it can be seen in (1) and (2), both conditions are satisfied for SR-

algorithms, the interested reader is referred to [4], [15].

LLR and DD-BMP algorithms.

2) DD-BMP: Differential decoding with binary message With both the channel and the decoder being symmetric,

passing (DD-BMP) was introduced in [8], [9], as an attractive we can assume, without loss of generality, that the all-zero

alternative to purely hard-decision algorithms. This algorithm codeword is transmitted. In this case, the average fraction of

combines the simplicity of binary message-passing with the incorrect messages passed at iteration from variable nodes

to check nodes is calculated by

2The Bayesian network in this case will have new edges that represent the

dependency of B( ) on all the previous memory contents (and not just on B( −1)).

Pe(

)

=

P (B(

)

<

0)

+

1 2

P

(B(

)

=

0)

,

(3)

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

JANULEWICZ and BANIHASHEMI: PERFORMANCE ANALYSIS OF ITERATIVE DECODING ALGORITHMS WITH MEMORY OVER MEMORYLESS . . .

3559

where P (A) is used to denote the probability of an event

A. We refer to Pe( ) in (3) as the probability of bit error

at iteration . One should note that, since ΨM (·) is an odd

function, Equation (3) is in fact equal to the probability that a

B1

message sent from variable nodes to check nodes is negative.

V
III. BAYESIAN NETWORK REPRESENTATION OF

ITERATIVE DECODING ALGORITHMS WITH MEMORY

B

A. Bayesian Networks and Conditional Independence
A Bayesian network is a graphical model that represents a set of random variables and their statistical (in)dependence via a directed acyclic graph (DAG). In this work, we use a Bayesian network to represent the dependencies among different messages and memory contents of an iterative algorithm with memory. These dependencies are studied as a function of both the iteration number and the depth of the decoding tree d based on the model of Fig. 1(b). In fact, as we will see later, it is the conditional independence among different random variables that plays a crucial role in simplifying the derivation of the desired distributions.
We start by some basic definitions and notations. We use upper case, e.g., X, to denote a random variable and lower case, e.g., x, to denote a realization of a random variable. We consider a DAG G as an ordered pair G = (V, E), where V is the set of vertices (nodes), and E is the set of directed edges in G. Each node represents a random variable, and directed edges imply conditional (in)dependence among the random variables as explained in the following. Throughout this paper, we use the terms “nodes” and “random variables” interchangeably. Nodes W and U are referred to as the head and the tail of the directed edge (U, W ). We also call U a parent of W , and W a child of U . The sets of parents and children of a node W ∈ V are denoted by pa(W ) and ch(W ), respectively. We refer to the union of the sets ch(W ), ch(ch(W )), . . . as the descendants of W and denote them by de(W ). For a DAG, it is clear that the sets pa(W ) and de(W ) are disjoint. In a DAG, we define an undirected path between a node U and a node W as a sequence of nodes starting with U and ending with W such that successive nodes are connected by an edge.
The conditional independence between two sets of random variables X and Y given a third set Z is defined by

C
Fig. 2. Tree-like neighborhood of the edge (V, C) in a regular (3,4) code (the shaded circles will disappear for a memoryless decoding algorithm).
a general conditional independence statement X ⊥⊥ Y|Z is implied by a given DAG, where X , Y and Z are disjoint sets of random variables, we need the following definitions:
Definition 1 (undirected path blocked). An undirected path between two distinct nodes is blocked by a set of nodes X if the path includes a node U such that either: (a) U has headto-head arrows along the path (−→ U ←−), and neither U nor any of its descendants is in X , or (b) U does not have head-to-head arrows along the path and U is in X .
Definition 2 (d-separation). Given a set of nodes Z, if every undirected path from any node in X to any node in Y is blocked by Z, then X is said to be d-separated from Y by Z.
Fact 1 [7]: In a DAG with disjoint sets of nodes X , Y and Z, if Z d-separates X from Y, we have X ⊥⊥ Y|Z.
In fact, it can be seen that characterization of (5) for a joint distribution is equivalent to the satisfaction of all the conditional independence properties implied by the DAG through d-separation [7].
The notion of “d-separation” can be better understood and more easily examined through what is known as Bayes’ Ball algorithm. The reader is referred to [13] and [5] for the description of the algorithm and some examples.
B. Bayesian Networks of Iterative Decoders with Memory

X ⊥⊥ Y|Z ⇐⇒ P (X , Y|Z) = P (X |Z)P (Y|Z) , (4)
where P (X ) and P (X |Z) are the marginal distribution of X and the conditional distribution of X given Z, respectively. Marginal independence between X and Y is denoted by X ⊥⊥ Y and is defined as in (4) where Z = ∅ and P (X |∅) := P (X ).
A DAG G(V, E) is a Bayesian network with respect to the set of random variables V = {V1, . . . , VN }, if the joint probability distribution of V can be factored as follows:

In the sequel, we assume a practical (quantized) implemen-

tation of iterative decoders. The random variables representing

the messages and memory contents are thus discrete, and we

use the notation P (·) to denote their probability mass function.

Consider the of the directed

directed edge −→e

neighborhood Nd(→ −e ) of depth d [12] for a (dv, dc) Tanner graph.

Assuming a cycle-free graph, the neighborhood is tree-like. Fig. 2 shows an example of N2(−→e ), where → −e = (V, C), for a (3, 4) Tanner graph. Memory elements only appear at even

N
P (V) := P (V1, . . . , VN ) = P (Vi|pa(Vi)) , (5)
i=1
or in other words, if for any W ∈ V, we have
W ⊥⊥ V\{W ∪ de(W ) ∪ pa(W )} | pa(W ) . (6)
To go beyond the basic conditional independence relations given by the local constraints (6), and to ascertain whether

depths on the tree, and they are shown by shaded circles.
We use the notation Bd/2 to denote the memory elements that appear in Nd((V, C))\Nd−2((V, C)). It is easy to see |Bi| = {(dv−1)(dc−1)}i, for i = 1, 2, . . .. In Fig. 2, |B1| = 6. Notation Bi( ) is used to denote Bi at iteration .
Based on the principle of extrinsic message-passing, one can see that X( ) is a deterministic function of B1( −1) and M0. Moreover, as it can be seen in Fig. 1(b), B( ) is a function

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

3560

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 60, NO. 12, DECEMBER 2012

d B(0) M0 M0,1 B1(0) M0,2 B2(0) M0,3 B3(0)

B(1)

X (1)

B1(1)

B2(1)

where the sum is taken over the sample space of M0,0→

which has |S | 1−((dv−1)(dc−1)) +1

O

1−(dv −1)(dc −1)

elements. Note that the

number of elements, and thus the computational complexity,

grows doubly exponentially with . A more efficient approach to compute P B( ) is:

B(2)

X (2) B1(2)

P (B( ) = b) =

P (B( −1) = b , X( ) = x), ∀b ∈ SB,

B(3)

X (3)

Fig. 3. Bayesian network based on the model of Fig. 1(b).

(b ,x)∈SB×SX :b=ΨB (b ,x)
(8) where SX is the sample space of X( ). In order to efficiently derive the joint distribution P B( −1), X( ) of dependent
random variables B( −1) and X( ), we are interested in finding a set of random variables Ω so that B( −1) ⊥⊥ X( )|Ω .

of B( −1) and X( ). In addition, Bi( ) depends on Bi( −1) and Bi(+−11). The dependence on Bi( −1) is due to the fact that the memory content in each iteration is updated as a deterministic
function of its content in the previous iteration. The dependence on Bi(+−11) is a result of the fact that at any iteration, the content of memory elements at a certain layer of the decoding
tree is a deterministic function of the content of the memory
elements at the next layer of the tree in the previous iteration.
The Bayesian network corresponding to these relationships is
given in Fig. 3 in a two-dimentional space with one dimension
representing the iteration number and the other representing
the depth of the decoding tree d. In this figure, M0,i is defined as the initial messages corresponding to the variable nodes in N2i((V, C))\N2i−2((V, C)) (see Fig. 2). The dashed edges emanating from M0, M0,1, M0,2, . . . would disappear if X( ) is not a function of M0 which is, e.g., the case for DD-BMP.

The set of random variables Ω should also have a small
sample space to reduce the computational complexity. More importantly, Ω should be chosen so that we can compute P B( −1)|Ω , P X( )|Ω , and P (Ω ) using an iterative
approach, so that we may reuse the distributions computed in iteration to compute distributions at iteration + 1. In the following, we demonstrate that the set X(1→ ) =
X(1), X(2), ..., X( ) , ≥ 1 is a good choice for Ω .
First, we use Fact 1 to establish that

B(0) ⊥⊥ X (1) , and B( ) ⊥⊥ X ( +1)|X (1→ ) ,

(9)

and thus to obtain P B( −1), X ( )

=

P B( −1)|X (1→ −1) P X ( )|X (1→ −1) ×

x(1→ −1)

IV. DENSITY EVOLUTION

Based on (3), to obtain the error probability Pe( ) at each iteration , we need to compute P B( ) . It is easy to find

the distributions of B(0) and B(1). To find P (B(0) = b) for b ∈ SB, where SB is the sample space of B( ), one needs to

find the distribution of the channel output given that the input

is +1, and then find the probability of the channel output

being in the range that is mapped to the memory content b. The calculation of P B(1) is also straight-forward as B(1)

is a function of two independent random variables B(0) and X(1). The details of these calculations for DD-BMP over the

BIAWGN channel are given in [5].

To simplify the analysis for > 1, we assume that

for the algorithm under consideration, the dashed edges

in Fig. 3 do not exist.3 From Fig. 3, one can see that B( ) is a function of i.i.d. random variables in the set

M0,0→ := {M0, M0,1, . . . , M0, }. There are a total of

i=0 ((dv − 1)(dc − 1))i

=

1−((dv−1)(dc−1)) +1 1−(dv −1)(dc −1)

random

variables in M0,0→ , all with the same sample space SO. One

can use the following “naive” approach to compute P B( ) :

P B( ) =

P B( )|M0,0→ = m P (M0,0→ = m),

m∈SM0,0→
(7)

3For the case where the dashed edges do exist, the analysis is similar but more tedious and can be found in [5].

P X (1→ −1)

=

P B( −1)|X (1→ −1) P X (1→ ) .

x(1→ −1)

(10)

Note that unlike the naive approach, the number of random variables in Ω = X(1→ ) increases only linearly with .

From equation (10), one can see that computing P B( ) requires finding the conditional distribution P B( −1)|X(1→ −1) and the joint dstribution P X(1→ ) .

In the following, we show how these distributions can be

computed efficiently.

• Calculation of P B( )|X(1→ )

P B( )|X (1→ )

=

P B( )|X (1→ ), B( −1) P B( −1)|X (1→ )

b( −1)

=

P B( )|X ( ), B( −1) P B( −1)|X (1→ −1) ,

b( −1)
≥2,

(11)

where at the last step, we have used

B( )

⊥⊥

X (1→ −1)|{X ( ), B( −1)} and

B( −1) ⊥⊥ X( )|X(1→ −1), both based on Fact 1.

Note that in (11), P B( )|X( ), B( −1) is equal to

either 0 or 1 as B( ) is a deterministic function of

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

JANULEWICZ and BANIHASHEMI: PERFORMANCE ANALYSIS OF ITERATIVE DECODING ALGORITHMS WITH MEMORY OVER MEMORYLESS . . .

3561

B( −1) and X ( ), and P B( −1)|X (1→ −1) , ≥ 2,

is obtained from the previous iteration with the initial

distributions

P b(0) B(1)|B(0), X (1) P B(0)

for

= 1, and P B(0) for = 0. Also noteworthy is that

P B( )|X( ), B( −1) is independent of and needs to

be computed only once.

• Calculation of P X(1→ )

Consider the extrinsic incoming messages M1(→) dv−1 = M1( ), M2( ), ..., Md(v)−1 to a variable node V at it-

eration , (see Fig.

where 1(b)).

each The

Mi( ) has variables

the Mi(

sample space SM ) are i.i.d.. Since

X( ) = Ψv M1( ), M2( ), ..., Md(v)−1 , X( ) is condition-

ally independent of all other random variables given

M1(→) dv−1. We thus have

P X (1→ )

=

P X (1→ )|M1(→ 1→dv)−1 P M1(→ 1→dv)−1

m(11→→dv)−1

=

P

m(11→→dv)−1 i=1

X (i)|M1(→ i) dv−1

dv −1
P
j=1

Mj(1→ ) . (12)

Using (12), for each x ∈ SX , we need to sum over all m1(1→→dv)−1 ∈ SM(dv−1) which consists of |SM | (dv−1) elements. This is computationally expensive. To simplify the calculations, we define a function ΨV : SM(dv−1) → SX
such that

Efficient algorithms for finding Rx, Jx and |Rx,i| are given in [5]. Moreover, these entities do not depend on

the channel signal-to-noise ratio (SNR) or the check node

degree dc, and need to be computed only once for a particular , dv, and SM . Thus, once the code parameters are

set, varying the channel SNR (as is required for finding

the threshold) does not introduce more computations. For

each new iteration , however, the entities have to be

re-computed. As a result, the computational complexity

will still grow exponentially with and quickly becomes

intractable. Once the memory truncation is introduced in

the next section, we only need to compute Rx, Jx and |Rx,i| up to a particular iteration n, and the results may be reused for every iteration > n.

To carry out the computations in (15), we need to

calculate the products

dv −1 j=1

P

(pj

).

This

requires

the

distribution of Mi(1→ ), i ∈ 1, ..., dv − 1. These random

vectors are i.i.d.. we thus only need to find the distribution

of one of them. • Calculation of P Mi(1→ )

We define the set of incoming extrinsic messages to

a check node C at iteration − 1 as T1(→−d1c)−1 = T1( −1), T2( −1), ..., Td(c−−11) . The variables Ti( −1) are

i.i.d., and since Mi( ) = Ψc

T1(

−1), T2(

−1)

,

...,

Td(c

−1) −1

,

Mi( ) is conditionally independent of all other random variables given T1(→−d1c)−1. Hence, we have

P Mi(1→ )

ΨV M1(→ 1→dv)−1 = {ΨV M1(→ 1)dv−1 , ΨV M1(→ 2)dv−1 ,

..., ΨV M1(→) dv−1 } . (13)
We also define ≡ to be an equivalence on SM(dv−1) so that for a, b ∈ SM(dv−1), a ≡ b means ΨV (a) = ΨV (b). We thus have the equivalence class Rx ⊂ SM(dv−1), where Rx = u ∈ SM(dv−1) : ΨV (u) = x , for x ∈ SX . Note

that for SM(dv −1)

a, b ∈ . Since

SX r∈

, Ra Rx ⊂

∩ Rb = SM(dv−1),

∅ it

and can

∪x∈SX Rx = be represented

by the set {r1, r2, ..., rdv−1}, ri ∈ SM . We can then re-

write Equation (12) as follows:

=

P

Mi(1→

)

|T1(→ 0→dc

−1) −1

P

T1(→ 0→dc

−1) −1

t1(0→→dc−−11)

=

P

t1(0→→dc−−11) j=1

Mi(j)|T1(→ j−d1c)−1

dc−1
P
j=1

Tj(0→ −1) . (16)

Using Equation (16), for each element in the sample

space of Mi(1→ ), we need to sum over all the elements

in

the

sample

space

of

T1(→ 0→dc

−1) −1

which

consists

of

|SM | (dc−1) elements. This is computationally expensive.

To reduce the complexity, we use an approach similar to

the one that we used to simplify (12) to (15). This results

P X (1→ ) = x =

dv −1
P (rj) , x ∈ SX . (14)

in

P Mi(1→ ) = m

r∈Rx j=1

Jm

Further simplification can be made by observing that there may be a collection of distinct elements in Rx which have the same probability of occurrence. To exploit
this, we partition Rx to Jx subsets Rx,1, . . . , Rx,Jx, where all the elements in subset Rx,i have the same

= |Rm,i| × pm,i

i=1

Jm

dc−1

= |Rm,i| P (qj ) ,

i=1

j=1

m ∈ SM ,

(17)

pProbXab(1il→ity) p=x,xi, and px,i = px,j for i = j. We thus

have
Jx

Jx

dv −1

= |Rx,i| × px,i = |Rx,i|

P (pj) , x ∈ SX ,

i=1

i=1

j=1

(15)

where Rm,i for m ∈ SM is the i-th partition out of Jm partitions of Rm = {t ∈ SM(dc−1) : ΨC(t) = m} so that all the elements of Rm,i have the same probability
pm,i, and {q1, q2, ..., qdc−1}, qi ∈ SM , represents an arbitrary element of Rm,i. The entities Rm, Jm, and

where {p1, p2, ..., pdv−1}, pi ∈ SM , represents an arbitrary element of Rx,i.

|Rm,i| can also be calculated efficiently [5]. To perform the calculations in (17), we need the distribution of

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

3562

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 60, NO. 12, DECEMBER 2012

Ti(0→ −1), i ∈ 1, ..., dc −1. (Note that the random vectors for different values of i are i.i.d. and thus finding the distribution of one of them, say T1(0→ −1) is sufficient.) • Calculation of P T1(0→ −1) By defining B(0→ −1) = B(0), B(1), ..., B( −1) , we
have

P T1(0→ −1)

=

P T1(0→ −1)|B(0→ −1) P B(0→ −1)

b(0→ −1)

−1
=

P T1(i)|B(i) P B(0→ −1) .

i=0 b(i)
(18) In deriving (18), we have used the fact that T1(i) is a deterministic function of B(i) and thus given B(i) is

independent of other random variables. By substituting P B(0→ −1) in (18) with the following equation

P B(0→ −1)

−1
= P B(0)

P B(i)|B(i−1), X (i) ×

i=1 x(i)

P X (1→ −1) ,

(19)

we obtain

P T1(0→ −1)

=

P T1(0)|B(0) P B(0) ×

b(0)

−1

P T1(i)|B(i)

P B(i)|B(i−1), X (i) ×

i=1 b(i)

x(i)

P X (1→ −1) .

(20) Note that for the calculation of (20), P X(1→ −1)

is available from the previous iteration, and both P B(i)|B(i−1), X(i) and P T1(i)|B(i) take only zero
or one values.

This concludes the steps for calculating P B( −1), X( ) .

It is important to note that to obtain the distributions required to calculate P B( +1) , we use the distributions obtained in the previous iteration that were required to calculate P B( ) .

V. MEMORY TRUNCATION

the sample space of the conditioning set X(1→ −1) grows exponentially with . Now, consider making the following approximation:

B( −1) ⊥⊥ X ( )|X ( −n+1→ −1), n ≥ 2.

(21)

Regardless of , the conditioning set in (21) will always have a sample space with size |SX |n−1.
Consider the sequence B(0→ ). This sequence, in general,
is not a Markov process of some finite order. The memory truncation approximates B(0→ ) by a Markov process of order n, n ∈ N. This is represented by the following:

P B(n+k)|B(n+k−1), ..., B(0) ≈ P B(n+k)|B(n+k−1), ..., B(k) ,

(22)

and corresponds to removing the edges between X(i) and B(i)
for i = 1, . . . , k, in the Bayesian network of Fig. 3.
We refer to the approximation of (22) as memory truncation of order n (MT(n)). A consequence of this memory truncation is that B( −1) is conditionally independent of X( ) given X( −n+1→ −1). By using (22), the growing complexity of the
analysis with disappears and the computational complexity
remains constant for ≥ n. In other words, calculating P B( ) for ≥ n has the same complexity as computing P B(n) . We note that by using (22), the computation of P B( ) is exact for ≤ n.

B. Analysis
We consider a memory truncation of order n, and assume that we have already calculated (approximated) P B(k) , k ≥ n. For k = 1, . . . , n, the distributions are calculated based on the exact analysis of Section IV. We thus have the following distributions available:
• P B(k−1), X (k) • P B(k−1)|X (k−n+1→k−1) • P X (k−n+1→k) • P M1(k−n+1→k) • P T1(k−n→k−1)
We now derive P B(k+1) . To perform this, we still use (8), but the calculation of the joint distribution P B(k), X(k+1) is different as explained below.

A. Main Idea
The computational complexity and the memory requirement for the density evolution analysis grow exponentially with increasing the iteration number . To make the analysis tractable, we propose an approximation based on the truncation of the memory of iterative decoding. This corresponds to removing

P B(k), X (k+1)

=

P B(k)|X (k−n+2→k) ×

x(k−n+2→k)

P X (k+1)|X (k−n+2→k) P X (k−n+2→k)

some of the edges (dependencies) in the Bayesian network.
To explain the approximation, we consider the calculation of the joint distribution P B( −1), X( ) as performed in Section IV. This computation uses the fact that B( −1) ⊥⊥ X( )|X(1→ −1). The problem lies in the fact that the size of

=

P B(k)|X (k−n+2→k) P X (k−n+2→k+1) .

x(k−n+2→k)
In (23), the distribution P by

B(k)|X (k−n+2→k)

(23) is calculated

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

JANULEWICZ and BANIHASHEMI: PERFORMANCE ANALYSIS OF ITERATIVE DECODING ALGORITHMS WITH MEMORY OVER MEMORYLESS . . .

3563

P B(k)|X (k−n+2→k)

=

P B(k)|X (k), B(k−1) P B(k−1)|X (k−n+2→k) ,

Probability of error after 200 iterations for regular (3,6) ensemble 100

n=3

10−2

n=4 n=5

b(k−1)

10−4
(24)

where

10−6

Pe

P B(k−1)|X (k−n+2→k)

10−8

=

P B(k−1)|X (k−n+1→k) ×

x(k−n+1)

10−10

P X (k−n+1)|X (k−n+2→k)

= P

1 X (k−n+2→k)

P
x(k−n+1)

B(k−1)|X (k−n+1→k−1) ×

10−12 10−14
2

2.5

3

3.5

4

4.5

P X (k−n+1→k) .

Eb/N0 (dB)

(25)

Distributions

P B(k−1)|X (k−n+1→k−1)

and

P X(k−n+1→k) are available from the previous iteration and

Fig. 4. Pe after 200 iterations vs. Eb/N0 for the (3,6) ensemble with memory truncation orders 3, 4, and 5.

P X (k−n+2→k) =

P X(k−n+1→k) . Note that in

x(k−n+1)
(25), we have used the fact B(k−1) ⊥⊥ X(k)|X(k−n+1→k−1),

which is a consequence of memory truncation. The calculation of P X(k−n+2→k+1) in (23) can be

The distribution P X(k−n+1→k) in (27) is obtained from the previous iteration, and P B(k−n)|X(k−n+1) is obtained
from iteration k − n + 1.

performed exactly the same as in Section IV, if we have P M1(k−n+2→k+1) . Similarily, P M1(k−n+2→k+1) can be
derived using the same methods as in Section IV given that we have P T1(k−n+1→k) . The derivation of P T1(k−n+1→k) is
explained in the following. To calculate P T1(k−n+1→k) , we use (18) and thus need
P B(k−n+1→k) . This distribution is given by

VI. SIMULATION RESULTS
In general, the accuracy of P B( ) increases with increasing the memory truncation order n, and so does the complexity. It is however expected that after increasing n beyond a certain order n0, the accuracy improvement would be negligible. The goal is thus to find n0. Our results presented here are based on the application of DD-BMP algorithm over the BIAWGN channel. The received values at the channel

k
P B(k−n+1→k) =

P B(i)|B(i−1), X (i) ×

i=k−n+2 x(i)

P B(k−n+1), X (k−n+2→k) .

(26) In (26), to obtain P B(k−n+1), X(k−n+2→k) , we use the

fact that under the memory truncation approximation of order n, X (k−n+2→k) ⊥⊥ B(k−n)|X (k−n+1). We then have the

following:

P B(k−n+1), X (k−n+2→k)

=

P B(k−n+1)|B(k−n), X (k−n+1) ×

b(k−n) x(k−n+1)

P X (k−n+2→k)|B(k−n), X (k−n+1) P B(k−n), X (k−n+1)

=

P B(k−n+1)|B(k−n), X (k−n+1) ×

b(k−n) x(k−n+1)

P X (k−n+2→k)|X (k−n+1) P B(k−n), X (k−n+1)

output are i.i.d. with a Gaussian distribution of mean 1 and variance σ2. Assuming that the power spectral density of the AWGN is N0/2, we have σ = 1/ 2REb/N0, where Eb and R denote the energy per information bit and the code rate, respectively.
In Fig. 4, we have shown Pe( ) of the (3, 6) LDPC code ensemble for = 200 vs. Eb/N0 for different values of memory truncation order n. The results are obtained based on the analysis of Section V-B and are for an optimal uniform
quantization scheme [9] with the number of quantization bits q = 8. The curves demonstrate a convergence behavior as n is increased. In particular, the two curves for n = 4 and n = 5 are very close. We have also tried a number of other ensembles and observed a similar trend [5].
For the ensemble of (4, 5) codes, we have plotted Pe( ) vs. for truncation orders 3, 4 and 5, and for Eb/N0 values 4.34 dB and 4.14 dB in Figures 5 and 6, respectively. (Again, the
results are for an optimal uniform quantization scheme with q = 8.) These results indicate that the general behavior of the curves for different truncation orders is the same, namely,

=

P B(k−n+1)|B(k−n), X (k−n+1) ×

b(k−n) x(k−n+1)
P X (k−n+1→k) P B(k−n)|X (k−n+1) .

for the larger SNR (4.34 dB), they all converge to zero error probability as tends to infinity, and for the smaller SNR (4.14 dB), they are all bounded away from zero for larger values of . The figures suggest that the ensemble threshold is between

(27) the two SNR values.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

3564

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 60, NO. 12, DECEMBER 2012

100 Probability of error at Eb/N0 = 4.34 dB for regular (4,5) ensemble

3.4

n=3

n=3

n=4

n=4

10−2

n=5

n=5

3.35

10−4 10−6

Threshold = 3.26 dB
min
3.3

Eb/N0 (dB)

10−8

3.25

10−10

3.2

Pe

10−12 0

100

200

300

400

500

Iterations

0.02 0.04 0.06 0.08 0.1 0.12 0.14 0.16 0.18 0.2 Δ (Quantization Step)

Fig. 5. Pe( ) of the (4,5) ensemble vs. at Eb/N0 = 4.34 dB (above threshold) for memory truncation orders 3, 4, and 5.

Fig. 7. Threshold values of the (3, 6) ensemble vs. Δ for different memory truncation orders (q = 8).

Probability of error at Eb/N0 = 4.14 dB for regular (4,5) ensemble 0.5

n=3

0.45

n=4

n=5

0.4

0.35

0.3

0.25

100 10−1 10−2 10−3

N = 300,000 N = 400,000 N = 500,000

Pe

BER

0.2

0.15

0.1

0

100

200

300

400

500

Iterations

Fig. 6. Pe( ) of the (4,5) ensemble vs. at Eb/N0 = 4.14 dB (below threshold) for memory truncation orders 3, 4, and 5.

10−4

Threshold = 3.18 dB

10−5

10−6

10−7

To clearly see the effect of memory truncation on the

10−8

2.8

3

3.2

calculated thresholds, in Fig. 7, we show the threshold values of the (3, 6) ensemble for different memory truncation orders

E /N (dB)
b0

n. The thresholds for each truncation order are plotted versus the quantization step Δ for q = 8. As it can be seen, the thresholds demonstrate a convergence behavior as n increases.

Fig. 8. BER curves of (5, 10) LDPC codes with block lengths 300,000; 400,000; and 500,000 decoded by DD-BMP, and the corresponding threshold (q = 8).

In paricular, the calculated thresholds for n = 4 and n = 5 are

practically identical for different values of Δ. From Fig. 7, the

optimal threshold of the (3, 6) ensemble (as a function of Δ) 500, 000) with the threshold value of the ensemble (3.18 dB)

is seen to be about 3.26 dB. Based on the above results, in the in Fig. 8. (The cycles of length 4 in the Tanner graphs of the

following, we use n0 = 4 to derive the thresholds. In all cases, codes are avoided, and for the Monte Carlo simulation results, we use q = 8 and the optimal value of Δ that minimizes the 100 block errors per simulation point are obtained.) As it can

threshold.

be seen, the waterfall region of the bit error rate (BER) curves

To verify the calculated threshold, for the (5, 10) ensemble, sharpens as the block length increases, and matches well with

we compare the performance of randomly constructed (5, 10) the calculated threshold.

codes of large block length (N = 300, 000; 400, 000 and We have obtained the thresholds for all regular (dv, dc)

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

JANULEWICZ and BANIHASHEMI: PERFORMANCE ANALYSIS OF ITERATIVE DECODING ALGORITHMS WITH MEMORY OVER MEMORYLESS . . .

3565

TABLE I THRESHOLDS OF BP AND DD-BMP FOR REGULAR ENSEMBLES WITH
dv = 4 AND 5 ≤ dc ≤ 18

TABLE II THRESHOLDS OF BP AND DD-BMP FOR REGULAR ENSEMBLES WITH
dv = 6 AND 7 ≤ dc ≤ 18

dc rate
5 1/5 6 1/3 7 3/7 8 1/2 9 5/9 10 3/5 11 7/11 12 4/6 13 9/13 14 5/7 15 11/15 16 3/4 17 13/17 18 7/8

Eb N0

(dB)
BP

2.57

1.73

1.27

1.59

1.67

1.78

1.89

1.99

2.10

2.20

2.29

2.39

2.47

2.55

Eb N0

(dB)
DD−BM P

4.24

3.18

2.88

2.80

2.82

2.87

2.93

2.99

3.06

3.13

3.20

3.27

3.33

3.40

Gap (dB)
1.67 1.45 1.61 1.21 1.15 1.09 1.04 1.00 0.96 0.93 0.91 0.88 0.86 0.85

dc rate
7 1/7 8 1/4 9 1/3 10 2/5 11 5/11 12 1/2 13 7/13 14 6/7 15 3/5 16 5/8 17 11/17 18 2/3

Eb N0

(dB)
BP

5.14

3.52

2.90

2.61

2.47

2.41

2.39

2.40

2.43

2.46

2.50

2.54

Eb N0

(dB)
DD−BM P

6.46

4.71

4.00

3.63

3.45

3.34

3.26

3.24

3.24

3.24

3.26

3.28

Gap (dB)
1.32 1.19 1.10 1.02 0.98 0.93 0.87 0.84 0.81 0.78 0.76 0.74

TABLE III THRESHOLDS OF MS AND DD-BMP FOR RATE-1/2 ENSEMBLES

codes with 3 ≤ dv ≤ 8 and dv < dc ≤ 18 [5]. Here, we only provide a subset of the results. The results for dv = 4
and dv = 6 are shown in Tables I and II along with the corresponding threshold values of BP.4 These results show

dv dc
36 48 5 10

Eb N0

(dB)
MS

1.70

2.49

3.09

Eb N0

(dB)
DD−BM P

3.26

2.80

3.18

Gap (dB)
1.56 0.31 0.09

that for a fixed dv, the threshold gap between DD-BMP and

6 12

3.54

BP decreases with increasing the rate. As it can be seen,

7 14

3.91

3.34

-0.21

3.61

-0.30

at higher rates the performance gap is less than 1 dB. In comparison with MS,5 for codes with larger degrees, DD-BMP

outperforms MS. The results for rate-1/2 codes are reported in Table III. These results show that by increasing the degrees, the performance gap between MS and DD-BMP, which is to the advantage of MS for smaller degrees, disappears and then reverses to the advantage of DD-BMP.6 These performance results for DD-BMP are impressive considering that both the check node operations and the message-passing for DDBMP are much simpler than those of BP and MS. They also demonstrate the potential of iterative decoding algorithms with memory in achieving better performance/complexity tradeoffs compared to memoryless algorithms.

number and the depth of the decoding tree d. Using this, we demonstrated that for algorithms with memory, a message passed over an edge in the code’s Tanner graph at iteration
depends on the messages passed over that edge in all the previous iterations < , and that the existing dependencies cause the complexity of density evolution to grow at least exponentially with .
We also derived the density evolution equations and used techniques to make them tractable. In particular, we introduced an approximation by truncating the memory of the decoding process and estimating it with a Markov process of finite order.

As an example, we applied the proposed density evolution

VII. CONCLUSIONS AND DISCUSSIONS

technique to DD-BMP for a number of regular LDPC code

In this paper, we developed the framework for the density evolution analysis of iterative message passing algorithms with memory over memoryless channels. A general model for iterative decoding algorithms with memory was presented which includes DD-BMP and SR algorithms as special cases. Based on this model, we used the Bayesian network representation to capture the dependencies among different messages and memory contents in a space with two dimensions: iteration

ensembles and obtained their thresholds. A careful inspection of the threshold values of DD-BMP for regular LDPC codes in comparison with BP and MS thresholds revealed a number of interesting phenomena: (a) By increasing the rate, the performance gap between BP and DD-BMP reduces so that for high-rate codes the difference is less than one dB; (b) For a given rate, by increasing the degrees, the performance advantage of MS over DD-BMP decreases at first and then eventually reverses so that for large degrees, DD-BMP outper-

4The thresholds for BP are found using Gaussian Approximation [1]. 5For the density evolution analysis of MS, 8-bit messages were used in order to have a fair comparison. 6Note that for the best asymptotic performance at rate 1/2, one may choose the (3, 6) ensemble for MS and the (4, 8) ensemble for DD-BMP. In this case, MS outperforms DD-BMP by 1.1 dB. Moreover, in this case, the smaller degrees of the first ensemble are advantageous as they correspond to a smaller number of generated messages per iteration. There may however be scenarios where one would prefer to use ensembles with larger degrees. One example would be to reduce the error floor [2].

forms MS by a few tenths of a dB. These performance results for DD-BMP are impressive since both the message passing and the check node operations in DD-BMP are significantly less complex than those in BP and MS algorithms.
In addition to the above results, our work introduces a general framework for the presentation and analysis of iterative decoding algorithms with memory. This can be used to design irregular codes for existing algorithms or to devise new

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

3566

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 60, NO. 12, DECEMBER 2012

algorithms with improved performance/complexity tradeoffs. In particular, one can see that both SR-LLR and DD-BMP belong to a simple scenario of the model presented in Fig. 1(b), where ΨB is a linear function of both X( ) and B( −1), i.e., ΨB(X( ), B( −1)) = aX( ) + bB( −1). For SR-LLR, b = 1 − a = β, and for DD-BMP, a = b = 1. One interesting topic would be to investigate new iterative algorithms with different values of a and b.
VIII. ACKNOWLEDGEMENT
The authors wish to thank the anonymous reviewers whose comments improved the presentation of the paper.
REFERENCES
[1] S.-Y. Chung, T. Richardson, and R. Urbanke, “Analysis of sum-product decoding of low-density parity-check codes using a Gaussian approximation,” IEEE Trans. Inf. Theory, vol. 47, pp. 657–670, Feb 2001.
[2] C. Cole, S. Wilson, E. Hall, and T. Giallorenzi, “A general method for finding low error rates of LDPC codes,” CoRR, arxiv.org/abs/cs/0605051.
[3] R. Gallager, “Low-density parity-check codes,” IEEE Trans. Inf. Theory, vol. 8, pp. 21–28, Jan. 1962.
[4] S. Hemati and A. H. Banihashemi, “Dynamics and performance analysis of analog iterative decoding for low-density parity-check (LDPC) codes,” IEEE Trans. Commun., vol. 54, pp. 61–70, Jan. 2006.
[5] E. Janulewicz, “Performance analysis of iterative decoding algorithms with memory,” M.A.Sc. Thesis, Carleton University, 2010. Available: http://www.sce.carleton.ca/faculty/banihashemi/EJ-Thesis.pdf.
[6] E. Janulewicz and A. H. Banihashemi, “Performance analysis of iterative decoding algorithms with memory,” in Proc. 2010 IEEE Inf. Theory Workshop.
[7] F. V. Jensen and T. D. Nielsen, Bayesian Networks and Decision Graphs, 2nd edition. Springer Science, 2007.
[8] N. Mobini, A. H. Banihashemi, and S. Hemati, “A differential binary message-passing LDPC decoder,” in Proc. 2007 IEEE Global Telecommun. Conf., pp. 1561–1565.
[9] N. Mobini, A. H. Banihashemi, and S. Hemati, “A differential binary message-passing LDPC decoder,” IEEE Trans. Commun., pp. 2518– 2523, Sep. 2009.
[10] J. Pearl, Probabilistic Reasoning in Intelligent Systems; Networks of Plausible Inference. Kaufmann, 1988.
[11] T. J. Richardson, M. A. Shokrollahi, and R. L. Urbanke, “Design of capacity-approaching irregular low-density parity-check codes,” IEEE Trans. Inf. Theory, vol. 47, pp. 619–637, Feb. 2001.
[12] T. J. Richardson and R. L. Urbanke, “The capacity of low-density paritycheck codes under message-passing decoding,” IEEE Trans. Inf. Theory, vol. 47, pp. 599–618, Feb. 2001.
[13] R. D. Shachter, “Bayes-ball: the rational pastime (for determining irrelevance and requisite information in belief networks and influence diagrams),” in Proc. 1998 Conf. Uncertainty Artificial Intelligence, pp. 480–487.
[14] R. Tanner, “A recursive approach to low complexity codes,” IEEE Trans. Inf. Theory, vol. 27, pp. 533–547, Sep. 1981.

[15] H. Xiao and A. Banihashemi, “Comments on successive relaxation for decoding of LDPC codes,” IEEE Trans. Commun., vol. 57, pp. 2846– 2848, Oct. 2009.
Emil Janulewicz received a Bachelor of Engineering in Communications Engineering in 2007 and a Master of Applied Science in Electrical Engineering in 2010. He had the highest academic ranking for both streams which were completed at Carleton University in Ottawa, Canada. As a Masters student, he was the recipient of the Ontario Graduate Scholarship as well as the Ontario Graduate Scholarship in Science in Technology. His work was highly recognized by being awarded the University Medal for Outstanding Graduate Work at the Masters level. Emil’s research interests lie in the field of information theory, channel coding, and advanced graphical model techniques. Since 2010, he has been working as a Service Professional for Ericsson Canada Inc. helping introduce the latest LTE core networks into the field all over the U.S.
Amir H. Banihashemi (S90-A98-M03-SM04) received the B.A.Sc. degree in electrical engineering from Isfahan University of Technology, Isfahan, Iran in 1988, and the M.A.Sc. degree in communications engineering from the University of Tehran, Tehran, Iran, in 1991, with the highest academic rank in both classes.
From 1991 to 1994, he was with the Electrical Engineering Research Center and the Department of Electrical and Computer Engineering, Isfahan University of Technology. During 1994-1997, he was with the Department of Electrical and Computer Engineering, University of Waterloo, Waterloo, Ontario, Canada, for his Ph.D. degree. He held two Ontario Graduate Scholarships for international students during this period. In 1997, he joined the Department of Electrical and Computer Engineering, University of Toronto, Toronto, Ontario, Canada, where he worked as a Natural Sciences and Engineering Research Council of Canada (NSERC) Postdoctoral Fellow. He joined the Faculty of Engineering at Carleton University in 1998, where at present he is a Professor in the Department of Systems and Computer Engineering. His research interests include coding and information theory, digital and wireless communications, network coding, theory and implementation of communications algorithms, and compressed sensing and sampling. He has published more than 150 papers in refereed journals and conferences. Dr. Banihashemi served as an Associate Editor for the IEEE TRANSACTIONS ON COMMUNICATIONS from 2003 to 2009. He is the Director of the Broadband Communications and Wireless Systems (BCWS) Centre at Carleton University. He has been involved in many international conferences as chair, member of technical program committee, and member of organizing committee. These include co-chair for the Communication Theory Symposium of Globecom07, TPC co-chair of the Information Theory Workshop (ITW) 2007, member of the organizing committee for the International Symposium on Information Theory (ISIT) 2008, and co-chair of the Canadian Workshop on Information Theory (CWIT) 2009. Dr. Banihashemi is a recipient of Carleton’s Research Achievement Award in 2006 and 2012, and Faculty Graduate Mentoring Award in 2011. He is also awarded one of the NSERC’s one hundred 2008 Discovery Accelerator Supplements (DAS).

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 09:51:07 UTC from IEEE Xplore. Restrictions apply.

