2023 IEEE Global Communications Conference: Communication Theory

GLOBECOM 2023 - 2023 IEEE Global Communications Conference | 979-8-3503-1090-0/23/$31.00 ©2023 IEEE | DOI: 10.1109/GLOBECOM54140.2023.10437311

Implementation-Efficient Finite Alphabet Decoding of Polar Codes
Philipp Mohr, Syed Aizaz Ali Shah and Gerhard Bauch Institute of Communications, Hamburg University of Technology, Hamburg, Germany
{philipp.mohr; aizaz.shah; bauch}@tuhh.de

Abstract—An implementation-efficient finite alphabet decoder for polar codes relying on coarsely quantized messages and low-complexity operations is proposed. Typically, finite alphabet decoding performs concatenated compression operations on the received channel messages to aggregate compact reliability information for error correction. These compression operations or mappings can be considered as lookup tables. For polar codes, the finite alphabet decoder design boils down to constructing lookup tables for the upper and lower branches of the building blocks within the code structure. A key challenge is to realize a hardware-friendly implementation of the lookup tables. This work uses the min-sum implementation for the upper branch lookup table and, as a novelty, a computational domain implementation for the lower branch lookup table. The computational domain approach drastically reduces the number of implementation parameters. Furthermore, a restriction to uniform quantization in the lower branch allows a very hardware-friendly compression via clipping and bit-shifting. Its behavior is close to the optimal non-uniform quantization, whose implementation would require multiple high-resolution threshold comparisons. Simulation results confirm excellent performance for the developed decoder. Unlike conventional fixed-point decoders, the proposed method involves an offline design that explicitly maximizes the preserved mutual information under coarse quantization.
I. INTRODUCTION
Polar codes are the first class of linear block codes that have been shown to asymptotically achieve the capacity of binary-input discrete memory-less channels through successive cancellation (SC) decoding [1]. While the SC decoding does not reach capacity for practical code word lengths, the introduction of successive cancellation list (SCL) decoding with cyclic redundancy check (CRC) [2] made polar codes very competitive in the short-block length regime. Further advances eventually evolved polar codes to be standardized for the uplink and downlink control channels in 5G [3], making them widely used nowadays.
In a communication system, forward error correction requires a high proportion of the total energy and hardware resources for the baseband processing. In particular, the bitwidth w of the messages, which represent reliability information in the decoding process, should be chosen as small as possible to achieve the required performance with minimal space complexity. This led to the paradigm of finite alphabet decoding where w-bit integer-valued messages communicate reliability levels among lower and upper branch operations in the decoding graph of a polar code. Inherently, each multipleinput operation must involve a compression to maintain small bit widths in the output messages.

Recently, the information bottleneck (IB) method has been

introduced for designing mutual information maximizing de-

coding operations implemented via lookup tables [4]–[7].

However for a size N code, 2N − 2 individual lookup tables with size of up to 22w+1 are required [5].

In this paper, we propose to use another implementation

variant for decoding polar codes by using a so-called com-

putational domain that avoids the multi-input lookup tables.

The technique is inspired by the computational domain used

in mutual information maximizing decoding of low-density

parity check (LDPC) codes [8], [9]. For each update, two

messages are translated to representation levels and merged

using an addition whose result is compressed via threshold

comparisons. When symmetric representation levels are en-

forced, the number of implementation parameters is reduced to 2w+2w−1−1 for the two translations and the compression.

It can be shown that non-uniformly placed thresholds can

preserve the same amount of mutual information as the lookup

table approach [9].

In [10] a simplified computational domain approach for

LDPC decoding was proposed. We adopt the idea in the lower

branch update of a polar decoder: A restriction to uniformly

placed thresholds is exploited in order to effectively avoid

the threshold comparisons, reducing the number of calculation

operations

from

w

to

1

for

each

of

the

NL

1 2

N

log2(N )

lower

branch updates where NL is the list size in the SCL decoder.

The uniform quantization is done with a very simple clipping

and bit-shifting operation combined with properly scaled trans-

lated messages. For the upper branch, the idea from [6] is kept,

i.e., the upper branch updates are designed using the min-sum

rule. The overall result is a highly implementation-efficient finite alphabet decoder, specified by only 2w+1 instead of 22w+1 parameters per lower branch update compared to an

IB decoder [4], [5]. The contributions can be summarized as

follows:

• A computational domain approach, known from LDPC decoding, is adopted for the decoding of polar codes. Its behavior is equivalent to a mutual-informationmaximizing lookup table based IB decoder but reduces the number of design parameters drastically.
• A simplified computational domain update is proposed that avoids costly threshold comparisons in each lower branch update at close-to-optimal performance.
• Simulation results confirm that the proposed simplified decoder involves a loss of only up to 0.07 dB for most of the investigated code rates and message resolutions.

5318
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 14:06:36 UTC from IEEE Xplore. Restrictions apply.

2023 IEEE Global Communications Conference: Communication Theory

u0 ⊕ x0

p(y|x)

y0

u0

v0,2

v0,1

v0,0 y0

u1 = x1

p(y|x)

y1

u1

v1,2

v1,1

v1,0 y1

Fig. 1: Factor graph of the building block (dashed rectangle) of a polar code and the transmission channel.

u2

v2,2

v2,1

v2,0 y2

The rest of the paper is organized as follows: First section II briefly explains polar codes and their conventional LLR-based decoding. Then, section III introduces the principle of finite alphabet decoding. In section IV a new finite alphabet decoder variant with low complexity is described. Finally, section V evaluates the performance with block error rate simulations.

II. POLAR CODES A polar code with length N = 2n, where n = 1, 2, . . ., is described by its N × N generator matrix

G = F⊗nB,

(1)

where matrix F=

1 0

1 1

and B is the bit reversal permutation

matrix [1]. For a code rate of R=K/N , N −K bits in u = [u0, . . . uN−1]T are set to fixed values, e.g., ui=0, and referred
to as the frozen bits. The values and locations of the frozen bits

are known to the decoder. The remaining K positions in u,

specified in the information set A, carry the information bits.

The process of determining the information set is referred to

as the code construction. The encoding follows as x=Gu.

The matrix F, as depicted in the factor graph of Fig. 1,

serves as the building block of polar codes. It encodes the bits u=[u0, u1]T into the codeword x=[x0, x1]T which is transmitted over a channel with transition probabilities p(yi|xi). The received codeword is y=[y0, y1]T . Two virtual bit channels are created over the building block: The first bit channel treats u0 as input and y0 = y as output, where u1 is considered a hidden variable observed via y1. The second bit channel treats u1 as input and y1 = [y, u0]T as output, assuming true knowledge about u0.
The individual bit channels of a polar code of length N >2

are synthesized using a recursive application of the building

block [1]. For instance, Fig. 2 shows the factor graph of a polar

code for N =4, where the transmission channel is implicitly

included, i.e., the right most variable nodes correspond to

the (quantized) channel outputs yi. The code structure is composed of columns of building blocks referred to as levels,

j (dashed rectangles). Every node is labelled vi,j with row indices i=0, 1, . . . , N −1, referred to as stage, and column

indices j=0, . . . , n. Then vi,n are the encoder inputs ui and vi,0 correspond to the channel outputs yi. From the encoding

perspective, Fig. 2 shows the flow of bits from left to right.

From the decoding perspective, LLRs flow from right to left

in the code structure of Fig. 2.

A. Successive Cancellation Decoding
The successive cancellation decoder [1] exploits the bit channels created in the code structure. For a codeword length

u3 B

v3,2

v3,1

v3,0 y3

level j = n = 2 level j = 1

Fig. 2: Structure of a polar code with length N = 4.

N , the SC decoder estimates the input of the ith bit channel, i.e., uˆi, in a sequential manner from i = 0 to N − 1. With the bit channel output yi = [y, u0, . . . , ui−1]T , uˆi is estimated at each decoding stage i ∈ A as

uˆi =

0 1

Lui (yi) ≥ 0 otherwise.

(2)

The

LLR

Lui (yi)

=

log

p(yi |ui =0) p(yi |ui =1)

is

computed

in

recursive

steps that can be illustrated on the building block. In Fig. 1,

Lu0 (y) = Lx0 (y0) ⊞ Lx1 (y1),

(3)

for i = 0, where Lx0 (y0) and Lx1 (y1) are channel level LLRs

and the box-plus operation between two LLR values L0 and

L1

is

defined

as

L0

⊞ L1

=

log

. 1+eL0 eL1
eL0 +eL1

For

i

=

1,

Lu1 (y, uˆ0) = (−1)uˆ0 Lx0 (y0) + Lx1 (y1),

(4)

with the bit value uˆ0 available from the previous decoding
stage. For example, the LLR Lu2 (y2) in Fig. 2 is computed using (3) from the intermediate LLRs Lv1,1 (.) and Lv3,1 (.). The LLR Lv1,1 (.) is in turn computed according to (4) from the channel level LLRs Lx0 (y0) and Lx1 (y1) as well as the bit estimate vˆ0,1. The LLR Lv3,1 (.) is computed in a similar fashion from Lx2 (y2), Lx3 (y3) and vˆ2,1.

B. Successive Cancellation List Decoding

The SCL decoder [2] can be seen as multiple SC decoders working in parallel. Every time an estimate uˆi for i∈A has to be made, the decoder proceeds as an SC decoder for both possible decisions of uˆi instead of using (2). The number of decoding paths doubles at each decoding stage i∈A. If the number of decoding paths in the list exceeds NL at any stage, the decoder retains only the NL most likely decoding paths, dropping the rest. The likelihood of the correctness of a path l ∈ {0, 1 . . . NL − 1} in the list at stage i is conveyed by the path metric (small value corresponds to high correctness) [11],

Mi,l = Mi−1,l + log(1 + e−(1−2uˆi,l)Lui (yi,l)),

(5)

where Mi−1,l is the path metric of the lth path at decoding stage i − 1, uˆi,l is the bit value with which the path is being extended, and Lui (yi,l) is the LLR value for the lth path according to (3) or (4).

5319
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 14:06:36 UTC from IEEE Xplore. Restrictions apply.

2023 IEEE Global Communications Conference: Communication Theory

After the last decoding stage i=N −1, the most likely decoding path from the list is selected as the decoder output. In the CRC-aided settings, a CRC checksum of NCRC bits is appended to the K information bits and the K + NCRC bits are encoded into an N bit codeword using (1). The decoder output is then the most likely decoding path in the final list that passes the CRC check.

III. FINITE ALPHABET DECODING
Finite alphabet decoders are a family of quantized decoders that replace LLRs with integer valued messages in order to achieve a reduced space complexity. Instead of exchanging exact or approximated LLRs, w-bit messages t from a finite alphabet T of size |T |=2w are used to convey the reliability information w.r.t. a certain bit x. Thus, each message t corresponds to an LLR level Lx(t).
A general choice for the finite alphabet T is unsigned integers {0, 1, . . . , |T | − 1}, e.g., as in [4], [12]. However, this work uses a symmetric finite alphabet T ={−2w−1, . . . , −1, +1 . . . , +2w−1} that is convenient to describe the proposed simplified hardware implementation [10], [13]. The alphabet T is typically chosen such that it is sorted w.r.t. the underlying LLRs, i.e., Lx(t=−2w−1) < . . . < Lx(t=+2w−1). In the design of the decoder, the LLRs Lx(t) are enforced to exhibit odd symmetry as

|Lx(t)| = |Lx(−t)| ∀ t ∈ T .

(6)

The first half of such an alphabet translates to negative LLRs while the second half translates to positive LLR values.
The LLR computations in finite alphabet decoders are replaced with compression operations with some input y∈Y and output t∈T where |T |<|Y|. In order to minimize the loss in error correction performance of the decoder under the constrained resolution w, a mutual information maximizing decoder design aims at maxp(t|y) I(X; T ) when designing the operations. This kind of situation is classified as an IB setup where X is the relevant, Y is the observed and T is the compressed variable [14]. The IB framework provides algorithms for determining the mapping p(t|y) as well as the output joint distribution p(x, t) from an input joint distribution p(x, y). The mapping p(t|y) is designed by placing |T |−1 boundaries in the sorted observed alphabet Y and optimizing them to maximize I(X; T ). The distribution p(x, t) is used to obtain the LLRs Lx(t) and the distribution p(t) of the compressed messages. The deterministic mapping p(t|y) represents the compression operation in the form of a lookup table.

A. Mutual Information Maximizing Polar Decoders
In finite alphabet polar decoders the IB method can be used to construct lookup tables which replace (3) and (4) [4], [5]. This process is recapped here for the building block of Fig. 1 where the underlying channel p(yi|xi) is a quantized binary input AWGN channel.
Construction of the decoding lookup table begins by designing a mutual information maximizing channel quantizer such that yi∈T [12]. With the quantized channel outputs

y=[y0, y1]T at hand, the lookup table p(t0|y) is designed for the upper branch update with t0∈T which compresses the input alphabet of size 22w to an output alphabet of size 2w. Similarly, the lookup table p(t1|y, uˆ0) is designed for the lower branch update with t1∈T which compresses the input alphabet of size 22w+1 to an output alphabet of size 2w. Both
t0 and t1 can be translated to LLR values Lu0 (t0) and Lu1 (t1), respectively. The mappings p(t0|y) and p(t1|y, uˆ0) define a non-uniform quantization of the underlying input LLR space.
For a polar code of length N , there are N −1 distinct
decoding tables for upper branch updates as well as N −1
distinct tables for the lower branch updates [4], [5]. Each upper branch decoding table has a size of 22w while each lower branch decoding table is of size 22w+1. For further details,
the reader is referred to [4]–[6].

IV. PROPOSED EFFICIENT DECODER IMPLEMENTATION
A key challenge in finite alphabet decoders is the efficient implementation of the mutual information maximizing lookup tables. In that regard, the computational domain implementation of the lookup tables in [8], [9] offers an elegant solution for LDPC decoders which is adopted for polar decoders here.
Recall that (3) and (4) deliver the result of the upper and lower branch update as Lu0 (y) and Lu1 (y, uˆ0), respectively. For avoiding expensive propagation of high resolution messages to the next building block in the code structure, quantization of the two LLRs is indispensable. Consider an observed variable Y, y∈Y that models a high resolution LLR related to a relevant binary variable X, x∈X . It can be shown that threshold quantization of Y to a compressed variable T, t∈T using a set of thresholds τ ={τ0, . . . , τ|T |−2} can maximize the preserved mutual information maxτ I(X; T ) [15]. While the decoders designed in [4], [12] with the IB method use the result of such a threshold quantization in the form of a lookup table, [9] uses these thresholds for performing the quantization in a computational domain. In other words, the boundaries or thresholds determined during the lookup table design are used for implementing compression operations. Such a threshold quantization is henceforth represented as t = Q(y).
In order to simplify the implementation, symmetric quantization is considered where the sign is preserved and the magnitude is clustered using thresholds τ = {τ0, . . . , τ2w−1−2} in the following non-uniform quantization [10]:

1   Q(y)= sgn(y) i
2w−1

|y|≤τ0 τi−2<|y|≤τi−1, 2<i<2w−1−1 (7) |y|>τ2w−1−2

For the building block of polar codes in Fig. 1, we have
X =U0 and Y={Lu0 (y) : y∈Y0×Y1} for the upper branch. For the lower branch we have X =U1 and Y={Lu1 (y, u0) : y∈Y0×Y1 ∧ u0∈U0}.

A. Upper Branch Update

The mutual information maximizing update for the upper branch leads to t0=Q(Lu0 (y)). It can be implemented as a lookup table like in [4]–[6] or alternatively as a computation

5320
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 14:06:36 UTC from IEEE Xplore. Restrictions apply.

2023 IEEE Global Communications Conference: Communication Theory

TABLE I: Accurate binary conversions for w′ = 3.
a +0 000 +1 001 +2 010 +3 011 -3 111 -2 110 -1 101 -0 100 ϑ2’s(a) +0 000 +1 001 +2 010 +3 011 -3 101 -2 110 -1 111 +0 000
b +0 000 +1 001 +2 010 +3 011 -4 100 -3 101 -2 110 -1 111 ϑSM(b) +0 000 +1 001 +2 010 +3 011 - - -3 111 -2 110 -1 101
TABLE II: Approximated binary conversions w′ = 3.
a +0 000 +1 001 +2 010 +3 011 -3 111 -2 110 -1 101 -0 100 ϑ˜2’s(a) +0 000 +1 001 +2 010 +3 011 -4 100 -3 101 -2 110 -1 111
b +0 000 +1 001 +2 010 +3 011 -4 100 -3 101 -2 110 -1 111 ϑ˜SM(b) +0 000 +1 001 +2 010 +3 011 - - -2 110 -1 101 -0 100

with quantization [9], [10], [16]. Another, very hardware friendly solution is to approximate (3) using the so-called minsum rule. By making use of the symmetric alphabet T , no translation to LLRs is required and it naturally preserves the desired w-bit message resolution:

t0 = sgn(y0) sgn(y1) min(|y0|, |y1|)

(8)

The approximation causes only minor performance degradation as shown in [6] and is therefore the recommended choice for the upper branch update.

B. Lower Branch Update
The mutual information maximizing update for the lower branch leads to t1=Q(Lu1 (y, uˆ0)). It can be implemented as a lookup table like in [4]–[6] or alternatively as a computation with threshold quantization as in [9], [10], [16]. The lookup table implementation suffers from its large size to cover all the 22w+1 input combinations. This aspect is significantly improved when using the computation according to (4).
Up to this point, the operation’s internal computations have been considered with real-valued numbers. For a hardware implementation this is not acceptable. To reduce the internal resolution one option is to scale the real-valued LLRs to an integer range from −ι to +ι, with ι = 2w′−1−1, as follows:

ϕs(t) = ⌈sL(t)⌋ = sgn(L(t)) min (⌊s|L(t)| + 0.5⌋ , ι) (9)
where the scaling s ∈ R controls the LLR resolution ∆ = 1/s in the integer domain. Then, the integer computation yields
t1 = Q (−1)uˆ0 ϕs(y0) + ϕs(y1) ≊ Q(Lu1 (y, uˆ0)). (10)
Fig. 3a depicts a corresponding hardware schematic where the translations are assumed to be implemented with two (w−1)-bit lookup tables. The adder is assumed to work in a binary two’s complement format such that subtraction and addition can be performed with the same hardware module. The quantization expects a sign-magnitude format. Therefore, two conversions from sign magnitude into the 2’s complement format b=ϑ2’s(a) and vice versa a=ϑSM(b) must be part of the hardware. Table I describes an accurate conversion

b = ϑ2’s(a) = [a0, (|a| ⊕ a0) + (+12’s ∧ a0)] and a = ϑSM(b) = [b0, (|b| + (−12’s ∧ b0)) ⊕ b0]

(11a) (11b)

where a0 (resp. b0) refers to sign bit of a number a (resp. b), + is binary addition with carry propagation and, eventually, ∧ and ⊕ are bitwise logic AND and XOR operations. In

uˆ0 sgn(y0)
|y0|
sgn(y1) |y1|

XOR

ϕs,0 ϕs,1

ϑ2’s +
ϑ2’s

y

ϑSM

Q

τ3

|y|

≤

88

1

τ1 τ5

(a)
|y| MSB
8
OR

sgn(t1) |t1|

≤

8

1

τ0 τ2 τ4 τ6

≤
8

|t1|
13

|t1|
3

LSB

r=2

(b)

(c)

Fig. 3: (a) shows a hardware schematic for the lower branch processing that can be used with (b) non-uniform quantization or (c) uniform quantization [10]. For (b) and (c) we have w′=9-bit internal resolution and w=4-bit message resolution.

particular the + operation involves significant complexity. It is proposed to remove the offset operation, yielding a simpler approximated conversion according to

ϑ2’s(a) ≈ ϑ˜2’s(a) = [a0, |a| ⊕ a0] and ϑSM(b) ≈ ϑ˜2’s(a) = [b0, |b| ⊕ b0]

(12a) (12b)

which is illustrated in Table II. The technique involves a slight bias, since e.g. ϑ(+12′s)=+1SM but ϑ(−12′s)=−2SM. To distribute the bias fairly, one option is to let every second lower branch update invert the sign for inputs and output. In our simulations we used the accurate variant (11) but we expect only minor loss from the much simpler conversion (12).
Another bottleneck is the computation of (7). It requires w−1 threshold comparisons using a binary search procedure, depicted in Fig. 3b. As proposed in [10] for LDPC codes, a restriction to uniform thresholds enables a much simpler implementation of the quantization operation, which is shown in Fig. 3c. In that approach the quantization is achieved by a clipping and bit shifting operation defined as

Q(y) = sgn(y) min(⌊|y|/2r⌋ + 1, 2w−1)

(13)

where r denotes the number of right-shifted bit positions. By modifying r and the scaling factor s=1/∆ for the translation tables, any real-valued uniform threshold spacing ∆2r can be approached with the integer processing. A grid based search can find the optimal uniform quantization aiming for maxs,r I(U1; T1).

5321
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 14:06:36 UTC from IEEE Xplore. Restrictions apply.

2023 IEEE Global Communications Conference: Communication Theory

TABLE III: Complexity of lower branch updates.

variant

additions/ memory usage in bit (e.g. w′=6)

comparisons

general

w=4 w=3 w=2

IB-LUT

0

w · 22w+1

2048 384 64

CD (non-uni.)

w

(2(w′−1) + w′)2w−1 128 64 32

CD (uniform)

1

2(w′−1)2w−1

80 40 20

C. Complexity Analysis
For the upper branch processing the lowest complexity is observed with the min-sum update which requires only a single exclusive-or gate and a (w−1)-bit comparison (see (8)).
A comparison of the complexity for the discussed lower branch updates is provided in Table III. The total number of potentially different parameterized updates is N −1 for the complete decoder. The example memory usage is evaluated for an internal resolution of w′=6 bits which sacrifices only minor performance in the simulations.
Clearly, the computational domain solution with uniform quantization yields the lowest complexity. It only requires memory for the two translations to w′-bit LLR magnitudes and one addition operation. The non-uniform computational domain variant requires additional complexity to perform the non-uniform threshold quantization with w−1 comparisons in a binary search procedure having 2w−1 different w′-bit thresholds. In case of w=4 bits the lookup table solution requires more than 25 times the number of memory bits to specify the decoder. A conventional fixed point SC decoder from [11] calls for about w=6 bits to achieve similar performance as the proposed w=4-bit decoder.

V. PERFORMANCE ANALYSIS
This section presents the simulation results showing the error correction performance of the proposed quantized decoders. The proposed decoding scheme is compared with double-precision floating-point LLR-based decoding as well as finite alphabet decoders designed using the IB method [4], [5]. The LLR-based decoding represents the unquantized decoders. The SCL decoding is used here in the CRC-aided setting with list size of NL = 32 and CRC size of NCRC = 16. For the construction of polar codes, the method adopted in 5G NR [3] is used. Finally, all the simulations are performed for a codeword length of N = 1024 over an AWGN channel using BPSK modulation.
The SC decoding mainly performs two types of computations, i.e., for upper or lower branch on a building block in the polar code structure. Hence, the finite alphabet decoders in this work are labelled according to the design method used for upper and lower branch updates. The decoders from [4], [5] where both upper and lower branch updates are designed using the IB method are labelled IB-IB. The finite alphabet decoders of [6] that deploys min-sum (MS) and the IB for designing upper and lower branch updates, respectively, are labelled MS-IB. The proposed decoders which use min-sum rule for upper branch and computational-domain uniform quantization method for lower branch updates are labelled MS-CD.
The finite alphabet quantized decoders are constructed offline for a selected w= log2(|T |)-bit resolution. Each wbit quantized decoder deploys a w-bit mutual information

100

Block Error Rate

10−1

10−2

64-bit 4-bit

2-bit

LLR

IB-IB

MS-IB

10−3

MS-CD

1

2

3

4

5

Eb/N0 in dB Fig. 4: Block error rate comparison under SC decoding.

100

Block Error Rate

10−1

R=

1 4

R=

1 2

R=

3 4

10−2

LLR

IB-IB

MS-IB

10−3

MS-CD

0 0.5 1 1.5 2 2.5 3

Eb/N0 in dB Fig. 5: Block error rate comparison under CRC-aided SCL decoding with various code rates for w=4 bit.

maximizing channel quantizer designed using the IB method
[4], [5], [12]. The channel quantizer and, in turn, the quantized decoder are constructed for a specific Eb/N0, which is referred to as the design Eb/N0 of the decoder. For a given code rate R and resolution w, the design Eb/N0 for the IB-IB decoder is selected as the one which achieves a block error rate of 10−3 at the smallest channel Eb/N0. The same design Eb/N0 is then used to generate MS-IB and MS-CD decoders for the same R and w.

A. Successive Cancellation Decoding
Fig. 4 shows the block error rates under the successive cancellation decoding for a code rate R=0.5 and resolution of w=4 and 2 bits. The three finite alphabet decoders in the figure for w=4 bit resolution were designed for Eb/N0=1.5 dB. The w=2 bit decoders were designed for Eb/N0=2.0 dB. Compared to the double-precision LLR-based decoder, the 4bit decoder show a degradation of around 0.2 dB while the 2bit quantized decoders exhibit a significant performance loss of approximately 2.4 dB. Most importantly, the IB-IB, MS-IB and the proposed MS-CD decoders have practically the same error rate performance. Thus, the implementation friendly MS-CD approximation costs nothing in terms of performance loss.

5322
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 14:06:36 UTC from IEEE Xplore. Restrictions apply.

2023 IEEE Global Communications Conference: Communication Theory

100

Block Error Rate

10−1

10−2

4-bit 64-bit

3-bit

2-bit

LLR

IB-IB

10−3

MS-CD

0

1

2

3

4

Eb/N0 in dB Fig. 6: Block error rate comparison under CRC-aided SCL decoding with different message resolutions.

B. Successive Cancellation List Decoding

Fig. 5 presents the block error rates for 4-bit quantized CRC-aided SCL decoders for multiple code rates. For the low code rate R=0.25, the IB-IB [4] decoder exhibits a loss of 0.2 dB w.r.t. the double-precision LLR decoder while the proposed MS-CD decoder shows an approx. 0.07 dB of additional degradation. The figure also includes the block error rates of MS-IB [6] decoder. The error rate curves of the MSIB decoder are in between the error rate curves of IB-IB and MS-CD decoders. This is expected behaviour since the MSIB decoder design principle deploys an approximate design rule only for the upper branch while keeping the IB design rule for lower branch operations. The proposed MS-CD uses approximate design rules for both the upper and the lower branch operations. The three quantized decoders for R=0.25 were constructed for a design Eb/N0=0.0 dB.
For the rate R=0.5, the finite alphabet decoders were constructed for design Eb/N0=0.5 dB while the decoders for the code rate R=0.75 were designed for Eb/N0=1.75 dB. The additional performance loss of MS-CD w.r.t. the IB-IB decoder shrinks to approximately 0.05 dB at the code rate R=0.5.
Fig. 6 compares block error rates of the 4-bit IB-IB and MSCD decoders of Fig. 5 at code rate 0.5 with their respective 3 and 2 bit variants. The 3-bit IB-IB and MS-CD decoders were designed for Eb/N0=0.0 dB while the 2-bit decoders were designed for Eb/N0=0.5 dB. It can be seen that by decreasing the decoder resolution from 4 to 3 bits, the gap between the IB-IB and MS-CD widens to 0.13 dB. At w=2 bit, the MS-CD and IB-IB decoders are practically equivalent. At this resolution, the symmetry constraint on the placement of the quantization thresholds leave only a single boundary to be optimized. Thus, the uniform computational domain and the non-uniform IB optimization produce similar thresholds.
Overall, the results show minor performance degradation due to the use of uniform instead of non-uniform quantization, ranging from 0.0-0.13 dB for code rates R=0.25-0.75 and resolutions w=2-4 bits. Higher resolutions for the quantized decoding were not considered because the lookup tables become too large at only minor performance gains.

VI. CONCLUSIONS
In this paper, finite alphabet decoders are designed for
polar codes. This class of decoders replaces LLR-based com-
putations with mutual information maximizing table lookup
operations. The main contribution is the use of a computational
domain with uniform quantization instead of a lookup table
for a significant complexity reduction in the lower branch
update. The min-sum operation is used for the upper branch
processing. In the case of 4-bit message resolution, we es-
timate only 1/25 of the memory consumption compared to
a pure lookup table implementation. The uniform quantiza-
tion requires only 1/4 of the computational cost compared
to the optimal non-uniform quantization. It is shown that
at 4-bit resolution, the performance degradation due to the used hardware-friendly approximations remains below 0.07 dB
compared to the information-optimal lookup table design.
REFERENCES
[1] E. Arikan, “Channel Polarization: A Method for Constructing CapacityAchieving Codes for Symmetric Binary-Input Memoryless Channels,” IEEE Transactions on Information Theory, vol. 55, no. 7, pp. 3051– 3073, Jul. 2009.
[2] I. Tal and A. Vardy, “List Decoding of Polar Codes,” IEEE Transactions on Information Theory, vol. 61, no. 5, pp. 2213–2226, May 2015, conference Name: IEEE Transactions on Information Theory.
[3] 3GPP, “5G; NR; Multiplexing and channel coding (Release 15),” 3rd Generation Partnership Project, Technical Specification (TS) 38.212, 06 2019, version 15.6.0.
[4] S. A. A. Shah, M. Stark, and G. Bauch, “Design of Quantized Decoders for Polar Codes using the Information Bottleneck Method,” in SCC 2019; 12th International ITG Conference on Systems, Communications and Coding, Feb. 2019, pp. 1–6.
[5] ——, “Coarsely Quantized Decoding and Construction of Polar Codes Using the Information Bottleneck Method,” Algorithms, vol. 12, no. 9, p. 192, Sep. 2019.
[6] ——, “Space-Efficient Quantized Polar Decoders Designed using the Information Bottleneck Method,” in WSA & SCC 2023; 26th International ITG Workshop on Smart Antennas and 13th Conference on Systems, Communications, and Coding, 2023, pp. 1–6.
[7] T. Koike-Akino, Y. Wang, S. Cayci, D. S. Millar, K. Kojima, and K. Parsons, “Hardware-Efficient Quantized Polar Decoding with Optimized Lookup Table,” in 2019 OECC/PSC, July 2019, pp. 1–3.
[8] J.-S. Lee and J. Thorpe, “Memory-efficient decoding of ldpc codes,” in Proceedings. International Symposium on Information Theory, 2005. ISIT 2005., 2005, pp. 459–463.
[9] X. He, K. Cai, and Z. Mei, “On Mutual Information-Maximizing Quantized Belief Propagation Decoding of LDPC Codes,” in 2019 IEEE Global Communications Conference (GLOBECOM), 2019, pp. 1–6.
[10] P. Mohr and G. Bauch, “Uniform vs. Non-Uniform Coarse Quantization in Mutual Information Maximizing LDPC Decoding,” in GLOBECOM 2022 - 2022 IEEE Global Comm. Conference, 2022, pp. 3496–3501.
[11] A. Balatsoukas-Stimming, M. B. Parizi, and A. Burg, “LLR-Based Successive Cancellation List Decoding of Polar Codes,” IEEE Transactions on Signal Processing, vol. 63, no. 19, pp. 5165–5179, Oct. 2015.
[12] J. Lewandowsky and G. Bauch, “Information-Optimum LDPC Decoders Based on the Information Bottleneck Method,” IEEE Access, vol. 6, pp. 4054–4071, 2018.
[13] P. Mohr and G. Bauch, “A Variable Node Design with Check Node Aware Quantization Leveraging 2-Bit LDPC Decoding,” in GLOBECOM 2022 - 2022 IEEE Global Comm. Conference, 2022, pp. 3484–3489.
[14] N. Tishby, F. C. Pereira, and W. Bialek, “The Information Bottleneck Method,” in Proceedings 37th Allerton Conference on Communication and Computation, 1999.
[15] B. M. Kurkoski and H. Yagi, “Quantization of Binary-Input Discrete Memoryless Channels,” IEEE Transactions on Information Theory, vol. 60, no. 8, pp. 4544–4552, Aug. 2014.
[16] P. Mohr, G. Bauch, F. Yu, and M. Li, “Coarsely Quantized Layered Decoding Using the Information Bottleneck Method,” in ICC 2021 IEEE International Conf. on Communications, Jun. 2021, pp. 1–6.

5323
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 14:06:36 UTC from IEEE Xplore. Restrictions apply.

