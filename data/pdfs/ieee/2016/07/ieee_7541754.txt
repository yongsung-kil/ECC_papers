2016 IEEE International Symposium on Information Theory

Minimal Characterization and Provably Efficient Exhaustive Search Algorithm for Elementary
Trapping Sets of Variable-Regular LDPC Codes

Yoones Hashemi, and Amir H. Banihashemi Department of Systems and Computer Engineering, Carleton University, Ottawa, Ontario, Canada

Abstract—In this paper, we propose a new characterization and an efficient exhaustive search algorithm for elementary trapping sets (ETS) of variable-regular low-density parity-check (LDPC) codes. Recently, Karimi and Banihashemi proposed a characterization of ETSs, which was based on viewing an ETS as a layered superset (LSS) of a short cycle in the code’s Tanner graph. Compared to the LSS-based characterization, which is based on a single LSS expansion technique, the new characterization involves two additional expansion techniques. The introduction of the new techniques mitigates two problems that LSS-based characterization/search suffers from: (1) exhaustiveness: not every ETS structure is an LSS of a cycle, (2) search efficiency: LSS-based search algorithm often requires the enumeration of cycles with length much larger than the girth of the graph, where the multiplicity of such cycles increases rapidly with their length. We prove that using the three expansion techniques, any ETS structure can be obtained starting from a simple cycle, no matter how large the size of the structure a or the number of its unsatisfied check nodes b are, i.e., the characterization is exhaustive. We also demonstrate that for the proposed characterization to exhaustively cover all the ETS structures within the (a, b) classes with a ≤ amax, b ≤ bmax, for any value of amax and bmax, the maximum length of the required cycles is minimal. The proposed characterization corresponds to a provably efficient search algorithm, significantly more efficient than the LSS-based search.
I. INTRODUCTION
Finite-length low-density parity-check (LDPC) codes under iterative decoding algorithms suffer from the error floor phenomenon. It is well-known that error-floor performance of LDPC codes is related to the presence of certain problematic graphical structures in the Tanner graph of the code, commonly referred to as trapping sets (TS). Among TSs, the most harmful ones are known to be the elementary trapping sets (ETSs), whose induced subgraphs contain only degree-1 and degree-2 check nodes. There has been a flurry of activity in characterization of trapping sets [2], [10], [6], [3], and in developing search algorithms to find trapping sets [1], [7], [8]. Most of the works on TSs in the literature are non-exhaustive [1], [7], are limited to structured codes [12], only deal with a certain variable node degree [10], or are applicable to relatively short block lengths [8].
Recently, in [6], [3], the authors characterized the ETSs of variable-regular LDPC codes. They studied the graphical structure of the ETSs, and demonstrated that all the nonisomorphic structures of ETSs are layered super sets (LSS) of some basic structures. These basic structures were called

prime with respect to the LSS property, or “LSS-prime” in brief, implying that they are not layered super sets of smaller ETSs. This characterization corresponds to a simple LSSbased search algorithm that can find all the instances of ETSs of variable-regular LDPC codes, for any range of a and b values, in a guaranteed fashion, starting from the instances of LSS-prime structures. Although, the search algorithm itself is simple, one may need to enumerate basic structures with large size in the Tanner graph of the code, as the input to the search algorithm. The multiplicity of such structures increases rapidly with the size and thus the enumeration, storage and processing of these structures pose a practical hurdle in implementing the search algorithm. Motivated by this, in this paper, which can be found in more details in [4], we develop a new characterization of ETS structures of variable-regular LDPC codes. The new characterization is based on a hierarchical graphbased expansion approach which involves LSS expansion plus two additional expansion techniques. It characterizes each ETS structure with an embedded sequence of ETS structures that starts from a simple cycle and expands, at each step, to a larger ETS using one of the three expansions, until it reaches the ETS structure of interest. The characterization is minimal, in the sense that, none of the expansion steps can be divided into smaller expansions such that the resulting new sub-structures are still ETSs. The new characterization corresponds to a provably efficient exhaustive search algorithm for ETSs (significantly more efficient than LSS-based search), and requires only short-length simple cycles of the graph as the input. The maximum length of the input cycles is often smaller than that of the LSS-based search algorithm, and is, in fact, provably minimal.
II. DEFINITIONS AND NOTATIONS
Consider an undirected graph G = (F, E), where the two sets F = {f1, . . . , fk} and E = {e1, . . . , em}, are the sets of nodes and edges of G, respectively. We say that an edge e is incident to a node f if e is connected to f . The degree of a node f is denoted by deg(f), and is defined as the number of edges incident to f . The minimum degree of a graph G, denoted by δ(G), is defined to be the minimum degree of its nodes. Given an undirected graph G = (F, E), a walk between two nodes f1 and fk+1 is a sequence of nodes and edges f1, e1, f2, e2, . . . , fk, ek, fk+1, where ei = fifi+1, ∀i ∈ [1, k]. In this definition, the nodes f1, f2, . . . , fk+1 are not necessarily

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 15:27:59 UTC from IEEE Xplore. Restrictions apply.

978-1-5090-1806-2/16/$31.00 ©2016 IEEE

2524

2016 IEEE International Symposium on Information Theory

distinct. The same applies to the edges e1, e2, . . . , ek. A path is a walk with no repeated nodes or edges, except the first and
the last nodes that can be the same. If the first and the last
nodes are distinct, we call the path an open path. Otherwise,
we call it a cycle. The length of a walk, a path, or a cycle is
the number of its edges. A lollipop walk is a walk f1, e1, f2, e2, . . . , fk, ek, fk+1, such that all the edges and all the nodes are distinct, except that fk+1 = fm, for some m ∈ (1, k). A simple cycle is a cycle which does not have any chord.
Throughout this paper, we use the notation sk for a simple cycle of length k. A tree is a connected graph that contains
no cycles. A rooted tree is a tree in which one specific node
is assigned as the root. The depth of a node in a rooted tree is
the length of the path from the node to the root. The depth of
a tree is the maximum depth of any node in the tree. Depth-
one tree is a tree with depth one. A node f is called leaf if deg(f ) = 1. A leafless graph is a connected graph G with δ(G) = 2. The graphs G1 = (F1, E1) and G2 = (F2, E2) are isomorphic if there is a bijection p : F1 → F2 such that nodes f1, f2 ∈ F1 are joined by an edge if and only if p(f1) and p(f2) are joined by an edge. Otherwise, the graphs are non-isomorphic.
Any m × n parity check matrix H of an (n, k) LDPC code C can be represented by its bipartite Tanner graph G = (V ∪ C, E), where V = {v1, v2, . . . , vn} is the set of variable nodes and C = {c1, c2, . . . , cm} is the set of check nodes. The degrees of nodes vi and ci are denoted by dvi and dci , respectively. A Tanner graph is called variableregular with variable degree dv if dvi = dv, ∀ vi ∈ V . A (dv, dc)-regular Tanner graph is a variable-regular graph in which dci = dc, ∀ ci ∈ C. For a subset S of V , the subset Γ(S) of C denotes the set of neighbors of S in G. The induced subgraph of S in G, denoted by G(S), is the graph with the set of nodes S ∪ Γ(S) and the set of edges {fifj ∈ E : fi ∈ S, fj ∈ Γ(S)}. The set of check nodes with odd and even degrees in G(S) are denoted by Γo(S) and Γe(S), respectively. In this paper, the terms unsatisfied check nodes and satisfied check nodes are used to refer to the check nodes in Γo(S) and Γe(S), respectively. The size of an induced subgraph G(S) is defined to be the number
of its variable nodes. We assume that an induced subgraph is
connected. Disconnected subgraphs can be considered as the
union of connected ones. The length of the shortest cycle in
a Tanner graph is called girth, and is denoted by g. We study the variable-regular graphs that have dv ≥ 3, and are free of 4-cycles (g > 4). All the induced subgraphs with the same size a, and the same number of unsatisfied check nodes b, are considered to belong to the same (a, b) class.
Given a Tanner graph G, a set S ⊂ V is called an (a,b) trapping set (TS) if |S| = a and |Γo(S)| = b. Alternatively, S is said to belong to the class of (a,b) TSs. Parameter a is
referred to as the size of the TS. In the rest of the paper,
depending on the context, the term “trapping set” may be used to refer to the set of variable nodes S, or to the induced subgraph G(S) of S in the Tanner graph G. Similarly, we may use S to mean G(S). An elementary trapping set (ETS)

c7 v1 c1 v2 f1

f2 c7 v1 c1 v2 f1

f2

c5 c4

c2

c6

c4

c2

v4

c3 v3 f4

f3

c5 v4 c3 v3 f4

f3

c6 (a)

c8 (b)

Fig. 1. (a) A LETS in the (4, 2) class and its leafless normal graph, (b) An ETS in the (4, 4) class and its normal graph which has a leaf (f1).

is a trapping set for which all the check nodes in G(S) have degree 1 or 2. A set S ⊂ V is called an (a,b) absorbing set (AS) if S is an (a, b) trapping set and all the variable nodes in S are connected to more nodes in Γe(S) than in Γo(S). A fully absorbing set (FAS) S ⊂ V is an absorbing set for which all the nodes in V \S have strictly more neighbors in C\Γo(S) than in Γo(S). A set S ⊂ V is called an (a,b) fully elementary absorbing set (FEAS) if S is an (a, b) EAS and if all the nodes in V \S have strictly more neighbors in C\Γo(S) than in Γo(S). Elementary trapping sets are the subject of this paper. To simplify the representation of ETSs, similar to [6], [3], in the rest of the paper, we use an alternate graph representation of ETSs, called normal graph. The normal graph of an ETS S is obtained from G(S) by removing all the check nodes of degree one and their incident edges, and by replacing all the degree-2 check nodes and their two incident edges by a single edge. It is easy to see that there is a one-to-one correspondence between the bipartite graph G(S) and the normal graph of S. We call a set S ⊂ V an (a,b) leafless ETS (LETS) if S is an (a, b) ETS and if the normal graph of S is leafless.
Example 1. Fig. 1(a) represents a LETS in the (4, 2) class and its leafless normal graph, while Fig. 1(b) represents an ETS in the (4, 4) class and its normal graph with a leaf. Symbols
and are used to represent satisfied and unsatisfied check nodes in the induced bipartite subgraphs, respectively, and
the symbol ◦ is used to represent variable nodes in both the
induced subgraphs and normal graphs.
To the best of our knowledge, almost all the structures reported in the literature as error-prone structures of variableregular LDPC codes are LETSs. LETSs have thus been the subject of many studies including [6], [3] and [10]. In the rest of this paper, also, we focus on LETSs. In the following, the proofs of theorems are omitted due to the limitation of space. They can, however, be found in [4].
III. DPL-BASED CHARACTERIZATION/SEARCH APPROACH
To characterize LETSs of variable-regular LDPC codes, in [6], Karimi and Banihashemi first generated all the graphical structures of LETSs in each (a, b) class. They then examined each structure S to find out whether S is a layered super set (LSS) of any of its cycles, i.e., whether S corresponds to a sequence of LETS structures that starts from a cycle and grows one variable node at a time to reach S. It was shown in [6] that a vast majority of non-isomorphic LETS structures are LSS of cycles. The relatively small number of structures that do not lend themselves to LSS characterization (starting

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 15:27:59 UTC from IEEE Xplore. Restrictions apply.
2525

2016 IEEE International Symposium on Information Theory

from cycles) are labeled as “NA” in [6]. Subsequently, it was shown in [3] that NA structures, themselves, are also LSSs of some basic graphical structures which are only slightly more complex than cycles. These basic structures are called prime with respect to the LSS property, or LSS-prime in brief, implying that they are not layered super sets of smaller structures. This characterization provided a simple LSS-based search algorithm that can find all the instances of ETSs of variable-regular LDPC codes, for any range of a and b values, in a guaranteed fashion, starting from the instances of LSSprime structures. In a given Tanner graph, to exhaustively search for (a, b) LETS instances within the range of interest a ≤ amax and b ≤ bmax, one needs to initially enumerate instances of prime structures that are needed as parents of the LETS structures under consideration. Some of these prime structures have relatively large a and b values. For practical codes, the multiplicity of instances of such prime structures can be easily in the range of tens of millions. This imposes a huge computational burden and memory requirement on the LSS-based search algorithm.
Example 2. Consider a (4, 8)-regular LDPC code with g = 6, and n = 4000 [9]. Suppose that one is interested in finding all the LETSs of this code in the range a ≤ 9 and b ≤ 8, using the LSS-based search algorithm. To do this, based on Table V of [6], one must enumerate all the instances of the simple cycle prime structure, s7 (simple cycles of length 14 in the Tanner graph). It appears that while there are 123, 111, 331 instances of s7 in the Tanner graph of this code, there is not a single instance of a LETS structure in all the classes of interest which has s7 as its prime sub-structure, in the Tanner graph.
To overcome the shortcomings of the LSS-based search algorithm, we propose a new characterization approach. It is based on three graph expansion techniques, depth-one tree, path and lollipop expansions, that are applied in the space of normal graphs, to generate larger LETS structures from smaller ones. The new characterization describes each and every LETS structure as an expansion of a simple cycle, using a combination of dot, path and lollipop expansion techniques, thus the name dpl-based characterization, or dpl characterization, in brief. In the following, we briefly explain these expansions.
Consider an (a, b) LETS structure S of a dv-regular Tanner graph with g ≥ 6. In Fig. 2(a), the expansion of a LETS structure S using a depth-one tree is shown. The notation dotm is used for a depth-one tree (dot) expansion with m edges. For the dotm expansion to result in a valid normal graph for a LETS structure of a variable-regular Tanner graph with variable degree dv, the m edges of the tree will have to be connected to nodes of S with degree less than dv. In addition, the degree m of the root node must be at least 2 and at most dv. Fig. 2(b) shows the path expansion of S using open and closed paths of length m + 1. The path expansion of S is a new LETS structure S of size a + m, that is constructed by appending a path of length m + 1 to S. The first and the last nodes of the path are common with S, and can be identical, in

G(S )

m-2

(a)

G(S )

m-1 edges

G(S )

m-1 edges

(b)

G(S )

d edges
(c)

c-2 edges

Fig. 2. Expansion of the LETS structure S with (a) a depth-one tree expansion with m edges, dotm, (b) an open and closed path of length m + 1, pam o , pam c , (c) a lollipop walk of length m + 1 = d + c, locm.

that case, the path is closed. It is clear that for an open-path expansion, the degrees of the two nodes that are common with S must be strictly less that dv (in G(S)), and for the closedpath expansion, the degree of the one common node must be strictly less than dv −1 in G(S). We use the notations pam o and pam c for open and closed paths of length m + 1, respectively. The notation pam is used to include both open and closed paths. In Fig. 2(c), the expansion of a LETS structure S using a lollipop walk of length m + 1 is shown. The notation lom c is used for a lollipop walk of length m + 1 (m is the number of nodes added to S), which consists of a cycle of length c (c ≥ 3) and a path of length d (d ≥ 1). Clearly, the common node between S and the lollipop expansion must have a degree
strictly less than dv in G(S). In Figs. 2(a)-(c), the symbol ◦
is used to represent the common node(s) between S and the
expansion graph, and the symbol • is used to represent the
other m nodes of the expansion graph. In [4], we have demonstrated, through a sequence of inter-
mediate results, that any LETS structure of variable-regular Tanner graphs can be generated from simple cycles using a combination of the three expansion techniques: dot, path and lollipop.
Theorem 1. All LETS structures of variable-regular Tanner graphs (that are not simple cycles), for any variable degree dv, and in any (a, b) class, can be generated by applying a combination of depth-one tree (dot), path, and lollipop expansions to simple cycles.
Compared to the LSS-based characterization, Theorem 1 provides a new characterization of LETS structures. Instead of using one expansion technique (dot) and some prime structures (simple cycles plus some other prime structures) to characterize the LETS structures, the new characterization uses more expansion techniques (dot, path and lollipop) but has fewer prime structures (only simple cycles). The dpl characterization is based on describing each LETS structure S as a hierarchy of embedded LETS structures that starts from a simple cycle, and expands to S in multiple steps, each step involving one

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 15:27:59 UTC from IEEE Xplore. Restrictions apply.
2526

2016 IEEE International Symposium on Information Theory

of the three expansion techniques, i.e., dot, path or lollipop. The basic idea behind the new characterization is to generate as many LETS structures as possible within the interest range of a and b values by using smaller LETS structures that have a and b values also within the range. When translated to a search algorithm, this has the benefit that we will only search for instances of the structures that are themselves of direct interest to us (have a and b values within the target range). The details of the dpl characterization algorithm can be found in [4].
One of the important properties of the dpl characterization is to generate a LETS structure in each level of expansion. With this constraint, i.e., all the sub-structures in the embedded sequence are LETSs, it is easy to see that the proposed characterization is minimal, in the sense that, none of the expansion steps can be divided into smaller expansions. The following result shows that minimal characterizations are based only on the three expansions dot, path and lollipop.
Theorem 2. Consider a characterization of a LETS structure S, based on a hierarchy of embedded LETS structures that starts from a simple cycle and generates S through a series of graph expansions. Assume that the characterization is minimal, in the sense that, none of the expansions can be broken into a sequence of smaller expansions such that the resulting sub-structures are still LETSs. Then, any graph expansion in the series, corresponding to a minimal characterization, is either a dot, a path or a lollipop expansion.

TABLE I CHARACTERIZATION (CYCLE PRIME SUB-STRUCTURES AND EXPANSION
TECHNIQUES) OF NON-ISOMORPHIC LETS STRUCTURES OF (a, b) CLASSES FOR VARIABLE-REGULAR GRAPHS WITH dv = 3 AND g = 8
FOR a ≤ amax = 12 AND b ≤ bmax = 4

a = 4 a = 5 a = 6 a = 7 a = 8 a = 9 a = 10 a = 11 a = 12

b=0 -

-

s4(1) −

-

s4(2) −

-

s4(5) s5(1)
−

-

s4(20) s5(2)

b=1 -

-

s4(1) - lo54 -
lo55

s4(4) −

s4(22) - s5(1)
−

-

b=2 -

-

s4(1) dot pa3 pa4

-

s4(5)
dot
pa3 pa4 lo44

-

s4(27) s5(1) dot

-

s4(179) s5(11)
−

s4(1)

dot

s4(3)

s4(16)

s4(115)

b = 3 - pa4 lo44 lo45

s4(1)

b=4

dot pa2

-

pa3

-
s4(2) dot pa2 pa3 lo44

dot - s5(1) -

pa4

dot, pa3

s4(9)

s4(57)

-

s5(1) dot

-

s5(6) dot

pa2

pa2

s5(7) dot

s4(481)

-

s5(48) s6(10)

−

b=5

-

s5(1) pa2

-

dot pa2

- dot, pa2

-

dot

-

pa3

b=6 -

-

s6(1) pa2

-

dot pa2

-

dot

-

−

Dpl characterization obtains minimal characterizations for LETS structures. The following result shows that this approach performs this task efficiently and effectively, by making sure that all the intermediate LETS structures generated in the characterization are within the target range of a and b values, if possible.
Theorem 3. For given values of dv, g, amax and bmax, dpl characterization provides an optimal minimal characterization of all non-isomorphic (a, b) LETS structures of variableregular graphs with variable degree dv and girth g, with a ≤ amax and b ≤ bmax, in the sense that, to generate all such structures starting from simple cycles and using dot, path, and lollipop expansions, the maximum length K of the required simple cycles, and the maximum range bmax of the b values of the LETS structures that are generated in the characterization process, are minimized by the dpl characterization.
In this paper, as an example, a characterization table for dv = 3, g = 8, a ≤ 12 and b ≤ 4 is provided in Table I. More results for different values of dv, g, amax and bmax are reported in [4].
In Table I, columns and rows correspond to different values of a and b, respectively. For each (a, b) class of LETSs, the top entries in the table (separated by a dashed line from the bottom entries) show the prime simple cycles that are parents of the LETS structures within that class, and the multiplicity of non-isomorphic structures in the class with those parents. The bottom entries show the expansion techniques applied to

all the LETS structures within the class. As discussed before, to cover a given range of a and b values exhaustively, it is sometimes required to generate LETS structures with b values larger than bmax and up to some bmax > bmax. In the characterization table, the LETS classes with b > bmax are separated from the rest by a double-line. For classes with b > bmax, the multiplicity of non-isomorphic LETS structures are not reported in the table. Having the symbol “-”, as the only entry for a class means that it is impossible to have LETS structures in that class. Having the symbol “-” as the only bottom entry means that no expansion technique is applied to the structures in that class. In Table I, the simple prime cycles that are needed to generate all the structures within the table are boldfaced (s4, s5 and s6).
Consider the LETS class (8, 4) of Table I. There are 10 non-isomorphic LETS structures in this class. Nine and one of these structures are generated starting from s4 and s5, respectively. To obtain all the LETS structures in the table, one needs to apply dot and pa2 expansions to each one of the 10 structures in this class. This results in the generation of LETS structures in (9, 1), (9, 3) and (10, 4) classes.
The characterization of LETS structures corresponds to an efficient search algorithm to find the instances of all (a, b) LETS structures of a variable-regular Tanner graph for a given range of a and b values, in a guaranteed fashion. The search algorithm starts by the enumeration of simple cycles that are identified in the characterization table as the required

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 15:27:59 UTC from IEEE Xplore. Restrictions apply.
2527

2016 IEEE International Symposium on Information Theory

TABLE II MULTIPLICITIES OF LETS AND FEAS STRUCTURES OF CODES C1 , C2
AND C3 WITHIN THE RANGE a ≤ 12 AND b ≤ 4

C1

C2

C3

(a, b) Total Total [8] Total Total [8] Total Total [8]

class LETS FEAS FAS LETS FEAS FAS LETS FEAS FAS

(4,4) 465 0

0 802 760 760 2 2 2

(5,3) 155 155 155 14 14 14 0 0 0

(6,4) 930 0

0 985 849 849 1 1 1

(7,3) 930 0

0

57 47 47 0 0 0

(8,2) 465 465 465 5 4 4

0 00

(8,4) 5115 1395 1395 2629 2270 2270 49 46 nr

(9,1) 0

0

0

1 11

0 00

(9,3) 1860 930 930 156 146 146 1 1 nr

(10,2) 1395 1395 1395 6 6 6

0 00

(10,4) 29295 17670 17670 8868 7399 nr 173 148 nr

(11,3) 6200 5270 5270 628 577 nr 9 9 nr

(12,2) 930 930 930 (12,4) 180885 115320 nr†
dpl 442 sec. / 28 sec. *

27 26 26 32549 26936 nr 561 sec. / 38 sec.

0 00 512 466 nr 157 sec. / 17 sec.

LSS

- / 392 sec.

- / 1563 sec.

- / 1466 sec.

† “nr” stands for not reported. * Run-time to find instances in all classes/in the first 10 classes.

prime structures. After the enumeration of all instances of a simple cycle, these instances are expanded by dot, path and lollipop expansions, following the characterization tables to find instances of other LETS structures up to size amax.
IV. NUMERICAL RESULTS
We have applied the LSS-based and dpl-based search algorithms to a large number of variable-regular random and structured LDPC codes with a wide range of variable degrees, rates and block lengths. In this paper, the results of only three LDPC codes, denoted by C1, C2 and C3, are reported. The results for more LDPC codes can be found in [4]. For all the run-times reported in this paper, a desktop computer with 2.4-GHz CPU and 8-GB memory is used.
Multiplicities of instances of LETSs and FEASs in all the nonempty classes within the range a ≤ 12 and b ≤ 4, for the three codes are listed in Table II. All the codes have dv = 3 and g = 8. The search algorithm to find all the instances of LETSs in the range of interest is based on the information provided in Table I.
Code C1 is the Tanner (155, 64) code [11] with dc = 5. The error-prone structures of this code have been widely investigated [7], [12], [8], [6]. It is well-known that the LETS structures in the (8, 2) and (10, 2) classes are the dominant LETS structures of this code. It took the dpl-based search algorithm 442 seconds to find all the instances of LETS structures reported in Table II for this code. The notation “-” in the table for the run-time of the LSS-based search means that this algorithm was not able to exhaustively find all the instances of the LETS structures of interest within a day. If we limit the range of search to a ≤ 10 and b ≤ 4, it takes the proposed algorithm only 28 seconds to find all the instances of LETS structures of C1 within this range. However, it takes the LSS-based algorithm 392 seconds to find all the instances of LETS structures of C1 within the range a ≤ 10 and b ≤ 4. For this code, its structure is not used to simplify the search, and

all the run-times are based on exhaustive search of the code’s
Tanner graph without taking into account the automorphisms
that exist in the graph. Codes C2 and C3 are rate-1/2 (3, 6)-regular LDPC codes
with n = 504 and 1008, respectively, constructed by the PEG algorithm [5]. The dpl-based search algorithm finds all the instances of LETSs for codes C2 and C3 within the range a ≤ 12 and b ≤ 4, in 561 and 157 seconds, respectively. Again, the exhaustive search of LETSs for both codes within the range a ≤ 12, b ≤ 4, is out of the reach of the LSS-based search algorithm. If we limit the range of search to a ≤ 10 and b ≤ 4, it takes the proposed algorithm only 38 and 17 seconds to find all the instances of LETSs of C2 and C3 within this range, respectively. It, however, takes the LSS-based algorithm 1563 and 1466 seconds to find all the instances of LETSs of C2 and C3, respectively, within the range a ≤ 10 and b ≤ 4.
In Table II, we have also reported the multiplicity of
instances of FASs obtained by the exhaustive search algorithm
of [8]. It can be seen that the results of [8] match those
obtained here. One should, however, note that while the range
of classes reported in Table II is much larger than the range
of classes reported in [8], it took the exhaustive search of
[8] more than 5 hours, compared to only a few minutes for
the proposed algorithm, to generate the fully absorbing sets
of each of these codes on a desktop computer with an Intel
Core2 2.4-GHz CPU with 2GB memory [8].
REFERENCES
[1] S. Abu-Surra, D. DeClercq, D. Divsalar, and W. E. Ryan, “Trapping set enumerators for specific LDPC codes,” in Proc. Inf. Theory Appl. Workshop (ITA), 2010, SanDiego, CA, USA, Jan./Feb. 2010, pp. 1–5.
[2] L. Dolecek, Z. Zhang, V. Anantharam, M. J. Wainwright, and B. Nikolic, “Analysis of absorbing sets and fully absorbing sets of array-based LDPC codes,” IEEE Trans. Inf. Theory, vol. 56, no. 1, pp. 181–201, Jan. 2010.
[3] Y. Hashemi and A. H. Banihashemi, “On characterization and efficient exhaustive search of elementary trapping sets of variable-regular LDPC codes,” IEEE Comm. Lett., vol. 19, pp. 323–326, Mar. 2015.
[4] Y. Hashemi and A. H. Banihashemi, “New characterization and efficient exhaustive search algorithm for elementary trapping sets of variableregular LDPC codes,” Submitted to IEEE Trans. Inf. Theory, available online at: http://arxiv.org/pdf/1510.04954.
[5] X.-Y. Hu, E. Eleftheriou, and D.-M. Arnold, “Regular and irregular progressive edge-growth Tanner graphs,” IEEE Trans. Inf. Theory, vol. 51, no. 1, pp. 386–398, Jan. 2005.
[6] M. Karimi and A. H. Banihashemi, “On characterization of elementary trapping sets of variable-regular LDPC codes,” IEEE Trans. Inf. Theory, vol. 60, no. 9, pp. 5188–5203, Sep 2014.
[7] M. Karimi and A. H. Banihashemi, “Efficient algorithm for finding dominant trapping sets of LDPC codes,” IEEE Trans. Inf. Theory, vol. 58, no. 11, pp. 6942–6958, Nov. 2012.
[8] G. B. Kyung and C.-C. Wang, “Finding the exhaustive list of small fully absorbing sets and designing the corresponding low error-floor decoder,” IEEE Trans. Commun., vol. 60, no. 6, pp. 1487–1498, Jun. 2012.
[9] D. Mackay, “Encyclopedia of sparse graph codes,” URL: http://www.inference.phy.cam.ac.uk/mackay/codes/data.html.
[10] D. V. Nguyen, S. K. Chilappagari, M. W. Marcellin, and B. Vasic, “On the construction of structured LDPC codes free of small trapping sets,” IEEE Trans. Inf. Theory, vol. 58, no. 4, pp. 2280–2302, Apr. 2012.
[11] R. M. Tanner, T. D. Sridhara, and T. Fuja, “A class of group-structured LDPC codes,” in Proc. Int. Symp. Commun. Theory Appl., Ambleside, U.K., Jul. 2001, pp. 365–370.
[12] S. Zhang and C. Schlegel, “Controlling the error floor in LDPC decoding,” IEEE Trans. Commun., vol. 61, no. 9, pp. 3566–3575, Sep. 2013.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 15:27:59 UTC from IEEE Xplore. Restrictions apply.
2528

