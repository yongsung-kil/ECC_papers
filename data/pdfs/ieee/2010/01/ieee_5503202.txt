Performance Analysis of Iterative Decoding Algorithms with Memory
Emil Janulewicz and Amir H. Banihashemi Dept. of Systems and Computer Engineering, Carleton University, Ottawa, Ontario, Canada

Abstract—Density evolution is often used to determine the performance of an ensemble of low-density parity-check (LDPC) codes under iterative message-passing algorithms. Conventional density evolution techniques over memoryless channels are based on the independence assumption amongst all the processed messages in variable and check nodes. This assumption is valid for many algorithms such as standard belief propagation (BP) and min-sum (MS) algorithms. However, there are other important iterative algorithms such as successive relaxation (SR) versions of BP and MS, and differential decoding with binary message passing (DD-BMP) algorithm of Mobini et. al., for which this assumption is not valid. The dependence created among messages for these algorithms is due to the introduction of memory in the iterative algorithm. In this work, we propose a model for iterative decoding algorithms with memory which covers SR and DD-BMP algorithms as special cases. Based on this model, we derive a Bayesian network for iterative algorithms with memory over memoryless channels and use this representation to analyze the algorithms using density evolution. The density evolution technique is developed based on truncating the memory of the decoding process and approximating it with a finite order Markov process, and can be implemented efficiently. As an example, we apply our technique to analyze the performance of DD-BMP on regular LDPC code ensembles, and make a number of interesting observations with regard to the performance/complexity trade off of DD-BMP in comparison with BP and MS algorithms.
I. INTRODUCTION
Low-density parity-check (LDPC) codes [1] are known to have good performance when decoded with iterative decoding algorithms, also known as message-passing algorithms [2]. An analytical tool called density evolution can be used to find the threshold of a particular code ensemble under a given iterative decoding algorithm [2]. The threshold is an asymptotic measure of performance and is defined as the worst channel parameter (e.g., largest noise variance) for which the probability of error still converges to zero as the number of iterations tends to infinity. Density evolution is also a powerful technique for constructing irregular LDPC codes through the optimization of the degree distributions [3]. In general, there is a large body of literature devoted to the derivation and applications of density evolution for a variety of iterative decoding algorithms, most notably for belief propagation (BP) and min-sum (MS) algorithms and their variants. To the best of our knowledge however, all the message-passing algorithms analyzed by density evolution in the literature are memoryless, i.e., the output messages of a variable node (check node) at iteration ℓ are only a function of the input messages to that node at iteration ℓ (ℓ − 1) and also the initial message of the channel in the case of variable nodes. There exist however a

number of iterative decoding algorithms, such as successive relaxation (SR) variants of BP and MS [4], [5], and DD-BMP (differential decoding with binary message-passing) [6], [7], that have memory. In fact, the presence of memory in these algorithms improves the performance but makes the density evolution analysis much more complex. So far, the analysis of such algorithms has been limited to Monte Carlo simulations.
In performing density evolution and in the calculation of thresholds, one assumes a cycle-free (tree-like) graphical representation of the ensemble [2], and tracks the probability distribution of messages under this assumption. In the density evolution analysis of memoryless algorithms over memoryless channels, the cycle-free assumption leads to the independence between all messages being processed at variable and check nodes. This significantly simplifies the analysis, and makes the complexity of density evolution in each iteration independent of the iteration number ℓ, and thus the total complexity of density evolution scales only linearly with ℓ.
In this paper, we develop the framework for the density evolution analysis of iterative algorithms with memory over memoryless channels. We first present a general model for iterative decoding algorithms with memory which includes DD-BMP and SR algorithms as special cases. Based on this model, we employ the Bayesian network representation, via a directed acyclic graph (DAG), to capture the dependencies among different messages and memory contents in a space with two dimensions: iteration number ℓ and the depth of the decoding tree d. Using this, we show that for algorithms with memory, the incoming messages to a node are no longer independent, and that the existing dependencies cause the complexity of density evolution to grow at least exponentially with ℓ. We also derive the density evolution equations and use techniques to make them tractable. In particular, we introduce an approximation by truncating the memory of the decoding process and estimating it with a Markov process of finite order m (not an increasing function of ℓ). This reduces the complexity of density evolution to linear in ℓ. As an example, we apply the proposed density evolution technique to DDBMP for a number of regular LDPC code ensembles and obtain the thresholds. By comparing these threshold values to those of BP and MS, a number of interesting observations with regard to the performance/complexity trade off of DDBMP in comparison with BP and MS algorithms are made. In addition to the above results, one of the main contributions of this work is to introduce the general framework for presentation and analysis of iterative decoding algorithms with

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:01:57 UTC from IEEE Xplore. Restrictions apply.

memory. This can be used to design irregular codes for the existing algorithms or to devise new algorithms with improved performance/complexity trade offs.

II. GENERAL MODEL FOR ITERATIVE DECODING ALGORITHMS WITH MEMORY
A. Model
Fig. 1(a) shows a snapshot of the Tanner graph of an LDPC code at iteration ℓ for a memoryless decoding algorithm, where variable and check nodes are represented by circles and squares, respectively. Under cycle-free assumption over memoryless channels and based on the principle of extrinsic message passing, all incoming messages M1(ℓ), . . . , Md(vℓ)−1 to node V are independent of each other and of the channel mesage M0 (dv is the variable node degree). Moreover, all the incoming messages are identically distributed. The outgoing message MV(ℓ→) C is thus a function of dv − 1 independent identically distributed (i.i.d.) random variables and the channel message. Similarly, the outgoing message of a check node is a function of dc −1 i.i.d. variables corresponding to the extrinsic incoming messages, where dc is the check node degree. The distribution of a function of independent random variables is relatively easy to find since the joint distribution of these variables is the product of their marginal distributions. In this case, the complexity of finding the message distributions in each iteration, does not change with increasing ℓ.

C1

M1(ℓ)

C2 M2(ℓ)

V MV(ℓ→) C C

Md(vℓ)−1 Cdv−1
M0

(a) Memoryless

C1

M1(ℓ)

C2 M2(ℓ)

Md(vℓ)−1 Cdv−1

B(ℓ−1) D

V X(ℓ) ΨB

B(ℓ)

M0

MV(ℓ→) C C ΨM

(b) With Memory

Fig. 1. Message passing between variable and check nodes in iterative algorithms a)without and b)with memory.

In this work, we consider the existence of the memory on
the links from variable nodes to check nodes. This can be
generalized to the case where memory exists in both directions.
Our model is shown in Fig. 1(b) on a snapshot of the Tanner graph at iteration ℓ. Similar to Fig. 1(a), we have a set of i.i.d. extrinsic incoming messages to a variable node V . The outgoing message from V is denoted by X(ℓ). The straight pass through line between V and C in Fig. 1(a) is now replaced by a memory unit whose content B(ℓ) is updated by ΨB(X(ℓ), B(ℓ−1)). The incoming message MV(ℓ→) C to node C from V is then obtained by ΨM (B(ℓ)) as a deterministic

function of the memory content B(ℓ). It is important to note that while the message X(ℓ) is a function of independent random variables, the outgoing message, MV(ℓ→) C , is a function of dependent random variables X(ℓ) and B(ℓ−1).

B. SR and DD-BMP Algorithms as Special Cases
We consider the application of regular (dv, dc) binary LDPC codes over a binary-input output-symmetric memoryless channel, where coded bits 0 and 1 are mapped to channel input symbols +1 and −1, respectively.
1) SR Algorithms: Any standard memoryless iterative algorithm, such as BP or MS, can be turned into an SR algorithm by proper introduction of memory [4], [5]. SR algorithms can be performed in different message domains. In this work, we assume log-likelihood ratio (LLR) domain for messages (SR-LLR). Under this assumption, the SR algorithms are symmetric [2]. Consider a memoryless iterative algorithm with variable and check node maps denoted by ΨV and ΨC , respectively. Based on the model of Fig. 1(b), the SR version is defined by the following variable node map (the check node map remains intact):

B(ℓ) = ΨB B(ℓ−1), X(ℓ) = (1 − β) B(ℓ−1) + βX(ℓ) ,
MV(ℓ→) C = ΨM B(ℓ) = B(ℓ) , (1)
where X(ℓ) = ΨV (M0, M1(ℓ), · · · , Md(vℓ)−1). In (1), β is called the relaxation factor, and can be optimized for the best performance. The optimal value of β is usually within (0, 1).
2) DD-BMP: Differential decoding with binary message passing (DD-BMP) was introduced in [6], [7], as an attractive alternative to purely hard-decision algorithms. This algorithm combines the simplicity of binary message-passing with the good performance of soft-decision algorithms, where the soft information is stored in edge- or node-based memories. In the former case, studied in this paper, the variable node map, following the model of Fig. 1(b), is defined by:

B(ℓ) = ΨB B(ℓ−1), X(ℓ) = B(ℓ−1) + X(ℓ) , (2)
MV(ℓ→) C = ΨM B(ℓ) = sgnr(B(ℓ)) ,

where sgnr(x) = 1 for x > 0, and = −1 for x < 0. (For x = 0,

sgnr(x) takes +1 or -1 randomly with equal probability.) In

(2), X(ℓ) = ΨV (M1(ℓ), · · · , Md(vℓ)−1), which for the BIAWGN

channel reduces to X(ℓ) =

dv −1 i=1

Mi(ℓ).

The

memory

content

B(0) is initialized by the channel message M0. The check

node operation, performed on extrinsic binary messages from

the alphabet space {+1, −1}, is simply a product.

C. Symmetry of the Decoder and Error Probability
The analysis of iterative decoders is greatly simplified assuming that both the channel and the decoder are symmetric. The decoder symmetry conditions for variable and check nodes are defined in [2]. All the decoders considered in this work are assumed to be symmetric. In particular, the variable node symmetry condition has some implications on the choices of

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:01:57 UTC from IEEE Xplore. Restrictions apply.

the mappings ΨB and ΨM : ΨB should be sign inversion invariant [2], and ΨM (−x) = −ΨM (x).
With both the channel and the decoder being symmetric,
we can assume, without loss of generality, that the all-zero
codeword is transmitted. In this case, the average probability of bit error in iteration ℓ is calculated by

Pe(ℓ)

=

P (B(ℓ)

<

0)

+

1 2

P

(B(ℓ)

=

0)

,

(3)

where P (A) is used to denote the probability of an event A.

B1 V B C
Fig. 2. Tree-like neighborhood of the edge (V, C) in a regular (3,4) code.

III. BAYESIAN NETWORK REPRESENTATION OF ITERATIVE DECODING ALGORITHMS WITH MEMORY
A. Bayesian Networks and Conditional Independence
A Bayesian network is a graphical model that represents a set of random variables and their statistical (in)dependence via a directed acyclic graph (DAG). In this work, we use a Bayesian network to represent the dependencies among different messages and memory contents of an iterative algorithm with memory. These dependencies are studied as a function of both the iteration number ℓ and the depth of the decoding tree d based on the model of Fig. 1(b). In fact, as we will see later, it is the conditional independence among different random variables that plays a crucial role in simplifying the representation and derivation of the desired distributions.
We start by some basic definitions and notations. We use upper case, e.g., X, to denote a random variable and lower case, e.g., x, to denote a realization of a random variable. We consider a DAG G as an ordered pair G = (V, E), where V is the set of nodes, and E is the set of directed edges in G. Each node represents a random variable, and directed edges imply conditional (in)dependence among the random variables as explained in the following. Throughout this paper, we use the terms “nodes” and “random variables” interchangeably. Nodes V and U are referred to as the head and the tail of the directed edge (U, V ). We also call U a parent of V , and V a child of U . The sets of parents and children of a node V ∈ V are denoted by pa(V ) and ch(V ), respectively. We refer to the union of the sets ch(V ), ch(ch(V )), . . . as the descendants of V and denote them by de(V ). For a DAG, it is clear that the sets pa(V ) and de(V ) are disjoint. In a DAG, we define an undirected path between a node U and a node V as a sequence of nodes starting with U and ending with V such that successive nodes are connected by an edge.
The conditional independence between two sets of random variables X and Y given a third set Z is defined by
X ⊥⊥ Y|Z ⇐⇒ P (X , Y|Z) = P (X |Z) P (Y|Z) , (4)
where P (X ) and P (X |Z) are the marginal distribution of X and the conditional distribution of X given Z, respectively. Marginal independence between X and Y is denoted by X ⊥⊥ Y and is defined as in (4) where Z = ∅ and P (X |∅) := P (X ).
A DAG G(V, E) is a Bayesian network with respect to the set of random variables V = {V1, . . . , VN }, if the joint

probability distribution of V can be factored as follows:
N
P (V) := P (V1, . . . , VN ) = P (Vi|pa(Vi)) , (5)
i=1
or in other words, if for any V ∈ V, we have

V ⊥⊥ V\{V ∪ de(V ) ∪ pa(V )} | pa(V ) .

(6)

To go beyond the basic conditional independence relations
given by the local constraints (6), and to ascertain whether a general conditional independence statement X ⊥⊥ Y|Z is implied by a given DAG, where X , Y and Z are disjoint sets
of random variables, we need the following definitions: Definition 1: An undirected path between a node V and a
node W is blocked by a set of nodes X if the path includes a node U such that either: (a) U has head-to-head arrows along the path (−→ U ←−), and neither U nor any of its descendants is in X , or (b) U does not have head-to-head arrows along the path and U is in X .
Definition 2: Given a set of nodes Z, if every undirected path from any node in X to any node in Y is blocked by Z, then X is said to be d-separated from Y by Z.
Fact 1 [8]: In a DAG with disjoint sets of nodes X , Y and Z, if Z d-separates X from Y, we have X ⊥⊥ Y|Z.
In fact, it can be seen that characterization of (5) for
a joint distribution is equivalent to the satisfaction of all
the conditional independence properties implied by the DAG
through d-separation [8].

B. Bayesian Networks of Iterative Decoders with Memory

Consider the of the directed

deidrgeecte−→de

neighborhood Nd(−→e ) of depth d [2] for a (dv, dc) Tanner graph.

Assuming a cycle-free graph, the neighborhood is tree-like. Fig. 2 shows an example of N2(−→e ), where −→e = (V, C), for a (3, 4) Tanner graph. Memory elements only appear at even

depths on the tree, and they are shown by shaded circles.

We use the notation Bd/2 to denote the memory elements that appear in Nd((V, C))\Nd−2((V, C)). It is easy to see |Bi| = {(dv − 1)(dc − 1)}i, for i = 1, 2, . . ..

Based on the principle of extrinsic message-passing, one can see that X(ℓ) in Fig. 1(b) is a deterministic function of B1(ℓ−1) and M0. Moreover, as we discussed before, B(ℓ) is a function of B(ℓ−1) and X(ℓ). In addition, Bi(ℓ) depends on Bi(ℓ−1) and Bi(+ℓ−11). The Bayesian network corresponding to

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:01:57 UTC from IEEE Xplore. Restrictions apply.

d B(0) M0 M0,1 B1(0) M0,2 B2(0) M0,3 B3(0)

B(1) ℓ
B(2)

X (1)

B1(1)

X (2) B1(2)

B2(1)

B(3)

X (3)

Fig. 3. Bayesian network based on the model of Fig. 1(b).

these relationships is given in Fig. 3. In this figure, M0,i is defined as the initial messages corresponding to the variable nodes in N2i((V, C))\N2i−2((V, C)) (see Fig. 2). The dashed edges between M0 and nodes X(ℓ) would disappear if X(ℓ) is not a function of M0 which is e.g. the case for DD-BMP.
In the following section, we demonstrate how the Bayesian
network of Fig. 3 can help in simple representation and
derivation of probability distributions.

IV. DENSITY EVOLUTION

In the following, for the sake of simplicity, we assume that all the random variables are discrete. Based on (3), to obtain Pe(ℓ) at each iteration ℓ, we need to compute P B(ℓ) . To further

simplify the analysis, we assume that for the algorithm under

consideration, the dashed edges in Fig. 3 do not exist.1 From Fig. 3, one can see that B(ℓ) is a function of i.i.d. random

variables in the set M0,0→ℓ := {M0, M0,1, . . . , M0,ℓ}. There

are a total of

ℓ i=0

((dv

− 1)(dc

− 1))i

=

1−((dv −1)(dc−1))ℓ+1 1−(dv −1)(dc−1)

random variables in M0,0→ℓ, all with the same sample space

SO. One can use the following “naive” approach to compute

P B(ℓ) :

P B(ℓ) =

P B(ℓ)|M0,0→ℓ = m P (M0,0→ℓ = m),

m∈SM0,0→ℓ
(7)

where the sum is taken over the sample space of M0,0→ℓ which

has |S | 1−((dv −1)(dc−1))ℓ+1

O

1−(dv −1)(dc−1)

elements. Note that the number of

elements grows doubly exponentially with ℓ.

A more efficient approach to compute P B(ℓ) is:

P (B(ℓ) = b) =

P (B(ℓ−1) = b′ , X(ℓ) = x)

(b′ ,x)∈SB ×SX :b=ΨB (b′ ,x)
∀b ∈ SB, (8)
where SB and SX are the sample spaces of B(ℓ) and X(ℓ), respectively.
Let X(1→k) = X(1), X(2), ..., X(k) for k ≥ 1. To com-
pute the joint distribution of the dependent random variables

1For the case where the dashed edges do exist, the analysis is more tedious and is omitted here due to the space limitation.

B(ℓ−1) and X(ℓ), which is required in (8), we use Fact 1 to establish that

B(0) ⊥⊥ X(1) , and B(k) ⊥⊥ X(k+1)|X(1→k) ,

(9)

and thus to obtain

P B(ℓ−1), X(ℓ) =

P B(ℓ−1)|X (1→ℓ−1) P X (1→ℓ) .

x(1→ℓ−1)

(10)

From equation (10), one can see that computing P B(ℓ) requires finding the conditional distribution P B(ℓ−1)|X(1→ℓ−1) over a space of size |SB| × |SX |ℓ−1, and the joint dstribution P X(1→ℓ) over a space of size |SX |ℓ. Both sizes grow exponentially in ℓ. In the following we show how P B(ℓ−1)|X(1→ℓ−1) can be computed efficiently.
Similar techniques are used to simplify the computation of P X(1→ℓ) .

P B(ℓ)|X(1→ℓ)

=

P B(ℓ)|X(1→ℓ), B(ℓ−1) P B(ℓ−1)|X(1→ℓ)

b(ℓ−1)

=

P B(ℓ)|X(ℓ), B(ℓ−1) P B(ℓ−1)|X(1→ℓ−1) ,

b(ℓ−1)

(11)

where at the last step, we have used B(ℓ) ⊥⊥

X(1→ℓ−1)|{X(ℓ), B(ℓ−1)} and B(ℓ−1) ⊥⊥ X(ℓ)|X(1→ℓ−1),

both based on Fact 1. Note that in (11), P B(ℓ)|X(ℓ), B(ℓ−1)

is equal to either 0 or 1 as B(ℓ) is a deterministic function of B(ℓ−1) and X(ℓ), and P B(ℓ−1)|X(1→ℓ−1) is obtained from

the previous iteration.

V. MEMORY TRUNCATION
To avoid the exponential increase of the computational resources (memory and CPU time) required for the density evolution analysis with increasing iteration number ℓ, we approximate the memory sequence B(0−→ℓ) =: {B(0), B(1), . . . , B(ℓ)} by a Markov process of some finite order m, i.e., we assume

P B(m+k)|B(m+k−1), ..., B(0) (12)
≈ P B(m+k)|B(m+k−1), ..., B(k) .
We refer to this as memory truncation of order m. Increasing the value of m improves the accuracy of the analysis at the expense of higher computational complexity. A proper value of m is chosen to strike the right balance between the complexity and the accuracy.
VI. SIMULATION RESULTS AND DISCUSSIONS
In this paper, due to the limitation of space, we only present our results on DD-BMP applied to regular LDPC codes over the BIAWGN channel. The details of the algorithm can be found in [6], [7]. For different ensembles, we analyze the performance of DD-BMP by computing its threshold. In general, increasing the number of quantization bits q improves

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:01:57 UTC from IEEE Xplore. Restrictions apply.

3.4 3.35
3.3

Threshold = 3.26 dB
min

m=3 m=4 m=5

E /N (dB)
b0

3.25

3.2
0.02 0.04 0.06 0.08 0.1 0.12 0.14 0.16 0.18 0.2 ∆ (Quantization Step)
Fig. 4. Threshold values of the (3, 6) regular LDPC code ensemble vs. ∆ for different m.
100

10−1 10−2 10−3

N = 300,000 N = 400,000 N = 500,000

BER

10−4

Threshold = 3.18 dB

10−5

TABLE I THRESHOLDS OF BP AND DD-BMP FOR ENSEMBLES WITH dv = 6 AND
7 ≤ dc ≤ 18

dc rate
7 1/7 8 1/4 9 1/3 10 2/5 11 5/11 12 1/2 13 7/13 14 6/7 15 3/5 16 5/8 17 11/17 18 2/3

Eb N0

(dB)
BP

5.14

3.52

2.90

2.61

2.47

2.41

2.39

2.40

2.43

2.46

2.50

2.54

Eb N0

(dB)
DD−BM P

6.46

4.71

4.00

3.63

3.45

3.34

3.26

3.24

3.24

3.24

3.26

3.28

Diff (dB)
1.32 1.19 1.10 1.02 0.98 0.93 0.87 0.84 0.81 0.78 0.76 0.74

TABLE II THRESHOLDS OF MS AND DD-BMP FOR RATE-1/2 ENSEMBLES

dv dc
36 48 5 10 6 12 7 14

Eb N0

(dB)
MS

1.70

2.49

3.09

3.54

3.91

Eb N0

(dB)
DD−BM P

3.26

2.80

3.18

3.34

3.61

Diff (dB)
1.56 0.31 0.09 -0.21 -0.30

10−6

10−7

10−8 2.8

3

3.2

E /N (dB)
b0

Fig. 5. BER curves of (5, 10) regular LDPC codes with block lengths 300,000, 400,000, and 500,000 decoded by DD-BMP, and the corresponding threshold.

the performance, but the performance improvement beyond q = 8 is negligible. For a particular ensemble and a given value of q, there exists an optimal quantization step ∆ which results
in the best threshold value. To study the effect of truncation
order on the accuracy of simulation results, we have shown the threshold values for a (3, 6) ensemble versus ∆ for different truncation orders m in Fig. 4 (q = 8). The figure shows that the results for m = 4 and m = 5 practically coincide (within 0.01 dB accuracy). Fig. 4 also shows that the optimal value of ∆ is about 0.08 which corresponds to the threshold Eb/N0 = 3.26 dB. For the rest of the experiments, we use q = 8, m = 4, and the optimal value of ∆ in each case, to study the effect
of degree distributions on the threshold of DD-BMP. The thresholds are obtained for all regular (dv, dc) codes
with 3 ≤ dv ≤ 8 and dv < dc ≤ 18. Fig. 5 shows the bit error rate (BER) performance of three (5, 10) codes with different block lengths N . As expected, a threshold phenomena occurs
and the calculated threshold value matches well with the
waterfall region of the BER curves. For a fixed dv, the threshold gap between DD-BMP and BP
decreases with increasing the rate. The results for dv = 6 are shown in Table I. As can be seen, at higher rates the perfor-

mance gap is less than 1 dB. The same phenomenon does not necessarily occur in the comparison between DD-BMP and MS. However, for the codes with larger degrees, DD-BMP outperforms MS. The results for rate-1/2 codes are reported in Table II. These results show that by increasing the degrees, the performance gap between MS and DD-BMP, which is to the advantage of MS for lower degrees, disappears and then reverses to the advantage of DD-BMP. These performance results for DD-BMP are impressive considering that both the check node operations and the message-passing for DDBMP are much simpler than those of BP and MS. They also demonstrate the potential of iterative decoding algorithms with memory in achieving better performance/complexity trade offs compared to memoryless algorithms.
REFERENCES
[1] R. Gallager, “Low-density parity-check codes,” IEEE Trans. Inform. Theory, vol. 8, no. 1, pp. 21–28, January 1962.
[2] T. J. Richardson and R. L. Urbanke, “The capacity of low-density paritycheck codes under message-passing decoding,” IEEE Trans. Inform. Theory, vol. 47, no. 2, pp. 599–618, Feb 2001.
[3] T. J. Richardson, M. A. Shokrollahi, and R. L. Urbanke, “Design of capacity-approaching irregular low-density parity-check codes,” IEEE Trans. Inform. Theory, vol. 47, no. 2, pp. 619–637, Feb 2001.
[4] S. Hemati and A. H. Banihashemi, “Dynamics and performance analysis of analog iterative decoding for low-density parity-check (LDPC) codes,” IEEE Trans. Comm., vol. 54, pp. 61–70, Jan. 2006.
[5] H. Xiao, S. Tolouei, and A. H. Banihashemi, “Successive relaxation for decoding of LDPC codes,” 24th Biennial Symposium on Comm., pp. 107– 110, 2008.
[6] N. Mobini, A. H. Banihashemi, and S. Hemati, “A differential binary message-passing LDPC decoder,” IEEE Global Telecommunications Conference, pp. 1561–1565, Nov. 2007.
[7] ——, “A differential binary message-passing LDPC decoder,” IEEE Trans. Comm., to be published, Sept. 2009.
[8] F. V. Jensen and T. D. Nielsen, Bayesian Networks and Decision Graphs, 2nd ed., M. Jordan, J. Kleinberg, and B. Scholkopf, Eds. New York, NY: Springer Science, 2007.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:01:57 UTC from IEEE Xplore. Restrictions apply.

