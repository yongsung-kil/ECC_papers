ISIT 2000, Sorrento, Italy,June 25-30,2000

On the Efficiency of Some Suboptimal Algorithms for Bit Decoding of Binary Codes’

Elke Offer
Institute for Commun. Engineering Munich University of Technology Arcisstr. 21 D-82290 Munich, Germany
elkeQ1nt.e-technik.tn-mnenchen.de

Emina Soljanin
Mathematical Sciences Research Center,
Bell Labs, Lucent Technologies, Murray Hill, N J 07974,USA
eminaQlucent.com

Abshrrct - Several popular, suboptimal algorithms for bit decoding of binary block codes such as turbo decoding, threshold decoding, and message passing
for LDPC,were developed almost as a common sense
approach to decoding of some specially designed codes. We explain exactly how they approximate the optimal decoding algorithm, and show how good this approximation is in some special cases.
We propose an entirely new approach to the problem of iterative decoding, which is algebraic in nature and derives the well known suboptimal algorithms &om the bit-optimal as a starting point. This approach gives new insights into the issues of iterative decoding.
We are concerned with a binary block code C defined by its parity-check matrix H = (hij}(n-k)xn, i.e., by the group generators hi = (hij}lxn, i E 1,of its dual code C’, where Z
isusedtodenotetheindexsetZ={O,l, ...,n - k - 1 1 ) . We
consider suboptimal decoding algorithms for a binary code
C, e.g., a turbo decoding scheme (as introduced in [I], [2]) with two component codes C1 and CZ defined by their dual codes’ sets of generators ZI and ZZsuch that ZI nZz = 0 and
zl nz2= Z.
The channel is assumed to be memoryless and codewords equiprobable. We derive an expression for optimal decoding
of the whole code C based on the dual code (as in [3]), and
then rewrite it so that it explicitly involves only h,. Thus, for the log-likelihood of bit m over code C, L$, = log[P(c, = Olr)/P(% = llr)], we obtain the following:

[A2Iv+1 =
+ The corresponding expression for the log-likelihood of bit m
of the turbo decoding algorithm at iteration (Y 1) given by

-.-

+ where Aj = b(yj IO) - P(Tj Il)]/lp(rj 10) p(rj11)] and

{ AigA.-((xi.Aj)1-6i,j = A i . A j , i f j ,

1-

l,

i=j.

We then show that the turbo decoding algorithm can be represented as shown in the figure below:

‘This work was supported by the 1999 German-American Networking Research Grant given by the the national academies of engineering of Germany and the USA.

is then compared to the optimal solution (1). A similar analysis can be done for Gallager’s message pws-
ing algorithms for his LDPC codes as well as for threshold decoding.

REFERENCES

[l] C. Berrou, A. Glavieux, and P. Thitimajshima, “Near Shannon
limit error correcting coding and decoding: Turbmodes(1),” Pnx. 1993 IEEE Int. Conf. Commun. (ICC793), Geneva, Switzerland, May 1993, pp. 1064-1070.

[2] J. Hagenauer, E. Offer, and L. Papke, “Iterative decoding of binary block and convolutional codes,” IEEE %ns. Inform. T ~ w w ,Vol. IT-42, pp. 429-445, March 1996.

[3]C. R.P. Hartmann and L. D. Rudolph, “An optimum symbol

by symbol decoding rule for linear codes,” IEEE %ns. Inform.

IT-22, pp. 514617,

1976.

423
0-7803-5857-0/00/$10.0020000 IEEE.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:24:15 UTC from IEEE Xplore. Restrictions apply.

