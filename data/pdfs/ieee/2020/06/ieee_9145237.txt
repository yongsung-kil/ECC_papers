A Model-Driven Deep Learning Method for
Normalized Min-Sum LDPC Decoding
Qing Wang1, Shunfu Wang1, Haoyu Fang2, Leian Chen3, Luyong Chen1, Yuzhang Guo*, 1 1School of Electrical and Information Engineering, Tianjin University, Tianjin, China 2Electrical and Computer Engineering, New York University, New York, USA 3Department of Electrical Engineering, Columbia University, New York, USA Email: {wangq, sf wang}@tju.edu.cn, haoyu.fang@nyu.edu, chen.leian@columbia.edu, {3015204244, 2018234095}@tju.edu.cn

Abstract—With the applications of deep learning networks booming in physical layer communication, deep-learning-based channel decoding methods have become a research hotspot. However, the high complexity hinders the application of deep neural network (DNN) on long code. In this paper, we propose a model-driven deep learning method for normalized min-sum (NMS) low-density parity-check (LDPC) decoding. First, we propose a neural normalized min-sum (NNMS) LDPC decoding network. By unfolding the iterative decoding progress between checking nodes (CNs) and variable nodes (VNs) into a feedforward propagation network, we can make use of the advantages of both model-driven deep learning methods and conventional normalized min-sum (CNMS) LDPC decoding method. Second, considering that the NNMS decoder needs large number of multipliers, we propose a shared neural normalized min-sum (SNNMS) decoding network to reduce the number of correction factors. Experimental results show that the BER performance of the proposed NNMS decoder is 1.5dB better than the conventional LDPC decoder, using fewer iterations. Furthermore, the proposed SNNMS decoder outperforms the proposed NNMS decoder and reduces the computation complexity.
Index Terms—Deep learning, normalized min-sum decoding, LDPC decoding, model-driven neural network
I. INTRODUCTION
L Ow-density parity-check (LDPC) code was first proposed by Gallager in 1962 [1] and has been one of the research hotspots in the field of channel coding and decoding technology due to its high decoding throughput [2] [3] [4] and the bit error rate (BER) performance approaching Shannon capacity limit. The belief propagation (BP) or min-sum (MS) algorithm are two main LDPC decoding approaches. The BP decoding method achieves good performance but consumes a lot of logarithmic and multiplicative operations in the check nodes (CNs) computation [5]. Comparing with the BP decoding method, the MS decoding method significantly reduces the computation and hardware complexity at a cost of performance degradation. However, the modified MS decoding, a.k.s, normalized MS (NMS) or offset MS (OMS), that employs correction factors can offer competitive decoding performance with a low complexity [5].
Recently, researchers are making tremendous efforts to solve conventional communication physical layer problems,
This work is supported by the National Natural Science Foundation of China under Grant No. 61871282. (Corresponding author: Yuzhang Guo.)

like channel decoding, using deep learning. The reason is that, on one hand, the conventional communication physical layer problems are often formulated by a specific mathematical model, which is interpretable but not flexible. On the other hand, the data-driven methods, especially deep learning methods, have been widely used in computer vision and natural language processing due to its high flexibility and accuracy in dealing with complex problems [6] [7] [8], such as classification, recognition, and detection. Authors in [9] use the convolutional neural network (CNN) to estimate the noise accurately, and then concatenate a conventional LDPC BP decoder to improve the BER performance. In [10], a deep-learning-based decoder was proposed for short code to reduce the computational complexity, showing good BER performance by learning a huge amount of codewords. These BP unrolling based methods show the promising results for short code decoding [10].
As the code length grows, training the neural network will require a huge collection of codewords dataset [11]. It is worth mentioning that, the number of information bits k determines the number of classes (2k) which the network has to classify [12]. To this end, researchers try to reduce the decoding complexity via reducing the number of iterations or operations. Nachmani et al. assign weights to the edges of the Tanner graph for Bose-Chaudhuri-Hocquenghem (BCH) codes [13] [14], which achieves similar decoding performance with fewer iterations compared with conventional BP decoders. However, a large number of weights in these methods result in a high decoding complexity, making it impossible in long code applications. Considering the many logarithmic and multiplicative computations in BP decoding, Lugosch et al. propose a neural offset min-sum (NOMS) decoding algorithm without any multiplication operations [15], which performs well on hardware. But existing methods designed for regular codes can hardly be applied to long code, which refers to codes with length longer than one hundred comparing with short codes with length of several tens. In a word, the complexity of computation hinders the application of neural networks for long code decoding.
Aim to design a high accuracy and low complexity decoder for LDPC long code decoding, we firstly propose a novel neural normalized min-sum (NNMS) LDPC decoding

Authorized licensed use limited to: Yonsei Un9iv7.8D-1o-w7n2l8o1a-d7e4d40o-n2J/2u0n/e$2341,.20002©6 2a0t 2101:I0E1E:3E9 UTC from IEEE Xplore. Restrictions apply.

network, where the iterative decoding progress between check nodes (CNs) and variable nodes (VNs) are unfolded into feed-forward propagation network to accelerate the decoding progress, which is actually the BP unrolling methods for long code decoding. Furthermore, considering that the NNMS improves performance at the cost of a large number of multipliers, we then propose a shared neural normalized minsum (SNNMS) decoding network by using the same correction factors in each layer of the SNNMS networks to reduce the variety of learnable correction factors. We will demonstrate the performance and the complexity of the proposed NNMS and SNNMS methods in LDPC long codes decoding.
The rest of this paper is organized as follows: in Section II, we give a brief introduction of some related works; in section III, we propose a well-designed NNMS LDPC decoding network based on NMS and deep learning; in section IV, the SNNMS LDPC decoding network is proposed to reduce the complexity. Finally, a conclusion is provided in section V.
II. SYSTEM MODEL AND PROBLEM FORMULATION
LDPC code is a linear block code, where the parity check matrix H is sparse with very few ‘1’s. For (N, k) LDPC codes, N bits are consisted of k information bits and (N − k) check bits. Then, the k-bits information x is encoded by LDPC encoder [16] using equation u = xG, where G is the generation matrix equivalent to the parity check matrix H. We finally have the N -bits codeword u.
Our proposed LDPC long codes decoding networks are based on the following normalized min-sum (NMS) decoding method and the model-driven deep learning networks.
A. Normalized min-sum (NMS) LDPC Decoder
Tanner graph is a bidirectional graph consisting of three parts: CNs, VNs, and edges connecting CNs and VNs. At each pair of nodes, variable nodes V Ni and check nodes CNj are represented by squares and rings, respectively. If H(i, j) equals to ‘1’, then the i-th VN is connected with the j-th CN, otherwise it is not connected [17]. In this way, the LDPC check matrix H can be expressed by the Tanner graph.
For the hardware realization, the conventional normalized min-sum (CNMS) algorithm is preferred as it can be passing through iterative processing over the Tanner graph. The architecture of LDPC decoding scheme is shown in Fig.1.

CN1

CN2

CN3

CN4

L(rji ) L(qij )

VN1

VN2

VN3

VN4

VN5

VN6

VN7

VN8

L(ui )
×××× ×× ××
y Variable Node

2 s2
Check Node

L(Qi ) Decoding decision
oi

Fig. 1. Architecture of CNMS LDPC decoding scheme.

Let L (ui) denote the received channel log-likelihood ratio (LLR) corresponding to bit i, then the CN-to-VN messages

L (rji) and the VN-to-CN messages L (qij) are propagated

and updated iteratively according to the following steps:

Step 1: calculate the received channel LLR using received

symbol y for initialization, that is

(

)

P (u = 0|y) 2y

L(qij) = L(ui) = log P (u = 1|y) = σ2 ,

(1)

where σ2 is the noise variance under the additive white

Gaussian noise (AWGN) channel condition.

Step 2: calculate the CN-to-VN messages using

∏

L(rji) = α ×

sgn(L(qi′j)) min |L(qi′j)|, (2)

i′∈Vj \i

i′∈Vj \i

where α is the correction factor, Vj denotes the set of column location of the ‘1’ in the j-th row (represents the set of VNs

connected to CNj) and Vj\i represents Vj excluding variable node V Ni.

Step 3: calculate the VN-to-CN messages, given by

∑

L(qij) = L(ui) +

(L(rj′i)),

(3)

j ′ ∈Ci \j

where Ci denotes the set of row locations of the ‘1’ in the ith column (represents the set of CNs connected to V Ni) and Ci\j represents Ci excluding check node CNj.

Step 4: calculate the LLR L (Qi) of a variable node V Ni

as

∑

L(Qi) = L(ui) + L(rji).

(4)

j∈Ci

Repeat steps 2, 3 and 4 at each decoding iteration until the

convergence conditions are satisfied or the maximum number

of iterations is reached. Then the estimated bit value oi at the

variable node V Ni is determined by

{

1, oi = 0,

if L(Qi) ≤ 0 if L(Qi) > 0.

(5)

B. Model-driven Deep Learning Methods

Most deep learning networks are data-driven without considering the mathematical model of the task, which uses a black box and a large amount of data to train the neural network structure and parameters. The lack of understanding of the relationship between the algorithm and the network topology makes its structure unable to be interpreted and predicted [18]. Besides, a large amount of label data is not easy to obtain.
The model-driven deep learning method is a good solution to this problem, including model, algorithm and network components [18]. With the prior knowledge of the model and algorithm of a specific task, a interpretable network can be designed accordingly. For example, an iterative algorithm can be unfolded into a signal flow diagram [19] [20], then the deep neural network is obtained by training several learnable parameters through back propagation. Thus the defects caused by inaccurate model and predetermined parameters can be

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 11:01:39 UTC from IEEE Xplore. Restrictions apply.

compensated in this way. Moreover, the model-driven deep learning approach has many other advantages, such as fewer training data, over-fitting risk reduction, and rapid implementation.
To this end, our problem is to unfold the CNMS LDPC algorithm in Fig. 1 into a neural network by learning and compensating the uncertainty through back propagation, aim to make use of both the efficiency and interpretation of the neural network and the Tanner graph, respectively.
III. THE PROPOSED NEURAL NMS (NNMS) LDPC DECODING NETWORK
In this section, we propose a novel LDPC decoding network based on the normalized min-sum (NMS) decoding algorithm and the model-driven deep learning network. We will illustrate how the LDPC decoding algorithm is unfolded into a neural network and how the network parameters are selected.
A. Neural NMS (NNMS) LDPC Decoding Network
In the beginning, we give the block diagram of the proposed NNMS LDPC decoding network, as shown in Fig. 2. The kbits information bits x are encoded into a N -bits codeword u, which is then mapped through BPSK modulator into a symbol vector s and transmitted over the AWGN channel. At the receiver, the LLR values are calculated using the receiver signal y = s + n and then are fed into the deep feed-forward neural network. The task of the LDPC decoder is to estimate the transmitted information bits uˆ = o.

Gaussian noise

x

LDPC Encoder
(N, k)

u

BPSK

n s
+

y

Input layer

Hidden layer

Output layer

o

LDPC Decoder

Fig. 2. Block diagram of LDPC decoder with deep feed-forward neural network.

The receiver side detailed signal flow diagram of the pro-

posed NNMS decoding network is shown in Fig.3. The itera-

tive decoding algorithm is unfold into a forward-propagation

network. In the Tanner graph, the parity check matrix H

determines the edges of CNs and VNs connections. The mes-

sages between the CN layer and the VN layer are multiplied

by different correction factors αij and βji in each iteration,

which is equivalent to adding weight parameters to the edges

of Tanner graph. The process of calculating the CN-to-VN

messages and the VN-to-CN messages is realized by the CN

layer and the VN layer in the neural network, respectively.

There are three types of neuron functions: Neuron I (CN

neuron) calculates the CN-to-VN messages in the CN layer,

given by

∏

L(l)(rji) =

sgn(L(l−1)(qi′j ))

i′∈Vj \i

(6)

· min α(l−1)i′j × L(l−1)(qi′j ) .
i′∈Vj \i

Neuron II (VN neuron) calculates the VN-to-CN messages in

the VN layer using

∑(

)

L(l)(qij ) = L(ui)+

β(l)j′i ×L(l)(rj′i) . (7)

j ′ ∈Ci \j

and Neuron III (output neuron) calculates the final output in

the output layer as





∑(

)

oi = σ L(ui) +

β (lmax )
ji

×

L(lmax)(rji)

,

(8)

j∈Ci

where l is the number of iterations and lmax is the maximum number of iterations. We use activation functions of σ(x) = (1 + e−x)−1 and σ(x) = (1 + sgn(x))/2 for training stage
and testing stage, respectively, in output neurons.

B. The Signal Flow of the Proposed NNMS Network

We illustrate the details of the proposed NNMS LDPC
decoding network in Fig. 3. First, the received data L(ui) is put into the NNMS LDPC decoding network through the
input layer and obtained as the initialization data L(qij). Second, at the l-th hidden layer, to calculate the CN-to-
VN messages L(l)(rji) in (6), the data set L(l−1)(qi′j), which is connected to the CNj except the V Ni, is extracted from L(l−1)(qij) by Wv2c, where Wv2c is the row weight information of the parity check matrix H. L(l)(rji) is calculated through the neuron I of the CN layer.
Third, to calculate the VN-to-CN messages L(l)(qij) in (7), the data set L(l)(rj′i), which is connected to the V Ni except the CNj, is extracted from L(l)(rji) by Wc2v, here Wc2v comes from the column weight information of the parity check matrix H. L(l)(qij) is calculated in the neuron II (VN neuron) of the VN layer.
Similarly, we can easily increase the iterations by adding
more CN layers and VN layers in the hidden layers. For (N, k)
LDPC codes, the T iterations is unfolded into to a deep neural
network with 2lmax + 2 layers, consisting of lmax CN layers, lmax VN layers, one input layer and one output layer.
Finally, we use cross entropy loss function to train the
proposed network. The cross entropy loss measures the errors
of the neural network output o and the transmitted codeword
u, which is defined as

1 ∑ N

L(u, o) = − N

ui log(oi) + (1 − ui) log(1 − oi), (9)

i=1

where ui and oi are the i-th transmitted information bit (labeled) and the network soft estimation, respectively.

Example 1. Let us take (576, 432) LDPC code as an example, to see how we unfold the NMS decoding algorithm into an NNMS decoding network. As a result, the NNMS decoding network is the same as in Fig. 3, where a VN layer contains 576 VN neurons, the CN layer contains 144 CN neurons, and the output layer contains 576 output neurons.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 11:01:39 UTC from IEEE Xplore. Restrictions apply.

Input layer
L(qij )
Ă L(ui )

NNMS Iteration 1

Wv2c

L(ui )

L(qi' j )

Wc2v

L(1) (rj'i )

L(1) (rji )

N-k bits Ă

N bits Ă

A check node layer

A variable node layer

Hidden layers

NNMS Iteration 2

Wv2c

L(ui )

L(1) (qij )

L(1) (qi' j )

Wc2v
L(2) (rj'i )

L(2) (qij )
Ă

NNMS Iteration lmax

Wv2c

L(ui )

L(lmax -1) (qi' j )

Wc2v

L(lmax ) (rj'i )

(a) Neuron I (b) Neuron II (c) Neural III

L(2) (rji )

Õ min L(l) (rji ) =

sgn(L(l-1) (qi' j )) ×

a (l -1) i' j

´

L(l -1)

(qi '

j

)

i 'ÎVj \i

i 'ÎVj \i

å L(l) (qij ) = L(ui ) +

(b

(l ) j 'i

´

L(l

)

(rj

'i

))

ì

1

j 'ÎCi \ j

oi

=

ïïí ï1

1 + e-L(Qi ) , + sgn(L(Qi ))

îï

2

,

when training, when testing.

L(lmax ) (rji )

Output layer

L(lmax ) (qij )

L(Qi )

Ă

+

å

L(ui )

oi

Fig. 3. The proposed Model-Driven Deep Learning based NNMS decoding network.

IV. THE PROPOSED SHARED NEURAL NMS (SNNMS) LDPC DECODING NETWORK
The proposed NNMS LDPC decoding network use different weights for each connection edge between the CNs and the VNs in each iteration. Although the parity check matrix H of LDPC code is a sparse matrix, this process will consume a lot of multipliers for long codes. In this section, we propose a shared neural normalized min-sum (SNNMS) LDPC decoding network to reduce the number of correction factors. By sharing the same correction factors in the same layer, the SNNMS LDPC decoding network can achieve good performance with little increment of computation complexity.

A. Shared Neural NMS (SNNMS) LDPC Decoding Network

The SNNMS LDPC decoding network shares the same correction factors α(l−1) and β(l), as shown in Fig. 4. Comparing

with NNMS, we can see that the two decoding networks seem

to have similar network structure, but neurons are completely

different actually, as shown in Fig. 4 and Fig.3. The SNNMS

also has three types of neuron functions i.e., Neuron I (CN

neuron), Neuron II (VN neuron), and Neuron III (output

neuron), but the correction factors are different for each layer.

Neuron I calculates the CN-to-VN messages in the CN layer,

given by

∏

L(l)(rji) = α(l−1)×

sgn(L(l−1)(qi′j )) min L(l−1)(qi′j ) .

i′∈Vj \i

i′∈Vj \i

(10)

Neuron II calculates the VN-to-CN messages in the VN layer

using

∑(

)

L(l)(qij ) = L(ui)+β(l) ×

L(l)(rj′i) .

(11)

j ′ ∈Ci \j

As the performance and complexity of LDPC codes, with different structures, is different. The CNMS LDPC decoding method with the same predefined weight factor (i.e. correction factor α) achieves good performance, as shown in section IIB. However, in model-driven deep learning LDPC decoding networks, the NNMS decoding network changes the weight factor of all connection edges, i.e. the parity check matrix H has changed, resulting in the loss of the original LDPC code

structure; while the model-driven SNNMS decoding network uses the same predefined weight factor and shared the same correction factor in the same layer. Thus the SNNMS model mechanism is closer to the structure characteristics of the original LDPC code (using CNMS decoding method), and it is more helpful for the network to capture the topological relationship between the nodes of the entire LDPC code when propagating forward.

B. Complexity Analysis

Furthermore, the SNNMS decoding network needs fewer iterations or layers under the same BER performance, which means that the decoding process has much lower latency, higher throughput and fewer computational complexity, so this method is also suitable for long LDPC codes. Generally, for the parity check matrix H with low-density, we denote the weight in the j-th row by aj and the weight in the i-th column by bi. By counting the number of ’1’s [12], we have

N∑−k

∑ N

aj = bi.

(12)

j=0

i=1

Complexity can be evaluated by counting the operations of comparison (CMP), xor (XOR) multiplication (MUL) and addition (ADD). When computing the CN to VN messages, the NNMS decoder selects the first and second minimum messages among several passed messages, so the message computation progress can be realized using simple CMP, MUL and XOR operations. Since each output in the CNj is calculated by (aj-1) inputs, at the V Ni, (bi-1) adders are needed when adding bi variables, and an extra adder is used for calculating L (Qi). As a result, the VN needs (bi(bi-1)+1) adders.

V. SIMULATION RESULTS
A. Experimental Setup
In this section, we train and test the proposed NNMS and SNNMS LDPC decoding networks using Tensorflow framework [21]. The LDPC code is designed according to IEEE 802.16e standard with code block length 576 and rate 3/4, where the codeword is selected randomly and the parity check

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 11:01:39 UTC from IEEE Xplore. Restrictions apply.

Input layer

L(ui )

L(qij )

Ă L(ui )

SNNMS Iteration 1

Wv2c
L(qi' j )

L(ui )
Wc2v
L(1) (rj'i )

L(1) (rji )

N-k bits Ă

N bits Ă

A check node layer

A variable node layer

Hidden layers

SNNMS Iteration 2

Wv2c

L(ui )

L(1) (qij )

L(1) (qi' j )

Wc2v
L(2) (rj'i )

L(2) (qij )
Ă

SNNMS Iteration lmax

Wv2c

L(ui )

L(lmax -1) (qi' j )

Wc2v
L(lmax ) (rj'i )

(a) Neuron I (b) Neuron II (c) Neuron III

L(2) (rji )

Õ min L(l) (rji ) = a(l-1) ´

sgn(L(l-1) (qi' j )) ×

L(l-1) (qi' j )

i 'ÎVj \i

i 'ÎVj \i

å L(l) (qij ) = L(ui ) + b (l) ´

(L(l) (rj'i ))

j 'ÎCi \ j

ì

1

oi

=

ïïíïïî1

1 + e-L(Qi ) + sgn( L(Qi
2

, ))

,

when training, when testing.

L(lmax ) (rji )

Output layer

L(lmax ) (qij )

L(Qi )

Ă

+

å

L(ui )

oi

Fig. 4. The proposed Model-Driven Deep Learning based SNNMS decoding network.

matrix H is from [22]. The training data is generated under

10-1

multiple signal-to-noise (SNRs) ranging from 0dB to 5dB.

We use the min-batch gradient descent method to train the

10-2

network. Each mini-batch contains 120 blocks of data and each

SNR occupies the same proportion in one mini-batch. The

10-3

BER

Adaptive Moment Estimation (Adam) optimization method 10-4
with learning rate of 0.001 is used to search the optimal

network parameters. 1

10-5

training on 0-5dB training on 0dB training on 5dB

B. System Robustness
The first experiment tests the system robustness. The system robustness can be measured using BER performance with respect to the quality of the training data, i.e., the signal to noise ratio (SNR) of the training data. When the SNR of training data equals to 0db, which means that the training data is generated under a bad channel condition, resulting in a lot of errors in the labeled dataset, thus the network can not learn the decoder structure to correct errors. On the contrary, when the SNR is high, there will be fewer erroneous data are fed into the network. The network system can not learn the ability to remove noise.
As shown in Fig. 5, comparing with the network trained using 0dB or 5dB data only, we can see that the more diverse the training data, i.e., training under multi-channel conditions of 0-5dB, the better BER performance the network achieves. That is to say, the diversity of the training data helps enhance the robustness of the data-driven neural network.

10-6

1

1.5

2

2.5

3

3.5

4

4.5

5

SNR(dB)

Fig. 5. System robustness verse the quality of training data (NNMS).

BER

10-1 layers=10, iterations=4

layers=12, iterations=5

layers=22, iterations=10

10-2

layers=32, iterations=15

layers=42, iterations=20

10-3

10-4

10-5

10-6

10-7

1

1.5

2

2.5

3

3.5

4

4.5

5

SNR(dB)

Fig. 6. BER performance versus the number of network layers (NNMS).

C. BER Performance vs. Number of Network Layers
The second experiment analyzes the BER performance in terms of the number of network layers.
As shown in Fig.6, within a certain number of network layers, with the number of layers increase, the BER performance gets improved. It must point out that, the deeper network layers consume more multiplication and summation operations, thus the improvement of BER performance is at the cost of computation complexity. When the number of network layers reaches a certain depth, the performance of the decoder
11The source code to replicate the experiments is available on the web (https://github.com/tjuxiaofeng/A-Model-Driven-Deep-Learning-Method-forNormalized-Min-Sum-LDPC-Decoding).

will not be greatly improved, which also conforms to the nature of conventional LDPC decoding.
As a result, considering the trade-off between performance and complexity, we suggest to choose 10 iterations with 22 layers network.
D. BER Performance Comparsion between NNMS and SNNMS
The loss function of SNNMS decoding network and the training method are consistent with NNMS decoding network. We compare the performance of the proposed SNNMS decoding network with NNMS and CNMS.
As shown in Fig.7, in the case of 32 layers neural network

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 11:01:39 UTC from IEEE Xplore. Restrictions apply.

BER

100

10-1

10-2

10-3

10-4

CNMS iteration=5

CNMS iteration=10

10-5

CNMS iteration=15 SNNMS iteration=5

SNNMS iteration=10

10-6

SNNMS iteration=15 NNMS iteration=5

NNMS iteration=10

NNMS iteration=15 10-7

10-8

1

1.5

2

2.5

3

3.5

4

4.5

5

SNR(dB)

Fig. 7. BER performance verse difference decoding networks.

TABLE I COMPLEXITY COMPARISON OF LDPC DECODERS

Item

CNMS

NNMS

SNNMS

N∑−k CMP aj (aj −2)T
j=1

N∑−k aj (aj −2)T
j=1

N∑−k aj (aj −2)T
j=1

N∑−k XOR aj (aj −2)T
j=1

N∑−k aj (aj −2)T
j=1

N∑−k aj (aj −2)T
j=1

∑N

∑N

∑N

ADD (bi(bi −1)T+1)T

(bi(bi −1)+1)T

(bi(bi−1)+1)T

i=1

i=1

i=1

MUL

T

N∑−k

∑N

aj (aj−2)T+ bi(bi−2)T

2T

j=1

i=1

(i.e. 15 iterations) with BER of 10−7, the BER performance of SNNMS outperforms up to 0.4 dB compared with the NNMS LDPC decoding network. SNNMS decoding network is closer to the original LDPC code structure in model mechanism, so it has better performance in model-driven deep learning method.
E. Complexity
The computational complexity is listed in table I, where T is the number of iterations. We can find that the SNNMS decoder has lower complexity compared with the NNMS. When compared with CNMS, it has better BER performance without changing much computational complexity.
VI. CONCLUSIONS
In this paper, we firstly propose a NNMS LDPC decoding network based on a model-driven deep learning network, by unfolding the LDPC decoding algorithm into neural network and training the learnable correction factors. By sharing the correction factors, the SNNMS LDPC decoding network is proposed to reduce the complexity. Simulation results shows the proposed SNNMS decoder can achieve better BER performance with lower computation complexity compared with the NNMS decoder. In addition, the improved SNNMS decoder is acceptable for long codes application thank to the lower latency and less computational complexity, which is durable in practice. In the future work, we plan to design a fixed-point decoder and accelerate the neural network on FPGA.

[2] R. Tanner, “A recursive approach to low complexity codes,” IEEE Transactions on information theory, vol. 27, no. 5, pp. 533–547, 1981.
[3] M. G. Luby, M. Mitzenmacher, M. A. Shokrollahi, and D. A. Spielman, “Improved low-density parity-check codes using irregular graphs,” IEEE Transactions on information Theory, vol. 47, no. 2, pp. 585–598, 2001.
[4] J. Chen and M. P. Fossorier, “Near optimum universal belief propagation based decoding of low-density parity check codes,” IEEE Transactions on communications, vol. 50, no. 3, pp. 406–414, 2002.
[5] D. Oh and K. K. Parhi, “Min-sum decoder architectures with reduced word length for ldpc codes,” IEEE Transactions on Circuits and Systems I: Regular Papers, vol. 57, no. 1, pp. 105–115, 2009.
[6] D. Silver, A. Huang, C. J. Maddison, A. Guez, L. Sifre, G. Van Den Driessche, J. Schrittwieser, I. Antonoglou, V. Panneershelvam, M. Lanctot et al., “Mastering the game of go with deep neural networks and tree search,” nature, vol. 529, no. 7587, p. 484, 2016.
[7] G. Hinton, L. Deng, D. Yu, G. Dahl, A.-r. Mohamed, N. Jaitly, A. Senior, V. Vanhoucke, P. Nguyen, B. Kingsbury et al., “Deep neural networks for acoustic modeling in speech recognition,” IEEE Signal processing magazine, vol. 29, 2012.
[8] A. Krizhevsky, I. Sutskever, and G. E. Hinton, “Imagenet classification with deep convolutional neural networks,” in Advances in neural information processing systems, 2012, pp. 1097–1105.
[9] F. Liang, C. Shen, and F. Wu, “An iterative bp-cnn architecture for channel decoding,” IEEE Journal of Selected Topics in Signal Processing, vol. 12, no. 1, pp. 144–159, 2018.
[10] T. Gruber, S. Cammerer, J. Hoydis, and S. ten Brink, “On deep learning-based channel decoding,” in 2017 51st Annual Conference on Information Sciences and Systems (CISS). IEEE, 2017, pp. 1–6.
[11] W. Xu, Z. Wu, Y.-L. Ueng, X. You, and C. Zhang, “Improved polar decoder based on deep learning,” in 2017 IEEE International Workshop on Signal Processing Systems (SiPS). IEEE, 2017, pp. 1–6.
[12] H. Ye, G. Y. Li, and B.-H. Juang, “Power of deep learning for channel estimation and signal detection in ofdm systems,” IEEE Wireless Communications Letters, vol. 7, no. 1, pp. 114–117, 2017.
[13] E. Nachmani, Y. Be’ery, and D. Burshtein, “Learning to decode linear codes using deep learning,” in 2016 54th Annual Allerton Conference on Communication, Control, and Computing (Allerton). IEEE, 2016, pp. 341–346.
[14] E. Nachmani, E. Marciano, D. Burshtein, and Y. Be’ery, “Rnn decoding of linear block codes,” arXiv preprint arXiv:1702.07560, 2017.
[15] L. Lugosch and W. J. Gross, “Neural offset min-sum decoding,” in 2017 IEEE International Symposium on Information Theory (ISIT). IEEE, 2017, pp. 1361–1365.
[16] J. Liu, S. Jing, X. You, and C. Zhang, “A merged bp decoding algorithm for polar-ldpc concatenated codes,” in 2017 22nd International Conference on Digital Signal Processing (DSP). IEEE, 2017, pp. 1–5.
[17] N. Yang, S. Jing, A. Yu, X. Liang, Z. Zhang, X. You, and C. Zhang, “Reconfigurable decoder for ldpc and polar codes,” in 2018 IEEE International Symposium on Circuits and Systems (ISCAS). IEEE, 2018, pp. 1–5.
[18] H. He, S. Jin, C.-K. Wen, F. Gao, G. Y. Li, and Z. Xu, “Modeldriven deep learning for physical layer communications,” IEEE Wireless Communications, 2019.
[19] O. Solomon, R. Cohen, Y. Zhang, Y. Yang, Q. He, J. Luo, R. J. van Sloun, and Y. C. Eldar, “Deep unfolded robust pca with application to clutter suppression in ultrasound,” IEEE transactions on medical imaging, 2019.
[20] N. Doan, S. A. Hashemi, E. N. Mambou, T. Tonnellier, and W. J. Gross, “Neural belief propagation decoding of crc-polar concatenated codes,” in ICC 2019-2019 IEEE International Conference on Communications (ICC). IEEE, 2019, pp. 1–6.
[21] M. Abadi, P. Barham, J. Chen, Z. Chen, A. Davis, J. Dean, M. Devin, S. Ghemawat, G. Irving, M. Isard et al., “Tensorflow: A system for largescale machine learning,” in 12th {USENIX} Symposium on Operating Systems Design and Implementation ({OSDI} 16), 2016, pp. 265–283.
[22] M. Helmling, S. Scholl, F. Gensheimer, T. Dietz, K. Kraft, S. Ruzika, and N. Wehn, “Database of channel codes and ml simulation results,” www. uni-kl. de/channel-codes, University of Kaiserslautern, 2016.

REFERENCES
[1] R. Gallager, “Low-density parity-check codes,” IRE Transactions on information theory, vol. 8, no. 1, pp. 21–28, 1962.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 11:01:39 UTC from IEEE Xplore. Restrictions apply.

