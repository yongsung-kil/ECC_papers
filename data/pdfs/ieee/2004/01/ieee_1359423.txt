LDPC PRODUCT CODES

Zhang Qi, Ng Chun Sum

Dept. of Electrical and Computer Engineering, National University of Singapore, Singapore 119260,
{g0203542, elengcs }@nus.edu.sg

ABSTRACT
Low-density parity-check (LDPC) codes are well known for their abilities to achieve near Shannon channel capacity limit and low decoding complexity compared with the Turbo decoder. However, their encoding complexities grow with the square of the code length. In this paper, we proposed a type of LDPC product codes, which can significantly lower the encoding complexity at the expense of slight degradation in error-rate performance as well as slight increase in decoding complexity.
1. INTRODUCTION
Low-density parity-check (LDPC) codes were invented by Gallager [1] in 1962. He also presented an iterative decoding algorithms (or message-passing decoders) which he showed to be capable of achieving the performance remarkably close to the Shannon limit at reasonably low decoder complexity. However, one practical drawback of LDPC codes has been that of high encoding complexity. Encoding is, in general, performed by matrix multiplication and the complexity grows with the square of the code length. In [2], Luby slightly modified the construction of codes from bipartite graphs to a cascade of such graphs for erasure channel. In [3] and [4],the LDPC codes were designed based on fmite geometry and algebra. Low-density generator matrix (LDGM) codes were explored in [5]. All these works are aimed towards reducing encoding and/or decoding complexity of LDPC codes.
In this paper, we examine LDPC product codes in terms of their encoding/decoding efficiency and bit-error­ rate (BER) performance in additive white Gaussian noise (AWGN) channels. Compared with LDPC codes, LDPC product codes are shown to be able to achieve significantly lower encoding complexity with only a slight degradation in system BER performance. Also it is found that the corresponding decoders require only slightly more computation.

2. CODE STRUCTURE

2D Data Array

Checks on --I- Rows

Checks on Columns

-r- Checks on Checks

Fig 1. LDPC product codes

Consider an arbitrary two-dimensional (n,k) product

code whose component codes are (nj,kj) and (n2,k2)

HI H2, LDPC codes with parity check matrices

and

respectively. In the LDPC product code,the data bits are

protected by checks on rows, checks on colunms and

checks-on-checks. Clearly, n=njn2, k= kjk2' and the

code rate is given by R=k/n=(kj k2)/(njn2) ' Every

xiHJ row of the LDPC product codeword satisfies

=0,

where Xi is the i-th row of the codeword. Every colunm

x�H; of the codeword satisfies

= 0, where Xj is the j-th

colunm of the codeword. Consider the two-dimensional
codeword as a whole, it must satisfY n2 parity checks of

HI H2 , and nj parity checks of

. We incorporate all

H these parity checks into a single parity-check matrix
of the resulting product code, which has nj . n2 colunms

and (nj -kj)· n2 +(n2 -k2)· nj rows.

Suppose two regular LDPC codes are used as

component codes, and we denote their parity check

Hi matrices as

(i=1,2) with A i ones in each colunm

H and Pi ones in each row. The

matrix of LDPC

product code will have Aj +A2 ones in each colunm. Also

H (nj -kj)· n2 rows of

will have Pj ones in each row,

481
0-7803-8549-7/04/$20.00 © 2004 IEEE
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:53:41 UTC from IEEE Xplore. Restrictions apply.

482 ICCS 2004

and (n2 - k2) . nj rows have P2 ones in each row. If Aj = A2, the product code is itself a regular LDPC code,
except that number of rows increase by
(nj - kj)· (n2 - k2) when compared with a regular (n,k) LDPC code. Under this condition, pj = P2' If Aj *- A2 ,
the product code takes the form of an irregular LDPC code.
3. COMPLEXITY COMPARISON

The encoder and decoder complexities of LDPC codes and LDPC product codes are compared in this section.
Given k data bits xl' x2' • • •,xk , and a basis
g1,g2,...,gk , the straightforward LDPC encoding

algorithm comprises computing (1)
where gj (i = 1, 2,...k) is (n - k) x 1 colunm vector and

c is the checks of the data bits. Unlike the sparse parity

H check matrix

, the basis vectors g1,g2,...,gk are

always dense. The computational complexity of this
technique is proportional to (n - k) . k .

For the LDPC product code, the encoding
complexities of (np kj) and (n2, k2) component LDPC codes are proportional to (nj - kj)· kj and (n2 - k2)· k2 .

Since each row of the checks on rows is only related to

that row of data bits,we can encode different rows of data

bits separately. For the same reason,different colunms of

data bits can also be encoded separately. Since the checks­

on-checks for the checks on rows and the checks on

colunms are the same, we can get the checks-on-checks

by encoding either each row of the checks on colunms

where the overall encoding complexity is proportional to

((nj - kj)· kj)· n2 +((n2 - k2) ·k2) · kl '

(2)

or each colunm of the checks on rows where the overall

encoding complexity is proportional to

((nj - kj)· kj)· k2 +((n2 -k2)·k2)·nj .

(3)

The ratio of encoding complexity of the LDPC product code respect to the same length LDPC code can be calculated approximately by
((nj - kj) · kj) · n2 +((n2 - k2) ·k 2) · kj
(4)

or
((nj - kj) · kj) · k2 +((n2 - k2) ·k2) · nj
(5)
(nj · n2 - kjk2) · kj · k2
The LDPC decoder implements a suboptimal message-passing algorithm. The complexity of this algorithm grows linearly with the code length and the number of iterations for a fixed distribution of the number of one's per row and per colunm in parity check matrix
H. If the code length and the number of iterations are

fixed, the complexity of this algorithm is approximately

proportional to the average number of one's per colunm in
H.

Let the LDPC code and the LDPC product code have

the same code length and perform the same number of

H iterations at the decoder. If we generated the

matrix of

LDPC code,which has the same distribution of one's per

colunm as that of LDPC product code, the complexity of

the two code are equal even through the parity check

matrix of the latter has more rows. If the parity check

H matrix

of the LDPC code has different distribution of

one's per colunm as that of the LDPC product code, the

decoding complexity is different. The ratio of decoding

complexity of LDPC product code respect to LDPC code

is the ratio of average number of one's per column of two

codes when the two codes have same length. From the

H structure of parity check matrix

of LDPC product

H code,it can be shown that any colunm of

contains one

colunm from the component LDPC code parity check matrix H1 and one colunm from H2 . So generally,

LDPC product codes have larger average number of one's

H per colunm in

than that of LDPC codes. The decoding

algorithm of LDPC product codes will slightly increase

compared with LDPC codes.

4. SIMULATION RESULTS

We compared the BER performance of (900,400),(2025, 900) and (3600, 1600) LDPC codes with LDPC product codes of equivalent length in AWGN channels. The LDPC codes are all regular with have 3 one's in each colunm of the parity check matrices. The LDPC product codes use (30, 20), (45, 30) and (60, 40) LDPC codes as their component codes. Each component code is also regular with 2 one's in each colunm of the parity check matrix. Six iterations of the message-passing decoding algorithm are run for both of them. The ratios of encoding complexity of the LDPC product codes and the LDPC codes are 5.00%, 3.33% and 2.50%, respectively. The ratios of decoding complexity of the LDPC product codes and the LDPC codes are all 133.33%.
In Figures 2-4, we observe that the LDPC product codes outperform the LDPC codes at low SNR and this BER performance advantage of the LDPC product codes increases with the code length. However, at high SNR, the LDPC product codes are found to give poorer BER performance than the LDPC codes. At an SNR of 3.5 dB, we observed that the former performs about 1 dB worst than the latter for codes of (900,400) and (2025,900).
5. CONCLUSION

We proposed LDPC product codes in this paper. The codes can be seen as a special type of LDPC codes. Their highly structured parity check matrices can dramatically

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:53:41 UTC from IEEE Xplore. Restrictions apply.

reduce the encoding complexity. Although the performance ofLDPC product codes is not as good as that of LDPC codes for short code length and high SNR, it is equally well or better for very long code length and low SNR.
6. REFERENCES
[1] R. G. Gallager, Low-Density Parity-Check Codes .
Cambridge, MA: MIT Press, 1963.
[2] M. Luby, M. Mitzenmacher, M. A. Shokrollahi, and D. A.
Spielman, "Efficient erasure correcting codes," IEEE Trans . Inform . Theory, vol. 47, pp. 569-584, Feb. 2001.
[3] Y. Kou, S. Lin, and M. Fossorier, "Low density parity check codes based on finite geometries: A rediscovery and more,"
IEEE Trans . Inform . Theory, vol. 47, pp. 2711-2736, Nov. 2001.
[4] R. Lucas, M. Fossorier, Y. Kou, and S. Lin, "Iterative decoding of one-step majority-logic decodab1e codes based on
belief propagation," IEEE Trans . Comm ., pp. 931-937, Jun. 2000.
[5] J. Garcia-Frias, W. Zhong, "Approaching Shannon performance by iterative decoding of linear codes with low­
density generator matrix," IEEE Comm . Letters, vol. 7, pp. 266-
268, Jun. 2003.
10° .--�--�-�r==�c.:==: =C=C=�=Jl - Lope product code - LOPC code
a: w
"'
10�
Eb/No in dB
Figure 2. BERs of the (900, 400) LDPC code and LDPC product code in AWGN channel.

ICCS 2004 483

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:53:41 UTC from IEEE Xplore. Restrictions apply.

