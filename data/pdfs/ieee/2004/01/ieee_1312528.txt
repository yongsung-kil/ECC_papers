Reliability-Based Schedule for Decoding Low-Density Parity-Check Codes

Ahmed Nouh, Student Member, IEEE, and Amir H. Banihashemi, Member, IEEE
Broadband Communications and Wireless Systems (BCWS) Centre Dept. of Systems and Computer Engineering, Carleton University
1125 Colonel By Drive, Ottawa, Ontario, Canada K1S 5B6 Tel: +1 613 520-2600 ext. 8026, Fax: +1 613 520-5727 anouh, ahashemi@sce.carleton.ca

Abstract—A reliability-based message-passing schedule for iterative decoding of low-density parity-check codes is proposed. Simulation results for bit-flipping algorithms (with binary messages) show that reliability-based schedule can provide considerable improvement in performance and decoding speed over the so-called flooding (parallel) schedule as well as the existing graph-based schedules. The cost associated with this improvement is negligible and is equivalent to having a 2-bit representation for initial messages, instead of the standard 1-bit for hard-decision algorithms, only at the first iteration (all the exchanged messages are still binary).
Keywords-schedules for iterative decoding; message-passing schedule; reliability-based schedule; iterative decoding; low-density parity-check (LDPC) codes; message-passing algorithms; bitflipping algorithms; hard-decision algorithms
I. INTRODUCTION AND OVERVIEW
Low-density parity-check (LDPC) codes [2], and iterative decoding algorithms have been the subject of much recent research. In particular, one topic of practical interest has been to improve the performance of the iterative decoding of LDPC codes at shorter block lengths [1], [5], [10]-[12]. To decode an LDPC code, iterative algorithms, also referred to as “messagepassing algorithms,” exchange messages between the bit nodes and the check nodes of the Tanner graph (TG) [8] of the code in both directions and iteratively. Most commonly, the message-passing is implemented following the so-called “flooding (parallel) schedule,” i.e., all the bit nodes send messages to check nodes by processing the messages coming from check nodes and the channel, check nodes process these messages and then send back new messages to bit nodes, and this is repeated in a synchronous manner.
Recently, and motivated by the question of whether it would be possible to improve the performance of an iterative coding scheme while the TG and the decoding algorithm (identified by the operations in bit and check nodes) are fixed, new message-passing schedules have been proposed [5], [10]. These schedules control the exchange of messages between bitnodes and check-nodes at each iteration according to some graph parameters such as the girths of the nodes in the graph.
This research has been supported in part by Zarlink Semiconductor Corp. (formerly, Mitel Semiconductor Corp.), and in part by National Capital Institute of Telecommunications (NCIT).

In this paper, we propose “reliability-based schedule,” which unlike the schedules introduced in [5], [10], does not directly depend on the TG of the code. Instead, it controls the message passing based on the reliability of information available at each bit node. At each iteration, less reliable (or “unreliable”) bits are identified and are prevented from propagating their messages through the graph in that iteration. This can be thought of as adjusting the timing for the participation of different nodes in iterative decoding according to the reliability of their information. The idea can be applied to any iterative decoding algorithm, even ones with binary messages, as long as some measure of reliability for bits (soft information) is available at the input of the decoder. In the case of harddecision algorithms, the soft information would only be used at the beginning to partition the bits into two subsets of reliable and unreliable. (This is equivalent to using two bits for the initial messages, where one bit indicates whether the node is reliable or not and the other bit is just the initial estimate for the bit value. In our simulations, the former bit is only used in the first iteration.) The messages passed between the check and the bit nodes are still binary. Simulation results show that, despite its deceptive simplicity, the proposed schedule can improve the performance and decoding speed significantly (in one of our examples by about 1dB, and by up to more than a factor of 2, respectively).
This paper is organized as follows. In section II, we present the general framework for reliability-based scheduling. We however note that optimal scheduling in the general framework is too complex to derive. We thus limit our scope to the simplest scenario, where the scheduling is only applied in the first iteration and where all the coded bits have the same reliability threshold. This simplifies not only the optimization problem by reducing its dimension to one, but also the implementation of the schedule for practical purposes. In Section III, simulation results are given. These results indicate that using the simplified schedule is most beneficial for bitflipping algorithms, where significant improvements in performance and decoding speed are obtained with the minimal cost of just adding an extra bit of memory per coded bit to represent the reliability of the initial messages. Section IV is devoted to some concluding remarks.

IEEE CommAuuthnoircizaedtiloicnensseSdoucseielimtyited to: Yonsei Univ. Downloaded on J4u4ne423,2026 at 11:05:25 U0T-C78fro0m3I-E8E5E3X3p-lo0re/0. 4R/e$st2ric0ti.o0n0s a(pcp)ly2. 004 IEEE

II. RELIABILITY-BASED SCHEDULE (RBS)
Consider a binary LDPC code of length n and rate R used over a binary input additive white Gaussian noise (AWGN) channel using a BPSK modulation (±1). Suppose that the average energy per information bit and the power spectral density of AWGN are denoted by Eb and N0/2, respectively, and the received vector r is processed by an iterative decoder to estimate the transmitted message.

The main idea in reliability-based schedule is to prevent

unreliable information from propagating in the TG of the code

in early stages of iterative decoding. To identify unreliable

information, we define a sequence (α(ℓ)), ℓ = 1, 2, 3, …, of

“reliability threshold vectors.” Vector α(ℓ) in the sequence is

associated with the iteration ℓ of the decoding algorithm, and

has n real elements.

The

jth

element

α

(A) j

corresponds

to

bit

vj

in the code.

Also

suppose

that

the

non-negative

number

R

A j

is

an estimate of the reliability of information about vj at iteration

ℓ

of

the

decoding

algorithm

(e.g.,

R

A j

can be the magnitude of

the estimate for the log likelihood ratio (LLR) of vj). At each

iteration

ℓ,

bit

vj

is

labeled

as

“unreliable,”

if

R

A j

≤

α

(A) j

,

and

as

“reliable,” otherwise. A check node c with respect to a

connected bit node v is considered “reliable,” if all the other bit

nodes connected to c are reliable. (In this case the message

sent to v along the edge (c, v) is considered “reliable.”)

Otherwise, the check node c is “unreliable” with respect to v.

At each iteration, reliability-based schedule operates by

allowing only the reliable bits to send out messages, and by

enforcing that only reliable check nodes generate and send

back messages (note that the operations in bit and check nodes

remain the same as those in flooding). At the beginning of each iteration ℓ, the reliable messages coming from check

nodes

along

with

the

channel

message

m

c j

are

used

to

compute

the

reliability

R

A j

at

each

bit

node

vj

(for

the

first

iteration,

R

1 j

is

determined

only

by

m

c j

).

In a general framework, for a given code, a given decoding algorithm and a given value of Eb/N0, vectors α(ℓ) can be optimized to achieve the minimum error rate. Such a multi-

variable optimization however is very complex. Intuitively, as

the iteration process goes on, on average we expect more bits to

be designated “reliable.” In this work, to simplify the

optimization and the implementation of the schedule, we consider the case where α(ℓ)= 0, for ℓ ≥ 2, i.e., starting from the

second iteration all the bits are considered reliable (flooding

schedule). Moreover for the first iteration, we assume that

α

(1) j

is independent of j 1.

We thus use the notation α to denote

the constant “reliability threshold” for ℓ = 1. As the reliability

measure,

we

use

the

magnitude

of

the

received

values,

i.e.,

R

1 j

= |rj|. In general, for a given code, optimal α depends on the TG of the code, Eb/N0, and the decoding algorithm. Our

1 This is particularly a valid assumption for regular LDPC codes.

simulations however show that the optimal normalized α (α/σ) does not change much with Eb/N0 (parameter σ = (N0/2REb)½ is the standard deviation of noise).
III. SIMULATION RESULTS
In this work, we apply reliability-based schedule to belief propagation (BP) [2], [8], [9], Gallager’s algorithm A (GA) [2], [9], and a hard-decision algorithm described in Fig. 2 of [7]2. The latter appears to perform particularly well when applied to LDPC codes constructed by finite geometries [3]. BP algorithm is implemented in LLR domain [9], and our simulations show that not much improvement in performance or convergence speed is achieved by applying the (simplified) reliability-based schedule to BP. (Note that asymptotically, at very large block lengths, BP is optimal and we do not expect RBS-BP to provide any improvement in performance over BP for very long LDPC codes.) For our simulations, we apply BP to an optimized (1000, 500) irregular code [6] (C1), GA to a (273,191) regular code [4] (C2), and SS to a (273,191) projective geometry (PG) code [3] (C3). For every code, the received vectors are decoded simultaneously using two decoders, one with flooding schedule and the other one with (simplified) reliability-based schedule. The maximum number of iterations is chosen to be 200, and at each Eb/N0, enough code-words are simulated to generate 100 code-word errors.
In general, for the reliability-based schedule, the optimal value of α for each code and a given decoding algorithm is a function of Eb/N0. We have however observed that the optimal normalized value of α (α/σ) is rather insensitive to Eb/N0, and is almost equal to 0.3, 0.7 and 0.4 for the combinations {C1, BP}, {C2, GA} and {C3, SS}, respectively. It is also observed that in general, at lower values of Eb/N0, the error rate is less sensitive to the normalized value of α. We have plotted the bit error rate (BER) and the message error rate (MER) curves of GA and RBS-GA (α/σ = 0.7) for C2 in Fig. 1, and those of SS and RBS-SS (α/σ = 0.4) for C3 in Fig. 2. The curves of BP and RBS-BP for C1 are very close and thus have not been plotted. For all three codes, the statistics of the number of decoding iterations for converged cases is shown in Table 1. In Figures 1 and 2, we have also given the curves for BP and weighted bit-flipping (WBF) on AWGN [3] as reference. These curves are all based on flooding.
It can be seen that in general reliability-based schedule provides significant improvement in performance over flooding. In fact at BER = 10-5, RBS-GA outperforms GA with flooding by about 1dB. It is also interesting to note that RBS algorithms close a large part of the gap between bit-flipping (BF) and WBF algorithms. For C2, RBS-GA is only about 0.2dB inferior to WBF for BER at larger values of Eb/N0. This is while for MER, RBS-GA even outperforms WBF at high Eb/N0 values. For C3, the difference between RBS-SS and
2 We use a parallel version of this algorithm, i.e., we flip the value of every variable v in the set Si [7], where i is the greatest index for which Si is not empty. We refer to this algorithm as Sipser-Spielman’s (SS) algorithm. To fit SS algorithm into the message-passing framework of previous sections, one can think of a check-node message as the parity bit of the incoming extrinsic messages to the check node, and a bit node message as the value of the corresponding bit.

IEEE CommAuuthnoircizaedtiloicnensseSdoucseielimtyited to: Yonsei Univ. Downloaded on J4u4ne523,2026 at 11:05:25 U0T-C78fro0m3I-E8E5E3X3p-lo0re/0. 4R/e$st2ric0ti.o0n0s a(pcp)ly2. 004 IEEE

WBF is almost the same across the board, and RBS-SS is inferior to WBF by only about 0.25 and 0.1 dB for BER and MER, respectively. The significance of this would become more evident when one notices that, unlike RBS-BF algorithms, WBF passes soft information throughout the iteration process.

TABLE I.

STATISTICS OF THE NUMBER OF DECODING ITERATIONS FOR CONVERGED CASES

SS for C3 GA for C2 BP for C1

Algorithm

Eb/N0 (dB)
1.6
1.8
2.0 5.5 6.0 6.5 7.0 7.5 3.5 4.0 4.5 5.0

Conventional

Mean

Standard Deviation

14.65 17.68

11.96 9.90

10.22 6.15
2.29 0.77 2.13 0.69 1.93 0.66 1.73 0.65 1.52 0.57
4.78 3.69 3.63 2.42 2.88 1.24 2.48 0.70

RBS Version

Mean

Standard Deviation

13.68 9.56

11.49 6.14

9.84 3.92
2.20 1.11 1.73 1.12 1.29 0.95 0.91 0.82 0.60 0.7
3.05 2.71 2.27 1.21 1.99 0.61 1.81 0.48

We tested probabilistic schedule of [5] on C1 with BP. The improvement in performance is also small in this case and very close to what is obtained by RBS-BP. For the other two codes since all the bit nodes in the TG have the same girth, the probabilistic schedule, unlike RBS, performs on average the same as flooding.
In terms of speed of convergence (average decoding time) also, as Table 1 indicates, reliability-based schedule provides improvement over flooding. It is also important to note that RBS-BF algorithms have a much higher speed of convergence compared to WBF algorithm. For C3, for example, the average number of iterations for WBF at Eb/N0 values of 3.5, 4, 4.5 and 5dB are respectively 11.5, 9.3, 7.4 and 5.8, which are more than three times larger than the corresponding values for RBSSS given in Table 1.
For RBS-BP, although the decrease in the average number of iterations required for convergence, compared to BP with flooding, is negligible, the standard deviation has been reduced considerably.

tradeoff compared to flooding, graph-based schedules simply fail to provide any improvement due to uniform distribution of girths in the graph. For soft-decision algorithms such as belief propagation, our results show that the simplified version of RBS does not provide much improvement. Whether the application of more complex reliability-based schedules would provide non-negligibly larger improvements is to be investigated.

1.0E+00 1.0E-01 1.0E-02

BP WBF GA RBS-GA BP WBF GA RBS-GA

Error Rate

1.0E-03

1.0E-04

1.0E-05

2

3

4

5

6

7

8

9

Eb/N0 (dB)

Figure 1. BER (––) and MER (- - -) for (273,191) Regular LDPC Code (C2) Decoded by BP (AWGN), WBF (AWGN), GA and RBS-GA Algorithms.

ACKNOWLEDGMENT
The authors wish to thank Y. Kou, M. Fossorier and T. Richardson for providing them with the parity-check matrices of the simulated codes, and H. Xiao for simulating the (1000, 500) code with probabilistic scheduling.

1.0E+00 1.0E-01 1.0E-02

BP WBF SS RBS-SS BP WBF SS RBS-SS

Error Rate

IV. CONCLUDING REMARKS
In this work, we presented a reliability-based schedule (RBS) for iterative decoding of LDPC codes. To simplify the optimization and the implementation of the schedule, we focused on only applying the schedule to the first iteration and with a single reliability threshold for all the coded bits. This simplified schedule, which appears to be particularly effective for bit-flipping algorithms, outperforms conventional flooding schedule in both error performance and decoding speed. These advantages for bit-flipping algorithms come at the very low cost of calculating, and perhaps storing, 1-bit reliability information per coded bit for initial messages. In many cases where RBS provides a better performance/decoding-time

1.0E-03

1.0E-04

1.0E-05

2

2.5

3

3.5

4

4.5

5

5.5

Eb/N0 (dB)

Figure 2. BER (––) and MER (- - -) for (273, 191) PG-LDPC Code (C3) Decoded by BP (AWGN), WBF (AWGN), SS and RBS-SS Algorithms.

IEEE CommAuuthnoircizaedtiloicnensseSdoucseielimtyited to: Yonsei Univ. Downloaded on J4u4ne623,2026 at 11:05:25 U0T-C78fro0m3I-E8E5E3X3p-lo0re/0. 4R/e$st2ric0ti.o0n0s a(pcp)ly2. 004 IEEE

REFERENCES
[1] M. Fossorier, “Iterative Reliability-Based Decoding of Low-Density Parity Check Code,” IEEE Journal on Selected Areas in Communications, vol. 19, pp. 908-917, May 2001.
[2] R.G. Gallager, Low-Density Parity-Check Codes. Cambridge MA: MIT Press, 1963.
[3] Y. Kou, S. Lin and M.P.C. Fossorier, “Low-density parity-check codes based on finite geometries: a rediscovery and new results,” IEEE Trans. Inform. Theory, vol. 47, no. 7, pp. 2711-2736, Nov. 2001.
[4] D.J. MacKay, “Encyclopedia of sparse graph codes,” http://www.inference.phy.cam.ac.uk/mackay/codes/ data.html.
[5] Y. Mao and A.H. Banihashemi, “Decoding low-density parity-check codes with probabilistic scheduling,” IEEE Comm. Lett., vol. 5, no. 10, pp. 414-416, Oct. 2001.
[6] T.J. Richardson, private communication.

[7] M. Sipser and D.A. Spielman, “Expander codes,” IEEE Trans. Inform. Theory, vol. 42, issue 6, pp. 1710–1722, Nov. 1996.
[8] R.M. Tanner, “A recursive approach to low-complexity codes,” IEEE Trans. Inform. Theory, vol. 27, no. 5, pp. 533-547, Sept. 1981.
[9] T. Richardson and R. Urbanke, “The capacity of low-density paritycheck codes under message-passing decoding,” IEEE Trans. Inform. Theory, vol. 47, no. 2, pp. 599-618, Feb. 2001.
[10] H. Xiao, “Message-passing schedules for decoding low-density paritycheck codes,” master’s dissertation, Carleton University, Ottawa, Canada, 2002.
[11] M. R. Yazdani, S. Hemati and A. H. Banihashemi, ``Improving Belief Propagation on Graphs with Cycles," IEEE Comm. Letters, vol. 8, no. 1, pp. 57 - 59, Jan. 2004.
[12] J. S. Yedidia, W. T. Freeman, and Y. Weiss, “Constructing Free Energy Approximations and Generalized Belief Propagation Algorithms,” submitted to IEEE Trans. Inform. Theory, also available at http://www.merl.com/papers/TR2002-35.

IEEE CommAuuthnoircizaedtiloicnensseSdoucseielimtyited to: Yonsei Univ. Downloaded on J4u4ne723,2026 at 11:05:25 U0T-C78fro0m3I-E8E5E3X3p-lo0re/0. 4R/e$st2ric0ti.o0n0s a(pcp)ly2. 004 IEEE

