ISIT 2004, Chicago, USA, June 27 – July 2, 2004

Analysis of an Algorithm for Irregular LDPC Code Construction
Aditya Ramamoorthy and Richard D. Wesel Dept. of Electrical Engineering
University of California, Los Angeles {adityar, wesel}@ee.ucla.edu

Abstract — This work presents a rigorous analysis of an algorithm proposed by Tian et. al [1] for the construction of irregular LDPC codes with reduced stopping sets and low error floors. Computation of the expected number of stopping sets of a given size proves that the algorithm significantly outperforms a random construction. We show that the algorithm provably reduces the expected number of stopping sets up to a certain size (based on the input parameters). The expected number of cycles of a given size is computed for both constructions.

I. Stopping Set Analysis of ACE Constrained Codes
Tian et. al [1] proposed an algorithm for the construction of irregular LDPC codes where they constrained the cycles in the code based on a metric called the “Approximate Cycle Extrinsic Message Degree” (ACE).

Definition 1 Approximate Cycle EMD (ACE): The

ACE of a length 2d cycle in the Tanner graph of an LDPC

code is given by

d i=1

(di

− 2)

where

di

is

the

degree

of

the

ith

variable node in the cycle.

Their construction takes the parameters (dACE, η) as input and guarantees that all cycles in the code of size ≤ 2dACE have an ACE value ≥ η. In addition cycles consisting only of degree-2 nodes are prohibited. They claimed that their con-
struction reduced the incidence of small stopping sets. The
codes designed based on this criterion were found to have much
lower floors than random and girth-constrained codes over the
AWGN channel. We compare the expected number of stopping sets in a “Random” ensemble (G (n, λ(x), ρ(x))) with the “ACE-constrained” ensemble (GdACE,η(n, λ(x), ρ(x))) based on a generating function approach. For convenience we work with the distributions from the node perspective (λ˜(x), ρ˜(x))

Lemma 1 Given a stopping set U ⊆ V of size ‘l’ such that deg(v) ≥ 2, ∀v ∈ U , and its neighbor set N (U ), such that |N (U )| = k, there exists at least one cycle of length ≤ min(2l, 2k) involving a subset A ⊆ U in the induced subgraph formed by U and N (U ).

is upper-bounded by,

e=dv l

EsAsCE [l] ≤

coeff(Π2dv (1 + yzi)λ˜in, ylze)

e=2l+η

(1)

×

coeff(Πdj=c 2((1

+ x)j
E

−

jx)ρ˜j m,

xe)

e

Theorem 2 Let ESASCE[l] and ESRS[l] be the expected number of stopping sets of size l > dACE in the ensem-
bles GdACE,η(n, λ(x), ρ(x)) and G (n, λ(x), ρ(x)) respectively. Then,

ESRS [l] − ESASCE [l]

≥

λ˜2n l

coeff(Πdj=c 2((1 + x)j − jx)ρ˜j m, x2l)
E

2l

2l+η−1

+

coeff(Πd2v (1 + yzi)λ˜in, ylze)

(2)

e=2l+1

I ⊂{1,..,m} |I |=dAC E

coeff(Πj∈I ((1 + x)deg(cj ) − deg(cj )x), xe)
E e

Note that both summations are dominated by low values of e. A slightly more involved argument yields Theorem 2. Thus the WER rate of ACE-constrained codes over the BEC is substantially lower than random codes. Similar analysis for the expected number of cycles for the Random and the ACEconstrained ensembles reveals that the ACE-constrained ensemble does not have a significantly lower number of cycles compared to the Random ensemble.

II. Conclusion
It is shown (in the full paper) that the ACE algorithm provably reduces the expected number of stopping sets for all sizes up to ≈ dc · dACE/2 for a right concentrated degree code. Numerical computation of the upper bounds suggests that the expected number of stopping sets of size ≤ dACE decreases exponentially with η. The vastly improved performance of ACE constrained codes over random codes suggests that “cycles consisting of low-degree variable nodes” are responsible for most of the performance degradation over the AWGN channel.

Constraining the code to be ACE-compliant, causes the number of edges in any stopping set less than 2dACE to be ≥ 2l+η. Based on the above Lemma and the work of [2] [3], we obtain
Theorem 1 Consider the ensemble GdACE,η(n, λ(x), ρ(x)). The expected number of stopping sets of size l where l ≤ dACE
1This work was supported by Texas Instruments and the state of California through UC MICRO grant 03-093 and NSF grant CCR0209110.

References
[1] T. Tian, C. Jones, J. D. Villasenor, and R. D. Wesel, “Construction of Irregular LDPC Codes with Low Error Floors,” in IEEE Intl. Conf. on Comm., 2003, pp. 3125-3129
[2] A. Orlitsky, K. Viswanathan, and J. Zhang, “Stopping Set Distribution of LDPC Code Ensembles,” in IEEE Intl. Symposium on Info. Th., 2003, pp. 123-123
[3] D. Burshtein and G. Miller, “Asymptotic Enumeration Methods for analyzing LDPC Codes,” Submitted to IEEE Trans. on Info. Th.

,(((



Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 11:18:03 UTC from IEEE Xplore. Restrictions apply.

