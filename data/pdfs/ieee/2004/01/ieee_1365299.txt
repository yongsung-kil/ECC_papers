ISIT 2004, Chicago, USA, June 27 – July 2, 2004

On the Dynamics of Continuous-Time Analog Iterative Decoding
Saied Hemati and Amir H. Banihashemi1 BCWS Centre, Dept. of Systems and Computer Engineering, Carleton University
1125 Colonel By Drive, Ottawa, Ontario, Canada K1S 5B6 E-mail: {hemati,ahashemi}@sce.carleton.ca

Abstract — Iterative decoding with flooding schedule can be formulated as a fixed-point problem solved iteratively by successive substitution (SS) method. In this work, we model continuous-time analog (asynchronous) iterative decoding by a first-order differential equation, and show that it can be approximated as the application of the well-known successive over relaxation (SOR) method for solving the fixed-point problem. Simulation results for belief propagation (sumproduct) and min-sum algorithms confirm that SOR, which is in general superior to the simpler SS method, can considerably improve the performance of iterative decoding for short codes. The improvement in performance increases with the maximum number of iterations and by reducing the step size in SOR, and the asymptotic result, corresponding to infinite maximum number of iterations and infinitesimal step size represents the performance of continuous-time analog iterative decoding. This means that under ideal circumstances continuous-time analog decoders can outperform their discrete-time digital counterparts by a large margin. Moreover, the results obtained by the proposed model are surprisingly close to the results of circuit simulation of a min-sum analog decoder presented in [1]. Our work also suggests a general framework for improving iterative decoding algorithms on graphs with cycles, even for synchronous digital implementations. 1

SUMMARY OF RESULTS
Analog circuits in general are better than digital circuits in terms of power/speed efficiency. They also consume less fabrication area and generate less noise. These advantages have motivated some recent work on analog iterative decoders (see [1] and the references therein). As we will discuss in this paper, the dynamics of analog iterative decoding however could be quite different than that of synchronous flooding schedule, commonly used in literature and practice for iterative decoding. In order to analyze the dynamics of analog iterative decoders, we model the propagation delay on each interconnection (corresponding to an edge in a Tanner graph (TG)) between the processing modules (corresponding to variable nodes and parity-check nodes in a TG) by a lumped resistor-capacitor pair, as shown in Fig. 1.

f (X,R)
var iable nodes

f (X(t  T1), R)

ri

Vini

ci

Vouti

rici

dVouti dt

Vini  Vouti

X(t )

Vouti

rici

dVouti dt

ri Vini
ci
Vini  Vouti

Y(t)

g(Y(t  T2 ))

g(Y)
parity  check nodes

Figure 1: a model for analog iterative decoding.

1 This work has been supported in part by Communication and Information Technology Ontario (CITO).

In this figure, vectors T1 and T2 represent processing delay at variable and check nodes, respectively. Vectors X and Y represent incoming messages along the edges of the graph to variable nodes and check nodes, respectively, and R is the received vector from the channel (note that X(t-T1) is a vector of size E, defined as (X1(t-t1),…,XE(t-tE)), where E is the number of edges of the graph). Also, f and g are operations in variable nodes and parity-check nodes, respectively. We further simplify this model by assuming that propagation delay is the dominant delay and by restricting the analysis to the dominant pole based on Elmore’s delay approximation [2]. We then have

ĲY (t) f (g(Y), R)  Y(t) h(Y(t), R)  Y(t) ,

(1)

where W is a diagonal matrix which contains different time constants, representing different propagation delays affecting the corresponding Yi variables. By assuming that all time constants are equal, we have solved this differential equation using backward Euler method for the min-sum decoder reported in [1]. Interestingly, our transient responses are very close to those of [1], which are obtained by the simulation of a circuit with more than 3000 short-channel MOS transistors. A simpler approach to solve (1) is to use forward Euler method. In this case, we have:
Yk1 Yk  'tW 1h(Yk , R)  Yk  Yk  E h(Yk , R)  Yk 
(2) where E is called relaxation factor and can be substituted by a scalar if all time constants are equal. It appears that equation (2) corresponds to yet another well-known numerical method for solving fixed-point problems. The method is referred to as ``successive over relaxation (SOR),” and is known to be superior to successive substitution method in terms of convergence rate [3].Our simulation results for the application of SOR to belief propagation and min-sum algorithms demonstrate improvements of up to about 0.7 dB over the standard flooding schedule for randomly constructed low-density parity-check codes. The largest improvement is obtained asymptotically by tending the number of iterations to infinity and ¨tǕ-1 (E) to zero. This corresponds to the exact solution of (1) and thus the performance of analog iterative decoding. We therefore conclude that an analog asynchronous decoder which has the dynamics of (1) is superior in performance compared to a conventional iterative decoder (synchronous with flooding schedule).

REFERENCES
[1] S. Hemati and A. H. Banihashemi, “Iterative Decoding in Analog CMOS,” Proceedings of the 13th ACM Great Lakes Symposium on VLSI, ACM GLSVLSI 2003 , Washington D.C., USA, pp. 15-20, April 2003.
[2] J. Rabaey, A. Chandrakasan, and B. Nikolic, Digital Integrated Circuits, A Design Perspective. Second Edition, Prentice Hall, 2003.
[3] C. T. Kelley, Iterative Methods for Linear and Nonlinear Equations, SIAM, Philadelphia, USA, 1995.

,(((



Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 11:12:04 UTC from IEEE Xplore. Restrictions apply.

