ISlT 2003, Yokohama, Japan, June 29 -July 4,2003

Stopping Set Distribution of LDPC Code Ensembles
Alon Orlitsky, Krishnamurthy Viswanathan and Junan Zhang
ECE Department, UC San Diego, La Jolla, CA 92093, USA {alon,kviswana, j6zhang)CIucsd. edu

I. INTRODUCTION
A stopping set S is a subset of the set of variable nodes such that all its neighboring check nodes are connected t o it at least twice. Stopping sets determine the performance of LDPC codes under iterative decoding over erasure channels. More precisely, t h e block and bit errors are determined respectively by the existence of a stopping set, and the size of the maximal stopping set, in the erased variable nodes. In this paper, we study the asymptotic behavior of two quantities: the distribution of stopping sets, and the stopping number s*-the size of the smallest nonempty stopping set in a code and use it to bound the error probability of LDPC codes with large blocklength.
We derive an expression for the normalized average stopping set distribution of regular and irregular LDPC codes and use it t o obtain a critical fraction of the block length above which codes have exponentially many stopping sets of that size. Similar results were independently derived in [l].
Di et al. [a] studied the minimum distances of code en-
sembles. We show the stopping number’s counterpart of their results in the sense that the likely values of these two quantities are determined by degree distributions in a similar fashion. We also derive the exact fraction of codes with a linear stopping number. More precisely, we show that for code en-
sembles with X’(O)p’(l) < 1, the stopping number is linear in the blocklength for a J1 - X’(O)p’(l) fraction of the codes.
We show that this result also applies t o minimum distances. We give bounds on the average block error probability as
a function of the erasure probability E. In particular for the code ensembles which have degree-2 variable nodes, if E is below a certain threshold, the asymptotic average block error probability is 1 - J1 - X’(o)p‘(l)~.

11. RESULTS
Given a Tanner-graph ensemble B ( n , A(z), p ( z ) ) of LDPC codes and an integer s >_ 0, let the average stopping set dis-
tribution E ( s )!Sf E( I{S : (SI= s}l )
denote the average number of s-element stopping sets in a randomly chosen code. For both regular and irregular ensembles, we use polynomial characteristic functions t o identify stopping sets and obtain expressions for E ( s ) .
To evaluate the exponential behavior of the average stopping set distribution as the block length increases, we define the normalized average stopping set distribution to be

~

(

a!Zf ) lim n-oQ

-n1 logE(

l{S : IS1 = an}I )

To obtain $a), we evaluate the asymptotic expressions for E(an) using techniques similar t o the ones used in [3] for evaluating weight distributions of LDPC codes.

iThis workwas suppo%ed by CORE Grant 00-10066

Theorem 1. For the sequence of regular ensembles
G(n,IC‘-’, z d - ’ ) ,

where 50 is the only positive solution of
We have also derived ?(a)for irregular ensembles. The critical exponent stopping ratio,

= a. 1

is the smallest fraction a for which a code is likely to contain exponentially many stopping sets of size an.
For any 0 < a < a*,the probability that there exists a
stopping set of size an in a code selected randomly from the ensemble decreases exponentially with n. However this does not suffice t o show that with high probability the stopping number is no more than an because the definition of y ( a )
does not address stopping sets of size o(n). Here we evaluate
the asymptotic value of Pr(s* 5 an).

Theorem 2. For the sequence of ensembles
G(n,X(z), p(s)), if X’(O)p’(l) < 1, then V a < a*,

lim ~ r ( s 5* an>= 1- J1- A’(O)~’(I).
n-m
If X’(O)p’(l) > 1, the result in [2] and the fact that s* 5 W* imply that 36 > 0, s.t.

Pr(s* 5 6logn) 2 1 - c f i

where 0 < c < 1, is an appropriate constant.

I

A similar theorem holds for minimum distance. The stopping set distribution can be used t o bound
E(PLT(c))t,he average block error probability of iterative decoding over the erasure channel with erasure probability e , where the average is taken over all codes from the ensemble. Let 1, be the smallest variable degree of the ensemble and

+ def
Eef = sup{€ : max ( y ( a )

(E - a)h(-€) - a

- h ( ~ ) <) 0).

aE(0,cl

1-ff

Theorem 3. When E < eef, E(PLT(e))= Q ( ~ z ’ - ~ + l ) . In particular, when E < Eef and 1, = 2,

lim E ( P ~ $ ~ ( E=)1)- J 1 - x’(o)~’(I)E.

I

n+m

REFERENCES [l]D.Burshtein, G. Miller, “AsymptoticEnumeration Method for
analyzing LDPC Codes”, Submitted to IEEE Transactions o n
Information Theory, 2002;
[2] C . Di, R. Urbanke, T. Richardson, “Weight distributions: How deviant can you be?”, Proceedings of ISIT 2001, pp. 50;
[3] R. G. Gallager, “Low-density parity check codes”, M.I. 2’. Press,
1963.

0-7803-7728-1/03/$17.0002003 IEEE.

123

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:19:01 UTC from IEEE Xplore. Restrictions apply.

