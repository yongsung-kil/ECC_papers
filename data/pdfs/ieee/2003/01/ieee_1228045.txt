ISlT 2003, Yokohama, Japan, June 29 -July 4,2003

Graph-Theoretic Construction of Low-Density Parity-Check Codes
Ivana Djurdjevic, Shu Lin, and Khaled Abdel-Ghaffar’ Department of Electrical and Computer Engineering University of California Davis, CA 95616, USA e-mail: idjurdje,shulin,ghaffar@ece.ucdavis.edu

Abstract - This paper presents a novel graph-

100

theoretic method for constructing low-density parity-

10

check (LDPC) codes from connected graphs without

16‘

the requirement of large girth. The method is based

on finding a set of paths in a connected graph, which satisfies the constraint that any two paths in the set

104
8b 104

are either disjoint or cross each other at one and only

one vertex. Two trellis-based algorithms for finding 104
these paths are devised. Good LDPC codes of prac-

tical lengths are constructed and they perform well

10-7

with iterative decoding.

104

I . CODECONSTRUCTION

10-0

1

2

3

4

5

6

7

8

8

10

EWNo [dB]

Let G = (VIE) be an undirected, connected graph with vertex
set V of size q and edge set E. For 15 L < q, let P be a set of
n paths of length L, which satisfies the constraint that any two paths in P are either disjoint (i.e., have no vertex in common), or singularly crossing ( i e , have exactly one vertex in common).
This constraint is called the disjoint-crossing (DC) constraint. Let matrix H over GF(2) display the incidence relationship between the paths in P and the vertices in G. It follows from the constraints on the paths in P that no two columns (or two rows) in H can have more than one 1-component in common. This ensures that the Tanner graph [Z] of H does not contain cycles of length 4. If L is chosen to be much smaller than the number of vertices in G, then H is a sparse matrix. The null space of H gives an LDPC code [l] of length n.

11. TRELLIRSEPRESENTATAIONND Two TRELLIS-BASEADLGORITHMS

+ Given a graph G, its paths of length L can be represented
by a trellis 7 L of L sections with L 1 levels of nodes. Each
level consists of q nodes which are the vertices of G. For
0 5 k < L, a node U; at the kth level and a node vj at the (k+ 1)th level are connected by a branch if and only if (vi, vj)
is an edge in E. To find a set P of paths of length L in G that satisfies the DC constraint, an extend-select-eliminate (ESE) algorithm is devised to parse the path trellis 7~of G.

The algorithm consists of L steps. At the end of each step
i, 1 5 i 5 L, a set of paths of length i that satisfies the DC

constraint is obtained.

For a large connected graph G, it may be prohibitively com-

plex to process its path trellis by the ESE algorithm. To overcome this problem, we take a divide-and-conquer approach.
+ + Assume that q is divisble by L+ 1. Then, G can be partitioned
into L 1 subgraphs, GOG, I , ...,G L ,each having q / ( L 1)

~

~~

‘This research was supported by NSF under Grants CCR-

0096191, CCR-0117891, NASA under Grants NAG 5-10480, NAG

5-12789 and fellowship provided by Accel Partners.

Fig. 1: Bit error performances of four LDPC codes obtained by the ESE algorithm and divide-and-conquer approach.
distinct vertices. Using the ESE algorithm, a set of paths of length L, P;, that satisfies the DC constraint in each subgraph can be constructed, as well as a set of connecting paths P c . Each path in Pc has one and only one vertex in each sub-
+ + graph. The union P = P cU k oPi is the set of paths in G that
satisfies the DC constraint. When q / ( L 1) = ps 2 L 1for
some prime p and some positive integer s, we can establish a one-to-one correspondence between the p6 elements in the finite field G F W ) and the p s vertices in any subgraph. P c
+ can be formed as the set of the pZs paths corresponding to
all the codewords in the shortened ( L 1 , 2 , L) Reed-Solomon (RS) code over G F W ) .
111. SOME CODES AND THEIRPERFORMANCES
Fig. 1 depicts the bit error performances of three LDPC codes constructed by applying the ESE algorithm on three complete graphs with 100, 200, and 400 vertices, respectively, and decoded with the sum-product algorithm. For their lengths and rates, they perform very well with respect to their Shannon limits. Also depicted in Fig. 1is the bit performance of the code obtained by applying the divide-and-conquer approach on a complete graph of 582 vertices. In this example, L = 5 and the shortened (6,2,5) RS code over GF(97) is used.
REFERENCES
[l] R. G. Gallager, “LowDensity Parity Check Codes,” IRE %ns. Inform. Theory, vol. IT-18, pp. 21-28, January 1962.
[2] R. M. Tanner, “A Recursive Approach to Low Complexity Codes,” IEEE ’Runs. Inform. Theory, vol. IT-27, pp. 533-547, September 1981.

31
0-7803-7728-1/03/$170.20003 IEEE.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:29:24 UTC from IEEE Xplore. Restrictions apply.

