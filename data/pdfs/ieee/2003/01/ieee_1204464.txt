Design of Low Density Parity Check Codes for Turbo Multiuser Detection

Guosen Yue Dept. of Electrical Engineering
Texas A&M University College Station, TX 77843

Xiaodong Wang Dept. of Electrical Engineering
Columbia University New York, NY 10027

Krishna R. Narayanan Dept. of Electrical Engineering
Texas A&M University College Station, TX 77843

Abstract— We consider the analysis and design of low density parity check (LDPC) codes for turbo multiuser detection in multipath code-division multiple-access (CDMA) channels. We develop techniques to compute the probability density function (pdf) of the extrinsic information at the output of the multiuser detector. We show that the output pdf can be modeled as symmetric Gaussian for synchronous CDMA in additive white Gaussian noise (AWGN) channel and as a mixture of symmetric Gaussian densities for asynchronous CDMA system over fading channel. The expectation-maximization (EM) algorithm can be used to compute the parameters of this mixture. Using these techniques, we are able to accurately compute the thresholds for LDPC codes and design good irregular LDPC codes. Simulation results are in good agreement with the computed thresholds and the designed irregular LDPC codes outperform regular ones significantly.
I. INTRODUCTION
The design of turbo-like codes for CDMA channels has been an area of high interest since the results on turbo multiuser detection appeared [2], [10]. Most of the results in this area are confined to the use of convolutional codes or parallel concatenated convolutional codes (PCCC). Recent results [7], [8] show that carefully designed irregular low density parity check (LDPC) codes can outperform PCCC for long code lengths and provide near capacity performance on memoryless channels. It is then natural to attempt to design good LDPC code ensembles for turbo multiuser detection.
The main idea used in the design of LDPC codes is to employ the technique of density evolution [8], where the probability density function (pdf) of extrinsic information (messages passed in the iterative procedure) is computed as a function of iteration and the given degree profiles for the LDPC code, in order to compute the thresholds (in SNR or Eb/No). Then, an optimization procedure is used to find optimum degree profiles that result in the least thresholds (or, near capacity performance). It has been shown that for a small sacrifice in the resulting thresholds, the design procedure can be simplified by making the assumption that the messages (extrinsic information) at the output of the check nodes and the variable nodes have a Gaussian distribution [1].
For turbo multiuser detection, the LDPC codes will be used in conjunction with a soft-input soft-output (SISO) multiuser detector. In order to extend the aforementioned technique to design good LDPC codes for turbo multiuser receiver, we need

a technique to characterize the pdf of the extrinsic information at the output of the detector as a function of the input pdf and channel signal-to-noise ratio. The thresholds of regular LDPC codes and the design of optimum irregular LDPC for multiuser detector haven’t been discussed in previous literature.
In this paper, we consider the multiuser detector in synchronous and asynchronous CDMA systems based on soft interference cancellation (SIC) and instantaneous linear MMSE filtering, a technique first proposed in [10]. Other receivers, i.e., optimal receiver and matched filter are also discussed. We show how to characterize the input-output pdf’s of the extrinsic information analytically for these multiuser detectors and use this to design good LDPC codes.

II. SYSTEM DESCRIPTIONS

A. System Model
We consider a LDPC coded CDMA system with K users, employing normalized modulation waveforms s1,s2, · · · , sK , and signaling through respective multipath channels with additive white Gaussian noise, shown in Fig. 1. The binary information data dk(m) for user k, k = 1, · · · , K, are LDPC encoded with code rate R. A code-bit interleaver is used to reduce the influence of the error bursts at the input of each channel decoder. The interleaved code-bits of the kth user are BPSK symbol mapped, yielding data symbols of duration T . Each data symbol bk(i) is then modulated by a spreading waveform sk,i(t), and transmitted through the multipath channel. The received signal is given by

K

M −1

P

r(t) = Ak bk(i) gkl(i)sk,i(t − τkl) + σn(t), (1)

k=1

i=0

l=1

where M is the total number of symbols transmitted by each user; Ak is signal amplitude of the kth user; P is the number of paths in each user’s channel; n(t) is a zero-mean white
Gaussian noise process with unit power spectral density, and σ2 is the variance of the ambient channel noise.

B. Iterative (Turbo) Multiuser Receiver Structure
Similar to [10], the overall receiver is an iterative receiver which performs turbo multiuser detection by passing extrinsic messages on the code bits between a soft-input soft-output (SISO) multiuser detector and an LDPC decoder. In each

0-7803-7802-4/03/$17.00 © 2003 IEEE
2703
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:40:29 UTC from IEEE Xplore. Restrictions apply.

turbo iteration, several inner iterations are performed within the LDPC decoder during which extrinsic messages are passed along the edges in the bipartite graph.

1) Notation: All extrinsic messages (information) are in log-likelihood (LLR) form and the variable L is used to refer to extrinsic messages. The variable f is used to denote the probability density function of the extrinsic information L. Lbp→ ,q c(ebi,j) denotes the extrinsic LLR passed from a bit node to a check node along the jth edge connected to the ith bit node, during the pth iteration within the LDPC decoder and the qth iteration between the LDPC decoder and the detector. A subscript m → L denotes quantities passed from the multiuser detector to the LDPC decoder, and vice versa. For the pdf’s f and mean m, no argument is used since they do not depend on the particular edge, bit node or check node.

2) The Turbo Multiuser Detection Algorithm for LDPC-

coded CDMA Systems:

•

Initialization: Lb0← ,0 c(ebi,n) =

L0m←L [bk (i)] 0, ∀(i, n).

=

0, ∀(i, k), and

• Turbo multiuser detection iterations: For q = 1, 2, . . . , Q

– SISO multiuser detection: The SISO multiuser detection computes extrinsic messages

Lm q →L[bk(i)] = g({yk(i)}, {Lm q−←1 L[bk(i)]}k =k), (2)
where g(·) denotes the SISO multiuser detector.
– LDPC decoding: For k = 1, · · · , K

Lm q ←L[bk(i)] = h(Lm q−←1 L[bk(i)], {Lb0← ,q c(ebi,n)}), (3)
where h(·) denotes the LDPC decoder.
• Final hard decisions on information and parity bits:

ˆbk(i) = sign Lm Q→L[bk(i)] + LQm←L[bk(i)] . (4)

III. SISO MULTIUSER DETECTORS

A. Synchronous CDMA Systems

For the synchronous CDMA system over AWGN channel, the received (real-valued) s

K

M −1

r(t) = Ak bk(i)sk,i(t) + σn(t).

(5)

k=1

i=0

It is easily seen that a sufficient statistic for demodulating the

ith code bits of the K users is given by the K-vector y(i)

whose kth component is the output of a filter matched to sk

in the ith code bit interval, i.e., yk(i) =

(i+1)T iT

sk,i(t)r(t)dt.

Thus sufficient vector y(i) can be written as

y(i) = R(i)Ab(i) + σn(i),

(6)

where [R(i)]k,l = ρkl =

T 0

sk,i(t)sl,i(t)dt;

A

=

diag(A1, · · · , Ak); b(i) = [b1(i), · · · , bK (i)]T ; and n(i) ∼

N (0, R(i)) is a Gaussian noise vector, independent of b(i). In

this section, we outline three different SISO turbo multiuser

detectors [10].

1) Exact SISO Multiuser Detector: Denote Bk+ = b(i) ∈ {−1, +1}K : bk(i) = +1 . Similarly define Bk−. From (6), the extrinsic information Lm q →L[bk(i)] delivered by the SISO multiuser detector is

Lm q →L[bk(i)]

=

2Ak yk (i) σ2

+

log

b∈Bk+ (b) b∈Bk− (b)

(7)

(b) = exp −bT ARAb/(2σ2)

1 + bj tanh(Ajyj(i)/σ2)

1

+

bj

tanh(

1 2

Lm q−←1 L[bj

(i)])

.

j=k

2) SIC-MMSE SISO Multiuser Detector: A low-complexity approximate SISO multiuser detector was developed in [10] which is based on soft interference cancellation and instantaneous linear MMSE filtering and is summarized as follows. Denote ek denotes a k-th unit vector in IRK . Define

˜bj (i)

=

tanh

1 2

Lm q−←1 L[bj

(i)]

.

(8)

and V k(i) =

A2j [1 − ˜bj (i)2]ej ejT + Ak2 ekeTk . (9)

j=k

Define ˜b(i) = [˜b1(i) · · · ˜bK (i)], and ˜bk(i) = ˜b(i) − ˜bk(i)ek, where ek denotes a k-th unit vector in IRK . Then we have

Lm q →L[bk(i)]

=

2zk (i) 1 − µk(i)

(10)

where zk(i) = AkekT [V k(i) + σ2R(i)−1]−1

[R(i)−1y(i) − A˜bk(i)].

(11)

µk(i) = A2kekT V k(i) + σ2R(i)−1 −1 ek.(12)

3) SIC-MF SISO Multiuser Detector: A further simplifica-

tion on the above SIC-MMSE detector is to skip the linear

MMSE filtering step after the soft interference cancellation.

In this case, we have





Lqm→L[bk(i)] =

2 γk (i)

yk(i) − Aj[R(i)]k,j˜bj(i) ,
j=k

(13)

where γk(i) = R(i)V k(i)R(i) + σ2R(i) k,k − 1. (14)

B. Asynchronous CDMA Systems with Mulitpath Fading

In this section, we are to discuss the signal of all K users transmitted through a multipath time variant Rayleigh fading channels. Denote y(i) = [y1(i), · · · , yK (i)]T , b(i) = [b1(i), · · · , bK (i)]T , b(i) = [b(i − 1), b(i), b(i + 1)]T , A =
diag (A1, · · · , AK ) , A = diag (A, A, A) . Then we can write

y(i) = H(i)Ab(i) + σv(i),

(15)

where H(i) = H[1](i) H[0](i) H[−1](i) and v(i) ∼
Nc 0, H[0](i) . Based on (15), both the SIC-MMSE and the SIC-MF
SISO multiuser detectors can be similarly applied as in the

2704
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:40:29 UTC from IEEE Xplore. Restrictions apply.

synchronous and AWGN case. Specifically, the extrinsic information Lm q →L[bk(i)] is given by

4 Lqm→L[bk(i)] =

µk (i)zk (i)

νk (i)2

,

(16)

where as before, zk(i), µk(i) and νk(i) are respectively the output, mean, and variance of the MMSE or matched filter (after soft interference cancellation). The definition and derivation details are seen in [4].

IV. DISTRIBUTION OF MULTIUSER DETECTOR EXTRINSIC MESSAGES

A. Synchronous CDMA with AWGN Channel
The extrinsic information Lqm→L[bk(i)] from SIC-MMSE multiuser detector has a Gaussian distribution with mean and variance given respectively by

E {Lqm→L [bk (i)]}

=

2µk(i)bk(i) , 1 − µk(i)

(17)

Var{Lm q →L[bk(i)]}

=

4µk(i) . 1 − µk(i)

(18)

Hence the extrinsic message has a Gaussian distribution of

the

form

Lm q →L[bk(i)]

∼

N (mbk(i), 2m),

with

m

=

2µk (i) 1−µk (i)

.

Here we assume each user has equal power, i.e., A1 = A2 =

· · · = AK = A. Given the distribution of the input code bit

LLR’s, as well as the channel and noise statistics, we can

compute m via Monte Carlo evaluation.

Note that the a priori code bit LLR from the LDPC

decoder is typically modeled as mixture symmetric Gaussian

distributed, i.e.,

J

fmq−←1L = λj N (mj , 2mj )

(19)

j=1

, where mj and 2mj are respectively the mean and the variance of the jth component. Here λj is the fraction of the bit nodes of degree j and we assume that the output extrinsic LLR at a node of degree j is symmetric Gaussian with mean mj [1], [3], [9].
We also approximate pdf of extrinsic LLR from optimal receiver as symmetric Gaussian distribution. To obtain m for optimal receiver, we have to simulate the system. The results of the simulation shows the Gaussian approximation agrees well with the histogram. When Eb/No is small, the match filter output can still be approximated as Gaussian distribution. Therefore, the output extrinsic information can be approximated as symmetric Gaussian pdf.
Fig. 2 shows the histograms of the extrinsic information at the optimal, SIC-MMSE, MF multiuser detectors by simulating the channel and the detector as well the symmetric Gaussian pdf’s with same means. It can be seen that the match is quite close for each detector, indicating that the underlying pdf is well approximated by the symmetric Gaussian.

B. Asynchronous CDMA with Fading Channel

Conditioned on the channel gk(i) = [gk1(i), · · · , gk P (i)],

the extrinsic message from the multiuser detector has

a Gaussian distribution, i.e., ξk(i) ∼ fmq →L(gk(i)) =

N

4µk (i) 1−µk (i)

,

8µk (i) 1−µk (i)

. gk(i) is random and hence the pdf of

the output extrinsic message of multiuser detector is given by

fmq →L = fmq →L gk(i) p gk(i) dgk(i).

(20)

In general, the pdf in (20) can not be well approximated as Gaussian. However, we can approximate fmq →L as a mixture
of symmetric Gaussian pdf’s, i.e.,

J

fmq →L =∼ πj N (mj , 2mj ).

(21)

j=1

Note that in the limit as J → ∞, (21) can approximate (20) arbitrarily closely. However, here we are interested in using the pdf (21) with finite J to closely approximate the pdf in (20). For fixed number of mixtures J, based on the observations,
the parameters θ = {πj, mj, j = 1, · · · , J} can be estimated using the expectation-maximization (EM) algorithm [6].
Consider a five-user asynchronous CDMA system in independently Rayleigh fading channel and an Es/No = 0 dB employing MMSE multiuser detector when the input LLR distribution is fmq−←1L = 0.05N (0.1, 0.2) + 0.25N (1.0, 2.0) + 0.7N (5.0, 10.0). Fig. 3 shows the histogram of the multiuser detector output extrinsic information obtained using Monte Carlo simulations and the approximation of the pdf using a mixture of symmetric Gaussian distributions computed via the EM algorithm. The two curves are almost indistinguishable indicating that the approximation is very accurate. A symmetric Gaussian pdf which has the same mean as that of the histogram is also shown. It is seen that such a single symmetric Gaussian approximation of the extrinsic information distribution is quite inaccurate. This confirms that the extrinsic information delivered by the SIC-MMSE multiuser detector in fading channels cannot be assumed to be Gaussian, whereas a mixture of symmetric Gaussian pdf offers a good approximation.

C. Design of LDPC Codes
To compute the thresholds for LDPC codes with the turbo multiuser detection, the principal idea is density evolution [1], [5], [8]. Here we treat the more general case where the pdf at the multiuser detector output is a mixture of symmetric Gaussians and the steps of computing threshold involve in computing the pdf’s of the extrinsic LLRs at each iteration which is similar to [1]. The procedure for computing the thresholds for a given degree profile (λ(x), ρ(x)) can be used in conjunction with an optimization procedure to design optimal LDPC codes for the multiuser detection [7].

V. RESULTS
Two-user Synchronous CDMA System with Periodic Spreading Sequences: With periodic spreading sequences, the cross correlation matrix is fixed. Set ρjk = 0.5 for j = k. Three

2705
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:40:29 UTC from IEEE Xplore. Restrictions apply.

different receivers were simulated (i.e., optimal, SIC-MMSE

and matched filter). The theoretical thresholds for a (3, 6) rate

1 2

regular

LDPC

code,

and

the

simulation

results

with

code

length N = 100, 000 bits are shown in Fig. 4. The turbo

receiver performs 15 inner iterations inside LDPC decoder

every outer iteration. Total 10 outer iterations are performed

to ensure the well convergence. It is seen that the actual simu-

lation results are within 0.2dB from the theoretical thresholds for three different detectors at BER of 10−6, indicating that

the Gaussian assumption and the characterization of the input-

output pdf of the multiuser detector extrinsic information is

quite accurate. Optimum degree profiles were computed. The

results presented here show that the irregular codes provide

about 0.5dB better performance than the regular codes.

Five-user Synchronous System with Aperiodic Spreading

Sequence: Consider a five-user synchronous system in AWGN

channel. For each user, the spreading code is random code

with processing gain Nc = 10 which varies with symbol i. The theoretical thresholds for the (3, 6) regular LDPC code

with maximum number of iterations between the multiuser

detector and decoder, P = 30, are shown in Fig. 5 for MMSE

and MF receivers. Both receivers have the performance for

the regular LDPC code within 0.05 dB from the thresholds.

The irregular LDPC code was designed and about 0.65 dB

better than the (3, 6) regular LDPC code for MMSE receiver,

0.5 dB for MF receiver, indicating the usefulness of the

proposed techniques for designing good LDPC codes. Shown

in Fig. 5, the performance of simulation is only 0.1 dB from

the threshold.

Five-user Asynchronous System in Fading: Consider a 5-

user asynchronous CDMA system in random fading channel

with random spreading. Each user’s channel contains four

paths, i.e., P = 4. The relative path power gains are 0, −3, −6 and −9 dB and the relative delay is 0, Tc, 2Tc, 3Tc. The theoretical thresholds for a (3, 6) regular LDPC code and

simulation results are shown in Fig. 6. It is seen that the

simulated BER performance matched quite well with the

thresholds, indicating that the threshold is fairly accurate. The

optimal

degree

profiles

with

dlmax

= 20,

and

rate-

1 2

for

both

receivers were designed. The simulation results for a randomly

constructed LDPC code with these degree profiles are also shown in Fig. 6. At a BER of 10−6, the performance is

about 0.2 dB away from the thresholds. The irregular codes

outperform the regular ones by about 0.6 dB for both receivers.

These results show that by using the EM algorithm, we can

accurately model the extrinsic information as a mixture of

Gaussian densities and use this to design good irregular LDPC

codes.

VI. CONCLUSIONS
In this paper, we have shown how to characterize the pdf of the extrinsic information at the output of the multiuser detector as a function of the pdf of the input extrinsic information, the Eb/No and cross correlation matrix of spreading codes for both synchronous and asynchronous CDMA systems. For synchronous system, we have shown that the pdf can be

assumed to be symmetric Gaussian, whereas for asynchronous system with fading the pdf can be approximated as a mixture of symmetric Gaussian densities. By modelling the pdf of the extrinsic information as a mixture of Gaussian densities, we have shown how to compute the thresholds for a given irregular LDPC code degree profile and to design good irregular LDPC codes. In all cases, the computed thresholds match very well with simulations, showing that the approximation is good. Irregular codes designed using these techniques significantly outperform regular LDPC codes.
REFERENCES
[1] S.Y. Chung, T. Richardson, and R. Urbanke. Analysis of sum-product decoding of low-density parity check codes using a Gaussian approximation. IEEE Trans. Info. Theory, pages 657–670, Feb. 2001.
[2] A.D. Damnjanovic and B.R. Vojcic. Iterative multiuser detection/decoding for turbo coded CDMA. In IEEE Int. Symp. Spread Spectrum Techniques and Applications, pages 603–607, 2000.
[3] H. El Gamal and R. Hammons. Analyzing the turbo decoder using the Gaussian assumption. IEEE Trans. Info. Theory, pages 671–686, Feb. 2001.
[4] Q. Li, X. Wang, and C.N. Georghiades. Turbo Multiuser Detection for Turbo-Coded CDMA in Multipath Fading Channels to appear in IEEE Trans. Vehicular Tech.
[5] M. G. Luby, M. Mitzenmacher, M.A. Shokrollahi, and D. A. Spielman. Analysis of low density codes and improved designs using irregular graphs. In Proc. of 30th ACM Symp. on Theory of Computing (available from http://www.icsi.berkeley.edu/ luby), 1998.
[6] G.J. McLachlan and T. Krishnan. The EM algorithm and extensions. Wiley: New York, 1997.
[7] T. Richardson, A. Shokrohalli, and R. Urbanke. Design of capacity approaching irregular low density parity check codes. IEEE Trans. Info. Theory, pages 619–637, Feb. 2001.
[8] T. Richardson and R. Urbanke. Capacity of low density parity check codes under message passing decoding. IEEE Trans. Info. Theory, pages 599–618, Feb. 2001.
[9] S. TenBrink. Convergence of iterative decoding. Electronics Letters, pages 1117–1118, 24 June 1999.
[10] X. Wang and H.V. Poor. Iterative (turbo) soft interference cancellation and decoding for coded CDMA. IEEE Trans. Commun., 47:1046–1061, July 1999.

{d1 }

LDPC encoder

interleaver

symbol mapper {b1} spreader s1

{d2 }

LDPC encoder

interleaver

symbol mapper {b2} spreader s2

r(t)

+

channel

{dK}

LDPC encoder

interleaver

symbol mapper {bK} spreader sK

...
SISO r(t) multiuser
detector

deinterleaver

LDPC decoder

deinterleaver

LDPC decoder

interleaver information bits d1
interleaver information bits d2

deinterleaver

LDPC decoder

interleaver information bits dK

Fig. 1. An LDPC coded CDMA system with iterative receiver.

2706
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:40:29 UTC from IEEE Xplore. Restrictions apply.

Historgram (OPT)

Gauss. Approx. (OPT)

0.25

Historgram (MMSE)

Gauss. Approx. (MMSE)

Historgram (MF)

Gauss. Approx. (MF)

0.2

(x)
L

0.15

fqmud→

0.1

0.05

0

−6

−4

−2

0

2

4

6

8

10

x

Fig. 2. The histograms for the multiuser detectors extrinsic information in a 2user synchronous CDMA system, and the symmetric Gaussian approximations by Monte Carlo evaluation.

(x)

mud → L

0.18

Histogram

0.16

Gauss. Approx.

Mixture Gauss. Approx.

0.14

0.12

0.1

0.08

0.06

0.04

0.02

0

−10

−5

0

5

10

15

20

x

fq

10−1
MMSE
10−2

MF

MMSE

MF

10−3

Bit Error Rate

10−4

10−5

10−6 10−7
1

(3,6) regular code − threshold (3,6) regular code − simulation (MMSE) (3,6) regular code − simulation (MF) Opt. irreg code − threshold Opt. irreg code − simulation (MMSE) Opt. irreg code − simulation (MF)

1.2

1.4

1.6

1.8

2

2.2

E /N (dB)

b0

Fig. 5. Thresholds and simulation results for the (3, 6) regular LDPC codes and for the optimum irregular LDPC codes in a 5-user synchronous system with MMSE and MF receivers.

Fig. 3. The histogram for the SIC-MMSE multiuser detector extrinsic information in a 5-user asynchronous CDMA system with fading, and the approximations by a single symmetric Gaussian pdf, and by a mixture of symmetric Gaussian pdf’s obtained using the EM algorithm.

10−1

MMSE

MF

10−2

MMSE

MF

Bit Error Rate

10−1

OPT

MMSE

10−2

MF

OPT MMSE

MF

10−3 10−4

10−3

10−5

Bit Error Rate

10−4

10−5

10−6

10−7

0.6

0.8

(3,6) regular code − threshold (3,6) regular code − simulation (OPT) (3,6) regular code − simulation (MMSE) (3,6) regular code − simulation (MF) Opt. irreg code − threshold Opt. irreg code − simulation (OPT) Opt. irreg code − simulation (MMSE) Opt. irreg code − simulation (MF)

1

1.2

1.4

1.6

1.8

E /N (dB)

b0

(3,6) regular code − threshold

10−6

(3,6) regular code − simulation (MMSE)

(3,6) regular code − simulation (MF)

Opt. irreg code − threshold

Opt. irreg code − simulation (MMSE)

Opt. irreg code − simulation (MF)

10−7

1.7

1.8

1.9

2

2.1

2.2

2.3

2.4

2.5

2.6

2.7

Eb/N0 (dB)

Fig. 6. Thresholds and simulation results for the (3, 6) regular LDPC codes and for the optimum irregular LDPC codes in a 5-user asynchronous system with fading using MMSE and MF receivers.

Fig. 4. Thresholds and simulation results for the (3, 6) regular LDPC codes and for the optimum irregular LDPC codes in a 2-user synchronous system with different receivers.

2707
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:40:29 UTC from IEEE Xplore. Restrictions apply.

