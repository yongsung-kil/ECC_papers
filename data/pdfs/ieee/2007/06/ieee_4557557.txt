ISIT2007, Nice, France, June 24 - June 29, 2007
Designing LDPC Codes without small trapping sets by using Tanner Graph Covers

Milos Ivkovic
Department of Mathematics University of Arizona
Tucson, AZ 85721, USA Email: milos@math.arizona.edu

Shashi Kiran Chilappagari
Dept. of Electrical and Computer Eng. University of Arizona
Tucson, AZ 85721, USA Email: shashic@ece.arizona.edu

Bane Vasic
Dept. of ECE and Dept. of Mathematics University of Arizona
Tucson, AZ 85721, USA Email: vasic@ece.arizona.edu

Abstract- We present a method for lowering the error floor of low-density parity check (LDPC) codes. It is based on Tanner graph covers that do not have trapping sets from the original code. The advantages of the method are that it is universal, as it can be applied to any LDPC code/channel model/decoding algorithm and it improves performance at the expense of increasing the code length, without losing the code regularity, without changing the decoding algorithm, and, under certain conditions, without lowering the code rate. We illustrate the method by modifying Tanner, MacKay and Margulis codes to improve performance on the binary symmetric channel (BSC) under the Gallager B decoding algorithm.
I. INTRODUCTION
The error-floor problem is arguably the most important problem in the theory of low-density parity check (LDPC) codes and iterative decoding algorithms. Roughly, error floor is an abrupt change in the frame error rate (FER) performance of an iterative decoder in the high signal-to-noise ratio (SNR) region (see [11] for more details and [1], [2], [4], [3], [5] for general theory of LDPC codes).
The error floor problem for iterative decoding on the binary erasure channel (BEC) is now well understood, see [9], [10] and references therein.
In the case of the additive white Gaussian noise (AWGN) channel, MacKay and Postol in [6] pointed out a weakness in the construction of the Margulis code [23] which led to high error floors. Richardson in [11] presented a method to estimate error floors of LDPC codes and presented results on the AWGN channel. He pointed out that the decoder performance is governed by a few most likely error events related to certain topological structures in the Tanner graph of
the code, called trapping sets (or stopping sets on BEC [9]).1
The approach from [11] was further refined by Stepanov et al. in [12]. Zhang et al. [13] presented similar results based on hardware decoder implementation. Vontobel and Koetter [14] established a theoretical framework for finite length analysis of message passing iterative decoding based on graph covers. This approach was used by Smarandache et al. in [15] to analyze performance of LDPC codes from projective geometries and for LDPC convolutional codes [16].
To the best of our knowledge the first account on the most likely error events on the binary symmetric channel (BSC) is given by Forney et al. in [17]. Some results on LDPC codes
1The necessary definitions will be given in the next section.

over the BSC appear in [15], also. In this work we shall use the partial results on trapping sets classification from [18].
A significant part of the research on error floor analysis has also focused on methods for lowering the error floor. The two distinct approaches taken to tackle this problem are (1) modifying the decoding algorithm and (2) constructing codes avoiding certain topological structures. Numerous modifications of the sum-product decoding algorithm were proposed, see, for example, [19] and [20], among others.
Among the methods from the second group, there have been novel constructions of codes with high Tanner graph girth [22], [8], as it was observed that codes with low girth tend to have high error floors. While it is true that known trapping sets have short cycles [12], [18], the example of projective geometry codes, (that have short cycles, but perform well under iterative decoding) suggests that maximizing the girth is not the optimal procedure. As the understanding of the error floor phenomena and its connection with trapping sets grows, avoiding the trapping sets directly (rather than short cycles) seems to be a more efficient way (in terms of code rate and decoding complexity), to suppress error floors.
Code modification for improving the performance on the binary erasure channel (BEC) was studied by Wang in [21]. To the best of our knowledge, it is the first paper on code modification with maximization of the size of stopping (or trapping) sets as the objective. Edge swapping within the code was suggested as a way to break the stopping sets. The method that we propose is similar. Roughly speaking, it consists of taking two (or more) copies of the same code and swapping edges between the code copies in such a way that the most dominant trapping sets are broken. It is also similar to the code constructions that appear in Smarandache et al. [16], Divsalar and Jones [25] and Kelley, Sridhara and Rosenthal [26].
The advantages of the method are: (a) it is universal as it can be applied to any code/channel model/decoding algorithm and (b) it improves performance at the expense of increasing the code length only, without losing the code regularity, without changing the decoding algorithm, and, under certain conditions, without lowering the code rate. If the length of the code is fixed to n, the method can be applied
by taking t copies of a (good) code C of length n/t and eliminating the most dominant trapping sets of C.
We apply our method and construct codes based on Mar-

1-4244-1429-6/07/$25.00 ©2007 IEEE

2266

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:00:34 UTC from IEEE Xplore. Restrictions apply.

ISIT2007, Nice, France, June 24 - June 29, 2007

gulis [23], Tanner [22] and MacKay [24] codes and present results on the BSC when decoded using the Gallager B algorithm [1]. It is worth noting that the error floor on the AWGN channel not only depends on the structure of the code but also on implementation nuances of the decoding algorithm, such as numerical precision of messages [11]. Since the Gallager B algorithm operates by passing binary messages along the edges of a graph, any concern about the numerical precision of messages does not arise.
The rest of the paper is organized as follows. In Section II we introduce the notion of trapping sets and their relation to the performance of the code. We explain the proposed method in detail in Section III. We present numerical results in Section IV and conclude in Section V.
II. BASIC CONCEPTS
The Tanner graph of an LDPC code, 9, is a bipartite graph with two sets of nodes: variable (bit) nodes and check (constraint) nodes. The check nodes (variable nodes) connected to a variable node (check node) are referred to as its neighbors. The degree of a node is the number of its neighbors. The girth g is the length of the shortest cycle in 9. In this paper, * represents a variable node, D represents an even degree check node and * represents an odd degree
check node.
The notion of trapping sets, sometimes referred to as nearcodewords, was first introduced in [6], but here we follow the formalism from [20].
Definition 1: For a given m x n matrix U = (Ui,j) with
1 <i < m, 1 <j < n, the projection of a set of h columns
indexed by jl, 2 ..... jh is a m x h matrix consisting of the elements ui,j, 1 < i < m, j =1 2,jh
Definition 2: Let H be a parity check matrix of an LDPC code. An (a, b) trapping set T is a set of a columns of H with a projection that contains b > 0 odd weight rows.
The definition of the trapping set above is purely topological, that is, a trapping set can be seen as a subgraph of
the Tanner graph. In other words, a (a, b) trapping set T is
a subgraph with a variable nodes and b odd degree checks. The most probable noise realizations that lead to decoding failure are related to trapping sets ([11], [12]). A measure of noise realization probability is referred to as pseudoweight. Following the terminology in [12], an instanton can be defined as the most likely noise realization that leads to decoding failure.
The instantons on the BSC consist of the received bit configurations with minimal number of erroneous bits that lead to decoding failure. Following [18], the notion specific to BSC, analogous to pseudo-weight, can be defined as:
Definition 3: The minimal number of variable nodes that have to be initially in error for the decoder to end up in the trapping set T will be referred to as the critical number k for that trapping set.
Remark: To "end up" in a trapping set T means that, after a finite number of iterations, the decoder will be in error, on at least one variable node from i, at every iteration. Note that the variable nodes that are initially in error do not have to be within the trapping set.

We illustrate the above concepts with an example.

El

.
(a) (5,3) trapping set (b) (4,4) trapping set
Fig. 1. Trapping sets

Example 1: The (5, 3) trapping set in Fig. 1(a). appears (among other codes) in the Tanner (155, 64) code [18]. This trapping set has critical number k = 3 under the Gallager B decoding algorithm [1], meaning that, if three variable nodes, on the diagonal from bottom left to top right, are initially in error, the decoder will fail to correct the errors.
Fig. l(b) illustrates a (4, 4) trapping set. This trapping set, although smaller, has critical number k = 4, (all the variable nodes have to be in error initially for the decoder to fail). So, if a code has both (5, 3) and (4, 4) trapping sets, the FER performance is dominated by the (5, 3) trapping set.
At the end of this example, we note that the (5, 3) trapping set above is an example of an oscillatory trapping set, i.e, if three variable nodes on the diagonal are initially in error, after the first iteration those nodes will be decoded correctly, but the remaining two will be in error. In the decoding attempt after the second iteration those two will be correct, but the initial three will be in error again, and so on.
Remark: Note that on the BEC the critical number is just the size of the stopping set, see [21].
We now clarify what "the most dominant trapping sets" means and how these effect code performance.
Let a be the transition probability of the BSC and ck be the number of configurations of received bits for which k channel errors lead to a codeword (frame) error. The frame error rate (FER) is given by:
n
FER(a) = Z Ckak(1-al)(n-1k)
k=i
where i is the minimal number of channel errors that can lead to a decoding error (size of instantons) and n is the length of the code.
On a semilog scale the FER is given by the expression
n
log(FER(a)) = log( ckak(1l a)n-k) (1)
k=i
= log(Ci) +( log(a) + log((1 _a)n 2i) (2)
+log + ai+ 0(l o)-o +t + Cnaan-i(t _ t)i-n (3)

In the limit ag -> O we note that lim [log((l- a)1 t)] 0

and

lim [log (I + +l a(1 a0 ) .. .+ ca _ (1 a) 2)] 0

a--C~~i~~O

Ci~ ~ U

2267
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:00:34 UTC from IEEE Xplore. Restrictions apply.

So, the behavior of the FER curve for small a is dominated by
log(FER(a)) log(ci) + i log(a)

ISIT2007, Nice, France, June 24 - June 29, 2007

The log(FER) vs log(a) graph is close to a straight line with slope equal to i, the minimal critical number or cardinality of the instantons.
Therefore, if two codes C, and C2 have instanton sizes
i1 and i2, such that i1 < i2, then the code C2 will perform better than Ci for small enough a, independent of the number of instantons, just because log(a) -> -oc as a -> 0. Note also that the critical number of the most dominant trapping sets cannot be greater than half the minimum distance. If it is the case, the performance of the decoder is dominated by the minimum weight codewords.

Fig. 2. Trapping set elimination Fig. 3. Reintroducing trapping set by swapping two edges

III. THE METHOD FOR ELIMINATING TRAPPING SETS
In this section we present a method to construct an LDPC code C(2) of length 2n from a given code C of length n.
Let H and H(2) represent the parity check matrices of C and C(2) respectively. H(2) is initialized to
H(2) [H 0]

In simple words H(2) is formed by taking two copies of H

say Ci and C2. It can be seen that if H has dimensions rm x n, then H(2) has dimensions 2rm x 2n. Every edge e

in the Tanner graph 9 of C is associated with a nonzero

entry Ht,k. The operation of changing the value of H (2) and

H(2)mt+nt,n+kto

"0",

and

H(~2m±)tt,k

and

H (m2,)rn±k

to

"1"

t,k
is termed

as swapping the edge e. Fig. 2 illustrates edge swapping in

two copies of a (5, 3) trapping set. We assume that the most

dominant trapping sets for C are known. The method can be

described in the following steps:

Algorithm:

1) Take two copies C, and C2 of the same code. Since the
codes are identical they have the same trapping sets. 2) Initialize SwappedEdges=q; FrozenEdges=q; 3) Order the trapping sets by the critical number.
4) Choose a trapping set T, in the Tanner graph of Cl,
with minimal critical number. Let E,_ denote the set of all edges in T,. If (E,1 n SwappedEdges 0) go
to 6. Otherwise go to 5.
5) Swap an arbitrarily chosen edge e e E,1 \ FrozenEdges (if it exists). Set SwappedEdges
SwappedEdges U e.
6) "Freeze" the edges E,_ from T, so that they cannot
be swapped in the following steps. Set FrozenEdges = FrozenEdges U E,1 . Remove T, (and T) from the list of trapping sets. 7) Repeat steps 2 to 4 until it is possible to remove the
trapping sets of desired size.

Step 6 is needed because swapping additional edges from the (former) trapping sets might introduce trapping sets of the same size again. Fig. 3 illustrates such a swapping which corresponds to just interchanging the check nodes.

The Tanner graph of the the newly made code is a special double cover of the original code's Tanner graph, interested readers are referred to [14].
Remark: There are several approaches which may improve the efficiency of the algorithm. Firstly, instead of swapping the edges at random at step 3, edges could be swapped based on the number of trapping sets they participate in, or by using some other schedule that would (potentially) lead to the highest number of trapping sets eliminated. The structure of the code can also be exploited. For example, the Margulis
(2640, 1320) code [23], has 1320 (4, 4) minimal trapping sets with the property that each trapping set has one edge that does not participate in any other minimal trapping set. So,
instead of swapping edges at random, the edges appearing in only one trapping set can be swapped, and such a procedure is guaranteed to eliminate all the minimal trapping sets.
Note, however, that any edge swapping schedule can be seen as a special case of the algorithm above, that is, as a particular realization of the random edge swapping. For all the codes that we considered, all the trapping sets with minimal critical number were eliminated by the algorithm with random edge swapping.
The following theorem shows how this method affects the code rate.
Theorem 1: If the code C, with parity check matrix H, and rate r (and length n) is used in the algorithm above, the resulting code C(2) will have rate r(2) (and length 2n), such that r(2) < r.
Proof: Each edge swapping operation in the algorithm can be seen as matrix modification. At the end of the algorithm, code C(2) is determined by
H(2) H' B

where H' and B are matrices such that H' + B = H, and
Htl,k (or Bt,k) can be equal to "1" only if Ht,k = 1.
If the second block row is added to the first in H(2), and
then the the first block column is added to the second, we
end up with

H' B

HH

H0

H'- H'- -->

-->

B

B

BH

(4)

2268
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:00:34 UTC from IEEE Xplore. Restrictions apply.

The last matrix in (4) has rank which is greater than or equal to twice the rank of H. Therefore, the code C(2) has rate
r(2) < r where r is the rate of C.El

Note, that r(2) = r if B = CH+HD, for some matrices C
and D, where C corresponds to linear combinations of rows of H and D corresponds to linear combinations of columns of H. We also have a following corollary.

Corollary 1: If the matrix H has full rank, then r(2) r.

Proof: This follows from the fact that if H has full rank, then the last matrix in (4) has full rank also. D

At the end of this section, we briefly discuss the minimal distance of the modified code.

Theorem 2: If the code C has minimal distance dmin, the
modified code C(2), will have the minimal distance d(2)

such that, 2dmin > d(in > dmin
Proof: We first prove that d(2) > dmin. Suppose that the
minimal weight codeword of C(2) is c(2), where c(2) is a
column vector consisting of two vectors cl and c2 of length n. Then H(2)C(2) = 0 is equivalent to

H' B ] [ Cl
B HI L C2

[ H'cl + BC2 1 Bc1 + H'C2 ]

(5)

Note that cl + c2 = c is a column vector of length n, with Hamming weight Wh(C) < Wh (C(2)) , where Wh (c(2)) is the Hamming weight of the c(2). Now:

Hcf= (H'+B)(cl +c2) = H'cl +Bci+H'cl +BC2 =0 (6)

because the last expression in Eq. (6) is equal to the sum of
entries of the last column vector in Eq. 5. So, c is a codeword
of C. From the fact that Wh(C) < Wh (C(2)) it follows that
d(m2i)rnn > dmmirn,
If c = 0 then cl = c2, and from Eq. (5) follows that
Hcl = 0, so cl is a codeword of C and again d(2) > dmin.
Similarly, if we assume that cl is a minimal weight codeword of C, we have that:

H' B

= c]

0

(7)

B H'j[-cij

so 2dmin >_d(m2)in'

We finish this proof by mentioning that it is not difficult to

construct

examples

where

2dmin

=

d

(2)
mnm

or d(m2i)n > d , mint

so the statement of the theorem is "sharp". Also, this theorem

can be seen as a generalization of Lemma 2.4 from [16].FI

We described the algorithm in its basic form. H(2) can

be initialized by interleaving the copies Ci and C2 in an

arbitrary order, but we choose concatenation to keep the

notation simple. The method, as well as all the proofs, will

hold for any interleaving. It is also possible to consider more

than two copies of the code to further eliminate trapping sets

with higher critical number.

IV. NUMERICAL RESULTS
In this section we illustrate the proposed method by modifying Tanner [22], Margulis [23] and MacKay [24] codes to eliminate trapping sets under the Gallager B decoding algorithm. We use trapping sets reported in [18].

ISIT2007, Nice, France, June 24 - June 29, 2007
Example 2: (Tanner (155, 64) code) This code has (5, 3) trapping sets (Fig. 1(a)) with critical number i = 3 as the most dominant ones. There are 155 such trapping sets [18]. The algorithm with random edge selection produced a (310, 126) code for which the minimal trapping sets are type (4,4) (eight cycles) with critical number k = 4 (see Fig 1(b)). This was confirmed by numerical simulations in Fig. 4.

10O

3// slope P3 I.,.....7.

//

/

iY

f/

/

E

/ /

IL

/

/

- 10

.-I

/4

/ /

,Zslope 4

P10
/| + Tanner code * Modified Tanner code

10

10o-

Transition probability (a)

Fig. 4. Tanner code performance

Note that there was a small rate penalty to this procedure. The original Tanner code has rate 0.4129, whereas the modified code has rate 0.4065. The performance of the codes for a wider range of a is given in Fig. 5. The FER curve changes the slope, for higher a, where FER contribution from the expression (3) is not negligible.

10

10

slope ~3

E

slope 4

L10o

---Tanner code Modified Tanner code

10 10-

10-'
Transition probability (cx)

Fig. 5. Tanner code performance for a longer range of a

Example 3: (Margulis (2640,1320) code) The parity check of this matrix has full rank, so the modified code is an (5280, 2640) code, and has the same rate as the original code, i.e., r(2) = r = 0.5.
This code has 1320 (4, 4) trapping sets with critical number 4 as the most dominant ones. The modified (5280, 2640) code has no (4, 4) trapping sets and the performance is governed by (5, 5) trapping sets (ten cycles), that have critical
number k = 5, see Fig. 6.
Example 4: (MacKay's (1008, 504) codes) This is an example of how the method can be used to produce better codes of a fixed length. We have taken a 504 length MacKay code and constructed a 1008 (2 * 504) length code. The new code performs better than MacKay codes of length 1008.
Both original 504 and 1008 length codes have two types of trapping sets with critical number k = 3, (5,3) and (3,3) (six

2269
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:00:34 UTC from IEEE Xplore. Restrictions apply.

44
Margulis code
ModifiedMdalguslicoM
10 I

10

IL

sloipe 4

7:

*ts

"slop5 e

10 -7~~~~~~~~~~~~o10-2

Transition probab1ility (ct

Fig. 6. Margulis code performance

cycles). We ran the algorithm so that all (3,3) trapping sets are eliminated from the newly constructed, but none of the (5,3) trapping sets. The results are shown in Fig. 7. It can be

10 Modified MacKay's Code (lenght 1008) MacKay's Code (lenght 1008)
LU
!LL
o 10

slope z 3

. lope 3

10 0.0008

0.0009

0.001

0.0011

Transition probability (cc)

0.0012

Fig. 7. MacKay's codes performance

seen that, although the FER performance is improved, since the number of bit configuration with 3 errors that can lead to decoding failure is smaller, the slope of the FER curve is approximately the same.
V. CONCLUSION
The proposed method allows construction of codes with good FER performance, but low row/column weight (as opposed to projective geometry codes) and therefore relatively low decoding complexity. Although numerical results for Gallager B decoder are presented, we reiterate that the method can be used for code optimization based on the trapping sets of an arbitrary decoder. For example, results from [12] can be used to optimize the codes for the AWGN channel under the min-sum decoding algorithm.
The algorithm can also be used to determine the pseudoweight spectrum of a code as follows. Once the most likely trapping sets (those with the smallest pseudo-weight) are determined and eliminated by the method, numerically obtained decoding performance of a modified code offers an estimate of the pseudo-weight of the next most likely trapping sets.
ACKNOWLEDGMENT
Authors would like to acknowledge valuable discussions with Robert Indik and Misha Stepanov.
This work was supported by grants from INSIC-EHDR and NSF-CCR (Grant no. 0634969).

ISIT2007, Nice, France, June 24 - June 29, 2007
REFERENCES
[1] R. G. Gallager, Low Density Parity Check Codes. Cambridge, MA: MIT Press, 1963.
[2] D.J.C. MacKay and R. M. Neal, "Good Error Correcting Codes Based on Very Sparse Matrices," http ://wol.ra.phy. cam.ac.uk/mackay.
[3] T. Richardson, A. Shokrollahi, and R. Urbanke, "Design of capacity
approaching irregular low-density parity-check codes," IEEE Trans. Inform. Theory, vol. 47, pp. 619-637, 2001. [4] T. J. Richardson and R. Urbanke, "The Capacity of Low-Density Parity-Check Codes Under Message-Passing Algorithm," IEEE Trans. Inform. Theory, vol. 47, no. 2, pp. 599-618, Feb. 2001. [5] Y. Kou, S. Lin, and M.P.C. Fossorier, "Low-density parity-check codes based on finite geometries: a rediscovery and new results," IEEE Trans. Inform. Theory, vol. 47, Nov. 2001, pp. 2711 -2736. [6] D. MacKay and M. Postol, "Weaknesses of Margulis and RamanujanMargulis low-density parity check codes," Electronic Notes in Theoretical Computer Science, vol. 74, 2003. [7] M. A. Margulis, "Explicit Group-Theoretic Constructions for Combinatorial Designs with Applications To Expanders and Concentrators," Probl. Pered. Inform., vol. 24, no. 1, pp. 51-60, 1988. [8] J.K. Moura. J. Lu, and H. Zhang, "Structured LDPC codes with large girth," IEEE Signal Proc. Mag., vol. 21, no. 1, pp. 42-55, Jan. 2004
[9] C. Di, D. Proietti, 1. E. Telatar, T. J. Richardson, R. L. Urbanke, "Finite length analysis of low-density parity-check codes on the binary erasure channel," IEEE Trans. on Information Theory, vol. 48, no. 6, pp. 1570-
1579, Jun 2002. [10] C. Wang, S. R. Kulkarni, H. Vincent Poor, "Upper Bounding the
Performance of Arbitrary Finite LDPC Codes on Binary Erasure Channels," in Proc. IEEE ISIT, 2006, Seattle, WA. [11] T. J. Richardson, "Error Floors of LDPC Codes," in Proc. 41st Annual Allerton Conf. on Communications, Control and Computing, 2003. [12] M.G. Stepanov, V. Chernyak, M. Chertkov, B. Vasic, "Diagnosis of weaknesses in modern error correction codes: a physics approach," Phys. Rev. Lett. 95 (22) 228701 (2005). [13] Z. Zhang, L. Dolecek, B. Nikolic, V. Anantharam and M Wainwright "Investigation of Error Floors of Structured Low- Density Parity-Check Codes by Hardware Emulation, in Proc. IEEE Globecom, 2006. [14] P. 0. Vontobel and R. Koetter, "Graph-cover decoding and finitelength analysis of message-passing iterative decoding of LDPC codes," preprint, available online at http://www.arxiv.org/. [15] R. Smarandache and P. 0. Vontobel,"Pseudo-Codeword Analysis of Tanner Graphs from Projective and Euclidean Planes," preprint, available online at http://arxiv.org/. [16] R. Smarandache, A. E. Pusane, P. 0. Vontobel and D.J Costello, Jr.,"Pseudo-Codeword Performance Analysis for LDPC Convolutional Codes," preprint, available online at http://arxiv.org/. [17] G. D. Forney, Jr., R. Koetter, F. R. Kschischang, and A. Reznik, "On the effective weights of pseudocodewords for codes defined on graphs with cycles," in Codes, Sys- tems, and Graphical Models (Minneapolis, MN, 1999) (B. Marcus and J. Rosenthal, eds.), vol. 123 of IMA Vol. Math. Appl., pp. 101112, Springer Verlag, New York, Inc., 2001. [18] S. K. Chilappagari, S. Sankaranarayanan, and B. Vasic, "Error Floors of LDPC Codes on the Binary Symmetric Channel," presented at the International Conference on Communications, ICC 2006, Istanbul, Turkey, June 2006. [19] N. Varnica and M. Fossorier, "Improvements in belief-propagation decoding based on averaging information from decoder and correction of clusters of nodes," in it IEEE Communications Letters, vol. 10, no 12. pp 846-848, Dec. 2006. [20] S. Laendner, T. Hehn, 0. Milenkovic and J. Huber, "When does one redundant parity-check equation matter?" Proc. IEEE Globecom, 2006. [21] C. Wang, "Code annealing and the suppressing effect of the cyclically lifted LDPC code ensemble," in 2006 IEEE Information Theory Workshop, Chengdu, China, October 22-26. [22] R. M. Tanner, D. Sridhara and T. Fuja, "A class of Group-Structured LDPC codes," in Proc. ISCTA, 2001. [23] J. Rosenthal and P. 0. Vontobel, "Constructions of LDPC Codes Using Ramanujan Graphs and Ideas From Margulis," in Proc. of the 38-th Annual Allerton Conference on Communication, Control, and Computing, pp. 248-257, 2000. [24] D. J. C. MacKay, "Encyclopedia of Sparse Graph Codes," Online: http://www.interference.phy.cam.ac.uk/ mackay/codes/data.html. [25] D. Divsalar, C. Jones, "Protgraph LDPC Codes with Node Degrees at Least 3," in Proc. IEEE Globecom, 2006. [26] C. Kelley, D. Sridhara, J. Rosenthal,"Tree-Based Construction of LDPC Codes Having Good Pseudocodeword Weights," preprint, available online at http://arxiv.org/.

2270
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:00:34 UTC from IEEE Xplore. Restrictions apply.

