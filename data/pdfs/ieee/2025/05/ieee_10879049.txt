888

IEEE COMMUNICATIONS LETTERS, VOL. 29, NO. 5, MAY 2025

Memory-Assisted Quantized LDPC Decoding

Philipp Mohr , Graduate Student Member, IEEE, and Gerhard Bauch , Fellow, IEEE

Abstract— We enhance coarsely quantized LDPC decoding by reusing computed check node messages from previous iterations. Typically, variable and check nodes update and replace old messages every iteration. We show that, under coarse quantization, discarding old messages entails a significant loss of mutual information. The loss is avoided with additional memory, improving performance by up to 0.23 dB. We optimize quantization with a modified information bottleneck algorithm that considers the statistics of old messages. A simple merge operation reduces memory requirements. Depending on channel conditions and code rate, memory assistance enables up to 32 % better area efficiency for 2-bit decoding.
Index Terms— LDPC, 5G NR, decoding, message passing, layered schedule, coarse quantization, information bottleneck.
I. INTRODUCTION
EFFICIENT and reliable decoding of low-density paritycheck (LDPC) codes is vital in modern technologies with high data rate requirements, such as 5G New Radio (NR) [1]. Particularly the exchange of messages in iterative message passing decoding algorithms like belief propagation demands significant complexity [2]. To overcome this bottleneck, many prior works focus on reducing the bit width of exchanged messages in these algorithms through quantization operations, see e.g. [3], [4], [5], [6], [7], [8], [9], and [10].
The quantized messages represent reliability levels that encode probability information exchanged between variable nodes (VNs) and check nodes (CNs). The choice of reliability levels is crucial for excellent decoding performance with low-resolution messages [10]. Information optimum reliability levels can be found with the information bottleneck (IB) method which is a clustering framework, that enables the design of compression operations for maximizing preserved relevant mutual information [7], [8], [9], [10], [11], [12].
Typically, old messages from a previous iteration are replaced by updated messages from the current iteration in the prior works. One might question whether discarding previously computed and exchanged messages wastes valuable information. Indeed, under coarse quantization, this work confirms that preserving old messages of the previous iteration can significantly improve the decoding performance. For the design of a memory-assisted decoder we modify the sequential IB algorithm from [7] to be aware of the messages retained in memory. This algorithm is specifically suited for the design of deterministic compression mappings realized with symmetric thresholds. It has significantly reduced computational costs compared to more general solutions [12].
Received 2 December 2024; revised 21 January 2025; accepted 30 January 2025. Date of publication 10 February 2025; date of current version 12 May 2025. The associate editor coordinating the review of this letter and approving it for publication was Y. Fang. (Corresponding author: Philipp Mohr.)
The authors are with the Institute of Communications, Hamburg University of Technology, 21073 Hamburg, Germany (e-mail: philipp.mohr@tuhh.de; bauch@tuhh.de).
Digital Object Identifier 10.1109/LCOMM.2025.3540277

We combine the memory-assisted decoder structure with our recently proposed region-specific CN-aware quantizer design [10]. Region-specific quantization allows individual alphabets of reliability levels for subsets of exchanged messages particularly improving low-resolution decoding of highly irregular 5G LDPC codes. A CN-aware quantizer design for the VN extends the optimization scope to maximize preserved relevant information at the output of the subsequent CN update [9]. The combination of this work and [10] achieves up to 0.68 dB gain w.r.t. 2-bit decoding without those techniques.

II. PRELIMINARIES ON LDPC DECODING WITH MUTUAL INFORMATION MAXIMIZING QUANTIZATION

We consider LDPC codes defined through a base matrix Hb with entries Hbij∈{−1, . . . , Z} [1]. The base matrix Hb can be represented by a Tanner graph. Each column j becomes a
VN and each row i becomes a CN. The non-negative entries Hbij are edges between VNs and CNs. The node degree, i.e., the number of edges connected to a node, is djv for a VN and dic for a CN. Lifting replaces each edge with Z edges, which are cyclically permuted by Hbij. The lifted graph can be equivalently represented by a lifted parity check matrix H.
The encoder maps the information bits u of length K to
the code bits b such that Hb = 0. Under puncturing only
a subset of b is transmitted. We consider binary-phase shift-
keying (BPSK) modulation and a memoryless additive white
Gaussian noise (AWGN) channel.
All code bits related to a base column j are modeled with
the random variables Bj whose realizations are bj∈{0, 1}. The decoder observes wch-bit channel messages modeled with Tjch, tjch∈ wch using a sign-magnitude alphabet ζ = {−2ζ−1, . . . , −1, 1, . . . , 2ζ−1} with ζ=wch. Message passing
decoding computes and exchanges messages between VNs
and CNs to aggregate soft information for error correction
from the parity check constraints. Each edge of the base graph, enumerated by n∈N ={1, . . . , j dvj}, contains two memory locations: one for messages from VNs and one for
messages from CNs. A memory location stores Z messages after lifting the graph. The sets U v⊆N and U c⊆N specify
target memory locations for VN and CN updates, respectively.
The decoding schedule defines the order in which memory locations are updated as U = (U0v, U1c, U2v, U3c, . . .) followed by a final hard decision update using the latest updated CN
messages. We model exchanged w-bit VN and CN messages with discrete random variables Tvn, tnv ∈ w and Tnc , tcn∈ w, respectively.
Updating a VN memory location n∈U v yields

ℓnv = ϕcchol(n)(tcchol(n)) +

ϕcα(m)(tm c )

(1)

m∈n˜

with extrinsic CN locations n˜ = Nv(col(n))\{n} where Nv(j) = {m∈N : col(m) = j}. The reconstruction functions

1558-2558 © 2025 IEEE. All rights reserved, including rights for text and data mining, and training of artificial intelligence and similar technologies. Personal use is permitted, but republication/redistribution requires IEEE permission. See https://www.ieee.org/publications/rights/index.html for more information.
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:21:02 UTC from IEEE Xplore. Restrictions apply.

MOHR AND BAUCH: MEMORY-ASSISTED QUANTIZED LDPC DECODING

889

Fig. 1. Quantizer design with memory-assisted reconstruction.

ϕcjh(t) = rndκ(L(Bj|Tjch=t)) and ϕac (t) = rndκ(L(T¯ca = t|X¯a)) map incoming messages to integers corresponding to

the underlying log-likelihood ratio (LLR) levels. The LLR

functions

are

defined

as

L(T=t|X)

=

log

p(T=t|X=0) p(T=t|X=1)

and

L(X|T=t)

=

L(T=t|X)

+

log

p(X=0) p(X=1)

.

Further,

rndκ(ℓ)

=

sgn(ℓ)⌊|ℓ|/κ + 0.5⌋ scales and rounds ℓ to integers with

resolution parameter κ [10]. An implementation can use ϕac (t) = sgn(t)ϕac (|t|) for reducing complexity.
For the CN messages we allow different sets of LLR levels

ϕac , a∈A. Each memory location n is assigned to a region a with alignment function α : N →A. This work considers a

row-alignment α(n) = row(n) or matrix-alignment α(n) = 1. We use averaged random variables T¯ac = En{Tnc |α(n)=a} and X¯a = En{Xn|α(n)=a} with Xn = Bcol(n) [10].
The hard decision is modeled with ˆbj=(1− sgn(ℓˆj))/2 obtained from the a-posteriori probability (APP) LLR ℓˆcol(n) = ℓnv + ϕαc (n)(tcn). Row-layered decoder
structures, such as in Fig. 6, typically use the APP LLR for computing (1) efficiently as ℓvn = ℓˆcol(n) − ϕαc (n)(tnc ). Those decoders initialize the APP LLR with ℓˆj = ϕcjh(tjch). Hence,
the channel message reconstruction must be done only once

for all iterations. Updating a CN memory location n∈Uc using the min-sum

approximation [3] yields

tcn = Qα(n)(ℓcn) using ℓnc =

sgn(ℓm v

)

min
m∈n˜

|ℓvm

|

(2)

m∈n˜

with extrinsic VN locations n˜ = Nc(row(n))\{n} and the CN neighborhood set Nc(i) = {m∈N : col(m) = i}. Considering

the quantizer Qa after the CN update enables a CN-aware quantizer design as proposed in [10]. For all a∈{α(n):n∈Uc},

the quantizer design objective is to maximize mutual information maxQa I(X¯a; T¯ca) preserved by any CN message with
index n where α(n) = a. The optimization of Qa can be

performed with the sequential IB algorithm [7], also described

in Section IV. This algorithm requires the average joint probability mass function p(x¯a, ℓ¯ac ) = En|a{p(Xn=x¯a, Lcn=ℓ¯ac )}.
With the same behavior regarding tcn in (2), the threshold
quantizer Qa can be moved before the CN update, reducing

complexity of the min operation.

III. DESIGN WITH MEMORY-ASSISTED RECONSTRUCTION
Lowering the bit width w of the exchanged messages simplifies decoder complexity for reconstruction, quantization, shifting networks and the min-sum CN update. However, very coarse quantization w<4 can significantly degrade the decoding performance [10]. This section proposes to overcome most of the degradation by preserving the previous iteration’s CN message tcn as snc to serve as side information in the VN reconstruction, as shown in Fig. 1.

The message scn conveys information w.r.t. xn such that I(Xn; Scn)>0. The optimization of the quantizer shall optimize preservation of additional mutual information I(Xn; Tcn|Scn). The variable node update now takes into account scn as

ℓnv = ϕcchol(n)(tcchol(n)) +

ϕcα(m)(tcm, scm).

(3)

m∈n˜

For

the

design

of

Qa

:

L¯ca

→

¯

c a

in

(2)

we

measure

the

joint distribution p(x¯a, ℓ¯ca, s¯ca) which considers correlations

between tnc and snc . Therefore, we generate a large set of

decoding messages xn, ℓcn, snc for a specific design-Eb/N0.

In the next Section IV we introduce a side-information-

aware IB algorithm used for optimization of the quantization thresholds. We define X¯a, L¯ac , T¯ca, and S¯ac as the rele-
vant, observed, compressed and side-information variable,

X, Y, T, and S, respectively. The optimization aims for

maxp(t|y) I(X; T|S). We remark that the alphabet Y is only

approximately LLR-sorted as defined in (5) as a result of the

min-sum approximation.

IV. INFORMATION BOTTLENECK ALGORITHM WITH SIDE-INFORMATION AWARENESS
Typically, an IB setup is defined by a relevant, observed and compressed random discrete variable X, x∈X = {0, 1}, Y, y∈Y={1, . . . , |Y|} and T, t∈ = {1, . . . , | |}, which form a Markov chain X→Y→T. The IB method is a clustering framework for designing compression operations p(t|y) with optimization objective maxp(t|y) I(X; T) − β−1 I(Y; T). The choice of β > 0 allows to trade preservation of relevant information I(X; T) for compression. Of very high practical interest is the case β→∞ because it can be achieved with a deterministic mapping through threshold quantization

t = Q(y) = [k] τk≤y<τk+1, 0≤k<| |

(4)

with outer thresholds τ0=0 and τ| | = |Y|, and [k] identifying the k-th element of the ordered set . The mapping with
thresholds can be information-optimum if [13]

L(x|y = 1) ≤ L(x|y = 2) ≤ . . . ≤ L(x|y = |Y|). (5)

This section extends the conventional setup with a fourth variable S which provides side-information about X [12]. Prior works [11], [12] also consider a setup with side information, however, those works do not explicitly provide a low-complexity solution for optimizing a threshold quantizer in the context of LDPC decoding. We propose Alg. 1 which is a modified variant of the sequential IB algorithm from [7] taking into account the side information S. The algorithm exploits (5) to sequentially optimize initial random boundaries τk defining the target clusters Yt = {y:τt≤y<τt+1}⊂Y. To avoid local optima, we run 500 different initializations in parallel. We enforce symmetric thresholds τk=| |−τ| |−k, reducing the number of design and implementation parameters.

A. Merging Costs With Side Information In line 14, the element y and counterpart element y′ are
moved into singleton clusters Y| |+1 and Y| |+2, respectively.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:21:02 UTC from IEEE Xplore. Restrictions apply.

890

IEEE COMMUNICATIONS LETTERS, VOL. 29, NO. 5, MAY 2025

Algorithm 1 Side-Information-Aware IB Alg. Extending [7] an AWGN channel with BPSK modulation. All decoders use

Input: p(x, y, s), | | Output: p(t|y), p(x, t, s)
1: Create random symmetric clustering p(t|y); 2: Compute

p(x, t, s) = p(t|y)p(x, y, s)

(6)

y

3: Extend clusters Y1, . . . , Y| | with empty singleton clusters Y| |+1 and Y| |+2;
4: repeat 5: Save p(t|y) as pold(t|y);

6: for t ∈ {1, . . . , | |/2 − 1} do

7: for (t1, t2) ∈ {(t, t + 1), (t + 1, t)} do

8:

repeat

9:

if t1 == t then

10:

y is rightmost element in cluster Yt1 ;

11:

else if t1 == t + 1 then

12:

y is leftmost element in cluster Yt1 ;

13:

end if

14:

Move y into Y| |+1, and y′ = |Y| + 1 − y into Y| |+2;

15:

Update p(x, t, s) with (6) where p(t|y) considers Ln. 14;

16:

Compute optimal cluster using (11):

k∗ = arg min Csym(y, k)

(7)

k∈{1,2}

the initialization schedule described in [10] to avoid useless CN updates resulting from processing punctured messages. If not mentioned otherwise, the remaining schedule follows the flooding scheme with a maximum of 30 decoder iterations. We emphasize that the decoders are designed in an offline-design phase using training data with 10000 transmitted and received code words generated for a design-Eb/N0 which minimizes frame error rate (FER). This means that the decoder does not adapt or modify itself dynamically during execution.
A. Evolution of Mutual Information Fig. 2 depicts the evolution of mutual information I(ι)(B; Bˆ)
between code bit B and the corresponding hard decision Bˆ for every iteration ι. The quantized messages are matrix-aligned (α(n) = 1) such that all messages use the same alphabet of reliability levels in one iteration. In Fig. 2 the design process is initialized with the same design-Eb/N0 for a fair comparison among the configured bit widths. Particularly under 2-bit decoding, the mutual information gains per iteration are

significantly improved with the proposed memory-assistance.

For 3-bit decoding those gains appear smaller. Nevertheless,

the proposed 3-bit decoding almost achieves the same perfor-

17:

Merge Y| |+1 into Ytk∗ , and Y| |+2 into Y| |+1−tk∗ ;

18:

Update p(x, t, s) and p(t|y);

19:

until tk == t1

20: end for

21: end for

22: until p(t|y) == pold(t|y)

23: return p(t|y), p(x, t, s)

mance as the conventional 4-bit decoding.
B. Boundary Placement for Memory-Assisted Reconstruction This section analyzes the placement of quantizer bound-
aries τk for every iteration to explain the performance gains achieved with the proposed structure. Now, all decoders use an individual design-Eb/N0 so that the mutual information converges to I(30)(B; Bˆ)≈1 − 10−5 after 30 iterations.

The temporary decompression is modeled with a discrete random variable T¨, t¨∈ ¨ = ∪ {| | + 1, | | + 2}. Line 16 optimizes the deterministic mapping p(t|t¨) = δ(t−fk(t¨)) with fk : ¨ → . The algorithm restricts merging y into an adjacent
cluster t1 or t2. Thus, two mapping options exist k∈{1, 2} with

 

t¨



fk(t¨) = tk

 

tk′

t¨∈ t¨ = | | + 1 t¨ = | | + 2

and tk′ = | | + 1 − tk. (8)

The mutual information loss from merging is

Csym(y, k) = I(X; T¨|S) − I(X; T|S)

(9)

= p(t¨, s) DKL(p(x|t¨, s)||p(x|fk(t¨), s)) (10)

t¨,s

= p(s)(C(y, tk|s) + C(y′, t′k|s))

(11)

s

where the individual merging costs are

C(y, t|s)=p(T¨=t|s) DKL{p(x|T¨=t, s)||p(x|T=t, s)} (12)
+p(Y=y|s) DKL{p(x|Y=y, s)||p(x|T=t, s)}

with DKL(p||q) = x p(x) log2(p(x)/q(x)).

V. PERFORMANCE AND COMPLEXITY WITH 5G CODES

Fig. 3 shows boundary levels which increase for higher iterations as the reliability of messages improves. One key observation is that boundary magnitudes for the proposed 2-bit decoder show up an alternating rising and falling trend. Thus, memory-assistance enhances the resolution by using different quantizers in successive iterations. This behavior shows the effectiveness of the side-information aware IB algorithm: A CN message t is a compressed version of the non-quantized LLR ℓnc in (2) from the current iteration. A CN message s is a compressed version of ℓnc from the previous iteration. The difference ∆ℓnc = ℓnc − ℓcn is sufficiently small on average, such that s can approximately resolve ℓcn. Different boundaries for Q and Q, help to reconstruct ℓnc from the two coarsely quantized messages s = Q(ℓnc ) and t = Q(ℓnc ). The combined resolution approaches the 3-bit quantizer in Fig. 3.
We remark that the side information s is only actively used during reconstruction as ϕ(t, s). For example, consider a sign-magnitude alphabet sorted by underlying LLR with t∈{−2, −1, +1, +2}. For t=+2 the reconstructed LLR ϕ(t=+2, s) is more reliable if the message in memory is matching, e.g., s=+2. It is less reliable if s does not agree, e.g., s=−2. Thus, ϕ(t=+2, s=+2)>ϕ(t=+2, s=−2). Hence, additional knowledge of s helps to counteract loss in resolution resulting from coarse quantization with t=Q(ℓcn).

This section investigates the performance and simplifies operations of the proposed memory-assisted decoders. C. Complexity Reduction of Memory-Assisted Decoding

As in [10] we use a 5G LDPC code with K=8448, base The memory-assisted decoder presented so far suffers from

graph 1 and various code rates [1]. Furthermore, we consider additional memory demand and the more expensive recon-

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:21:02 UTC from IEEE Xplore. Restrictions apply.

MOHR AND BAUCH: MEMORY-ASSISTED QUANTIZED LDPC DECODING

891

Fig. 4. The compression operations Υ and Ψ reduce the complexity for the VN’s reconstruction ϕ and memory to store side information sΨ.

Fig. 2. Evolution of mutual information with code rate 1/3. All results obtained with same design-Eb/N0 = 1.0 dB.

Fig. 3. Evolution of quantizer boundary placement each with code rate 1/3 and individual design-Eb/N0 = .55, .73 and 1.0 dB.

struction ϕ(tc, sc) shown in Fig. 1 which requires a lookup table with 22w−1 entries. This section proposes a modified
setup depicted in Fig. 4 with reduced complexity using a new reconstruction ϕ(tΥ) where the input is a message tΥ∈ wΥ with wΥ = w + 1. The message tΥ is obtained by combining side information sΨ∈ 2 = {−2, −1, +1, +2} and a CN message tc∈ w using Υ(s, t) = sgn(t)(2|t| + s′), where
s′=Υ′(sΨ, sgn(tc))= 1 sgn(tc)= sgn(sΨ), |sΨ|=2 (13) 0 otherwise .

The side information sΨ in the next iteration is a compressed version of tΥ where only the sign bit and the most significant
magnitude bit are preserved:

sΨ = Ψ(tΥ) = sgn(tΥ)⌊|tΥ|/2w−1⌋

(14)

We remark that the rules (13) and (14) were discovered by
closely inspecting lookup tables (LUTs) designed with the
IB method realizing Υ and Ψ. The rules approximate the
IB LUTs with high accuracy but much less complexity. Only
two logic gates (XOR, AND) are required to implement (13)
and no logic at all is required to implement (14). Moreover, using fixed rules allows the quantizer design for tc = Q(ℓc) to be aware of the subsequent mapping tΥ = Υ(tc, sΨ). The design objective becomes maxQ I(X; Tc|S′) solved with the side information aware IB algorithm using p(x, ℓc, s′) =
sΨ:s′=Υ′(sΨ,sgn(ℓc)) p(x, ℓc, sΨ).

D. Frame Error Rate Performance (Flooding Schedule)
The performance using a flooding schedule with different resolutions and code rates is depicted in Fig. 5. Unless stated otherwise, all quantized decoders apply CN-aware rowaligned quantization [10]. The label (w wΨ wΥ) specifies the bit widths: w for exchanged messages, wΨ for side information, and wΥ for reconstruction inputs. The conventional

decoders, labeled (w 0 w), are designed with analytical tracking of probabilities as in [10]. The proposed ones from Section V-C labeled (w w 2w) and (w 2 (w + 2)), respectively, are designed with measured probabilities from training data. At rate 1/3, the proposed (2 2 4) memory-assisted decoder improves performance by.23 dB over the conventional (2 0 2) decoder, while the (3 3 6) decoder improves by.08 dB over the (3 0 3) decoder. Relative to a conventional (2 0 2) decoder with matrix alignment, no CN-aware quantization and no memoryassistance, the (2 2 4) decoder achieves a gain of.68 dB. The (3 3 6) memory-assisted decoder is able to slightly outperform a (4 0 4) decoder. The improvements also translate to other code rates 2/3 to 8/9. Remarkably, the (w 2 w + 2) decoders, using approximations (13) and (14), show up almost no performance loss.
E. Complexity Versus Performance (Layered Schedule)
Fig. 6 depicts the processing elements for a row-layered architecture from [10]. Each row i of the base-matrix corresponds to one of Nl(ary)ers=⌈22/r⌉−20 layers depending on rate r. Thus, Z rows of the lifted matrix are updated during each pair of steps (Ukv, Ukc+1) of the schedule. We limit the analysis to a partial-parallel processing of each layer with Zp=32, i.e., the hardware only processes Zp=32 of Z=384 rows in parallel to limit the chip area [6]. During Ukv, a VN update computes tiv = Qi(ℓvi ) with ℓiv = ℓiv−1 + ℓci−1 − ℓic,−. Lookup tables reconstruct the two LLRs ℓic−1 and ℓic,− from the CN message tΥi−1 of the last layer update and the non-extrinsic CN message from the last iteration tΥi ,−, respectively. Messages are preserved in static random-access memory (SRAM) between layer updates. A permutation network performs a cyclic shift of Zp=32 messages. A CN update step Ukc computes messages for all connected VNs according to (2). After an inverse permutation, a CN message tci is merged with the side information sΨi .
Table I evaluates space complexity A(gr) by accumulating the logic area to realize the decoder operations of Fig. 6, expressed in terms of NAND gate area as in [10]. For the time complexity we count the number of logic gates Nd on the critical path. The throughput per iteration is TP ∝1/(NdNl(ary)ers). The area efficiency is AE ∝ TP /(ιavgA(gr)), using the average number of iterations ιavg until all parity checks are fulfilled. We normalize results for TP and AE w.r.t. configuration (4 0 0) at rate 1/3. Each configuration results in same FER of 0.01 at the given operation-Eb/N0 by adjusting ιmax. At high rates, 8/9 to 11/12, the memory-assisted (2 2 3) setting can achieve the best area efficiency. However, at lower rates (e.g., 1/3), SRAM area occupies 61% of the overall area which makes the savings in the remaining processing logic of the decoder less significant. We remark that relative area for memory can be reduced by increasing parallelism Zp or using a flooding schedule.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:21:02 UTC from IEEE Xplore. Restrictions apply.

892

IEEE COMMUNICATIONS LETTERS, VOL. 29, NO. 5, MAY 2025

Fig. 5. FER performance of memory-assisted (solid lines) and non-assisted (dotted lines) setups for 5G LDPC decoding (flooding schedule, ιmax = 30).

Fig. 6. A row-parallel decoder architecture with exchange of VN and CN messages (R-VC) from [10] extended with memory assistance.
TABLE I COMPLEXITY USING THE R-VC ARCHITECTURE FROM [10]

on the quantization thresholds, whose design was improved with a new side-information-aware IB algorithm. Further, we proposed a simple rule to combine old and new messages to reduce memory and lookup complexity. Enabling memory-assistance for a 2-bit (3-bit) flooding decoder achieved SNR gains of up to .23 dB (.08 dB). Finally, we compared the space-time complexity for a row-layered decoder architecture at same FER performance by allowing different maximum iteration count among the configurations. Particularly at high code rates where the area used for memory is smaller, our estimates indicate up to a 32% increase in area efficiency for the 2-bit memory-assisted decoder.
Looking ahead, for rate-compatible decoding, a hybrid approach with memory-assisted high-rate and non-memoryassisted low-rate decoding could be beneficial. In such a setup, high-rate decoding can potentially utilize the additional memory for low-rate decoding to store side information.

REFERENCES

[1] 5G NR: Multiplexing and Channel Coding, document TS 38.212, 3GPP,

2018. [2] R. G. Gallager, “Low-density parity-check codes,” IRE Trans. Inf.

Theory, vol. 8, no. 1, pp. 21–28, Jan. 1962.

[3] J. Chen et al., “Reduced-complexity decoding of LDPC codes,” IEEE

Trans. Commun., vol. 53, no. 8, pp. 1288–1299, Aug. 2005.

[4] P. Kang et al., “Generalized mutual information-maximizing quantized

decoding of LDPC codes with layered scheduling,” IEEE Trans. Veh.

Technol., vol. 71, no. 7, pp. 7258–7273, Jul. 2022.

[5] L. Wang et al., “Reconstruction-computation-quantization (RCQ):

A paradigm for low bit width LDPC decoding,” IEEE Trans. Commun.,

vol. 70, no. 4, pp. 2213–2226, Apr. 2022.

[6] Y. Ren et al., “A generalized adjusted min-sum decoder for 5G LDPC

codes: Algorithm and implementation,” IEEE Trans. Circuits Syst. I,

Reg. Papers, vol. 71, no. 6, pp. 2911–2924, Jun. 2024.

[7] J. Lewandowsky and G. Bauch, “Information-optimum LDPC decoders

based on the information bottleneck method,” IEEE Access, vol. 6,

pp. 4054–4071, Jan. 2018.

[8] M. Stark et al., “Decoding rate-compatible 5G-LDPC codes with coarse

quantization using the information bottleneck method,” IEEE Open J.

Commun. Soc., vol. 1, pp. 646–660, May 2020.

[9] P. Mohr and G. Bauch, “A variable node design with check node aware

quantization leveraging 2-bit LDPC decoding,” in Proc. IEEE Global

Commun. Conf. (GLOBECOM), Dec. 2022, pp. 3484–3489.

[10] P. Mohr and G. Bauch, “Region-specific coarse quantization with

check node awareness in 5G LDPC decoding,” IEEE Trans. Commun.,

Jan. 2025. [Online]. Available: https://ieeexplore.ieee.org/document/

10879552

[11] G. Chechik and N. Tishby, “Extracting relevant structures with side

information,” in Proc. Adv. Neural Inf. Process. Sys. Cambridge, MA,

USA: MIT Press, Jan. 2002, pp. 881–888.

VI. CONCLUSION

[12] S. Steiner and V. Kuehn, “Distributed compression using the information bottleneck principle,” in Proc. IEEE Int. Conf. Commun. (ICC),

This letter enhanced the reconstruction of coarsely quan-

Jun. 2021, pp. 1–6.

tized CN messages by utilizing messages from the previous

[13] B. M. Kurkoski and H. Yagi, “Quantization of binary-input discrete memoryless channels,” IEEE Trans. Inf. Theory, vol. 60, no. 8,

iteration as side-information. The reconstruction levels depend

pp. 4544–4552, Aug. 2014.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:21:02 UTC from IEEE Xplore. Restrictions apply.

