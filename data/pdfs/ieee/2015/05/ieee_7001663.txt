IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 63, NO. 5, MAY 2015

1497

Density Evolution and Functional Threshold for the Noisy Min-Sum Decoder
Christiane Kameni Ngassa, Valentin Savin, Elsa Dupraz, and David Declercq, Senior Member, IEEE

Abstract—This paper investigates the behavior of the Min-Sum decoder running on noisy devices. Our aim is to evaluate the robustness of the decoder to computation noise caused by the faulty logic in the processing units. This type of noise represents a new source of errors that may occur during the decoding process. To this end, we first introduce probabilistic models for the arithmetic and logic units of the finite-precision min-sum decoder and then carry out the density evolution analysis of the noisy min-sum decoder. We show that, in some particular cases, the noise introduced by the device can help the min-sum decoder to escape from fixed points attractors and may actually result in an increased correction capacity with respect to the noiseless decoder. We also point out a specific threshold phenomenon, referred to as functional threshold, which accurately describes the convergence behavior of noisy decoders. The behavior of the noisy MS is demonstrated in the asymptotic limit of the code length through a noisy version of density evolution and is also verified in the finite-length case by Monte Carlo simulations.
Index Terms—LDPC codes, transient errors, noisy density evolution.
I. INTRODUCTION
I N traditional models of communication or storage systems with error correction coding, it is assumed that the operations of an error correction encoder and decoder are deterministic and that randomness exists only in the transmission or storage channel. However, with the advent of nanoelectronics, the reliability of forthcoming circuits and computation devices is becoming questionable. It is then becoming crucial to design and
Manuscript received May 27, 2014; revised October 6, 2014 and December 20, 2014; accepted December 23, 2014. Date of publication January 6, 2015; date of current version May 14, 2015. This work was supported by the European Union’s Seventh Framework Program FP7/2007-2013 under Grant 309129 (i-RISC project). Part of this work has been presented at Asilomar Conference on Signals, Systems and Computers; IEEE Global Communications Conference; and IEEE Information Theory and Applications Workshop. The associate editor coordinating the review of this paper and approving it for publication was L. Dolecek.
C. Kameni Ngassa was with the Laboratoire d’électronique des technologies de l’information (CEA-LETI), 38054 Grenoble, France, and also with ETIS, ENSEA/University of Cergy-Pontoise/CNRS UMR-8051, 95000 CergyPontoise, France. She is now with Thales Communications and Security, 92230 Gennevilliers, France (e-mail: christiane.kameni@thalesgroup.com).
V. Savin is with the Laboratoire d’électronique des technologies de l’information (CEA-LETI), 38054 Grenoble, France (e-mail: valentin.savin@ cea.fr).
E. Dupraz and D. Declercq are with ETIS, ENSEA/University of CergyPontoise/CNRS UMR-8051, 95000 Cergy-Pontoise, France (e-mail: elsa. dupraz@ensea.fr; declercq@ensea.fr).
Color versions of one or more of the figures in this paper are available online at http://ieeexplore.ieee.org.
Digital Object Identifier 10.1109/TCOMM.2015.2388472

analyze error correcting decoders able to provide reliable error correction, even if they are made of unreliable components.
Over the past years, the study of error correcting decoders, especially Low-Density Parity-Check (LDPC) decoders, running on noisy hardware attracted more and more interest in the coding community. In [1], [2], analytical methods have been proposed to evaluate the performance of one step majority logic LDPC decoders constructed from faulty gates. In [3], the concentration and convergence properties were proved for the asymptotic performance of noisy message-passing decoders, and density evolution equations were derived for the noisy Gallager-A and Belief-Propagation (BP) decoders. In [4]–[8], the authors investigated the asymptotic behavior of the noisy Gallager-B decoder defined over binary and non-binary alphabets. Finite Alphabet Iterative Decoders (FAIDs) under processing errors have also been investigated in [9], [10]. Recently, the Min-Sum decoding under unreliable message storage has been investigated in [11], [12]. In all these papers the noisy implementation of the iterative message passing decoders was emulated by passing each of the exchanged messages through a noisy channel.
In this paper, we investigate the asymptotic and finite-length behavior of the noisy MS decoder. We refine the probabilistic error models for the noisy MS decoder we used previously in [13], [14], and discuss their symmetry properties. We derive density evolution equations and conduct a thorough asymptotic analysis of the noisy MS decoder. We also identify and illustrate non-conventional behaviors of noisy iterative decoders and propose a novel noisy threshold definition, which is referred to as functional threshold. Finally, the asymptotic results are also corroborated through finite length simulations.
The remainder of the paper is organized as follows. Section II discusses the probabilistic error models for the noisy arithmetic operators and Section III introduces the noisy MS decoder. Section IV shortly discusses the density evolution for the noisy MS decoder, and provides the notation and definitions required to understand the asymptotic analysis of the noisy MS decoder, which is then conducted in Section V. Finally, Section VI illustrates the asymptotic analysis through finite-length simulations, and Section VII concludes the paper.
II. PROBABILISTIC ERROR MODELS FOR THE NOISY ARITHMETIC OPERATORS
A. Noisy Message-Passing Decoders
The model for noisy message-passing (MP) decoders proposed in [3] incorporates two different sources of noise: computation noise due to noisy logic in the processing units, and

0090-6778 © 2015 IEEE. Personal use is permitted, but republication/redistribution requires IEEE permission. Authorized licenSseeedhuttspe:/l/imwiwtewd.itoee: eY.oonrgs/epiuUbnliivc.atDioonwsn_lsotaadneddarodns/Jpuunbeli2ca4t,i2o0n2s6/raigt h1t7s:/0in2d:5e2x.UhtTmCl ffroormmIEoEreEinXfpolromrea.tiRone.strictions apply.

1498

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 63, NO. 5, MAY 2015

message-passing noise due to noisy wires (or noisy memories) used to exchange messages between nodes. The computation noise is modeled as a random variable, which describes the variable node or the check node probabilistic processing. The message-passing noise is simply modeled as a noisy channel. Hence, transmitting a message over a noisy wire is emulated by passing that message through the corresponding noisy channel.
However, in [3] it has been noted that “there is no essential loss of generality by combining computation noise and message-passing noise into a single form of noise.” Consequently, the approach adopted has been to merge computation noise into message-passing noise, and to emulate noisy decoders by passing the exchanged messages through different noisy channel models. As examples, the noisy Gallager-A decoder has been emulated by passing the exchanged messages over independent and identical BSC wires, while the noisy BP decoder has been emulated by corrupting the exchanged messages with bounded and symmetrically distributed additive noise (e.g., uniform noise or truncated Gaussian noise).
The approach we follow in this work differs from the one in [3] in that the computation noise is modeled at the lower level of arithmetic and logic operations that compose the variable node and check node processing units. This finer grained noise modeling is aimed at determining the level of noise that can be tolerated in each type of operation. As the main focus of this work is on computation noise, we shall consider that messages are exchanged between neighbor nodes through error-free wires (or memories). However, we note that our derivations can be extended to include different error models for the messagepassing noise (as defined in [3]). Alternatively, we may assume that the message-passing noise is merged into the computation noise, in the sense that adding noise in wires would modify the probabilistic model of the noisy logic or arithmetic operations.
Finally, we note that we limit our density evolution analysis to the case of symmetric decoding functions without memory. Since noise is part of the decoder, this limitation imposes similar constraints on the transient error models considered in this paper, which have to be symmetric and without memory. While we do not expect such constraints to be satisfied by realistic error models, the theoretical analysis conducted in this paper is aimed at understanding the limits of iterative MS decoding under faulty hardware, thus providing boundary conditions on the allowed level of hardware-induced errors.

error injection function must satisfy the following symmetry condition:

∑ pE(e) = ∑ pE(e),

{e|ı(v,e)=w}

{e|ı(−v,e)=−w}

∀ v, w ∈ V. (1)

A particular case in which the symmetry condition is fulfilled

is when ı(−v, e) = −ı(v, e), for all v ∈ V and e ∈ E. In this case,

the error injection model is said to be highly symmetric.

Two error injection functions will be used in this work, both

of which are based on a bitwise XOR operation between the

noiseless output v and the error e. The two functions differ

mainly in the definition of the error set E, which is chosen such

that the bitwise XOR operation may or may not affect the sign of

the noiseless output. In the first case the error injection function

is said to be full-depth, while in the second it is said to be sign-

preserving.

We fix a signed number binary representation, which can be

any of the sign-magnitude, one’s complement, or two’s complement representation. There are exactly 2θ signed numbers

that can be represented by θ bits in any of the above formats,

one of which does not belong to V (note that V contains only 2Θ + 1 = 2θ − 1 elements for symmetry reasons). We denote

this element by ζ. For instance, in two’s complement format,

ζ = −(Θ + 1), with binary representation 10 · · · 0.

Full-Depth Error Injection: For this error model the error set

is E = V. For symmetry reasons, all errors e = 0 are assumed to

occur with the same probability. It follows that pE(0) = 1 − pa

and

pE(e) =

pa 2Θ

,

∀e =

0,

where

pa

>

0

is

referred

to

as

the

error injection probability. Finally, the error injection function

is defined by:

ı(v, e) =

v ⊕ e, e,

if v ⊕ e ∈ V if v ⊕ e = ζ

(2)

where ⊕ denotes the bitwise XOR operation. In can be seen that

this error model is tantamount to passing v through a |V|-ary

symmetric channel, where |V| is the number of elements of V.

Sign-Preserving Error Injection: For this error model the

error set is E = {0, +1, . . . , +Θ}. The error injection probability

is denoted by pa, and all errors e = 0 are assumed to occur

with the same probability (for symmetry reasons). It follows

that

pE(0) = 1 − pa

and

pE(e) =

pa Θ

,

∀e =

0.

Finally,

the

error

injection function is defined by:

B. Error Models for Noisy Adders

v ⊕ e, if v = 0 and v ⊕ e ∈ V

We consider a θ-bit adder (θ ≥ 2). The inputs and the
output of the adder are assumed to belong to V = {−Θ, . . . , −1, 0, +1, . . . , +Θ}, where Θ = 2θ−1 − 1. For inputs (x, y) ∈ V, the
output of the noiseless θ-bit adder is given by v = sV(x + y), where sV : Z → V denotes the θ-bit saturation map, defined by sV(z) = sgn(z) · min(|z|, Θ).
The output of the noisy adder vpr = ı(v, e) is defined by
injecting errors in the output of the noiseless one. The error e is

ı(v, e) = ±e, if v = 0

(3)

0, if v ⊕ e = ζ.

In the above definition, ı(0, e) is randomly set to either −e or +e, with equal probability. Note also that the last two conditions, namely v = 0 and v ∧ e = ζ, cannot hold simultaneously (since e = ζ).
We note that both of the above models are highly symmetric,

assumed to be drawn from an error set E ⊆ V, according to an if one of the sign-magnitude or the one’s complement rep-

error probability distribution pE : E → [0, 1], and ı : V × E → resentation is used. In the case where the two’s complement V is an error injection function that will be defined shortly. representation is used, they are both symmetric, but not highly

To ensure the symmetry of the decoder (see Section IV), the symmetric.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

KAMENI NGASSA et al.: DENSITY EVOLUTION AND FUNCTIONAL THRESHOLD FOR THE NOISY MIN-SUM DECODER

1499

Finally, for any of the above error injection models, the output of the noisy adder is given by:

apr(x, y) = ı (sV(x + y), e) ,

(4)

where e is drawn randomly from E according to the probability distribution pE. The error probability of the noisy adder, i.e., Pr apr(x, y) = sV(x + y) , assuming uniformly distributed inputs, equals the error injection probability parameter pa.
Remark: It is also possible to define a variable depth error injection model, in which errors are injected in only the λ least significant bits, with λ ≤ θ [13]. Hence, λ = θ corresponds to the above full-depth model, while λ = θ − 1 corresponds to the sign-preserving model. However, for the two’s complement representation, such a model is not symmetric if λ < θ − 1.
Note that it might be possible to define more general error injection models, in which the injected error depends on the data (currently and/or previously) processed by the adder. Such an error injection model would certainly be more realistic, but it would also make it very difficult to analytically characterize the behavior on noisy MP decoders. As a side effect, the decoding error probability would be dependent on the transmitted codeword, which would prevent the use of the density evolution technique for the analysis of the asymptotic decoding performance.

C. Error Models for Noisy Comparators and Noisy XOR Operators

B. Noisy Min-Sum Decoding
We consider a finite-precision MS decoder, in which the a priori information γn and the exchanged extrinsic messages, αm,n and βm,n, are quantized on q bits. The a posteriori information γ˜n is quantized on q˜ bits, with q˜ > q (usually q˜ = q + 1, or q˜ = q + 2). We also denote by M the alphabet of the a priori information and of the extrinsic messages, and by M the alphabet of the a posteriori information. Thus:
• M = {−Q, . . . , −1, 0, +1, . . . , Q}, where Q = 2q−1 − 1; • M = {−Q, . . . , −1, 0, +1, . . . , Q}, where Q = 2q˜−1 − 1.
We further consider a function q : Y → M that maps the output alphabet of the channel, denoted by Y, to the input alphabet of the decoder. Hence, q encompasses both the computation of the soft unquantized log-likelihood ratio (LLR) value and its quantization. By a slight abuse of terminology, we shall refer to q as quantization map.
The noisy finite-precision MS decoder is presented in Algorithm 1. We assume that q˜-bit adders are used to compute both αm,n messages in the VN-processing step, and γ˜n values in the AP-update processing step. This is usually the case in practical implementations, and allows us to use the same type of adder in both processing steps. This assumption explains as well the q-bit saturation of αm,n messages in the VN-processing step. Note also that the saturation of γ˜n values is actually done within the adder (see Equation (4)).

The noisy minimum operator with error probability pc, denoted by mpr, is defined as follows:

mpr(x, y) =

min(x, y), max(x, y),

with probability 1 − pc with probability pc.

(5)

The noisy XOR operator, denoted by xpr, is defined by flipping the output of the noiseless operator with some probability value, which will be denoted in the sequel by px. It follows that:

xpr(x, y) =

x ⊕ y, x ⊕ y,

with probability 1 − px with probability px.

(6)

Assumption: We further assume that the inputs and the output of the XOR operator may take values in either {0, 1} or {+1, −1} (with the usual 0 ↔ +1 and 1 ↔ −1 conversion). This assumption will be implicitly made throughout the paper.

III. NOISY MIN-SUM DECODER

A. Notation

We consider an LDPC code defined by a Tanner graph H,

with N variable nodes denoted by n ∈ {1, 2, . . . , N}, and M Finally, we note that the hard decision and the syndrome

check-nodes denoted by m ∈ {1, 2, . . . , M}. The set of nodes check steps in Algorithm 1 are assumed to be noiseless. We

connected to a variable node n (resp. a check node m) will note however that the syndrome check step is optional, and

be denoted by H(n) (resp. H(m)). For a message passing if missing, the decoder stops when the maximum number of

decoder, we denote by γn and γ˜n the a priori and the a posteriori iterations is reached.

information of the variable node n (i.e., decoder’s input and output values), and by αm,n and βm,n the variable-to-check

C. Sign-Preserving Properties

message sent from n to m and the check-to-variable message Let U denote any of the VN-processing or CN-processing

sent from m to n, respectively.

units of the noiseless MS decoder. We denote by Upr the

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

1500

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 63, NO. 5, MAY 2015

corresponding unit of the noisy MS decoder. We say that Upr is sign-preserving if for any incoming messages and any noise realization, the outgoing message is of the same sign as the message obtained when the same incoming messages are supplied to U.
Clearly, CNpr is sign-preserving if and only if the XORoperator is noiseless (px = 0). In case that the noisy XORoperator severely degrades the decoder performance, it is possible to increase its reliability by using classical faulttolerant techniques. The price to pay, when compared to the size or the energy consumption of the whole circuit, would be reasonable.
Concerning the VN-processing, it is worth noting that the VNpr is not sign-preserving, even if the noisy adder is so. This is due to the fact that multiple adders must be nested in order to complete the VN-processing (see footnote remark in Algorithm 1). The motivation behind the sign-preserving noisy

3) [Concentration Around the Cycle-Free Case] For any δ > 0, the probability that EC( ) lies outside the interval E∞( )(x) − δ, E∞( )(x) + δ converges to zero exponen-
tially fast in N.
Sketch of Proof: Let ı : V × E → V by any of the full-depth
or sign-preserving error injection models used the define the noisy adder. For any random variable V defined on V, let φV(ı) and φ(−ı)V denote the probability mass functions of the random variables obtained by injecting errors on the output of V
and −V , respectively. From Equation (1) it follows easily that:

φV(ı)(w) = φ(−ı)V (−w), ∀ w ∈ V.

(7)

Finally, using Equation (7), the proof of Theorem 1 can be derived using essentially the same arguments as in [3, Theorems 1–3].

adder model is to investigate its possible benefits on the decoder

performance. If the benefits are worth it (e.g., one can ensure a target performance of the decoder), the sign-bit of the adder could be protected by using classical fault-tolerant techniques.

B. Density Evolution Equations
The density evolution technique allows recursively computing the probability mass functions of the exchanged extrinsic

IV. DENSITY EVOLUTION A. Concentration and Convergence Properties

messages (αm,n and βm,n) and of the a posteriori information (γ˜n), through the iterative decoding process. This is done under the independence assumption of exchanged messages, holding
in the asymptotic limit of the code length, in which case the

First, we note that our definition of symmetry is slightly decoding performance converges to the cycle-free case. Due

more general than the one used in [3]. Indeed, even if the to the symmetry of the decoder, the analysis can be further

error injection models satisfy the symmetry condition from simplified by assuming that the all-zero codeword is transmitted

Equation (1), the noisy MS decoder does not necessarily verify through the channel.

the symmetry property from [3]. Nevertheless, the concentra- The density evolution equations for the noisy MS decoder

tion and convergence properties proved in [3] for symmetric were included in Appendix A. Here, we only provide the

noisy message-passing decoders, can easily be generalized to notation and definitions required to understand the asymptotic

our definition of symmetry (1).

analysis of the noisy MS decoder conducted in Section V.

We summarize below the most important results. We con-

sider an ensemble of LDPC codes, with length N and fixed degree distribution polynomials [15]. We choose a random code

C. Decoding Error Probability

C from this ensemble and assume that a random codeword x ∈ {−1, +1}N is sent over a binary-input memoryless symmetric
channel. We fix some number of decoding iterations > 0, and denote by EC( )(x) the expected fraction of incorrect messages1 at iteration .

The error probability at decoding iteration ≥ 0, is defined as:

∑ Pe( ) =

−1

C(

)(z˜) +

C(

)(0) ,

2

(8)

z˜=−Q

Theorem 1: For the noisy MS decoder from Algorithm 1, the
following properties hold:
1) [Conditional Independence of Error] For any decoding
iteration > 0, the expected fraction of incorrect messages EC( )(x) does not depend on x. Therefore, we define EC( ) := EC( )(x). 2) [Cycle-Free Case] If the graph of C contains no cycles of length 2 or less, EC( ) does not depend on the code C or the code-length N, but only on the degree distribution
polynomials; in this case, it will be further denoted by E∞( )(x).

where C( )(z˜) := Pr(γ˜( ) = z˜) is the probability mass function of

a posteriori information at decoding iteration . Hence, in the

asymptotic limit of the code-length, Pe( ) gives the probability

of the hard bit estimates being in error at decoding iteration .

The following lower bounds can be derived from the proba-

bility of error injection at the last of the nested adders used to

compute the a posteriori information value in Algorithm 1.

Proposition 1: The error probability at decoding iteration

is lower-bounded as follows:

(a)

For

the

sign-preserving

noisy

adder:

Pe(

)

≥

1 2Q

pa.

(b)

For the full-depth noisy adder: Pe(

)≥

1 2

pa

+

1 4Q

pa.

Noiseless decoders exhibit a threshold phenomenon, sepa-

1Here, messages may have any one of the three following meanings: “variable-node messages,” or “check-node messages,” or “a posteriori information values.”

rating the region where the decoding error probability goes to zero (as the number of decoding iterations goes to infinity), from that where it is bounded above zero [15]. This definition

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

KAMENI NGASSA et al.: DENSITY EVOLUTION AND FUNCTIONAL THRESHOLD FOR THE NOISY MIN-SUM DECODER

1501

does not hold anymore in the case of noisy decoders. First, the decoding error probability is always bounded above zero if pa > 0 (see Proposition 1), because of the noise in the a posteriori computation. Second, the decoding error probability has a more unpredictable behavior. In particular, it does not always converge when the number of iterations goes to infinity (see discussion in Section V). Following [3], we define in the next section the notions of useful decoder and target error rate threshold.

value. The threshold definition we propose make use of the Lipschitz constant of the function χ → Pe(∞)(χ) in order to detect a sharp change of Pe(∞)(χ) with respect to χ. The definition of
the Lipschitz constant is first restated for the sake of clarity.
Definition 1: Let f : I → R be a function defined on an interval I ⊆ R. The Lipschitz constant of f in I is defined as

L(

f , I)

=

sup
x=y∈I

|

f

(x) |x

− −

f (y)| y|

∈

R+

∪

{+∞}.

(11)

D. Useful Region and Target Error Rate Threshold

We consider a channel model depending on a channel pa-

rameter χ, such that the channel is degraded by increasing χ

(for example, the crossover probability for the BSC, or the noise

variance for the BI-AWGN channel). In order to account for the
fact that Pe( ) depends also on the value of the channel parameter, it will be denoted in the following by Pe( )(χ). Furthermore, if the following limit exists, we denote Pe(∞)(χ) = lim Pe( )(χ).
→∞
1) Useful Region: The useful region is defined as the set

of channel and hardware parameters yielding an asymptotic

probability of error less than the input error probability. The

latter

probability

is

given

by

Pe(0)(χ)

=

∑−z=1−Q C(z) +

1 2

C(0),

where C is the probability mass function of the quantized

a priori information of the decoder (γ = q(y), see Algorithm 1).

Then the decoder is said to be useful if Pe(∞)(χ) exists and

Pe(∞)(χ) < Pe(0)(χ).

(9)

For a ∈ I and δ > 0, let Ia(δ) = I ∩ (a − δ, a + δ). The (local) Lipschitz constant of f in a ∈ I is defined by:

L( f , a) = inf L ( f , Ia(δ)) ∈ R+ ∪ {+∞}.

(12)

δ>0

Note that if a is a discontinuity point of f , then L( f , a) = +∞. On the contrary, if f is differentiable in a, then the Lipschitz constant in a corresponds to the absolute value of the derivative. Furthermore, if L( f , I) < +∞, then f is uniformly continuous on I and almost everywhere differentiable. In this case, f is said to be Lipschitz continuous on I.
The functional threshold is then defined as follows. Definition 2: For given hardware parameters and a channel parameter χ, the decoder is said to be functional if (a) The function x → Pe(∞)(x) is defined on [0, χ] (b) Pe(∞) is Lipschitz continuous on [0, χ] (c) L Pe(∞), x is an increasing function of x ∈ [0, χ].
Then, the functional threshold χ¯ is defined as:

The ensemble of parameters that satisfy this condition consti- χ¯ = sup {χ|conditions (a), (b) and (c) are satisfied} . (13)

tutes the useful region of the decoder. The useful region indicates what are the faulty hardware conditions and the maximum channel noise such that the decoder can reduce the bit error probability (even though it does not indicate how much the bit error probability can be reduced).

The use of the Lipschitz constant allows a rigorous defi-
nition of the functional threshold, while avoiding the use of the derivative (which would require Pe(∞)(λ) to be a piecewise differentiable function of λ). As it will be further illustrated in

2) Target Error Rate Threshold: For a target error proba- Section V, the functional threshold corresponds to a transition bility η, the η-threshold is defined as the maximum channel between two modes. The first mode corresponds to the channel

noise such that the decoder can reduce the bit error probability parameters leading to a low level of error probability, i.e., for

below η:

which the decoder can correct most of the errors from the

χ∗(η) = sup χ|Pe(∞)(χ) exists and Pe∞(χ) < η .

channel. In the second mode, the channel parameters lead to a (10) much higher error probability level. If L Pe(∞), χ¯ = +∞, then

E. Functional Threshold
Although the η-threshold definition allows determining the maximum channel noise for which the bit error probability can be reduced below a target value, there is no significant change in the behavior of the decoder when the channel noise parameter χ increases beyond the value of χ∗(η). In this section, a new threshold definition is introduced in order to identify the channel and hardware parameters yielding to a sharp change in the decoder behavior, similar to the change that occurs around the threshold of the noiseless decoder. This threshold will be

χ¯ is a discontinuity point of Pe(∞) and the transition between the two levels is sharp. If L Pe(∞), χ¯ < +∞, then χ¯ is an
inflection point of Pe(∞) and the transition is smooth. With the Lipschitz constant, one can characterize the transition in both cases. However, the second case corresponds to a degenerated one, in which the hardware noise is too high and leads to a nonstandard asymptotic behavior of the decoder. That is why a set of admissible hardware noise parameters is defined as follows.
Definition 3: The set of admissible hardware parameters is the set of hardware noise parameters (pa, pc, px) for which L Pe(∞), χ¯ = +∞.

referred to as the functional threshold. The aim is to detect a In the following, as each threshold definition helps at illus-

sharp increase (e.g., discontinuity) in the error probability of the trating different effects, one or the other definition will be used, noisy decoder, when λ goes beyond this functional threshold depending on the context.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

1502

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 63, NO. 5, MAY 2015

V. ASYMPTOTIC ANALYSIS OF THE NOISY MIN-SUM DECODER

We consider the ensemble of regular LDPC codes with variable-node degree dv = 3 and check-node degree dc = 6. The following parameters will be used throughout this section with regard to the finite precision MS decoder:
• The a priori information and extrinsic messages are quantized on q = 4 bits; hence, Q = 7 and M = {−7, . . . , +7}.
• The a posteriori information is quantized on q˜ = 5 bits; hence, Q = 15 and M = {−15, . . . , +15}.
We restrict our analysis to the BSC channel with crossover probability p0, and further assume that the channel input and output alphabet is Y = {−1, +1}. For each µ ∈ {1, . . . , Q} we define the quantization map qµ : Y → M by:

qµ(−1) = −µ and qµ(+1) = +µ.

(14)

Thus, the a priori information of the decoder γn ∈ {±µ}. The parameter µ will be referred to as the channel-output scale factor, or simply the channel scale factor.
The infinite-precision MS decoder is known to be independent of the scale factor µ. This is because µ factors out from all the processing steps of the decoding algorithm, and therefore does not affect in any way the decoding process. This is no longer true for the finite precision decoder (due to saturation effects), and we will show shortly that even in the noiseless case, the scale factor µ may significantly impact the performance of the finite precision MS decoder.
We start by analyzing the performance of the MS decoder with channel scale factor µ = 1, and then we will analyze its performance with an optimized value of µ.

Fig. 1. Effect of the noisy adder on the asymptotic performance of the MS decoder (p0 = 0.06).

A. Unconventional Behavior of the Min-Sum Decoder (µ = 1)

The case µ = 1 leads to an unconventional behavior, as in

some particular cases the noise introduced by the device can

help the MS decoder to escape from fixed points attractors, Fig. 2. Useful and non-convergence regions of the MS decoder with signand may actually result in an increased correction capacity with preserving noisy adder.

respect to the noiseless decoder. This behavior will be discussed
in more details in this section.
In noiseless decoder case, the decoder exhibits a classical
threshold phenomenon: there exists a threshold value pth, such that Pe(∞) = 0 for any p0 < pth. This threshold value, which can be computed by density evolution, is pth = 0.039. Now, we consider a p0 value above the threshold of the noiseless decoder, and investigate the effect of the noisy adder on the decoder
performance. Let us fix p0 = 0.06. Fig. 1 shows the decoding error probability at iteration , for different error probability parameters pa ∈ {10−30, 10−15, 10−5} of the noisy adder. For each pa value, there are two superimposed curves, corresponding to the full-depth (“fd,” solid curve) and sign-preserving
(“sp,” dashed curve) error models of the noisy adder. The error
probability of the noiseless decoder is also plotted (solid black
curve): it can be seen that it increases rapidly from the initial value Pe(0) = p0 and closely approaches the limit value Pe(∞) = 0.323 after a few number of iterations. When the adder is noisy,

the limit value from the noiseless case, but starts decreasing after some number of decoding iterations. However, note that it remains bounded above zero (although not apparent in the figure), according to the lower bounds from Proposition 1, and it can actually be numerically verified that these bounds are nearly tight.
The above behavior of the MS decoder can be explained by examining the evolution of the probability mass function of the a posteriori information, denoted by C( ) (see Appendix A), for
≥ 0. In the noiseless case, we observed by simulation that C( ) reaches a fixed point of the density evolution for ≈ 20. In the noisy case, C( ) has a similar evolution as in the noiseless case during the first iterations. However, for > 20, the noise present in the adder results in a progressive perturbation of C( ), which eventually allows the decoder to escape from the fixed point attractor.
We focus now on the useful region of the noisy MS decoder. We assume that only the adder is noisy, while the comparator

the error probability increases during the first few decoding iter- and the XOR-operator are noiseless. The useful region for the

ations, behaving similarly to the noiseless case. It may approach sign-preserving noisy adder model is shown in Fig. 2. The

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

KAMENI NGASSA et al.: DENSITY EVOLUTION AND FUNCTIONAL THRESHOLD FOR THE NOISY MIN-SUM DECODER

1503

Fig. 3. Threshold values of noiseless and noisy MS decoders with various channel scale factors.

Fig. 4. Regions of the MS decoder with sign-preserving noisy adder.

useful region is shaded in gray and delimited by either a solid
black curve or a dashed red curve. Although one would expect that Pe(∞) = p0 on the border of the useful region, this equality only holds on the solid black border. On the dashed red border, one has Pe(∞) < p0. The reason why the useful region does not extend beyond the dashed red border is that for points located on the other side of this border the sequence (Pe( )) >0 is periodic, and hence it does not converge. The region shaded in
brown in Fig. 2 is the non-convergence region of the decoder.

B. Optimization of the Channel Scale Factor

In this section we show that the decoder performance can

be significantly improved by using an appropriate choice of

the channel scale factor µ. Fig. 3 shows the threshold values Fig. 5. Regions of the MS decoder with full-depth noisy adder. for the noiseless and several noisy decoders with channel scale

factors µ ∈ {1, 2, . . . , 7}. For the noisy decoders, the threshold within the VN-processing step are noisy (pa > 0), while the values are computed for a target error probability η = 10−5 (see CN-processing step is noiseless (px = pc = 0). These regions

Equation (10)).

are represented in Figs. 4 and 5, for the sign-preserving and the

The corresponding threshold values are equal to those ob- full-depth noisy adder models, respectively.

tained in the noiseless case for µ ∈ {2, 4, 6}. For µ ∈ {1, 3, 5}, the MS decoders with noisy-adders exhibit better thresholds
than the noiseless decoder. Except for the noisy XOR-operator with px = 3 × 10−4, the best choice of the channel scale factor is µ = 6. For the noisy XOR-operator with px = 3 × 10−4, the best choice of the channel scale factor is µ = 3.
Remark: For the noiseless decoder, numerical results obtained by density evolution show that µ = 6 is the best choice of

The useful region is delimited by the solid black curve. The vertical lines delimit the η-threshold regions, for η = 10−3, 10−4, 10−5, 10−6 (from right to the left).
Note that unlike the case µ = 1 (Section V-A), there is no non-
convergence region when the channel scale factor is set to µ = 6.
Hence, the border of the useful region corresponds to points (pa, p0) for which Pe(∞) = p0. However, it can be observed that there is still a discontinuity line (dashed red curve) inside the

the channel scale factor, for any dv ∈ {3, 4} and any dv <dc ≤20. useful region. This discontinuity line does actually correspond

Assumption: In the following sections, we will investigate to the functional threshold defined in Section IV. This discon-

the impact of the noisy adder, comparator and XOR-operator on tinuity line corresponds to the occurrence of an early plateau

the MS decoder performance, assuming that the channel scale phenomenon in the convergence of (Pe( )) (for details see [16]).

factor is µ = 6.

In Fig. 6, we plotted the asymptotic error probability Pe(∞) as

C. Study of the Impact of the Noisy Adder (µ = 6)
In order to evaluate the impact of the noisy adder on the MS decoder performance, the useful region and the η-threshold regions have been computed, assuming that only the adders

a function of p0, for the noiseless decoder (pa = 0), and for
the sign-preserving noisy adder with error probability values pa = 10−4 and pa = 0.05. In each plot we have also represented two points p(0U) and p(0FT), corresponding respectively to the values of p0 on the upper-border of the useful region, and on the

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

1504

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 63, NO. 5, MAY 2015

Fig. 6. Asymptotic error probability Pe(∞) as a function of p0; noiseless and noisy MS with sign-preserving noisy adder. (a) pa = 0 (noiseless decoder); (b) pa = 10−4; (c) pa = 0.05.

Fig. 7. Useful and η-threshold regions for p0 = 0.07 with sign-preserving noisy adder. (a) Noisy XOR-operator and noiseless comparator. (b) Noisy comparator and noiseless XOR-operator.

discontinuity line. Hence, p(0FT) corresponds to the functional noisy decoder to converge to the received word. For more threshold χ¯ from Definition 2. It coincides with the classical details, the reader may refer to [16].

threshold in the noiseless case, and can be seen as an appro-

priate generalization of the classical threshold to the case of noisy decoders. A similar behavior can be observed for the full- D. Comparison of the Impact of the Different Noisy depth noisy adder. Moreover, from Figs. 4 and 5, it can be seen Components (µ = 6)

that as pa goes to zero, the functional threshold value (red line) converges to 0.077, which is the classical threshold value of the noiseless decoder. In the following, the region located below the discontinuity line will be referred to as the functional region.
Finally, we note that the functional threshold phenomenon can also be observed for the MS decoder with noisy XORoperator (px > 0) or noisy comparator (pc > 0), although not illustrated in this paper due to space limitations. Similar to the noisy adder case, when px and pc go to zero, the functional threshold value converges to the threshold value of the noiseless decoder. It is also worth noting that the MS decoder with noisy comparator exhibits actually a classical threshold phenomenon, i.e., Pe(∞) = 0 for any point in the functional region.

To compare the impact of the different noisy components on the decoder performance, the useful region and the η-threshold regions have been plotted for a channel parameter value close to, but slightly below the functional threshold: p0 = 0.07 (this value is fixed throughout this section). Sign-preserving adders are considered in Fig. 7 and full-depth adders in Fig. 8.
In Fig. 7(a) comparators are assumed to be noiseless (pc = 0) and the regions are plotted with respect to pa and px. While for the useful region the maximum admissible value of px is slightly less than the maximum admissible value of pa, they tend to become increasingly closer as the target η value decreases. Hence, we conclude that the sign-preserving noisy adder and the noisy XOR-operator have comparable impact on

This is explained by the fact that if the crossover proba- the MS decoder performance, especially for target bit error rates bility of the channel is small enough, faulty gate errors in below 10−6.

the amplitude evaluation are less important than computing In Fig. 7(b) XOR operators are assumed to be noiseless (px = accurately the signs of check-node messages, in order for the 0) and the regions are plotted with respect to pa and pc. It can be

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

KAMENI NGASSA et al.: DENSITY EVOLUTION AND FUNCTIONAL THRESHOLD FOR THE NOISY MIN-SUM DECODER

1505

Fig. 8. Useful and η-threshold regions for p0 = 0.07 with full-depth noisy adder. (a) Noisy XOR-operator and noiseless comparator. (b) Noisy comparator and noiseless XOR-operator.
seen that the maximum admissible value of pc is significantly above the maximum admissible value of pa, especially for low η-values, which confirms that the MS decoder performance is much more sensitive to adder noise than to comparator noise. Moreover, it can be observed that the η-regions are delimited on the top by the same curve, given by pc = 0.023 for pa < 10−4. This actually corresponds to the threshold pc-value of the noisycomparator MS decoder, for a channel crossover probability p0 = 0.07 (as discussed in Section V-C, the MS decoder with noisy comparator exhibits a classical threshold phenomenon).
Fig. 8 presents a similar analysis, when considering the fulldepth noisy-adder model. It can be seen that the maximum admissible value of pa is less than the maximum admissible values of both pc and px, which shows that the full-depth noisy adder is the most critical component of the noisy MS decoder.

VI. FINITE LENGTH PERFORMANCE OF THE NOISY MIN-SUM DECODER
The goal of this section is to corroborate the asymptotic analysis from the previous section, through finite-length simulations. Unless otherwise stated, the (3, 6)-regular LDPC code with length N = 1008 bits from [17] will be used throughout this section.

Fig. 9. BER performance of noiseless and noisy MS decoders with various channel scale factors.
The reason why we stress the difference between the MS decoder with and without the syndrome check step is because the noiseless early stopping criterion may significantly improve the bit error rate performance of the noisy decoder in the error floor region (see Section VI-C).
Unless otherwise stated, the MS decoder is assumed to

implement the noiseless stopping criterion (syndrome check

A. Early Stopping Criterion

step). The maximum number of decoding iterations is fixed to

100 throughout this section. As described in Algorithm 1, each decoding iteration also

comprises a hard decision step, in which each transmitted bit is estimated according to the sign of the a posteriori information, and a syndrome check step, in which the syndrome of the

B. Finite-Length Performance for Various Channel Scale Factors

estimated word is computed. Both steps are assumed to be Fig. 9 shows the bit error rate (BER) performance of the

noiseless, and the syndrome check step acts as an early stopping finite-precision MS decoder (both noiseless and noisy) with

criterion: the decoder stops when either the syndrome is +1 various channel scale factors. For comparison purposes, we

(the estimated word is a codeword) or a maximum number of also included the BER performance of the Belief-Propagation

iterations is reached. We note however that the syndrome check decoder (solid black curve, no markers) and of the infinite-

step is optional and, if missing, the decoder stops when the precision MS decoder (dashed blue curve, no markers). These

maximum number of iterations is reached.

decoders are implemented in floating point, and the input LLR

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

1506

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 63, NO. 5, MAY 2015

Fig. 10. BER performance with and without early stopping criterion (sign- Fig. 11. BER performance, noisy MS, sign-preserving noisy adder, pc = pa,

preserving noisy adder, pa = 0.001).

px = 0.0001.

values are computed, as usual, by γn = yn log((1 − p)/p), with and the XOR-operator are assumed to be noiseless (pc =

yn ∈ ±1.

px = 0). Two codes are simulated, the first with length N =

It can be observed that the worst performance is achieved 1008 bits, and the second with length N = 10000 bits. In

by the infinite-precision MS decoder and the finite-precision the case that the early stopping criterion is not implemented

noiseless MS decoder with channel scale factor µ = 1. The BER (dashed curves), corresponding BER curves exhibit an error performance of the latter improves significantly when using a floor at ≈ 3.33 × 10−5, as predicted by Proposition 1. However,

sign-preserving noisy adder with error probability pa = 0.001 when the early stopping criterion is implemented (solid

(dashed red curve with empty circles).

curves), it can be seen that none of the BER curves show any

For a channel scale factor µ = 6, both noiseless and error floor down to 10−8.

noisy decoders have almost the same performance (solid and Hence, in practical designs, one can eliminate the error floor

dashed green curves, with triangular markers). Interestingly, the induced by the noisy components of the decoder, by protecting

achieved BER is very close to the one achieved by the Belief- a small part of the circuit that implements the syndrome check

Propagation decoder.

step (e.g., by using classical fault-tolerant design techniques,

These results corroborate the asymptotic analysis from or by implementing this step in a different, more robust tech-

Section V-B concerning the channel scale factor optimization. nology). The price to pay might be a possible increase in the

number of decoding iterations. However, our simulation results

C. Error Floor Performance

show that for the noisy decoders presented in Fig. 10, the

The BER curves of the noisy decoders from Fig. 9 do not show any error floor down to 10−7. However, according

to Proposition 1, the decoding error probability should be

lower-bounded

by

Pe(

)

≥

1 2Q

pa

=

3.33

× 10−5.

The

fact

that

the observed decoding error probability may decrease below

average number of decoding iterations increases by less than 0.5 iterations with respect to the noiseless decoders. Indeed, when the noiseless decoder converges, the probability of not having any erroneous bit within one iteration of the noisy decoder is sufficiently high, approaching 0.967 (for N = 1008). While this value is reflecting in the error floor performance, it has a

the above lower-bound is due to the early stopping criterion negligible impact on the expected number of extra iterations

implemented in the decoder. Indeed, as we observed in the to be run before the noiseless syndrome check step detects an

previous section, the lower-bound is tight, when is sufficiently error-free iteration.

large. Therefore, as the iteration number increases, the expected

number

of

erroneous

bits

gets

closer

and

closer

to

1 2Q

paN

=

0.034 (for N = 1008), and the probability of not having any

erroneous bit within one iteration approaches

1

−

1 2Q

pa

N
=

0.967. As the decoder performs more and more iterations, it will

D. Finite-Length Performance for Various Parameters of the Probabilistic Error Models
In this section we investigate the finite-length performance when all the MS components (adder, comparator, and XOR-

eventually reach an error free iteration. The absence of errors is operator) are noisy. In order to reduce the number of simula-

at once detected by the noiseless syndrome check step, and the tions, we assume that pa = pc ≥ px. The BER performance is

decoder stops.

evaluated for both sign-preserving and full-depth noisy adder

To illustrate this behavior, we plotted in Fig. 10 the BER error models. Simulation results are presented in Figs. 11 and 12.

performance of the noisy MS decoder, with and without early In case the noisy-adder is sign-preserving, it can be seen that

stopping criterion. The noisy MS decoder comprises a sign- the MS decoder can provide reliable error protection for all the

preserving noisy adder with pa = 0.001, while the comparator noise parameters that have been simulated. However, depending

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

KAMENI NGASSA et al.: DENSITY EVOLUTION AND FUNCTIONAL THRESHOLD FOR THE NOISY MIN-SUM DECODER

1507

a similar manner for the a priori and a posteriori information). The corresponding probability mass functions are denoted as follows:

C(z) = Pr(γ = z), C( )(z˜) = Pr γ˜( ) = z˜ ,

∀z ∈ M ∀ z˜ ∈ M

A( )(z) = Pr α( ) = z , ∀ z ∈ M

B( )(z) = Pr β( ) = z , ∀ z ∈ M.

Fig. 12. BER performance, noisy MS, full-depth noisy adder, pc = pa, px = 0.0001.
on the error probability parameters of the noisy components, there is a more or less important degradation of the achieved BER with respect to the noiseless case. But in all cases the noisy decoder can achieve a BER less than 10−7. This is no longer true for the full-depth noisy adder: it can be seen that for pc = pa ≥ 0.005, the noisy decoder cannot achieve bit error rates below 10−2.
VII. CONCLUSION
This paper investigated the asymptotic and finite length behavior of the noisy MS decoder. We demonstrated the impact of the channel scale factor on the decoder performance, both for the noiseless and for the noisy decoder. We also highlighted the fact that an inappropriate choice of the channel scale factor may lead to an unconventional behavior, in the sense that the noise introduced by the device may actually improve the performance compared to the noiseless decoder. We analyzed the asymptotic performance of the noisy MS decoder in terms of useful regions and target-BER thresholds, and proposed a novel decoding threshold phenomenon, which was referred to as functional threshold. Finally, we also corroborated the asymptotic analysis through finite-length simulations.
Note: An extended version of this paper, including results for both BSC and BI-AWGN channels, is available at http:// arxiv.org/abs/1405.6594.

1) Expression of the Input Probability Mass Function C: The
probability mass function C depends only on the channel and the quantization map q : Y → M, where Y denotes the channel output alphabet. We also note that for = 0, we have A(0) = C.
For the BSC, the channel output alphabet is Y = {−1, +1}. For a positive integer µ ≤ Q, the quantization map qµ is defined by qµ(y) = µ·y, ∀ y ∈ Y = {−1, +1}. Considering the all-zero (+1) codeword assumption, the probability mass function C can be computed as follows (where ε is the crossover proba-
bility of the BSC):

1 − ε, if z = µ

C(z) = ε, if z = −µ

(15)

0, otherwise.

2) Expression of B( ) as a Function of A( −1): In the sequel,

we make the convention that Pr(sgn(0) = 1) = Pr(sgn(0) =

−1) = 1/2. The following notation will be used:

y

• A[x,y] = ∑ A(z), for x ≤ y ∈ M
z=x

y

•

A[0+,y]

=

1 2

A(0)

+

∑

A(z),

for

y∈

M,

y

>0

z=1

−1

•

A[x,0−]

=

1 2

A(0)

+

∑
z=x

A(z),

for

x

∈

M,

x

<

0.

For the sake of simplicity, we drop the iteration index, thus B := B( ) and A := A( −1). We proceed by recursion on i =

2, . . . , dc − 1, where dc denotes the check-node degree. Let β1 := α1, and for i = 2, . . . , dc − 1 define:

βi = xpr (sgn(βi−1), sgn(αi)) mpr (|βi−1|, |αi|) .

Let also Bi−1 and Bi denote the probability mass functions of βi−1 and βi, respectively (hence, B1 = A).
First of all, for z = 0, we have:

Bi(0) = Pr(βi = 0) = A(0)Bi−1(0) + [Bi−1(0) (1 − A(0)) + A(0) (1 − Bi−1(0))] (1 − pc).

APPENDIX A DENSITY EVOLUTION

For z = 0, we proceed in several steps as follows. For z > 0:

In this appendix we derive density evolution equations for the noisy finite-precision MS decoding for a regular (dv, dc) LDPC code. The study can be easily generalized to irregular LDPC

Fi (z) d=ef Pr(βi ≥ z|px = 0) = Bi−1[0+,z−1]A[z,Q−1]+A[0+,z−1]Bi−1[z,Q−1] pc

codes, simply by averaging according to the degree distribution polynomials.

+ Bi−1[1−z,0−]A[−Q,−z]+A[1−z,0−]Bi−1[−Q,−z] pc

Let > 0 denote the decoding iteration. Superscript ( ) will be used to indicate the messages and the a posteriori information computed at iteration . To indicate the value of a message on a randomly selected edge, we drop the variable

+ Bi−1[z,Q−1]A[z,Q−1] + Bi−1[−Q,−z]A[−Q,−z] Fi(z) d=ef Pr(βi ≥ z)
= (1 − px).Fi (z) + px.Gi(−z)

and check node indexes from the notation (and we proceed in

Bi(z) = Pr(βi = z) = Fi(z) − Fi(z + 1).

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

1508

IEEE TRANSACTIONS ON COMMUNICATIONS, VOL. 63, NO. 5, MAY 2015

For z < 0: Gi(z) d=ef Pr(βi ≤ z|px = 0)
= Bi−1[0+,−z−1]A[−Q,z]+A[0+,−z−1]Bi−1[−Q,z] pc

Note that applying the saturation operator sM on the probability mass function Cdv−1 means that all the probability weights corresponding to values w˜ outside M must be accu-
mulated to the probability of the corresponding boundary value of M (that is, either −Q or +Q, according to whether w˜ < −Q

or w˜ < +Q).

+ Bi−1[−z,Q−1]A[z+1,0−] +A[−z,Q−1]Bi−1[z+1,0−] pc + Bi−1[−z,Q−1]A[−Q,z] + A[−z,Q−1]Bi−1[−Q,z] Gi(z) d=ef Pr(βi ≥ z)

Remark: For a noisy adder defined by the full-depth or the sign preserving error injection models defined in Section II, the third equation from the above recursion (expression of Ci as a function of c˜i) may be rewritten as follows:
Sign-preserving bitwise-XORed noisy adder

= (1 − px).Gi(z) + px.Fi (−z)
Bi(z) = Pr(βi = z) = Gi(z) − Gi(z + 1).
Finally, we have that B = Bdc−1. 3) Expression of A( ) as a Function of B( ) and C: We derive
at the same time the expression of C( ) as a function of B( ) and C. For simplicity, we drop the iteration index, so A := A( ), B :=
B( ), and C := C( ). We denote by ı : M × E → M the error injection model (either full-depth or sign-preserving) used to

⎧

⎪⎨⎪

(1

−

pa)c˜i(z˜)

+

1 Q

pa

c˜i[≤ 0−] − c˜i(z)

,

Ci

(z˜)

=

⎪⎩⎪

(1 (1

− −

pa pa

)c˜i(0) + )c˜i(z˜) +

1
Q 1
Q

pa pa

(1 − c˜i[≥

c˜i(0)) , 0+] − c˜i

(z)

,

if z˜ < 0 if z˜ = 0 if z˜ > 0
(16)

where

c˜ i [≤

0− ]

=

∑ω˜ <0 c˜i(ω˜ ) +

1 2

c˜i(0),

and

c˜ i [≥

0+ ]

=

1 2

c˜i(0)

+

∑ω˜ >0 c˜i(ω˜ ).

Full-depth bitwise-XORed noisy adder

define the noisy adder. We decompose each noisy addition into three steps (noiseless infinite-precision addition, saturation, and error injection), and proceed by recursion on i = 0, 1, . . . , dv, where dv denotes the variable-node degree:

Ci(z˜) = (1 − pa)c˜i(z˜) + 1 pa (1 − c˜i(z˜)) .

(17)

2Q

Finally, we note that the density evolution equations for

• For i = 0: Ω0 d=ef γ ∈ M ⊆ M,

the noiseless finite-precision MS decoder can be obtained by setting pa = pc = px = 0.

C0(z˜) d=ef Pr(Ω0 = z˜) =

C(z˜), 0,

if z˜ ∈ M if z˜ ∈ M \ M.

REFERENCES
[1] S. K. Chilappagari, M. Ivkovic, and B. Vasic, “Analysis of one step majority logic decoders constructed from faulty gates,” in Proc. IEEE

ISIT, 2006, pp. 469–473.

• For i = 1, . . . , dv:

[2] B. Vasic and S. K. Chilappagari, “An information theoretical frame-

work for analysis and design of nanoscale fault-tolerant memories based

ωi d=ef Ωi−1 + βmi,n ∈ Z,

on low-density parity-check codes,” IEEE Trans. Circuits Syst. I, Reg. Papers, vol. 54, no. 11, pp. 2438–2446, Nov. 2007.

ci(w) d=ef Pr(ωi = w)

[3] L. R. Varshney, “Performance of LDPC codes under faulty iterative decoding,” IEEE Trans. Inf. Theory, vol. 57, no. 7, pp. 4427–4444, Jul. 2011.

= ∑u Ci−1(u)B(w − u), ∀ w ∈ Z

[4] S. Yazdi, H. Cho, Y. Sun, S. Mitra, and L. Dolecek, “Probabilistic analysis of Gallager B faulty decoder,” in Proc. IEEE ICC, 2012, pp. 7019–7023.

ω˜ i d=ef sM(ωi) ∈ M,

[5] S. Yazdi, C. Huang, and L. Dolecek, “Optimal design of a Gallager B noisy decoder for irregular LDPC codes,” IEEE Commun. Lett., vol. 16, no. 12, pp. 2052–2055, Dec. 2012.

c˜i(w˜ ) d=ef Pr(ω˜ i = w˜ ) ⎧ ⎨⎪ ci(w˜ ),

if w˜ ∈ M \ {±Q}

[6] S. Yazdi, H. Cho, and L. Dolecek, “Gallager B decoder on noisy hardware,” IEEE Trans. Commun., vol. 61, no. 5, pp. 1660–1673, May 2013.
[7] C.-H. Huang, Y. Li, and L. Dolecek, “Gallager B LDPC decoder with transient and permanent errors,” in Proc. IEEE ISIT, Jul. 2013,

=

⎪⎩

∑w≤−Q ∑w≥+Q

ci ci

(w), (w),

if w˜ = −Q if w˜ = +Q

pp. 3010–3014. [8] C.-H. Huang, Y. Li, and L. Dolecek, “Gallager B LDPC decoder with
transient and permanent errors,” IEEE Trans. Commun., vol. 62, no. 1, pp. 15–28, Jan. 2014.

Ωi d=ef ı(ω˜ i, e) ∈ M,

[9] C.-H. Huang and L. Dolecek, “Analysis of finite-alphabet iterative decoders under processing errors,” in Proc. IEEE ICASSP, May 2013,

Ci(z˜) d=ef Pr(Ωi = z˜)

pp. 5085–5089. [10] E. Dupraz, D. Declercq, V. Vasic, and V. Savin, “Finite alphabet iterative
decoders robust to faulty hardware: Analysis and selection,” in Proc. IEEE

∑ ∑ = ω˜ e δız˜(ω˜ ,e) pE(e)c˜i(ω˜ ), ∀ z˜ ∈ M

ISTC, Aug. 2014, pp. 107–111. [11] A. Balatsoukas-Stimming, C. Studer, and A. Burg, “Characterization of

where δyx = 1 if x = y, and δyx = 0 if x = y. Note that in the definition of Ωi above, e denotes an error

min-sum decoding of LDPC codes on unreliable silicon,” in Proc. ITA Workshop, 2014, pp. 1–18. [12] A. Balatsoukas-Stimming and A. Burg, “Density evolution for minsum decoding of LDPC codes under unreliable message storage,” IEEE

drawn from the error set E according to the error probability

Commun. Lett., vol. 18, no. 5, pp. 849–852, May 2014.

distribution pE.

[13] C. L. Kameni Ngassa, V. Savin, and D. Declercq, “Min-sum-based decoders running on noisy hardware,” in Proc. IEEE GLOBECOM, 2013,

Finally, we have: A = sM(Cdv−1) and C = Cdv .

pp. 1879–1884.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

KAMENI NGASSA et al.: DENSITY EVOLUTION AND FUNCTIONAL THRESHOLD FOR THE NOISY MIN-SUM DECODER

1509

[14] C. L. Ngassa, V. Savin, and D. Declercq, “Analysis of min-sum based decoders implemented on noisy hardware,” in Proc. Asilomar Conf. Signals, Syst. Comput., 2013, pp. 866–870.
[15] T. J. Richardson and R. L. Urbanke, “The capacity of low-density paritycheck codes under message-passing decoding,” IEEE Trans. Inf. Theory, vol. 47, no. 2, pp. 599–618, Feb. 2001.
[16] C. L. Kameni Ngassa, V. Savin, and D. Declercq, “Unconventional behavior of the noisy min-sum decoder over the binary symmetric channel,” in Proc. ITA Workshop, 2014, pp. 1–10.
[17] D. J. MacKay, Encyclopedia of Sparse Graph Codes. [Online]. Available: http://www.inference.phy.cam.ac.uk/mackay/codes/data.html

Elsa Dupraz was born in Paris, France. She received the M.Sc. degree in advanced systems of radiocommunications (SAR) from ENS de Cachan, Cachan, France, in 2010, and the Ph.D. degree from University Paris-Sud, Orsay, in 2013. She currently holds a postdoctoral position at ENSEA, Cergy-Pontoise, France. Her research interests include distributed source coding, LDPC codes robust to faulty hardware, and information theory.

Christiane Kameni Ngassa received the Master’s degree in engineering from Supélec, Gif-sur-Yvette, France, in 2011, the M.Sc. degree in electrical and computer engineering from Georgia Institute of Technology, Atlanta, GA, USA, in 2011. From 2011 to 2014, she was a Ph.D. student at the Digital Communications Laboratory, CEA-LETI, Grenoble, France and received the Ph.D. degree from the University of Cergy-Pontoise, Cergy-Pontoise, France, in 2014. Since December 2014, she has been a Research Engineer at Thales Communications and Security, Gennevilliers, France. Her research interests include error-correction coding, physical-layer security, and signal processing.
Valentin Savin received the Master’s degree in mathematics from ENS de Lyon, Lyon, France, in 1997, and the Ph.D. degree in mathematics in October 2001 from Joseph Fourier Institute (University of Grenoble 1), Grenoble, France. Since 2005, he has been with the Digital Communications Laboratory, CEA-LETI, first as a two-year Postdoctoral Fellow, and then as a Research Engineer. Over the last years, he has been working on the design of lowcomplexity decoding algorithms for binary and nonbinary LDPC codes, and on the analysis and the optimization of LDPC codes for physical- and upper-layers applications. He is currently participating in or coordinating several French and European research projects in ICT. He has authored or coauthored over 50 papers in international journals and conference proceedings and is the holder of seven patents.

David Declercq (M’06–SM’11) was born in June 1971. He received the Ph.D. degree in statistical signal processing from the University of Cergy-Pontoise, Cergy-Pontoise, France, in 1998. He is currently a Full Professor at ENSEA, CergyPontoise. He is also the recipient of the junior position at “Institut Universitaire de France.” He has authored or coauthored over 40 papers in major journals (IEEE TRANSACTIONS ON COMMUNICATIONS, IEEE TRANSACTIONS ON INFORMATION THEORY, IEEE COMMUNICATIONS LETTERS, and EURASIP Journal on Wireless Communications and Networking) and over 120 papers in major conferences in information theory and signal processing. His research topics lie in digital communications and error-correction coding theory. He worked several years on the particular family of LDPC codes, both from the code and decoder design aspects. Since 2003, he has developed strong expertise on nonbinary LDPC codes and decoders in high-order Galois fields GF(q). A large part of his research projects are related to nonbinary LDPC codes. He mainly investigated two aspects: the design of GF(q) LDPC codes for short and moderate lengths and the simplification of the iterative decoders for GF(q) LDPC codes with complexity/performance tradeoff constraints. Prof. Declercq is the General Secretary of the National GRETSI Association.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 17:02:52 UTC from IEEE Xplore. Restrictions apply.

