Designing LDPC Codes Using Bit-Filling
Jorge Campellot, Dharmendra S. Modhat, Sridhar Rajagopalant
tIBM Almaden Research Center, 650 Harry Road, San Jose, CA 95120 emails: {campello,sridhar}@almaden.ibm.com $7074Via Ramada, San Jose, CA 95 139 email: modha@ieee.org

Absrrucr-Bipartite graphs of bit nodes and parity check nodes arise as Tanner graphs corresponding to low density panty check codes. Given graph parameterssuch as the number of check nodes, the maximum check-degree,the bit-degree,and the girth,we considerthe problem of constructing bipartite graphs with the largest number of hit nodes, that is, the highest rate. We propose a simple-to-implement heuristic BIT-FILLINGalgorithm for this problem. As a benchmark, our algorithm yields codes better or comparable to those in MacKay
[I].

ad-hoc, random constructions in use today that often have exponential time complexity, and, hence, can be made to work only for small column weights and for small values of g (for example, g = 6). As a benchmark, our algorithm 'nds better or comparable rates to some of the highest rate codes found by MacKay [11. .
Our algorithm can easily be adapted to the following important cases.

1. INTRODUCTION
Low density parity-check codes (LDPC) were introduced by Gallager [2]. Recently, it has been shown that LDPC

jixed rate, high girth: Given the number of check nodes m, the number of bit nodes n, the check-degree b, and bitdegree a (such that mb = nu),maximize the girth g. $xed girth, fuced length, high rate: Given the length of the

codes can perform very close to the Shannon capacity limit when their associated Tanner graphs possess certain desirable properties, see, for example, [31, [41.
A LDPC code can be described by specifying its parity check matrix H , which is an m x n binary matrix. The name "low density" refers to the fact that the matrix H is very sparse. We say that the length of the LDPC code is n and its rate is (n-m)/n.Given a parity check matrix H , we can de'ne its associated Tanner graph G ( H ) = (V,E ) as a
+ + bipartite graph with m n vertices V = { 1 , 2 , . . . ,m,m
1,.. . ,m + n}.The 'rst m vertices correspond to m parity
check equations, and are referred to as the check nodes. The
last n vertices are referred to as the bit nodes. For 1 5 i 5
+ m and 1 5 j 5 n, there is an edge ( i ,m j ) in E iff
Ht,j = 1.
The length of the smallest cycle in a graph is known as its girth. The importance of large girth stems from the fact that when decoding LDPC codes using the sum-product de-
coding algorithm, the number of independent iterations of

code n, the bit-degree a , and the girth g, minimize the number of check nodes m, that is, maximize the rate.
Finally, our algorithm can easily be modi'ed to construct several irregular graphs.
We now brie y review various approaches to constructing LDPC codes. In his original monograph, Gallager [ 2 , Appendix] gave an algorithm for the "xed rate construction. For various algebraicconstructions,also in "xed rate setting, see Margulis[5] and Lubotzky, Phillips, and Sarnak [6]. For various recent designs of graphs with girth 6 using mainly random constructions, see, for example, MacKay, Simon, and Davey [7] and MacKay and Davey [8]. For designs of irregular graphs using random constructions and linear programming, see Luby et al. [9]. Recently, Fan [lo] has considered design of LDPC codes using array codes, Mittelholzer [113 has considered design of LDPC codes using Steiner designs, and Yu, Lin, and Fossorier [121 have considered design of LDPC codes using 'nite geometries.

the algorithm is proportional to the girth of the Tanner graph

11. THEBIT-FILLINGALGORITHM

corresponding to the code [2, Lemma C. I]. In this paper, we are interested in the following design problem in construc-

A. Theldea

tive combinatorics. Suppose we are given positive integers Suppose we have already constructed a matrix H with n

a , b, g, and m;where g is required to be even. We are inter- columns, n 2 0, that satis'es all the constraints, that is, the
ested in constructing a m x n parity check matrix H with the weight of each of its columns is exactly a , no row has weight

+ largest possible rate (that is, the largest possible number of more than b, and the associated Tanner graph G G ( H )
columns n) such that H has exactly a ones in each column, has girth at least g. We now show how to add the ( n 1)-

at most b ones in each row, and G(H ) has girth 9.

th column to H . We think of the new column to be added

As our main contribution, we propose a simple-to- as a set V I which has size at most a and is initially empty.

implement heuristic BIT-FILLINGalgorithm for the above problem. Our algorithm has a computational complexity of 0(bm3)(a simpler version can be implemented using 0(bm2)operations). This is in sharp contrast to various

The set V I is a set of check nodes and, hence, is a subset
of Iml 3 {1 , 2 ,. . . ,m}. Further suppose we have already added i check nodes, 0 5 i < a , to U1. The following
+ procedure attempts to add (i 1)-th check node to U I . It

0-7803-7097-1/01/$10.00 02001 IEEE

55

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:33:01 UTC from IEEE Xplore. Restrictions apply.

1: set n = 0, A ==/mi,and U1 = 4 2: for c E Im(,set weight(c) = 0 andN, = 4

3: d o {

4: Vc E U,, set Hc,n = 1and increment deg(c) by 1
5: set a = 0, U1 = 4 , and U = 4

6: do{

7:

compute FO= A \ U

8:

if (Fo # 4 ) {

9:

choose c* from FOaccording to some heuristic

10:

‘de E U1, update Nc = NcU {c”}and update Nc*= Nc*U U1

11:

update U1 = U1 U {e*}, U = U U V(g/z,-l(c*), and A

11:

1

12:

i=i-tl

13: } while ( ( i < a) and (Fo# q5))

14: n = n + l .

15: } while ((2 = a ) and (Fo # 4 ) )

Fig. 1. The BIT-FILLINAGlgorithm.

may fail, and, in that case, the whole procedure stops.
In Figure I , we present the algorithm based on the outline
sketched above. Each iteration of the outer “do . . . while”
loop in lines 3-15 attempts to add a column to the parity check matrix. If an iteration succeeds, then, in the begin-
ning of the next iteration, line 4 adds updates the matrix H .
Each iteration of the inner “do . . . while” loop in lines 6-13
attempts to add a check node to the set lil.
To describe the procedure, it helps to think of the Tanner graph G; G has m check nodes and n bit nodes. The process
+ + of adding ( n 1)-th column of H is like adding ( n 1)-
th bit node to G. In this context, U1 can be thought of as
the set of check nodes that have been already connected to
+ the ( n 1)-th bit node that is being added. The process of
adding a check node c” to U1 is like adding an edge from
+ the ( n 1)-th bit to c*-th check node. This new edge must
not create any cycles of length (g - 2) or smaller; we now
describe a test to enforce this constraint.
For a check node 1 5 c 5 m, let N, denote the set of
all check nodes that share a bit node with it. In other words,
N,is the set of all check nodes that are exactly two distinct
edges away from c. For j 2 2, de’ne

U U j =

NC’

(1)

CEU,-l

Intuitively, there is a path of length 2 from every check node in U2 to some check node in U1. Now adding a check node e* to U1 will create a path of length 2 from c* to every check node in U1. So, if c* is in U,, then we are guaranteed a 4cycle. Hence, to avoid 4-cycles, we should avoid the check nodes in Uz. Continuing in this fashion, there is a path of length 2 from every check node in U, to some check node in Uj-1, and, hence, there is a path of length at most 2 j - 2 from from every check node in U, to some check node in U1.

Thus, adding a check node in U, to U1 will create a cycle of length 2 j or smaller. Hence, to satisfy the girth constraint, we should avoid adding check nodes in the set

U U =

U,

(Z!)

to U,.

1<,<(9/2)- 1

Let deg(c) denote the degree of the check node c. Let
A = {c E Iml : deg(c) < b }

denote set of check nodes that are connected to fewer than b

bit nodes. Then, the set of feasible check nodes that can be

added to U1without violating the girth or the check-degree

constraint is

Fo = A \ U.

The procedure terminates if FOis empty. Before we take up the important issue of how to select a check node from Fo, we brie y discuss an efcient implementation of ( 2 ) .
Observe that, as check nodes are added to U,, it is computationally more efcient to incrementally update U in line IO than recomputing it afresh using (l)-(2). Speci’cally, if we have added a check node c to U1, then we set Ul(c) = {c}
and, for 2 5 j 5 ( g / 2 ) - 1,compute

U U&) =

NCi

(3)

C’EU, - 1 ( c )

and compute

U qg/2)-1(4 =

U,(c).

(1‘)

15,5(9/2)--1

Finally, update U using U = U U V(,/+1(c).

B. Main Heuristic
The most crucial step of the BIT-FILLINGalgorithm IS the choice of the check node c* E FOin line 9 of Figure I.

0’7803-7097-1/01/$10.00 02001 IEEE

56

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:33:01 UTC from IEEE Xplore. Restrictions apply.

While any choice of c* from FOis valid, judicious selection of c* is crucial to achieving high-rate codes. Technically, using back-tracking and recursion, we can try every possible choice, and then “conditioned” on that choice try every future choice, etc., thus searching through an extremely large tree of possibilities. Of course, we often wish we could live life in this way! Unfortunately, while optimal, such exhaustive search is computationally infeasible. Also, a large number of choices actually lead to isomorphic graphs and are thus equivalent from our perspective. The role of heuristics is to pick one seemingly effective, and yet computationally feasible, path through this gargantuan tree of possibilities.
We let our choice of c* in line 9 be guided by the simple principle of keeping the Tanner graph G as homogeneous as possible, that is, from the set of all feasible check nodes pick the check node that is least used so far. This choice amounts to keeping all parts of the graph equally dense. Also, from the perspective of the (n+1)-th bit node that is being added, this choice connects it to the check node that is least used. We now make our choice precise.
As a ‘rst try, we look at the following subset of FO
Fl = {c1 E FO: Vc2 E Fo,deg(cl) i deg(c2)) (5)
namely, the set of smallest degree check nodes in Fo. As a FIRST-ORDEheRuristic, we may simply choose c* to be any element of F1. We shall refer to this heuristic as “1 -h”meaning ‘rst-order homogeneity. We will show in Section I11 that this ‘rst-order heuristic already yields quite competitive codes.
Typically, while F1 is smaller. than Fo, it does not uniquely determine a check node. To further narrow available choices, we appeal once more to homogeneity. The idea is to look at the degrees of check nodes that are two edges away from the check nodes in F1. We write
where

9A: set 3 = 0, EO= F o 9B: Vc E Fo,set Ul(c) = {c}
9C: while ((IF,I > 1)and (E, = F,)) {
9D: 3 = ~ + 1 9E: VC E F,-1,
compute W,(c) = Cc’EV,(c) deg(c’) 9F: compute FJ =
{Cl E F,-1 : Vc2 E Fj-1, W,(Cd L W,(C2))
9G: Vc E F,, compute V,+l(c) using (4)
9H: compute E, = {c E F, : V,+,(c) # V,(c)}
91: } endwhile 9J: if (IF,I = 1) 9K: select c* from F3 9L: else
9M: select c* from F3 \ E,
9N: endif
Fig. 2. The “c-h” heuristic for selecting the check node c* from Fo. This heuristic is meant to replace line 9 in Figure 1.
choices does not further reduce in cardinality. The set Ej in line 9H contains all check nodes in Fj such that their neighborhood can be further enlarged. The loop terminates if
Ej # F j , since, in this case, the check nodes in E? are guaranteed to be less homogeneous than those in Fj \ Ej. Fur-
thermore, since, using homogeneity as our guide, we cannot
further distinguish between the elements of Fj \ E j , we sim-
ply make some choice from this set. Speci’cally, we select the lexicographically smallest check node from this set.
111. RESULTSAND DISCUSSION
Remark 111.1 (“xedgirth, high rate) MacKay has collected a large number of LDPC codes at [I]. Amongst the codes listed there, he singles out 9 codes as “the following numbers give the highest rate codes I have made with column weights 3 and 4.” In the following table, we compare these codes to those generated by our algorithm.

W2(C) =

deg(c’)

c‘EVz(c)

and V2(.) is as in (4). Now, we may try selecting a check node from F 2 . However, once again, it is possible that F 2 has more than one element. Thus, to further narrow the set of choices, we may look at the degrees of check nodes that are four edges away from the check nodes in F1, and can continue in this fashion. The basic idea can now be described algorithmically, see Figure 2. The idea is to progressively look at larger and larger neighborhoods of the feasible check nodes in order to distinguish them; we refer to this heuristic as “c-h” for complete-homogeneity. The “while . . . endwhile” loop in lines 9C-91 terminates when the set of choices reduces to a set of cardinality one or till the set of

am

n

BIT-FILLING

MacKay l-h

c-h

~
3 60

492 437 485

3 62

495 464 483

3 90

998 970 1087

3 100

900 1229 1339

3 111

999 1515 1636

4 222

1998 2628 2752

4 282

4376 4293 4821

4 300

4096 4807 5499

4 444

3584 10839 12360

The “l-h” and “c-h” columns refer to selecting c* in line 9

0-7803-7097-1/01/$10.0020001 IEEE

57

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:33:01 UTC from IEEE Xplore. Restrictions apply.

~

of Figure 1,respectively, using the "rst-order heuristic in (5) and using the complete-homogeneity argument in Figure 2. Also, for all our codes, we required g = 6 and b was unrestricted. It is evident that in 7 out of 9 cases our algorithm yields higher rate codes, and in 2 cases, while not better, it yields virtually identical rates.
In Figure 3, we compare the performance of two codes, say, A and B, in [ l ] with parameters m = 222, n = 1998, a = 4, b = 36, and g = 6 to a code with the same parameters generated by the BIT-FILLING algorithm. The latter was obtained by deleting the excess columns of the code corresponding to the sixth row in the above table. The comparison was made in terms of bit error rate as a function of the signal-to-noise ratio (SNR) on a simulated AWGN channel. It can be seen that these three codes have virtually identical performance.
To summarize, on identical parameters, the BIT-FILLING codes perform on par with some of the best known codes. However, BIT-FILLING offers more exibility in the code design parameters. For example, one may choose to tradeoff the bit error rate performance against the code rate.
r-I
-+ \\

In this case, the number of bit nodes is n = ( m b ) / a . Our algorithm can be adapted to this case as follows. We simply apply the algorithm with given parameters m, a , b, and some girth 9. If the algorithm "nds exactly (resp. less than) n bit nodes, then the girth g is achievable (resp. not achievable) with our algorithm. By trying a few girth value:< (formally, doing a binary search on girth values), one can quickly determine the largest girth that is attainable using our algorithm.
Once again, we use the encyclopedia of codes compiled by MacKay [l] as our benchmark, All the Gallager codes listed in [l] have girth 6, that is, avoid 4 cycles. For all of these codes, our algorithm matches the girth. More interestingly, for codes with larger block lengths, our algorithm i:j able to improve the girth parameter. For example, for rate 1/2 codes with a = 3, b = 6, and m = 408 our algorithm is able to improve the girth from 6 to 8. In this case, the algorithm achieves n = 815-just one column less than the desired 816.
Remark 111.3 ("xedgirth, "xedlength, high rate) The following observation is due to Ajay Dholakia. In practice, we are often interested in highest rate codes with "xed girth 9, "xed length n, and "xed column weight a ; that is, given g, n, and a, we would like to minimize m. The BIT-FILLING algorithm can easily be used to construct codes that achieve as high a rate as possible by trying different values of m, in a binary search mode, and selecting the smallest m that eitheiexceeds or achieves the desired length; and, by deleting the excess columns, if any. For comparison purposes, we reuse three of the codes from [11 that were used in Remark 111.1, but this time we "x n and minimize m. The results are giver1 in the table below.

U

n

m

BIT-FILLING

MacKay I-h

c-h

3 900

100 86

82

3 999

111 91

86

4 1998

222 195 186

-8

I

5

55

6

65

7

75

8

SNR (indB)

Fig. 3. A comparison of three LDPC codes with parameters m = 222, n = 1998, a = 4,b = 36, and g = 6 .

Remark 111.2 ("xedrate, high girth) So far we focussed on maximizing the rate given constraints on the number of check nodes, the check-degree, the bit-degree, and the girth. Another interesting problem, "rst considered in [2], is given the number of check nodes m, the check-degree b, and bitdegree a, maximize the girth g. For various algebraic constructions of "xed rate, high girth graphs, see [ 5 ] , [61.

Remark 111.4 (irregulargraphs) We now discuss two ex-
tension to irregular graphs. The BIT-FILLING algorithm guarantees that the bit nodes,
are all regular. However, the check nodes are not necessarily regular, since, in general, the upper bound b may no1 be achieved by all of them. We now consider an extension where the bit nodes are also allowed to be irregular. In particular, we are given an upper bound a, and a lower bound
al 2 2 on the degree of the bit nodes. In this case, our ob-
jective is to "nd the largest integer n such that we "rst maximize the number of columns with weight a,, then maximize the number of columns with weight a, - 1,etc. The extension to achieve this goal is simple: start the BIT-FILLING

0-7803-7097-1/01/$10.0020001 IEEE

58

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:33:01 UTC from IEEE Xplore. Restrictions apply.

algorithm with a = a, and add as many columns as possi-
ble. At some point the algorithm will fail in line 13. Now, if
i < al, then the procedure stops. However, if i 2 al, then reset a = i, and continue.
In a recent work, Richardson, Shokrollahi, and Urbanke

be implemented using O ( m )operations, and, using (3)-(4), updating U can be implemented using O ( ( a b ) g - l ) = O ( m )
operations. Hence, each iteration of the inner “do . . . while” loop in lines 6-14 can be implemented in O ( m 2 )
operations. Since, the loop is iterated at most a times, the

[4] have shown that the performance of LDPC codes can entire loop in lines 6-14 can be implemented in O ( a m 2 )op-
be improved by using irregular constructions with a care- erations. Finally, the outer “do . . . while” loop in lines 3-16

fully chosen bit-degree sequence, that is, by using an op- is iterated at most ( m b ) / atimes, and, hence, the entire algotimized distribution of the bit-degrees. The BIT-FILLINGrithm requires O ( b m 3 ) operations. Observe that if, in line

algorithm can be easily extended to construct LDPC codes 9, we use the ‘rst-order heuristic algorithm in (5) instead

with a prescribed bit-degree distribution with a large girth. of the entire algorithm in Figure 2 , the entire resulting al-

Suppose that we want to construct a code with bit-degrees gorithm requires only O(b m 2 ) operations. Thus, computa-
a1 > a2 > . . . > a,, where there are n, bit nodes with tional complexity of BIT-FILLINiGs quite favorable, espedegree a,. We proceed as follows. We “x the girth g and we cially, when compared to various exponential-time random

start allocating the columns with the largest degree, a l . After allocating n1 of these columns, we allocate 722 columns of degree a2, and so on. If we succeed in allocating the
n = Er=,n, columns, we increase the target girth; oth-
erwise, we decrease it. In this fashion, we search for the
largest girth graph that attains the prescribed bit-degree sequence.

construction methods that are often employed currently.
ACKNOWLEDGMENTS We are grateful to Ajay Dholakia and Brian Marcus for a number of valuable discussions.
REFERENCES

Remark 111.5 (computational hardness) The formalization that follows was posed to us by Brian Marcus. To analyze the computational complexity of the problem of designing high rate codes satisfying given parameters, we consider the following decision problem.
GIRTH&BI-DEGREE Instance: ( a ,b, g , m,n )where a, b, g , m, and n are positive integers and g is even. Question: Does there exist a m x n binary matrix H such that H has exactly a ones in each column, at most b ones in each row, and G(H ) has girth g.
+ + + + We take size of the instance to be a b g m n,
and not l o g ( a b g m n ) . Now, given a m x n binary matrix H ,
+ the constraints on the weights of the rows and the columns
are easy to check. Furthermore, using roughly O(( n m ) 2 ) operations it is easy to determine the girth of G ( H ) .Hence, the problem GIRTH&BI-DEGREiEs clearly in NP. However, the input consists of simply ”ve integers, and does not contain any additional structure. Hence, assuming that
P # N P , it follows from Berman [13] that this problem
cannot be NP-hard.
Remark 111.6 (computational complexity) We will now roughly analyze the computational complexity of the BITFILLINGalgorithm in Figure 1. Line 7 implements a set difference, and can be implemented using O ( m )operations.

D. J. C. MacKay, “Encyclopedia of sparse graph codes,” 1999. http://wol.ra.phy.cam.ac.uWmackay/codes/data.html.
R. G. Gallager, Low-density parity-check codes. MIT Press, Cambridge, MA, 1963. D. J. C. MacKay, “Good error-correcting codes based on very sparse matrices,” IEEE Trans. Inform. Theory, vol. 45, pp. 3 9 9 4 3I , March 1999. T. J. Richardson, A. Shokrollahi, and R. Urbanke, “Design of provably good low-density parity check codes,” Submitted fo IEEE Trans. Iiform. Theory, 1999. G . A. Margulis, “Explicit group-theoretical construction of combinatorial schemes and their application to the design of expanders and concentrators,” Problems of lnjornz. Transmission, vol. 24, no. 1, pp. 3 9 4 6 , 1988. A. Lubotzky, R . Phillips, and P. Sarnak, “Explicit expanders and the Ramanujan conjectures,” Combinatorica, vol. 8, no. 3, pp. 261-277, 1988. D. J. C. MacKay, S. T. Wilson, and M. C. Davey, “Comparison of constructions of irregular Gallager codes,” IEEE Trans. Communications, vol. 10, pp. 1449-1454, October 1999. D. J. C. MacKay and M. C. Davey, “Evaluation of Gallager codes for short block length and high rate applications,” in Proc. IMA Workshop codes, Systems. & Graphicul Models, 1999. M. Luby, M. Mitzenmacher, A. Shokrollahi. and D. Spielman, “Analysis of low-density codes and improved designs using irregular graphs,” in Proc. 30th Ann. ACM Synip. Theory Compiiting, pp. 249258, 1998. J. L. Fan, ‘‘Array codes as ldpc codes,” in Proc. 2nd Int. Synzp. Turbo Codes and Related Topics, Bresf,Frunce, September 4-7.2000. T. Mittelholzer, 2000. personal communication. Y. Kou, S . Lin, and M. P. Fossorier, “Low density parity check codes based on ‘nite geometries: A rediscovery,” in Proc. Int. Symp. Inform. Theory, Sorrenfo,Ifuly,June 25-29, 2000. P. Berman, “Relationship between density and deterministic complexity of NP-complete languages,” in Fifth Intl. Colloy. on Ailtomafa, Languages, and Programming, Lecture Notes in Computer Science, vol. 62, pp. 63-71, 1978.

Line 9 ‘nds the check node c* using the algorithm in Figure 2, and can be implemented, in the worst case, using
O(m2)operations. Updating U, and JI(, on line 10 can both be implemented using O ( a )operations, updating A can

0-7803-7097-1/01/$10.00 02001 IEEE
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:33:01 UTC from IEEE Xplore. Restrictions apply.

