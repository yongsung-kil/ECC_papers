REDUCED COMPLEXITY DECODING ALGORITHM OF LDPC CODES USING NODE ELIMINATION
Joon-Sung Kim and Hong-Yeop Song
Coding and Information Theory Lab, Department of Electrical and Electronics Engineering, Yonsei University, 134 Shinchon-dong Seodaemun-gu, Seoul, Korea, 120-749. E-mail: {js.kim, hysong}@yonsei.ac.kr

ABSTRACT
We propose a modified belief propagation algorithm that uses node elimination for bit nodes of large log-likelihood ratio. With this algorithm, we can reduce decoding complexity and improve performance by enhancing the cycle properties. Simulation results show that the decoding complexity is reduced and the proposed algorithm outperforms the belief propagation algorithm in the high-Eb/No region.

1. INTRODUCTION
LDPC codes are linear codes described in terms of a paritycheck matrix containing a small number of ones. LDPC codes can achieve performance to the Shannon Limit with iterative probabilistic decoding algorithms [1]. However, since the decoding algorithms for LDPC codes are of high complexity, many reduced-complexity decoding algorithms have been proposed [2].
In one of the LDPC-decoding algorithms, the beliefpropagation (BP) algorithm, if the absolute value of the a posteriori log-likelihood ratio (LLR) of a bit node is large, we can say that the bit-error probability of that node is low. For those bit nodes of large LLR value, we can consider that we eliminate them from the bipartite graph in order to reduce decoding complexity.

2. MODIFIED BP ALGORITHM USING NODE ELIMINATION

The updating rule of a BP algorithm is as follows. Let zmn be the LLR of bit n which is sent from bit node n to check node m. Then, the check node message-updating rule can
be given as [2]

Lmn

=

ln

1 1

− +

n n

∈N (m)\n ∈N (m)\n

1−exp(zmn 1+exp(zmn
1−exp(zmn 1+exp(zmn

) )
) )

,

Support for this work was provided in part by Samsung Electronics under the project on 4G wireless communication systems.

Fig. 1. bipartite graph representation of node elimination

10100 H 01101
11010 00111

1010 H 0110
1101 0011

Fig. 2. The parity check matrix representation of Fig. 1

where Lmn denotes the LLR of bit n in check m and N (m)\n denotes the set of bits, except for bit n, that participate in check m . The bit node message-updating rule is

zn = Fn +

Lm n,

m ∈M (n)

zmn = zn − Lmn,
where zn denotes the LLR of bit n and Fn denotes the LLR of bit n, which is derived from the received value yn and M (n) denotes the set of checks that participate in bit n.
In the tentative decoding step, the zn is used to determine the hard decision of bit n. In this case, if the absolute value of zn is sufficiently large, the probability that the hard decision of bit n is correct is high. Therefore, the bit node that has a sufficiently large zn magnitude can be eliminated from the bipartite graph in order to stop the update of the bit node. Figure 1 shows a bipartite graph representation of the elimination of the fifth bit node. In the horizontal and vertical decoding step, the message-passing update from/to the eliminated node is not activated. It maintains only the hard decision value to satisfy the check equation of the associated check nodes. Figure 2 shows a parity check matrix representation of the bipartite graph shown in Fig. 1. In the parity check matrix, the eliminated node is represented in

1-4244-0411-8/06/$20.00 ©2006 IEEE.
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 19:07:59 UTC from IEEE Xplore. Restrictions apply.

BER

100

10−1

10−2

10−3

10−4 FER(BP)

FER(proposed, α=1E−3)

10−5

FER(proposed, α=1E−7)

BER(BP)

10−6

BER(proposed, α=1E−3)

BER(proposed, α=1E−7)

10−7

1

1.2

1.4

1.6

1.8

2

Eb/No

Fig. 3. BER performance of proposed algorithm

number of eliminated nodes

700 600 500 400 300 200 100
0 0

α=1E−3 α=1E−7

2

4

6

8

iteration

10

12

14

Fig. 4. Number of eliminated nodes through iteration

column elimination. By the elimination of the bit node that requires iterative computation, we can reduce the decoding complexity and also enhance the cycle properties. In Fig 1, we can see that after the fifth bit node is eliminated, the smallest length cycle associated with the third bit node is increased from 4-cycle to 6-cycle. Therefore not only can we reduce the complexity, but we can also improve the performance by enhancing the cycle properties.
In reducing decoding complexity and improving performance, the most important step is the selection of bit nodes to be eliminated. To prevent performance degradation, the probability that the hard decision of an eliminated node is incorrect must be smaller than the decoded bit-error probability for the received Eb/No. So if we assume that the transmitted bit cn = 0, the minimum magnitude of zn for node elimination can be determined as follows.

|zn| = = >

ln

Pr(cn Pr(cn

= =

1 0

at at

l l

s s

iteration) iteration)

ln

1

Pr(hard decision error at l s − Pr(hard decision error at l

iteration) s iteration)

ln

1

α(decoded BER at received Eb/No) − α(decoded BER at received Eb/No)

where α denotes the parameters for performance adjustment between 0 and 1 . In addition, we can consider the following conditions to improve the effectiveness of node elimination. -The signs of Fn and Lmn for all m ∈ M (n) are the same. -All associated check equations are satisfied. -All of the above conditions are satisfied during two consecutive iterations.

3. SIMULATION RESULTS AND CONCLUSIONS
The simulation results of the comparisons between the BP algorithm and the proposed modified BP algorithm are here

number of cycles

2000 1800 1600 1400

cycle 6 cycle 8 cycle 10 cycle 12 cycle 14

1200

1000

800

600

400

200

0

0

2

4

6

8

10

12

14

iteration

Fig. 5. Enhancement of cycle properties through iteration

presented. Figure 3 shows the performance of a rate 0.5 LDPC code of length 2304 used in IEEE standard 802.16e over AWGN channel [3]. For the proposed algorithm, α = 10−3 and α = 10−7 are used. The BER performances of the
proposed modified algorithm outperform those of the BP algorithm in the high-Eb/No region. However, the FER performance for α = 10−3 is slightly worse than that of the BP
algorithm since few incorrectly eliminated bit nodes cause
entire decoding failure. For the more strict eliminating condition α = 10−7, the FER performance of the proposed
algorithm is improved over that of the BP algorithm. Figure
4 shows the number of eliminated bit nodes at 1.5 dB for the proposed algorithm. At the 14th iteration for α = 10−3, about 30% of the bit nodes are eliminated, so that we can reduce the computational complexity by 30%. Figure 5 shows
the enhancement of the cycle properties of the proposed algorithm with α = 10−3. The smallest-length cycle associ-
ated with the bit nodes for the original code is only 6 and 8

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 19:07:59 UTC from IEEE Xplore. Restrictions apply.

cycles. However, by the bit node elimination, the number of 6 and 8 cycles are decreased and the number of 10, 12 and 14 cycles are increased.
These results show that the proposed algorithm can reduce decoding complexity and also improve performance.
4. REFERENCES [1] R. G. Gallager, “Low-density parity-check codes,” IRE
Trans. Inform. Theory, vol. IT-B, pp. 21-28, Jan. 1962. [2] J. Chen, M. P. C. Fossorier, “Near Optimum Univer-
sal Belief Propagation Based Decoding of Low-Density Parity Check Codes,” IEEE Trans. Comm., vol. 50, pp. 406-414, Mar. 2002. [3] IEEE 802.16e-2005, ”IEEE Standard for Local and metropolitan area networks - Part 16: Air Interface for Fixed and Mobile Broadband Wireless Access Systems”, Feb. 2006
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 19:07:59 UTC from IEEE Xplore. Restrictions apply.

