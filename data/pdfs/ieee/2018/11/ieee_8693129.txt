A Further Research on Two-phase Decoding for GC-LDPC Codes

Wei Zhou, Lijun Zhang, Qiang Sun School of Electronics and Information Engineering, Beijing Jiaotong University, Beijing, China
{16120194, ljzhang, qsun}@bjtu.edu.cn

Abstract—For GC-LDPC code, a targeted decoding scheme is the two-phase decoding scheme, i.e., the local decoding phase and the global decoding phase. For both the phases, we find the direct application of log-domin belief propagation (BP) algorithm will lead to error. Hence, we propose an improved log-domin BP algorithm and it will be used in the two-phase decoding scheme. Since the two-phase decoding scheme has a large gain loss, we present a modified two-phase decoding scheme in order to further accelerate the convergence rate. Simulation results show that the modified two-phase decoder has a gain of about 0.2 dB compared to the two-phase decoder. Moreover, it also can reduce complexity by 33.4% in high SNR compared with the whole decoder.
Keywords—GC-LDPC code, BP algorithm, the two-phase decoding algorithm, the local decoder, the global decoder
I. INTRODUCTION
Globally coupled low-density parity-check (GC-LPDC) codes were first proposed by Shu Lin[1, 2]. For GC-LDPC code, its parity-check matrix consists of a number of small disjoint blocks which are connected together by a coupled block. There are two types of GC-LDPC code. One is based on check-nodes GC-LDPC code (CN-based GC-LDPC), the other is based on variable-nodes GC-LDPC code (VN-based GC-LDPC). For CN-based GC-LDPC code, a group of overall check-nodes can give the only links between any two disjoint blocks denoted by global CNs. For VN-based GC-LDPC code, a group of overall variable-nodes can give the only links between any two disjoint blocks denoted by global VNs.
In 2016, finite fields (FF) based GC-LDPC codes were proposed in [1]. In 2017, the GC-LDPC codes were constructed based on RS codes[3-5]. A two-phase decoding scheme can be applied to decode these GC-LDPC codes. With intuition, this two-phase decoding algorithm can significantly reduce the complexity of decoding. However, in [1], the twophase decoding scheme has not been specifically implemented. Therefore, in this paper, we will use the two-phase decoding algorithm to decode and prove that it can reduce complexity, and propose a modified two-phase decoding algorithm.
As we know, compared to the min-sum algorithm (MSA), the log-domin belief propagation (BP) algorithm has the better performance [5-8]. Hence, we choose log-domin BP algorithm as the basic algorithm of decoding. When using the two-phase decoding scheme to decode the CN-based GC-LDPC code, we find that the direct application of the log-domin BP algorithm
This work is supported by NSFC under grant number U1534201.
978-1-5386-6063-8/18/$31.00 ©2018 IEEE

leads to errors. Hence, we propose an improved log-domin BP (imp-BP) algorithm. Based on the imp-BP algorithm, the twophase decoding scheme can be applied to decode CN-based GC-LDPC code. In order to further accelerate the convergence rate and approach to Shannon limit [8], we presented a modified two-phase decoding algorithm.

II. PRELIMINARIES
In general, the parity-check matrix H of GC-LDPC codes can be divided into two parts, the upper matrix and the lower matrix or the left matrix and the right matrix, as shown in (1) and (2), respectively. We call R the local matrix, X the global matrix, and H the whole matrix. They correspond to the local decoder, the global decoder, and the whole decoder, respectively.
In (1), if R is an mu n matrix and X is an s u nk matrix, Hgc,cn is an (mk  s)u nk matrix. There are k copies of the
matrix R which are independent of each other and they can only be connected through the s check nodes of the matrix X. In (2), if R is an mu n matrix and X is an mk u s matrix, Hgc,vn is an mk u (nk  s) matrix. There are k copies of the matrix R
which are independent of each other and they can only be connected through the s variable nodes of the matrix X. But in this paper, we only further research on (1).

ªR

º

« «

R

» »

H gc,cn

« «

«

» R»»

(1)

«¬

X »¼

ªR

º

«

H gc,vn

« «

R

X

» »

»

(2)

« ¬

R

» ¼

III. A FURTHER RESEARCH OF TWO-PHASE DECODING
A. The Improved Log-domin Belief Propagation Algorithm According to (1), the local matrix and the global matrix are

128
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 12:23:54 UTC from IEEE Xplore. Restrictions apply.

Fig. 1. A VN decoder.

both submatrices of the whole matrix. Hence, the parity-check relationships of the local decoder and the global decoder are both incomplete. As we know, floating overflow may occur in a simulation tool. During VN update, incomplete parity-check relationship and floating overflow maybe lead to negative infinity (-INF) LLR plus positive infinity (+INF) LLR. As shown in Fig.1, Ljoi (INF)  a  Lj  (INF) . (a is a finite
floating point number.) But (INF)  a  Lj  (INF) is Not
A Number (NAN) in the simulation tool and it also leads to wrong hard decision. So the log-domin BP algorithm cannot be used directly in the two-phase decoding scheme.
Hence, we propose the improved log-domin BP (imp-BP) algorithm. In the VN update, if Ljoi NAN , let Ljoi 0 as
shown in (3). In other words, this VN node does not provide information for its neighbor nodes in this iteration. The other steps are as the same as the Gallager Sum-Product Algorithm. The imp-BP algorithm will be applied in the two-phase decoding scheme and the modified two-phase decoding scheme in the next.

Ljoi

°­

0

, if Ljoi

¦ ®
°¯

Lj



Lico j

icN ( j ){i}

, else.

NAN

(3)

B. The Two-phase Decoding Scheme
From the local/global point of view, the two-phase iterative decoding scheme can be designed to correct local and global random errors. The local decoding phase is a process of correcting local errors (or erasures). The local errors are distributed randomly among k sections of a received codeword, each section containing few random errors (or erasures). Once the local decoders failed, the global decoder will be activated.
There are two possible methods to transmit information between the local phase and the global phase. The first method

Fig. 2. Error performance of the GC-LDPC codes given in Example 1.
is the output iteration information of all local decoders as the input vector of the global decoder, whether or not they are successfully decoded. The other method is that the input vector of the global decoder is a combination of channel information and reliable information successfully decoded by the local decoder.
Suppose a codeword v (v0 , v1, , vk1) is transmitted. Let r (r0 , r1, , rk1) be the received vector. Next, thelocal decoding phase will begin. For 0 d i  k , each receivedsection ri will be
decoded by a local iterative decoder. Theselocal decoders have a fixed number of iterations denoted byImax.If all the local decoders successfully decoded, we can get a local codeword, v* (v0*,v1*, , v*k1) which can be formed by the k sections of all local decoders. Then, we will check if v* satisfies the s global CN-constraints corresponding to the s rows parity-check constraints. If it does, v* is a codeword and it will be delivered to the user. If it doesn't, a global decoder will be activated. This global decoder will apply the iteration information (LLR) of all local decoders as input.
Example 1: According to [9], let m = 181, n = 543, and k = 3. Hence, the matrix R is 181u543 with column weight 4 and the matrix X is 181u1629 with column weight 3. The matrix Hgc,cn is 724u1629 with column weight 7 and two row weights, 12 and 27. The null space of matrix Hgc,cn gives a (1629, 908) CN-based GC-LDPC code with rate 0.5574. The BER of this code using the two-phase decoder with Imaxover the AWGN channel is shown in Fig.2. (Unless otherwise stated, the maximum number of iterations for the local decoder and the global decoder are both 50.) In addition, the BER of this code using the whole decoder with Imax = 50 and Imax = 10 are also included in Fig.2.
With measurements taken at a BER of 10-6, the performance gap between the two-phase decoder and the whole

129
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 12:23:54 UTC from IEEE Xplore. Restrictions apply.

decoder with Imax = 50 is about 0.5 dB and the performance gap between the two-phase decoder and the whole decoder with Imax = 10 is about 0.2 dB.

C. The Modified Two-phase Decoding Scheme
In order to further accelerate the convergence rate, we propose a modified two-phase decoding scheme. Suppose a codeword v (v0 ,v1, , vk1) is transmitted. Let r (r0 , r1,
, rk1) be the received vector. This local phase decoding is the same as the local phase decoding of the two-phase algorithm. Similarly, if all the local decoders successfully decode, we can get a local codeword v* (v0*, v1*, , vk*1) which can be formed by the k sections of all local decoders. If v* satisfies the s global CN-constraints, v* is a codeword and it will be delivered to users.

If one or more sections are unsuccessfully decoded or all decoded sections successfully decoded but the s global CNconstraints cannot be satisfied by v* , a global decoder will be

activated. Let

yout local

[ y , y , out

out

local ,0 local ,1

, y ] out local ,k 1

be

decoded

information (LLRs) of all sections. In order to smooth the

change of information (LLRs) in the iteration of decoding,

y out local

will

be

compressed

nonlinearly

and

denoted

by

y , in global

i.e.,

yin global

F

(

yout local

)

.

This

nonlinear

compression

function

is

F A
(1/ (1 ewy )  0.5) , where A and w can be determined

by

simulations.

In

the

global

decoding

phase,

y in global

will

be

decoded by a global decoder. Similarly, the global decoder also

has a fixed maximum number of iterations. Consequently,

y in global

can

be

used

as

the

input

vector

of

the

global

decoder.

Let

y out global

be

the

decoded

information

(LLRs)

of

the

global

decoder and vˆ (vˆ0 ,vˆ1, , vˆk1) be a global codeword of the

global decoder. If the global decoder successfully decoded, the global codeword vˆ will be checked by m local CN-constraints

corresponding to the m rows parity-check constraints. If it does, vˆ is a codeword and it will be delivered to the user.

If not, we also check all sections of vˆ by the matrix R.
Hence, we can know which sections cannot satisfy the m local CN-constraints. In the partial local phase of decoding, only sections failed in the global decoder will be re-decoded using the local decoders with a fixed number of iterations. Information (LLRs) from the global phase to the partial local phase will also be nonlinearly compressed.

Example 2: Considering example 1, let A = 50 and w = 0.09. The BER of this code using the modified two-phase decoder with Imax = 50 over the AWGN channel is shown in Fig.3. In addition, the BER of this code using the whole decoder with Imax = 50 and Imax = 10 are also included in Fig.3.

From Fig.3, the performance of the modified two-phase decoder relative to the two-phase decoder is improved by 0.2 dB. With measurements taken at a BER of 10-6, the gap of performance between the modified two-phase decoder and the whole decoder with Imax = 50 is about 0.3 dB and the performance of the modified two-phase decoder is similar to t-

Fig. 3. Error performance of the GC-LDPC codes given in Example 2
hat of the whole decoder with Imax = 10.
D. Complexity Analysis
Conventional BP decoding has about 6nvdv floating-point multiplications per iteration, where dv is the column weight of the parity-check matrix and nv is the number of variablenod-
es[10].Hence, the total multiplication operations is &  6u nv
udv u Ns , where Ns is the average number of iterations. We neglect the computation of nonlinear compression function F. For example 2, in high SNR such as 4.3 dB, 99.4% of all received vectors can be successfully decoded only using the local decoders. Moreover, in 4.3 dB, the average numbers of iterations of the local decoder is Nlocal = 3.7009 and the average numbers of iterations of the whole decoder with 50 iterations is Nwhole 3.2 . The multiplication operations of the modified two-phase decoder is about Clocal 6u nlocal u dlocal uNlocal u k , where dlocal = 4 is the column weight of the matrix R, nlocal = 543 is the number of variable nodes in the matrix R, and k = 3. The multiplication operations of the whole decoder is about Cwhole 6u nv u dv u Nwhole , where dv = 7 is the column weight of the matrix H and nv = 1629 is the number of variable nodes in the matrix H. Hence, the percentage reduction of multiplication operations is approximate 1 Clocal 33.4% . Therefore, with 0.3 dB gain loss, the
Cwhole proposed scheme can reduce complexity by 33.4%.
IV. CONCLUSIONS
Considering floating overflow and incomplete parity-check relationship, we propose the imp-BP algorithm. The two-phase decoding scheme based on the imp-BP as basic decoding

130
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 12:23:54 UTC from IEEE Xplore. Restrictions apply.

algorithm can be used to decoded CN-based GC-LDPC code. For narrowing the gap with the whole decoder, we present the modified two-phase decoding scheme which includes three phases, i.e., the local phase, the global phase, and the partial local phase. Simulation results show that the modified twophase decoder with 0.3 dB gain loss can reduce complexity by 33.4% compared with the whole decoder. Owing to low complexity, the modified two-phase decoder faces a promising perspective.
REFERENCES
[1] J. Li, S. Lin, K. Abdel-Ghaffar, W. E. Ryan, and D. J. Costello, Jr, "Globally Coupled LDPC Codes," in Proc. Inf. Theory Applic. Workshop, San Diego, CA., Jan. 31 - Feb. 5, 2016.
[2] J. Li, S. Lin, K. Abdel-Ghaffar, W. E. Ryan, and D. J. Costello, Jr, LDPC Code Designs, Constructions, and Unification, Cambridge, UK: Cambridge University Press, 2017.
[3] S. Lin and D. J. Costello, Jr., Error Control Coding: Fundamentals and

Applications, 2nd edition. Upper Saddle River, NJ: Prentice Hall, 2004
[4] J Li, K Liu, S Lin, K Abdel-Ghaffar, "Reed-Solomon based globally coupled quasi-cyclic LDPC codes." Information Theory and Applications Workshop IEEE, pp.1-10, 2017.
[5] Li, S. Lin, K. Abdel-Ghaffar, W. E. Ryan, and D. J. Costello, Jr, LDPC Code Designs Construcrions, and Unification, UK,:Cambridge Univ. Press, 2014.
[6] M. Fossorier, M. Mihaljevic, and H. Imai, "Reduced complexity iterative decoding of low density parity check nodes," IEEE Trans. Commun., vol.47,no.5,pp.673-680,1999
[7] T. Richardson and R. Urbanke, "The capacity of low-density paritycheck codes under message-passing decoding," IEEE Trans. Inf. Theory, vol.47, no.2, pp.599-618, Feb.2011
[8] W. E. Ryan and S. Lin, Channel Codes: Classical and Modern, New York: Cambridge Univ. Press, 2009.
[9] W. Zhou, L.J.Zhang, "Construction of Replacement Set in GC-LDPC Codes Based on BIBDs," unpublished.
[10] D. J. MacKay, "Good error correction codes based on very sparse matrices," IEEE Trans. Inf. Theory, vol. 45, no. 2, pp. 399–431, Feb. 1999.

131
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 12:23:54 UTC from IEEE Xplore. Restrictions apply.

