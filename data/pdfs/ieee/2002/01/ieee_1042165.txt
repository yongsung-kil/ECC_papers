2292

IEEE TRANSACTIONS ON MAGNETICS, VOL. 38, NO. 5, SEPTEMBER 2002

Cycle-Slip-Detector-Aided Iterative Timing Recovery
Xiaowei Jin and Aleksandar Kavcˇic´, Member, IEEE

Abstract—The latest developments in low-density parity-check codes have shown performances very close to the channel capacity. However, in order to use these capacity-approaching codes in the magnetic recording channel, the cycle-slip problem in the synchronizer has to be solved. In this paper, we develop a cycle-slip detector (CSD) for the binary intersymbol interference channel that uses the channel soft-output information as its input. This CSD is then used in an iterative timing recovery scheme to eliminate cycle slips. Our simulation results show that utilizing the CSD improves the convergence speed of iterative timing recovery.
Index Terms—Cycle-slip detection, intersymbol interference, iterative timing recovery, low-density parity-check (LDPC).

II. CHANNEL-TIMING MODEL
Let the binary input sequence { } be a sequence of equally probable independent identically distributed (i.i.d.) symbols that take values from { 1,1}. Assuming the channel is equalized to an ideal PR4 response and corrupted by additive white Gaussian noise, the received noisy pulse amplitude modulated (PAM) waveform is given by
(1)

I. INTRODUCTION

R ECENT research on low-density parity-check (LDPC) codes and turbo codes has shown that properly con-

structed codes can approach the capacities of both the additive

white Gaussian noise (AWGN) channel [1], [2] and the partial

response (PR) channel [3]. Since these capacity-approaching

codes can operate at very low signal-to-noise ratios (SNRs),

symbol synchronization at low SNRs becomes critical in order

to utilize their full-coding gain potentials. In the magnetic

recording channel, conventional timing-recovery devices (syn-

chronizers) tend to experience cycle slips at low SNRs, which

always results in a burst of symbol detection errors and, thus,

a burst of decoding errors. In [4], we developed a Bayesian

detection rule to detect cycle slips in the memoryless AWGN

channel by using soft-output information. Here, we generalize

the method and develop the cycle-slip detector (CSD) for the

ISI channel. We investigate the binary input PR4 (

)

channel where a rate 4/5 LDPC code with block length of 5120

is used.

When the channel experiences a slowly time-varying phase

drift, a typical phase tracking solution is the Mueller and Müller

(M&M) synchronizer [5]. This synchronizer may be used in an

iterative decoding and timing-recovery scheme, as in [6]. In this

paper, we show how to accelerate the decoding convergence by

utilizing the CSD.

The paper is organized as follows. In Section II, we give

the channel model for an ISI channel with timing errors. Sec-

tion III derives an approximate analytic form for the symbol-by-

symbol a posteriori probability (APP) and formulates the CSD

for the ISI channel. Section IV introduces a CSD-aided itera-

tive timing-recovery algorithm and provides simulation results.

Section V concludes the paper.

Here, is zero mean white Gaussian noise and

,

where is a slowly varying phase drift. For the derivations

in Section II, it is irrelevant what functional or stochastic form

takes, as long as it is slowly time varying. We assume the

pulse to be

Since

is unknown to the receiver, a timing-recovery device [5] is used

in the receiver to estimate the unknown phase drift in order to

determine the sampling instants.

The signal in (1) is matched filtered and sampled at time in-

stants

to produce an output sample (where “ ” can

be substituted by “ ” only if

)

(2)

Here, is the estimate of , and is i.i.d.

, where

denotes that the variable is normally distributed

with mean and variance . Denote the timing error by

. Since is slowly time varying for values of near

, we have

. For values

, the value of

is negligible, so (2) becomes

(3)

Since is cyclostationary with period (see [7]), the syn-

chronizer has stable operating points at

ZZ. At the start

of the waveform, at

, we assume a perfectly synchro-

nized system

. A cycle slip happens when the value of

changes from near 0 to near 1. Usually, takes continuous

values. For the purpose of classification, we discretize this con-

tinuum into two hypotheses, and

no cycle slip

Manuscript received February 13, 2002; revised May 19, 2002. This work was supported by the National Science Foundation under Grant no. CCR-9984297.
The authors are with the Division of Engineering and Applied Sciences, Harvard University, Cambridge, MA 02138 USA (e-mail: xjin@eecs.harvard.edu).
Digital Object Identifier 10.1109/TMAG.2002.801869.

cycle slip
where and are predetermined constants. The task of the CSD at any time is to decide whether or is a better hypothesis after observing the received waveform.

0018-9464/02$17.00 © 2002 IEEE Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:17:36 UTC from IEEE Xplore. Restrictions apply.

JIN AND KAVCˇ IC´ : CSD-AIDED ITERATIVE TIMING RECOVERY

2293

III. CYCLE-SLIP DETECTION FOR ISI CHANNELS
In [4], we developed a CSD for memoryless AWGN channels that uses the channel soft-output information as its input. A similar approach can also be applied to an ISI channel. We define the soft information for the symbol as the log-likelihood ratio of the a posteriori probabilities of . Assuming perfect timing, i.e.,

where

, is the threshold, and

is the number of soft-information values needed to detect

cycle slips. We assume that within the window of size ,

stays constant. Then from (8), by the central limit theorem [10],

for large, is approximately a Gaussian random variable

distributed as

(9)

(4) where

where is the block length. The value can be calculated by

the BCJR algorithm [8]. It is very hard to find an analytic form

for the probability density function (pdf) of , which prevents

us from applying directly the approach developed in [4] to the

ISI channel. We, therefore, use an approximate expression for

. We assume that the ISI channel has

tap-coefficients

. An approximate expression for was derived in

[9]

(10) (11)
The values of and can be analytically computed (see [4]). We define the probability of detection as the probability that is chosen when is true, and define the probability of false alarm as the probability that is chosen when is true. We can express and as

(5)

(12)

Here, is the hard decision (i.e., an estimate) of . For the PR4

channel,

and

,

,

. Then

(13)

(6)
Now we can derive the pdf of following an approach similar to [4]. Substituting (3) into (6), we have

(7)

Here,

is

by the sampling theorem for band-limited Gaussian

random processes [10]. We assume that and have the

same probability distribution as

and , respectively.

Then, under hypothesis

, we substitute

into (7) and approximate the pdf

by a Gaussian

mixture

. It follows that

where

. In order to design a good

CSD that reliably detects cycle slips, we need to choose and

so that is high and is low. In general, a longer window

size gives higher and lower . However, the assump-

tion that stays constant the window may become invalid

if is too long. Given (12) and (13), similar to [4], we can find

the threshold and the lowest window size that satisfy the

desired specification of and .

In our simulation, we set

,

,

.

For an SNR of 8.6 dB,1 the threshold is

and the lowest

window length is 90. A forward-only APP algorithm [11]

with survival memory-length six is used as the timing-loop de-

tector. It makes preliminary decisions of input symbols. The

same detector with survival memory 30 is used in the iterative

decoder. At each time , (

), the CSD computes and

updates the value of according to (8). If is greater than ,

then the CSD fires. Here, “fire” means that the CSD reports a

cycle slip. In general, the CSD keeps firing for a period of time,

which we call the cycle-slip region.

IV. CSD-AIDED ITERATIVE TIMING RECOVERY

where and denote the mean and variance, respectively. We use the same decision rule as the one developed in [4]

In [6], an iterative timing recovery scheme is proposed, where the soft information is exchanged iteratively between the LDPC decoder and the synchronizer. We use this method, but improve on it using the CSD. The CSD can detect the position of the cycle slip. We use the knowledge of the cycle-slip position to achieve faster decoding. The algorithm proceeds as follows.

(8) 1SNR is defined as 10 log P=2, where P is the power of the signal. For
PR4 channels, we assume P = 2.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:17:36 UTC from IEEE Xplore. Restrictions apply.

2294

IEEE TRANSACTIONS ON MAGNETICS, VOL. 38, NO. 5, SEPTEMBER 2002

CSD-Aided Iterative Timing Recovery Algorithm

1. Sample the waveform z(t) to get a sequence of samples {z }

2. Run the CSD.

3. IF the CSD fires,

 a) Record the position  of the firing instance.

z

if k 

b) Create: z = z z

 if k >  :
if k 

c) Create:z = z

if k >  :

d) Perform iterative decoding (turbo equalization

and LDPC decoding) on {z }, {z }, {z } to get {a^ }, {a^ }, {a^ }, respectively.
e) if one of the sequences {a^ }, {a^ }, {a^ } satis-

fies all parity checks, stop.

else -resample z(t) using {a^ } as prior information to
get a new sequence {z }. -resample z(t) using {a^ } as prior information to
get a new sequence {z }

-resample z(t) using {a^ } as prior information to get a new sequence {z }.

-go back to step d).

ELSE

f) Perform iterative decoding operations on {z } to get a decoded sequence {z^ }.
g) if the sequence {a^ } satisfies all parity

checks, stop.

else

-resample z(t) using {a } as prior information to

get a new sequence {z }.

-go to f).

Fig. 1. Performance comparison between non-CSD-aided [6] and CSD-aided iterative timing recovery. A rate 4/5 LDPC code is used.
TABLE I CONVERGENCE RATE COMPARISON
systems with LDPC codes. The CSD-aided iterative timing recovery algorithm is then introduced. This method uses the idea as in [6] and applies the CSD to get good initial values for iterative timing recovery. Our simulation results showed that the method accelerates the decoding convergence without compromising the performance.

The algorithm described above assumes that at most one cycle

slip happens in each coded block. For the LDPC code with rate

4/5 and block length 5120, our simulation results showed that

having two cycle slips in one block is very unlikely. This ob-

servation ensures the simplicity of the algorithm. The iterative

timing recovery scheme in [6] is simply the ELSE part of step

3 of our algorithm. Thus, the two algorithms have the same per-

formance. However, by using the CSD, our proposed scheme

reduces the number of resampling iterations significantly with a

slight increase in the receiver complexity in order to implement

the CSD. Fig. 1 and Table I present our simulation results. In

Table I, the number of synchronization iterations stands for the

number of times the waveform is resampled. The timing offset

in these simulations was chosen to be

.

V. CONCLUSION

We have described a cycle-slip detection algorithm for the ISI

channel that uses soft-output information as its input. In gen-

eral, a long delay (

) is needed in order to detect cycle

slips with high reliability. Since the iterative LDPC decoder in-

troduces a much longer delay, the CSD algorithm works well in

REFERENCES
[1] T. Richardson, A. Shokrollahi, and R. Urbanke, “Design of capacity-approaching irregular low-density parity-check codes,” IEEE Trans. Inform. Theory, vol. 47, pp. 619–637, Feb. 2001.
[2] S.-Y. Chung, “On the construction of some capacity-approaching coding schemes,” Ph.D. dissertation, MIT, Cambridge, MA, 2000.
[3] A. Kavcˇic´, X. Ma, and M. Mitzenmacher, “Binary intersymbol interference channels: Gallager codes, density evolution and code performance bounds,” IEEE Trans. Inform. Theory, p. 345, June 2001.
[4] X. Jin and A. Kavcˇic´, “Cycle-slip detection using soft-output information,” Proc. IEEE ICC’01, pp. 2706–2710, June 2001.
[5] K. H. Mueller and M. Müller, “Timing recovery in digital synchronous data receivers,” IEEE Trans. Commun., vol. COM-24, pp. 516–531, May 1976.
[6] A. R. Nayak, J. R. Barry, and S. W. McLaughlin, “Joint timing recovery and turbo equalization for coded partial response channels,” IEEE Trans. Magn., vol. 48, pp. 2295–2297, Sept. 2002.
[7] H. Meyr, M. Moeneclaey, and S. A. Fechtel, Digital Communication Receivers. New York: Wiley, 1997.
[8] L. R. Bahl, J. Cocke, F. Jelinek, and J. Raviv, “Optimal decoding of linear codes for minimizing symbol error rate,” IEEE Trans. Inform. Theory, vol. IT-20, pp. 284–287, Sept. 1974.
[9] Z. N. Wu and J. M. Cioffi, “Turbo decision aided equalization for magnetic recording channels,” in Proc. GLOBECOM, 1999, pp. 733–738.
[10] J. G. Proakis, Digital Communications. New York: McGraw-Hill, 1997.
[11] X. Ma and A. Kavcˇic´, “Forward-only path partitions and treillis algorithms,” IEEE Trans. Inform. Theory, to be published.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:17:36 UTC from IEEE Xplore. Restrictions apply.

