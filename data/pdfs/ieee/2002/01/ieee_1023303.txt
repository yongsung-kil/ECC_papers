ISlT 2002, Lausanne, Switzerland, June 30 -July 5,2002

An Approximate Analytical Model of the Message Passing Decoder of LDPC Codes

Frederic Lehmann and Gian Mario Maggio’ CWC - University of California, San Diego La Jolla, CA 92093-0407, USA,
e-mail: 1ehmannQece.ucsd.edu, gmaggio0ucsd.edu

Abstract - In this paper we introduce a novel onedimensional model of the message passing decoding algorithm of low-density parity-check (LDPC) codes, based on Gaussian densities. The model consists of a closed-form 1-D map whose iterates directly represent the error probability. This map allows a qualitative analysis of the nonlinear dynamics of the decoding algorithm. Moreover, it is shown that our approach leads to the correct stability condition and that the corresponding threshold values are in good agreement with density evolution.

Recently, it has been recognized that the message passing decoding algorithm of both regular and irregular low-density parity-check codes can be analyzed through a numerical procedure known as density evolution [l].In order to simplify the analysis and get some insight into the dynamics of the message passing algorithm, analytical methods have been developed. These methods approximate the densities of the messages by Gaussians, which makes the calculation of the distributions tractable [2, 31.

Here, we propose a Gaussian approximation based on a closed-form expression in terms of error probabilities.

Assume d, (resp. d,) is the maximum variable (resp.

check) node degree; we denote the variable (resp. check)

degree distribution polynomial of the graph by: X(z) =

E$, XZzZ-’ (resp. p(z) =

p Z z 2 - 1 ) [l].Throughout the

paper, we restrict ourselves to a binary-input (binary 0 + +1,

binary 1 + -1) AWGN channel and denote by U the noise

standard deviation. If the block length of the code tends to

infinity, a concentration theorem ensures that the messages

exchanged in the decoder a t every iteration are i.i.d. (indepen-

dent identically distributed) random variables. Without loss

of generality, we suppose that the all-zero codeword is sent,

therefore the error probability P i ( u ) at iteration 1 is simply

the average probability that the variable node messages are

negative [11.

Under these assumptions, we show that a nonlinear onedimensional map of the form:

describes the dynamics of the message passing algorithm in
terms of error probability, with P,“(u)= Q ($) and Q(z) =
’This work was supported by STMicroelectronics, Inc. and CIFRE convention 98300.

& Jz+me

-

t
T

Z
d

d

t

.

The nonlinear map f(z;U ) is defined as:

11’

where U acts as a control parameter. We study the convergence properties of the message passing
algorithm by means of the partial derivatives of the map. In particular, we show that z = 0 is a stable fixed point of the
map (2) if and only if X’(O)p’(l) < e*. Moreover, there is a threshold O* = sup {U > 0 : liml-+m P~(o=) O}. We
emphasize that both results are in agreement with density evolution. In our talk, we illustrate the threshold effect for a (d, = 3 , d c = 27) rate regular LDPC code and show that a saddle-node bifurcation occurs [4].
Finally, in Table I we give the thresholds for rate R =
% regular LDPC codes with d, = 3, for m = 1,... , 6 . It can
be seen that the proposed model estimates the threshold
with accuracy between 0.1 and 0.3 dB.

Table 1: Thresholds of regular LDPC codes obtained with
3. density evolution (ohE)and Gaussian analysis (oEA)
with their corresponding

(%)* dv dc rate ugE

(%)*

3 6 1/2 0.880 1.11 dB 0.848 1.43 dB

REFERENCES
[l] T.J. Richardson, M.A. Shokrollahi and R.L. Urbanke, “Design of capacity-approaching irregular low-density parity-check codes,” IEEE Trans. I f . Theory, vol. 47, no. 2, pp. 619-637, February 2001.
[2] S.-Y. Chung, T . J . Richardson and R.L. Urbanke, “Analysis of sum-product decoding of low-density parity-check codes using a Gaussian approximation,” IEEE Trans. Inf. Theory, vol. 47, no. 2, pp. 657-670, February 2001.
[3] D. Divsalar, S. Dolinar and F. Pollara, “Iterative turbo decoder analysis based on density evolution,” J. Select. Areas Comm., vol. 19, no. 5, pp. 891-907, May 2001.
[4] S.H. Strogatz, Nonlinear dynamics and chaos. Cambridge, MA, Perseus publishing, 1994.

31
0-7803-7501-7/02/$17.00@2002 IEEE.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:56:58 UTC from IEEE Xplore. Restrictions apply.

