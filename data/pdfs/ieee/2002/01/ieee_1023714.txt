ISlT 2002, Lausanne, Switzerland, June 30 -July 5,2002

Low-Density Parity-Check Codes for Multilevel Modulation

E. Eleftheriou and S. Olqer
IBM Research, Zurich Research Laboratory 8803 Ruschlikon, Switzerland

Abstract - The application of low-density parity-check (LDPC) codes to multilevel modulation systems is studied. A family of binary LDPC codes that offer good performance and do not suffer from error-floor effects at low bit-error rates is introduced. A number of properties render these codes attractive for practical applications: they are specified via a small number of parameters, are encodable in linear time, and

complex QAM symbol to be transmitted. The L-ary symbols

belong to the set A = ( A , = 2!-(L-1),

= 0,1, ..., 15-11 .

The real part of a QAM symbol conveys mr code bits and
mh uncoded bits, with mr + m ; = m . Similarly, the

imaginary part conveys m: code bits and m; uncoded bits,
with m: + m i = m . Symbol mapping relies on the partition

the generation of their parity-check matrix is deterministic, involving a small number of preprocessing operations. In addition, the symbol-mapping technique based on “double Gray-code labeling” for obtaining coded multilevel transmit symbols is described. Finally, it is shown that the proposed LDPC coding technique is well suited for multicarrier digitalsubscriber-line transmission applications as it permits a wide range of trade-offs between latency, complexity, and system performance.

of the set A into 2”; [ 2”; ] subsets such that the minimum Euclidean distance between the symbols in each subset is maximized. The mr [ m: ] less significant bits of v [w]label the subsets of A following a Gray-coding rule. The remaining
more significant bits m h [mh ] label symbols within a subset
following a separate Gray-coding rule. When b = 1, only the code bit vo is employed, and BPSK modulation is performed. By analyzing the capacity of the resulting bit-interleaved

SUMMARY
It has been shown that array codes [ l ] can be viewed as sparse binary block codes and thus can be decoded as LDPC codes [2] using the sum-product algorithm. The parity-check matrix defining the family of LDPC codes used in this paper is based on the notion of array codes. In particular, the following parity-check matrix is introduced [3]:

channel [4], it can be shown that this “double Gray-code labeling” approach allows a good trade-off in terms of achievable performance versus implementation complexity. LDPC decoding is achieved by the sum-product algorithm [2, 51, employing as soft input the a posteriori probabilities of individual code bits derived from received noisy QAM symbols.

The proposed scheme has been applied to coded

multicarrier modulation for digital-subscriber-line (DSL) systems [3], where symbol constellations of possibly different

sizes (typically from 2 to 214 symbols) are used for quadrature

amplitude modulation on each subcarrier. Simulations

where p denotes a prime number, k and j are two integers such
that k, j I p , Z is the p x p identity matrix, 0 the p x p null
matrix, and a p x p permutation matrix representing a single
left or right cyclic shift. The upper triangular nature of H

established that for a wide range of LDPC code parameters no

error floors exist at error rates of

i.e., at the rates of

interest for DSL systems. For I6-QAM, for example, a net

coding gain of -6 dB is achieved at a symbol error-rate of lo-’

with a (2209, 2021) LDPC code. For longer codes, but under a

latency constraint of 10 ms, the net coding gain can typically

be increased to -6.4 dB.

guarantees encoding that is linear in time, requiring
(N2) [ r (j+ 3)+ (j- 3)] XOR operations, where N and r denote

REFERENCES

the code length and rate, respectively. Furthermore, it can be
shown that the introduction of the 0 block-elements in H does
not lead to a decrease in the minimum distance of the code

[ 13 J. L. Fan, “Array codes as low-density parity-check codes,” in Proc. 2nd Int’l Symposium on Turbo Codes and Related Topics, Brest, France, pp. 543-546,Sept. 2000.

for j = 3 and 4.

[2] R. G. Gallager, “Low-density parity-check codes,” IRE

Trans. Inform. Theory, vol. IT-8, pp. 21-28, Jan. 1962.

For multilevel modulation, the transmit symbols are chosen [3] E. Eleftheriou and S. OlGer, “Proposed text on LDPC coding

from a 2’-point constellation, where b = 1 or 2m, with m a

for inclusion in Draft Recommendation,” Document SC-065,

positive integer. With square-shaped symbol constellations,

ITU-Telecommunication Standardization Sector, Study

soft demapping at the receiver is greatly simplified because the

Group 15, San Francisco, CA, Aug. 2001.

real and imaginary parts of the received noisy complex signals can be demapped independently. If b = 2m , two binary m-

[4] G. Caire, G. Taricco, and E. Biglieri, “Bit-interleaved coded modulation,” IEEE Trans. Inform. Theory, vol. 44, No. 3, pp.
927-946, May 1998.

tuples v = ( v ~ -v~,,-,~,..., vI, vo) and w = (wm+ w,-~, ...,wl, wo> [5] D. J. C. MacKay, “Good error-correctingcodes based on very

independently select two L-ary real symbols, L = 2”, repre-

sparse matrices,” IEEE Trans. Inform. Theory, vol. 45, No. 2,

senting the real and imaginary parts, respectively, of the

pp. 399-431, Mar. 1999.

0-7803-7501-7102/$17.00@2002IEEE.

442

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 09:59:46 UTC from IEEE Xplore. Restrictions apply.

