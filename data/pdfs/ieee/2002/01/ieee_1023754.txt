ISlT 2002, Lausanne, Switzerland, June 30 -July 5,2002

On Algebraic Construction of Gallager Low Density Parity Check Codes
Heng Tang, Jun Xu, Yu Kou, Shu Lin and Khaled Abdel-Ghaffar Department of Electrical and Computer Engineering University of California, Davis, CA 95616, USA
E-mail: hngtang, junxu, ykou, shulin, ghaffar@ece.ucdavis.edu

Abstract - This paper presents two classes of low density parity check codes. Codes in these two classes perform well with iterative decoding.

I. EUCLIDEAGNEOMETRIESAND PARALLEL STRUCTURE

Consider the m-dimensional Euclidean geometry (EG) over GF(pS)

[l], denoted EG(m,p"). This geometry consists of n = pmS points

andp("-l)"(pmS - l)/(p" - 1)lines. Each point is an m-tuple over

GF(pS). Every line is either a one-dimensional subspace or a coset

of a one-dimensional subspace of the vector space S of all the m -

tuples over GF(pS). Each line consists of p s points. Two lines are

either disjoint or they intersect at one and only one point. Two lines

that correspond to two cosets of a one-dimensional subspace of S are

Figure 1: Error performance comparison of the (512,256)

said to be parallel. Two parallel lines are disjoint. Since each onedimensional subspace has p("-l)" cosets, there are p("-')" lines in EG(m,pS)that are parallel to each other. These k = p ( m - l ) Sparallel lines are said to form a parallel bundle. The lines in EG(m,pS) can be grouped into K = (p"' - l)/(ps - 1) parallel bundles. Each parallel bundle contains all the p"" points of the geometry, every point appears once and only once.

Gallager EG-LDPC code with three computer generated Gallager codes and a rate-l/;? convolutional code.

widely used in digital communications. The (504,252) computer gen-

erated LDPC code is currently one of the best LDPC codes of rate 1/2

and length 504. The (512,256) E(;-Gallager code outperforms all the

other codes.

Example

2:

11. CONSTRUCTION OF GALLAGECRODES

Figure 2 shows the

The incidence vector of a line C in EG(m,p") is defined as an n-tuple over GF(2), VC.= (211, v2,...,wn), whose components correspond to
the points of EG(m,pS),and vi = 1 if and only if the i-th point of
EG(m,pS) is a point on C and vi = 0 otherwise. The weight of this incidence vector is p = p". Let P I ,Pz, ...,PK denote the K parallel bundles of lines in EG(m, p"). For 1 5 j 5 K , form a k x n
matrix H p ( j )over GF(2), whose rows are the incidence vectors of the lines in the j-th parallel bundle P' . Since the k lines in each parallel bundle are disjoint and they contain all the n points in EG(m,p"), the
weight of each column of H p ( j )is one. Note that n = k x p . For
1 5 y 5 K , form a Icy x k p matrix HGA with H p ( l ) .,..,H p ( y )
as its submatrices in a column. Then H G A is exactly in the form

error performance of two EG-Gallager codes constructed based on 4 and 6 parallel bundles of lines in EG(2,41), respectively. At the BER of the (1681,1520)
code performs 1.3 dB from the Shannon limit and the

2
;,04
5
to-
,
Figure 2:

I

, (I

,o

EbMo Id81

Error performances of the

of the parity check matrix for an LDPC code originally proposed by (1681'1440) 'Ode (1681,1520) and (1681,1440) Gallager EG

Gallager [2]. Since the rows of this matrix correspond to a set of performs 1.7 dB

lines, no two rows (or two columns) in this matrix can have more from the Shannon

than one "1-component'' in common. The null space over GF(2) of limit. For such a short length, they perform very well.

this matrix gives an LDPC code of length n with minimum distance

Let H $ i be the transpose of HGA.The null space of this matrix

at least y+l. This code is called an EG-Gallager code, denoted CGA, gives an LDPC code of length no = yp(m-l)S, called a comple-

+ and its Tanner graph does not contain cycles of length 4 [ 3 ] .For each mentary EG-Gallager code, denoted C$i. The minimum distance of
choice of m, s, and p , the above construction gives a sequence of this code is at least p s 1. As an example, suppose we construct a

EG-Gallager LDPC codes of length n for y = 1, ...,K .

complementary EG-Gallager code using 60 parallel bundles of lines

Example 1 : The geometry EG(3, z3) consists of 512 points and
4672 lines. Each line consists of 8 points. The lines can be grouped

in EG(3, 23). We obtain a (3840,3467) code with rate 0.9028 which
performs 1.1 dB from its Shannon limit at the BER of lop5.

into 73 parallel bundles, each parallel bundle consists of 64 parallel lines. Suppose we choose y = 6 parallel bundles to construct an EG-Gallager code. We obtain a rate-1/2 (512,256) code with minimum distance at least 7. The bit error performance of this code decoded with iterative decoding using the sum-product algorithm (SPA) is shown in Figure 1. Also included in this figure are the bit error performances of three computer generated rate-1/2 LDPC codes of the same length and the well known rate-1/2 64-state convolutional code

REFERENCES
[l] R. G. Gallager, "Low density piuity check codes," IRE Trans. Inform. Theory, vol. IT-8, pp. 21-28, Jan. 1962.
[2] S. Lin and D. J. Costello, Jr, Error Control Coding: Fundamentals and Applications, Prentice Hall, Engkwood Cliffs, NJ., 1983.
[3] R. M. Tanner, "A recursive approach to low complexity codes," IEEE Trans. Inform. Theory, vol. IT-27. pp. 533-547, Sept. 1981.

0-7803-750-17/02/$17.00@2002IEEE.

482

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:04:55 UTC from IEEE Xplore. Restrictions apply.

