ISlT 2002, Lausanne, Switzerland, June 30 -July 5,2002

Iterative Majority Logic Decoding of a Class of Euclidean Geometry Codes
Andrew Thangaraj and Steven W. McLaughlin
School of Electrical and Computer Engineering Georgia Inbstitute of Technology Atlanta, Georgia 30332, USA {andrew,swm)@ece.gatech.edu

Abstract - Hard decision decoding of low density The probability of block error at the output of the second

parity check (LDPC) codes has potential applications iteration (Pz)is given by,

in practical settings like data storage. For this purpose, it is important for the code to have an assured

Pz = Prob{X > 2'-'}.

(2)

minimum distance and, hence, guaranteed error cor- A direct use of Eqn. 2 is complicated by dependence among rection capability. In this paper, we show that with the decoding of different bits. The mean of X , however, can

very high probability the guaranteed error correction be accurately found using linearity of expectation. Using Eqn.

capability of Euclidean Geometry (EG) codes using 1,E [ X ]= n p l . From E [ X ] w, e can make a heuristic argument

threshold-optimized,iterative majority logic (ML) de- t o estimate the number of correctable channel errors.

coding is much greater than the usual single iteration

ML decoding, making these codes much more attrac- A Case I : T = 2s-1

tive for hard decision decoding. For instance, the

(262143,242461,t 2 256) EG code (a (512,512)-regular In this case, all error patterns of weight upto T = 2'-' can

LDPC code) can correct t = 580 bit errors with prob- be corrected. In reality, the decoder corrects almost all error

ability better than 1 - 1 x

patterns of weight upto 70% more than 2"-l. Consider the

case where there are t = 500 channel errors for the s = 9

I. INTRODUCTION
A class of cyclic Euclidean Geometry codes are 1-step majority logic decodable. The code parameters are as follows:
+ n = 22" - 1, IC = 22s - 3", &in 2 2' 1.

code. Using Eqn. 1, we have, m = E[X]= 83.5 (the p.d.f.'s

of NO and NI need t o be evaluated for the constant error

channel with t = 500 errors). The standard deviation of X

is found using simulations to be (T = 15.1. Using a Gaussian

(v) approximation for the p.d.f. of X, we can estimate the fraction

of correctable 500-bit errors to be Q

= 1.6 x lop3'.

These codes can correct upto 2'-l bit errors by 1-step ma- With very high confidence, we can claim that the code corrects

jority logic decoding [I]. The performance of EG codes under almost all error patterns of weight less than 500.

various message-passing decoding algorithms has been studied

extensively in [2]. Our main goal is to analyze the performance B Case I1 : Optimizing T
of the hard decision threshold decoder and determine the error

correcting capability of these codes.
11. THRESHODLEDCODER

For a binary symmetric channel with a given p o , the threshold T can be optimized, using Eqn. 1 to minimize p l . It can be shown that the optimal threshold, T* is given by,

Each bit of a codeword is connected to 2' parity checks. The

threshold decoder works as follows: if more than a threshold T

of these checks are violated for a bit i, the bit is flipped. Given

that bit i is recieved correctly, let NO be a random variable

denoting the number of parity check equations connected to

bit i that are in error. Let NIbe the corresponding random
variable given bit i is received in error. Then, the probability that bit i is decoded incorrectly ( p l ) is given by:

1- (1- 2 p o p

where P =

n

L

A decoder with threshold optimized for the first iteration

+ PI = poPr(N1 <= T } (1- po)Pr{No > T } ,

performs better than the case where T = 2'-'. To determine
(1) the number of correctable errors, we use an argument similar

where po is the probability that bit i is received in error. For a given channel error model, the p.d.f.'s of NOand NIneed to be determined t o facilitate the use of Equation 1. If T = 2'-l,

to that for Case 1and find that almost all 580-bit errors can be corrected for the code with s = 9. The fraction of correctable 580-bit errors is approximately 1.8 x

the decoder corrects all 2"-'-weight error patterns.

REFERENCES

111. TWO-ITERATTIHORNESHOLD DECODER

[l] S. Lin and D. J. Costello, Jr., E r r o r control coding: F u n d a m e n t a l s a n d Applications. Englewood Cliffs, NJ: Prentice Hall,

Let us consider a decoder that does two iterations of threshold decoding. Thresholds for the first and second iteration are set at T and 2'-' respectively. Let X be a random variable that denotes the total number of bit errors after the first iteration.

Inc., 1983.
[2] Y. Kou, S. Lin, and M. P. C. Fossorier, "Low density parity check codes based on finite geometries: A rediscovery and more." to appear in IEEE Trans. on Info. Theory.

0-7803-7501-7/02/$17.00@2002 IEEE.

365

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 10:12:50 UTC from IEEE Xplore. Restrictions apply.

