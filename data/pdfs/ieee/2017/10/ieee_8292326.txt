Density Evolution Thresholds for Noise-Against-Noise Min-Sum Decoders
F. Cochachin∗†, D. Declercq∗Senior Member, IEEE, E. Boutillon†Senior Member, IEEE and L. Kessal∗ ∗ETIS, ENSEA/University of Cergy-Pontoise/CNRS, 95014 Cergy-Pontoise, France
†Lab-STICC, UMR 6285, Université de Bretagne Sud, Centre de Recherche BP 92116, Lorient 56321, France

Abstract—In this paper, we define Noise-against-Noise MinSum (NAN-MS) decoders as decoders that incorporate a certain amount of random perturbation due to deliberate noise injection. We introduce a noise model which is used to implement quantized NAN-MS decoders, using a limited number of precision bits. The behavior of NANMS decoders is investigated in the asymptotic limit of the code length using a noisy version of density evolution (DE). We use the noisy-DE thresholds to analyze and optimize the noise model parameters. We show that a controlled injection of noise allows NAN-MS decoders to achieve better performance than noiseless MS decoders, especially for low precision. The finite-length simulations confirm the conclusions of the DE analysis.
I. INTRODUCTION
Nowadays, one of the most efficient error-correcting codes are Low-Density Parity-Check (LDPC) codes [1], which are widely used in communications standards like DVB-S2 [2], DVB-S2X [3], IEEE 802.3an, etc. Message-Passing (MP) decoders are iterative algorithms that use a Tanner graph [4] representation of the LDPC codes. One important type of MP decoding algorithm is the sum-product algorithm [5], also called Belief-Propagation (BP) algorithm. The BP decoder has high computational complexity and excellent decoding performance. The Min-Sum (MS) and offset corrected Min-Sum (OMS) decoders [6], [7], derived from the BP decoder, reduces the computational complexity, but also has a slight performance degradation, compared to BP, especially when it is implemented in finite precision. The effect of quantization on the messages of MP decoders has been extensively studied these past ten years. In [8], the authors show that for the BP decoder, at least 6 precision bits should be used for the Binary-Input Additive White Gaussian Noise (BI-AWGN) channel. The effects of uniform quantization of the BP decoder over the Binary Symmetric Channel (BSC) is studied in [9]. As for the MS and OMS decoders, the effects of clipping and quantization for the BI-AWGN channel are studied in [7], [10]. From these works, it can be concluded that although the MS-based decoders are less sensitive than BP decoder to quantization effects, there is a non-negligible performance loss when the number of precision bits becomes too small.
978-1-5386-3531-5/17/$31.00 © 2017 IEEE

In this paper, we investigate a modification of MS decoders, with the goal of improving the error correction performance when the number of precision bits is as low as 3 or 4, which could be the case when very low implementation complexity is required by the application. Our modification, named Noise-against-Noise Min-Sum (NAN-MS) decoders, consists in injecting a deliberate random perturbation during the decoding process, to modify the dynamics of the decoder, and help to correct channel error events that would not be corrected by a deterministic implementation. Recently, the study noisy bit-flipping (BF) decoders, the Noisy Gradient Descent BF (NGDBF) and Probabilistic Gradient Descent BF (PGDBF) [11], [12], have shown that the noise could help to achieve better performance than noiseless versions of the same algorithms. Of course, the model of the noise and its use in the MP decoder plays an important role in the achieved gains.
We investigate the asymptotic behavior of our NAN-MS decoders using noisy density evolution (DE) techniques, as introduced in [13], [14] for the study of faulttolerance of BP and Min-Sum decoders. Using the decoding thresholds obtained with the noisy DE, we propose to study the best localization for the noise injection and to optimize the noise model parameters. Our study shows that in the case of very low precision, the NAN-MS can surpass the error correction performance of the MS decoders, for various regular and irregular LDPC code families. This conclusion is corroborated by finite length Monte Carlo simulations.
The outline of the paper is as follows. Section II introduces the basic notions of quantized decoders and LDPC codes. Section III discusses the probabilistic error model used to inject noise to quantized decoders. In Section IV, we present three methods to inject noise in order to implement NAN-MS decoders, and briefly explain how to optimize the model parameters with density evolution. In sections V and VI, we present the results for the optimization of the noise models for regular and irregular LDPC codes, respectively. Section VII shown finite length performance validation of the gains obtained with our NAN-MS decoders, and section VIII concludes the paper.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 13:53:11 UTC from IEEE Xplore. Restrictions apply.

II. BASIC NOTIONS OF QUANTIZED DECODERS AND LDPC CODES

An LDPC code is a linear block code defined by a

sparse parity-check matrix H = [hmn] of M rows by N columns, where M < N . An LDPC code can also

be represented by a Tanner graph which is a bipartite

graph G composed of two types of nodes, the variable

nodes (VNs) vn, n = 1...N and the check nodes (CNs) cm, m = 1...M . A VN in the Tanner graph corresponds to a column of H and a CN corresponds to a row of H,

with an edge connecting CN cm to VN vn exists if and only if hmn = 0.

The degree of a node is the number of its neighbors

in G. A code is said to have a regular column-weight

dv if all variable nodes vn have the same degree dv. Similarly, if all check nodes cm have the same degree dc, a code is said to have a regular row-weight dc. In case

of irregular LDPC codes, the nodes can have different

connection degrees, defining an irregularity distribution,

which is usually characterized by the two polynomials

λ(x) =

dv,max i=2

λixi−1,

and

ρ(x)

=

dc,max j=2

ρj xj−1.

The parameters λi (respectively ρj) indicate the fraction

of edges connected to degree i VNs (respectively degree

j CNs) [15]. For regular codes, the polynomials reduce to monomials, λ(x) = λdv xdv−1 and ρ(x) = ρdc xdc−1. A codeword can be represented as a vector x = (x1, ..., xN ) ∈ {0, 1}N which satisfies H.xT = 0. The
codeword is sent through a noisy channel, in which an

additive memoryless noise is added at each transmitted

bit. The channel output is denoted by y = (y1, ..., yN ).
In this paper, we assume that transmission channel is the BI-AWGN channel with noise variance σ2.

For quantized decoders the message alphabet M, which

has to be symmetric around zero, is finite and composed of Ns = 2Nq + 1 states, with Nq = 2(q−1) − 1. Hence, M = {−Nq, ..., −1, 0, +1, ..., +Nq}, i.e. the messages are quantized on q bits. Let us denote AL the decoder
input alphabet, which we assume in this paper to be

equal to the message alphabet, i.e. AL = M. Let us also denote ∈ N the number of iterations, denote mv( →) c ∈ M the message sent from VN v to CN c in the th iteration, and denote m(c→) v ∈ M the message sent from CN c to VN v in the th iteration.

The log-likelihood ratio that can be computed at the

channel output is equal to:

LLR(yn) = log

Pr(yn | xn = 0) Pr(yn | xn = 1)

=

2yn σ2

.

(1)

To obtain quantized values belonging to M, LLR(yn) has to be quantized and saturated. Let us denote the quantizer by Q : R → Z, with Q (x) = x × α + 0.5 (the notation . depicts the floor function). Let us further denote the saturation function SM(x) = min(max(x, −Nq), +Nq). The parameter α is called channel gain factor and is used to enlarge or decrease

the standard deviation of quantized values at the input of the decoder. The value of α can be seen as an extra degree of freedom in the quantized decoder definition that can be analyzed and optimized for quantized decoders on the BI-AWGN channel. Note that if α is too large most of quantized values will be saturated to Nq . With those notations, we define the quantized version of the log-likelihood ratios that initialize the decoder by the vector L = (L(y1), ..., L(yN )) ∈ MN , with
L(yn) = SM (Q (LLR(yn))) ∀n = 1, . . . , N. (2)

A MP decoder exchanges messages between VNs and CNs along edges using a Tanner graph. During each iteration, the VN update (VNU) and CN update (CNU) compute outgoing messages from all incoming messages. Let us briefly recall the VNU and CNU equations for the Min-Sum based decoders, before introducing the NAN-MS decoders. We assume that v is any VN and c is any CN. Also, V(v) is the set of neighbors of a VN v, and V(c) is the set of neighbors of a CN c. The update rule at a CNU is given by





mc(m)→vn = 

sign mv( →) cm  ×

v∈V (cm )\{vn }

min
v∈V (cm )\{vn }

mv( →) cm

.

(3)

And the update rule at a VNU is expressed as





m(vn+→1)cm = SM Λ L(yn) +

mc(→) vn 

c∈V (vn )\{cm }
(4)

where the function Λ(svc) and svc are defined by

Λ (svc) =

min (svc + λv, 0) , max (svc − λv, 0) ,

if svc < 0 if svc ≥ 0

svc = L(yn) +

m(c→) vn .

c∈V (vn )\{cm }

With the above defined CNU and VNU, we define the offset corrected MS with offset value λv = 0, applied at the variable node, where the special case of λv = 0 corresponds to the classical MS decoder.

III. PROBABILISTIC ERROR MODEL FOR NOISE-AGAINST-NOISE MIN-SUM DECODERS
In order to define a NAN-MS decoder, we first express the constraints on the noise models, and then we present a noise model which will be used to perturb noiseless decoders. We assume that the noisy message alphabet is denoted by M˜ . Noisy messages m˜ v( →) c ∈ M˜ and m˜ c(→) v ∈ M˜ are obtained after corrupting the noiseless messages m(v→) c and mc(→) v, respectively, with noise. To simplify the notations is this section, we use the notation

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 13:53:11 UTC from IEEE Xplore. Restrictions apply.

m to denote any c-to-v message mc(→) v at iteration or any v-to-c message m(v→) c, and let sβ be the sign of β. In order to be able to perform DE analysis of NANMS decoders, the considered noise models need to be memoryless, i.e. the noise models are independent on the data streams processed by the decoders. Also, the considered noise models have to satisfy a symmetry condition defined as follows
Pr(m˜ = β2|m = β1) = Pr(m˜ = −β2|m = −β1), ∀β1, β2 ∈ M.

m −3 −2 −1
0 +1 +2 +3

m˜ −3 −2 −1 0 +1 +2 +3

ϕs ϕa ϕ0
1 1 − ϕs 1 − ϕa 1 − ϕ0

Figure 1: The mapping used for the noise model Υ.

When memoryless and symmetric noise models are
used to perturb noiseless decoders, the noisy-VNU and
noisy-CNU are symmetric, allowing to use the all-zero
codeword assumption and the independence assumption
necessary in DE [15]. Now, let us denote Υ : AN → A˜N the function which transforms a noiseless message m( ) ∈ M into a noisy message m˜ ( ) ∈ M˜ . Υ must be a symmetric function. Unless otherwise stated, the alphabet of the noise model will be the one of the messages, i.e. AN = A˜N = M. Υ is defined by the conditional probability density function (PDF) Pr(m˜ |m). We propose in this paper to analyze a noise model, defined the following way:

m˜ = Υ(m = β) =



β,



with prob. 1 − ϕs,

   

sβ.(|β| − 1),

with prob. ϕs,

 

β,

with prob. 1 − ϕa,

 sβ.(|β| − 1), with prob. ϕa,

  

β,



with prob. 1 − ϕ0,

 

sβ.(|β| − 1),

with prob. ϕ0,

β ∈ {±Nq} β ∈ {±Nq} β ∈/ {±Nq, ±1, 0} β ∈/ {±Nq, ±1, 0} β ∈ {±1}
β ∈ {±1} (5)

This noise model is parametrized by three different

transition probabilities, denoted ϕ = (ϕs, ϕa, ϕ0). ϕs denotes the conditional PDF Pr(m˜ = Nq − 1|m = Nq), ϕa denotes the conditional PDF Pr(m˜ = β − 1|m = β), ∀β ∈ {+2, ..., +(Nq − 1)}, and finally ϕ0 denotes the conditional PDF Pr(m˜ = 0|m = +1).

The rationale behind this model is to implement a

probabilistic offset. Let us discuss the case of ϕs = ϕa = ϕ0. The MS and the OMS with λv = 1 can be obtained as special cases of our noise model, i.e.

ϕ = (0, 0, 0) for the MS and ϕ = (1, 1, 1) for the

OMS. For other values of the transition probabilities,

the offset is only applied from time to time during the

decoding iterations, leading to a probabilistic weighted

combination of a deterministic MS and a deterministic

OMS.

We have also introduced two other probabilities ϕs and ϕ0, in order to study the effect of the noise on the
extreme values of the message alphabet. A special case

occurs when m = 1 because it will be changed to

m˜ = 0 or it will keep its value m˜ = 1. Since the value

m˜ = 0 propagates no information about the bit value,

ϕ0 has to be analyzed differently than ϕa. Additionally, in quantized decoders, all values which are greater than

Nq at the VNU are saturated to Nq, according to (4). As a result, many more configurations of the VNU states lead to an output message m = Nq compared to other values of m, and ϕs should also be analyzed differently than ϕa. As an example, Υ is depicted in Fig. 1 for (q = 3, Nq = 3). The noise model Υ could then be used to determine whether a NAN-MS can outperform
a noiseless decoder.

IV. NOISE-AGAINST-NOISE MIN-SUM DECODERS
The first issue that we would like to analyze is the localization of the noise injection inside the decoder flow. Indeed, an iterative decoder is composed of several sequential steps, and the results could be different depending on when the noise is injected. We decided to restrict the possibilities of noise injection to the main steps of the algorithm, that are the VNU and the CNU steps. In this section, we present different versions of the NAN-MS decoders, depending on the choice of the decoding step in which we inject the noise. The noise can be injected in three different ways: (i) during the VNU processing, (ii) after the VNU processing, or (iii) after the CNU processing. We decide in this paper to analyze the NAN-MS decoders with noise injection only in one of these steps, although one could easily extend our analysis by injecting noise during more than one step of the decoding iteration.

A. Noise Outside the Variable Node Update

The first choice is obtained by perturbing the outgoing message from the VNU, after the processing of (4) is completed. The update rule for the corresponding noisyVNU is given by

 



m˜ (vn+→1)cm = Υ SM Λ L(yn) +

mc(→) vn .

c∈V (vn )\{cm }

(6)

Note that the noise perturbs saturated v-to-c messages, which belong to M, and the noise model of (5) can be

used directly. We define this method to inject noise as

Noise Outside the VNU (NOV).

B. Noise Inside the Variable Node Update
The second method to inject noise is implemented perturbing unsaturated v-to-c messages, before the use

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 13:53:11 UTC from IEEE Xplore. Restrictions apply.

of function SM in (4). In this case, the update rule for the noisy-VNU is given by

 



m˜ v( n+→1)cm = SM Υ Λ L(yn) +

mc(→) vn .

c∈V (vn )\{cm }

(7)

We label this method to inject noise as Noise Inside

the VNU (NIV). Since Υ perturbs unsaturated values, the alphabet of the noise model AN is larger than M, and equal to AN = A˜N = {−(2(q−1) − 1) × dv, . . . , −1, 0, +1, . . . , +(2(q−1) − 1) × dv}. It follows
that there is no advantage in differentiating the transition probabilities ϕs and ϕa, as the largest values of AN will be saturated with function SM. Throughout the paper, we will then assume that ϕs = ϕa for the NIV model.

C. Noise Outside the Check Node Update
The third method to inject noise perturbs the outgoing c-to-v messages from the CNU, after the processing is completed. Note that because during the CNU, the min operations leave the messages in the same alphabet M, there is no need to consider two cases, like for the VNU. The update rule for a noisy-CNU is given by

m˜ c(m)→vn = Υ mc(m)→vn

(8)

where m(cm)→vn is obtained with (3). We label this method as Noise Outside the CNU (NOC).

channel gain factor α, and the values of the transition probabilities of the noise model (ϕs, ϕa, ϕ0). We can use δ˜ to jointly optimize the noise model parameters and the channel gain factor, for a fixed NANMS decoder and a fixed precision:

(ϕs∗, ϕ∗a, ϕ∗0, α∗) = arg max
(ϕs ,ϕa ,ϕ0 ,α)

δ˜ (λ(x), ρ(x), q, α, (ϕs, ϕa, ϕ0)) . (9)

This analysis is conducted in the next section.

V. ASYMPTOTIC ANALYSIS OF NAN-MS DECODERS
FOR REGULAR LDPC CODES
In this section, we consider the ensemble of (dv, dc)regular LDPC codes with variable-node degree dv ∈ {3, 4} and code rate R = 0.5, and quantized decoders with q ∈ {3, 4}. The DE thresholds of the noiseless MS and OMS decoders are given in Table I. For the
noiseless decoders, the optimization (9) is reduced to the optimum channel gain factor α∗. It can be seen that the OMS is almost always superior to the MS for
the considered cases, which was expected. The only exception is for regular (dv, dc) = (3, 6) LDPC codes with low precision messages q = 3, for which the MS threshold is better than the OMS’s. We can also note that the largest difference is obtained for dv = 4 codes and quantization q = 4.

D. Noisy Density Evolution for NAN-MS decoders

DE is used to analyze the asymptotic behavior of

an iterative MP decoder as a dynamical system. The

principle of DE is to follow the probability mass

function (PMF) of the messages in the Tanner graph

along the iterations. With DE, we can predict if

an ensemble of LDPC codes, parametrized by its

degree distribution, decoded with a given MP decoder,

converges to zero error probability in the limit of infinite

block length.

For the the BI-AWGN channel, the noise is characterized

by its standard deviation σ. For this channel, the

maximum value of σ at which the DE converges to

a zero error probability is called the DE threshold,

and denoted δ. The DE threshold could be equivalently

expressed by the minimum signal-to-noise ratio (SNR)

at which the DE converges to a zero error probability:

δdb = 10 log10

1 2 R σ∗2

.

For NAN-MS decoders, the DE equations can be

adapted easily, as long as the considered noise model

is symmetric and memoryless. We do not present the

details of the noisy-DE equations, and refer to [14], [16],

[17] for more details. The noisy-DE threshold is denoted δ˜. It is a function of the code family, parametrized by its

degree distribution (λ(x), ρ(x)), of the class of NAN-

MS decoder as introduced in the previous section, of

the number q of quantization bits, of the value of the

Table I: DE thresholds of noiseless MS decoders and noiseless OMS decoders with offset value λv = 1.

(dv, dc)-regular LDPC code, BI-AWGN channel

(dv, dc)

q

λv

α∗

δ

δdb

(3, 6)

3 bits 0 0.9375 0.8139 1.7888 1 1.0625 0.7759 2.2039

4 bits 0

2.0 0.8276 1.6437

1 1.8750 0.8562 1.3481

(4, 8)

3 bits 0 0.8125 0.7298 2.7360 1 1.2500 0.7654 2.3219
4 bits 0 1.6250 0.7465 2.5389 1 1.7500 0.8174 1.7509

In Table II, we indicate the noisy DE thresholds obtained with (9), and for the different noise injection locations, described in Section IV. Several conclusions can be derived from this analysis.
1) When comparing the noisy thresholds for the three methods to inject noise (NOV, NIV, and NOC), one can see that the best location for the noise injection is inside the variable node update, i.e. the NIV model of (7). When the noise is injected at the VNU, but after the quantization, i.e. the NOV model of (6), the noisy thresholds are also very good, and close to the NIV model. However, the injection of the noise after the CNU gives always the worse threshold. This gives us information about the best way to inject noise in NAN-MS decoders.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 13:53:11 UTC from IEEE Xplore. Restrictions apply.

2) A second conclusion that can be drawn is that
the thresholds of the NAN-MS decoders are
always better than the thresholds for the noiseless
decoders. When comparing the best thresholds
indicated in bold, the gains for NAN-MS decoders are quite important, around 0.22 dB, for the case of low precision q = 3, while the gains are smaller for the largest precision q = 4, 0.06 dB for dv = 3 and 0.01 dB for dv = 4. From this analysis, we can conclude that the noise injection for NAN-
MS decoders is more and more beneficial as the
decoders are implemented in low precision.
3) A third interesting remark comes from the interpretation of the optimum noise parameters ϕ∗ that we obtained through the DE analysis. For almost all cases, we have ϕ∗0 = 0, which means that the decoder should never apply an offset to
the message when the amplitude of the message
is equal to 1. This makes sense as transforming m = ±1 into m˜ = 0 would erase the bit value and remove the sign information contained in the message. An exception appears for dv = 4 and q = 4, for which we draw the evolution of the NAN-MS threshold versus the value of ϕ0 on Fig. 2, while keeping the other parameters constant.
One can see that for all models, there is an optimum value of the threshold around ϕ0∗ = 0.5, and each of them is beating the threshold of the
OMS. The NIV and NOV models are very much
better than the NOC model.
4) Finally, some of the obtained NAN-MS decoders
are not really probabilistic since the values of
the transition probabilities are very close to 0 or 1. This is the case for dv = 4 and q = 3. All models are close to (ϕ∗s, ϕa∗, ϕ0∗) = (1, 1, 0), which correspond to a deterministic OMS decoder for which the offset is not applied when m = ±1.
We have made the same analysis or LDPC codes with rate R = 3/4, i.e. (dv = 3, dc = 12) and (dv = 4, dc = 16) and obtained the same conclusions.

Table II: Noisy DE thresholds of NAN-MS decoders.

NAN-MS decoders, (dv, dc)-regular LDPC code, BI-AWGN channel

(dv, dc)

q

Method α∗

ϕs∗

ϕa∗

ϕ∗0

δ˜db

Υ-NIV 0.82 0.475 0.475 0.000 1.5711

3 bits Υ-NOV 0.77 0.338 0.313 0.000 1.5995

(3, 6)

Υ-NOC 0.77 0.850 0.363 0.000 1.6306 Υ-NIV 1.72 0.887 0.887 0.000 1.2877

4 bits Υ-NOV 1.7 0.000 0.912 0.000 1.2917

Υ-NOC 1.75 0.000 1.000 0.000 1.2931

(4, 8)

Υ-NIV 0.93 0.950 0.950 0.000 2.1056 3 bits Υ-NOV 0.92 1.000 0.912 0.000 2.1105
Υ-NOC 0.92 1.000 1.000 0.000 2.1119 Υ-NIV 1.67 1.000 1.000 0.463 1.7411 4 bits Υ-NOV 1.67 1.000 1.000 0.450 1.7411 Υ-NOC 1.69 1.000 1.000 0.475 1.7488

NAN−MS decoder, (4,8)−regular LDPC

1.755

4-bit, α =1.67, NAN-MS(1, 1, ϕ0), NIV 4-bit, α =1.69, NAN-MS(1, 1, ϕ0), NOC 4-bit, α =1.67, NAN-MS(1, 1, ϕ0), NOV Noiseless OMS (4-bit), λv = 1, α =1.75

1.75

DE threshold: δ˜db

1.745

1.74

0

0.2 0.4 ϕ0 0.6 0.8

1

Figure 2: Noisy DE thresholds of NAN-MS decoders as a function of ϕ0 for regular dv = 4 LDPC codes.

VI. ASYMPTOTIC ANALYSIS OF NAN-MS DECODERS FOR IRREGULAR LDPC CODES

In the previous section we have seen that the optimum noise parameters (ϕs∗, ϕa∗, ϕ0∗) and the respective gains of NAN-MS decoders depend on the VN degree. For
LDPC codes with irregular VN distribution, we propose
therefore to extend our approach by considering a noise injection model Υ with different values of the transition
probabilities for the different connection degree. We denote by Υ(2) : ϕ(2) = ϕs(2), ϕ(a2), ϕ0(2) the model which injects noise at VNs of degree dv = 2. Both the NIV and NOV methods can be considered. Similarly, let Υ(3) : ϕ(3) = ϕ(s3), ϕa(3), ϕ0(3) denote the noise model for the VNs of degree dv = 3. Finally, we decide to use the same model for all other VNs with degrees dv ≥ 4, denoted Υ(≥4) : ϕ(≥4) =
ϕs(≥4), ϕ(a≥4), ϕ0(≥4) . The optimization of the transition probabilities for an irregular LDPC code with distribution (λ(x), ρ(x)) is
still performed by the maximization of the noisy DE
thresholds:

ϕ(2)∗, ϕ(3)∗, ϕ(≥4)∗, α∗ =

arg

max

δ˜ λ(x), ρ(x), q, α, ϕ(2), ϕ(3), ϕ(≥4) .

(ϕ(2) ,ϕ(3) ,ϕ(≥4) ,α)

(10)

For our analysis, we consider the ensemble of irregular

LDPC codes which follows the distribution of the rate

R = 0.5, length N = 2304 code described in the

WIMAX standard [18]. Its degree distribution is λ(x) =

22 76

x

+

24 76

x2

+

30 76

x5

and

ρ(x)

=

48 76

x5

+

28 76

x6

.

For

this

distribution, we indicate in Table III the DE thresholds

of the noiseless MS and noiseless OMS decoders.

Noisy DE thresholds are summarized in Table IV,

where we indicate the optimum values of α and of

the noise parameters for the different degrees. Those

results confirm the conclusions of the regular LDPC

codes analysis: (i) the NIV model is again the best one,

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 13:53:11 UTC from IEEE Xplore. Restrictions apply.

Table III: DE thresholds of noiseless MS and noiseless OMS decoders for the WIMAX degree distribution.

Irregular LDPC code, BI-AWGN channel

q

λv α∗

δ

δdb

3 bits 0 0.44 0.8099 1.8310 1 0.40 0.5478 5.2283

4 bits 0 1.07 0.8517 1.3941 1 0.80 0.7233 2.8140

(ii) the optimum value for ϕ0∗ is 0 for dv = 3 VNs and (iii) some of the optimized models are not probabilistic
since the optimized values of the transition probabilities
are very close to 0 or 1.

Table IV: DE thresholds of NAN-MS decoders.

NAN-MS decoders, Irregular LDPC code, BI-AWGN channel

q

Method α∗ dv

ϕs∗

ϕ∗a

ϕ0∗

δ˜db

2 0.000 0.000 0.000

Υ-NIV 0.42 3 0.350 0.350 0.000 1.6236

≥ 4 1.000 1.000 0.225

3 bits

2 0.000 0.013 0.000

Υ-NOV 0.42 3 0.000 0.063 0.000 1.6302

≥ 4 0.000 1.000 0.300

Υ-NOC 0.44 - 0.000 0.000 0.000 1.8310

2 0.000 0.000 0.000

Υ-NIV 1.0 3 1.000 1.000 0.000 0.9995

≥ 4 1.000 1.000 0.000

4 bits

2 0.000 0.000 0.000

Υ-NOV 0.98 3 0.000 0.988 0.000 1.0077

≥ 4 0.000 1.000 0.000

Υ-NOC 0.83 - 0.000 0.375 0.000 1.2178

code and a (4, 8)-regular QC-LDPC code with length N = 1296 and circulant size L = 54, using the PEG algorithm from [19]. The considered decoders are the ones with the best DE thresholds, indicated in bold in Tables I and II. The floating point Belief Propagation performance are also shown as benchmark. Fig. 3 shows the FER performance comparisons between the noiseless MS, noiseless OMS, and best NAN-MS decoders, for the two considered precisions q = 3 and q = 4, and for the regular dv = 3 QC-LDPC code. A maximum of 100 iterations has been set for all decoders. Fig. 4 draws the same curves for the regular dv = 4 QC-LDPC code. A first conclusion is that the finite length FER performance are in accordance with the gains predicted by the DE analysis. We observe in the waterfall (i.e. at FER = 10−2) an SNR gain for the NAN-MS decoders which corresponds to the threshold differences: around 0.2 dB for the low precision q = 3, 0.05 dB for (q = 4,dv = 3), and same performance for (q = 4,dv = 4).
(3,6)−regular LDPC, N = 1296 100
10−2

Frame error rate (FER)

Another conclusion can be driven from this table. From the DE analysis we can conclude that the noise should not be injected on degree dv = 2 VNs, since we obtain always (ϕs(2), ϕ(a2), ϕ(02)) (0, 0, 0). This observation, combined with the fact that the optimum values for ϕ(≥4)∗ are almost always 0 or 1, lead to the conclusion that injecting noise in NAN-MS decoders for irregular LDPC codes is especially important for the degree dv = 3 VNs. Finally, the gains for NAN-MS decoder for irregular codes are larger than for the regular codes with a gain of 0.2074 dB for low precision q = 3 and a gain of 0.3946 dB for the largest precision q = 4. The optimization of the transition probabilities of the models Υ and the channel gain factor is made using a greedy algorithm which computes a local maximum DE threshold. For noiseless decoders, the optimum channel gain factor α∗ is computed performing a grid-search.
VII. FINITE LENGTH PERFORMANCE OF NAN-MS DECODERS
In this section we present the frame error rate (FER) performance for noiseless and NAN-MS decoders. In order to corroborate the asymptotic results obtained in the previous section, we analyze the quantized decoder performance over the BI-AWGN channel. For this purpose, we designed a (3, 6)-regular QC-LDPC

10−4

Noiseless BP, float-point Noiseless MS (3-bit), α =0.9375

Noiseless MS (4-bit), α =2

Noiseless OMS (3-bit), α =1.0625

Noiseless OMS (4-bit), α =1.875

3-bit, NAN-MS, α = 0.82, NIV

10−6

4-bit, NAN-MS, α = 1.72, NIV

1

1.5

2

2.5

3

Eb/N0(dB)

Figure 3: FER performance of noiseless MS and NANMS decoders for (3, 6)-regular LDPC code.

Simulation results for the WIMAX LDPC code are provided on Fig. 5. Again, the SNR gains in the waterfall correspond to what was predicted with the DE analysis, with a 0.4 dB gain for q = 4 and a 0.2 dB gain for q = 3. As a last remark, we can also see that the injection of errors with the proposed models Υ do not seem to have an influence in the error floor of the decoders, since all the curves have similar slopes in the low FER region. This means that the proposed models do not correct the dominant error events due to trapping sets.
VIII. CONCLUSION
In this paper we have proposed to use a noise injection method to introduce randomness in the

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 13:53:11 UTC from IEEE Xplore. Restrictions apply.

(4,8)−regular LDPC, N = 1296 100

Frame error rate (FER)

10−2

10−4

Noiseless BP, float-point

10−6

Noiseless MS (3-bit), α =0.8125 Noiseless MS (4-bit), α =1.625

Noiseless OMS (3-bit), α =1.25

Noiseless OMS (4-bit), α =1.75

3-bit, NAN-MS, α = 0.93, NIV

10−8

4-bit, NAN-MS, α = 1.67, NIV

1 1.5 2 2.5 3 3.5

Eb/N0(dB)

Figure 4: FER performance of noiseless MS and NANMS decoders for (4, 8)-regular LDPC code.

Irregular LDPC, N = 2304 100

Frame error rate (FER)

10−2

10−4

Noiseless BP, float-point Noiseless MS (3-bit), α =0.44

Noiseless MS (4-bit), α =1.07

Noiseless OMS (3-bit), α =0.4

Noiseless OMS (4-bit), α =0.8

3-bit, NAN-MS, α = 0.42, NIV

10−6

4-bit, NAN-MS, α = 1.00, NIV

1

1.5

2

2.5

3

Eb/N0(dB)

Figure 5: FER performance of noiseless MS and NANMS decoders for the WIMAX LDPC code.

message passing decoders, in order to improve the error correction performance of quantized iterative decoders. The analysis was conducted with density evolution techniques, to obtain the optimum noise parameters and the optimum channel gain factor. The DE thresholds results have shown that the effect of the injected noise is always beneficial for low precision NAN-MS decoders specially when the noise is injected at VNUs. The finite-length Monte Carlo simulations have corroborated the DE thresholds. Future works in this direction include the study of the noise injections approach to improve the performance of LDPC decoders in the error floor region.
REFERENCES
[1] D. Declercq, M. Fossorier, and E. Biglieri, Channel Coding: Theory, Algorithms, and Applications. Academic Press Library in

Mobile and Wireless Communications. ISBN:978-0-12-3964991, 2014. [2] ETSI, “ETSI EN 302 307-1 V1.4.1 (2014-07) Digital Video Broadcasting (DVB); Second generation framing structure, channel coding and modulation systems for Broadcasting, Interactive Services, News Gathering and other broadband satellite applications; Part 1: DVB-S2,” 2014. [3] ETSI, “ETSI EN 302 307-2 V1.1.1 (2014-10) Digital Video Broadcasting (DVB); Second generation framing structure, channel coding and modulation systems for Broadcasting, Interactive Services, News Gathering and other broadband satellite applications; Part 2: DVB-S2 Extensions (DVB-S2X),” 2014. [4] M. R. Tanner, “A Recursive Approach to Low Complexity Codes,” IEEE Transactions on Information Theory, vol. Vol-27, no. 5, pp. 533–547, September 1981. [5] F. R. Kschischang, B. J. Frey, and H.-A. Loeliger, “Factor Graphs and the Sum-Product Algorithm,” IEEE Transactions on Information Theory, vol. Vol-47, no. 2, pp. 498–519, February 2001. [6] J. Chen and M. P. C. Fossorier, “Density Evolution for Two Improved BP-Based Decoding Algorithms of LDPC Codes,” IEEE Communications Letters, vol. Vol-6, no. 5, pp. 208–210, May 2002. [7] J. Chen, A. Dholakia, E. Eleftheriou, M. P. C. Fossorier, and X.Y. Hu, “Reduced-Complexity Decoding of LDPC Codes,” IEEE Transactions on Communications, vol. Vol-53, no. 8, pp. 1288– 1299, August 2005. [8] L. Ping and W. K. Leung, “Decoding Low Density Parity Check Codes with Finite Quantization Bits,” IEEE Communications Letters, vol. Vol-4, no. 2, pp. 62–64, February 2000. [9] R. Singhal, G. S. Choi, and R. N. Mahapatra, “Quantized LDPC Decoder Design for Binary Symmetric Channels,” IEEE, pp. 5782–5785, 2005. [10] J. Zhao, F. Zarkeshvari, and A. H. Banihashemi, “On Implementation of Min-Sum Algorithm and Its Modifications for Decoding Low-Density Parity-Check (LDPC) Codes,” IEEE Transactions on Communications, vol. Vol-53, no. 4, pp. 549– 554, April 2005. [11] G. Sundararajan, C. Winstead, and E. Boutillon, “Noisy Gradient Descent Bit-Flip Decoding for LDPC Codes,” IEEE Transactions on Communications, vol. Vol-62, no. 10, pp. 3385–3400, October 2014. [12] A. Rasheed, P. Ivanis, and B. Vasic, “Fault-tolerant Probabilistic Gradient-Descent Bit Flipping Decoder,” IEEE Communications Letters, vol. Vol. 18, no. 9, pp. 1487–1490, Sept. 2014. [13] L. R. Varshney, “Performance of LDPC Codes Under Faulty Iterative Decoding,” IEEE Transactions on Information Theory, vol. Vol-57, no. 7, pp. 4427–4444, July 2011. [14] C. K. Ngassa, V. Savin, E. Dupraz, and D. Declercq, “Density evolution and functional threshold for the noisy min-sum decoder,” IEEE Transactions on Communications, vol. Vol-63, no. 5, pp. 1497–1509, May 2015. [15] T. J. Richardson and R. L. Urbanke, “The Capacity of LowDensity Parity-Check Codes Under Message-Passing Decoding,” IEEE Transactions on Information Theory, vol. Vol-47, no. 2, pp. 599–618, February 2001. [16] L. R. Varshney, “Performance of LDPC Codes Under Noisy Message-Passing Decoding,” ITW, pp. 178–183, September 2007. [17] E. Dupraz, D. Declercq, B. Vasic, and V. Savin, “Analysis and Design of Finite Alphabet Iterative Decoders Robust to Faulty Hardware,” IEEE Transactions on Communications, vol. Vol. 63, no. 8, pp. 2797–2809, August 2015. [18] “IEEE Standard for Local and metropolitan area networks Part 16: Air Interface for Fixed and Mobile Broadband Wireless Access Systems - Amendment 2: Physical and Medium Access Control Layers for Combined Fixed and Mobile Operation in Licensed Bands and Corrigendum 1,” 2005. [19] A. Venkiah, D. Declercq, and C. Poulliat, “Design of Cages with a Randomized Progressive Edge Growth Algorithm,” IEEE Communications Letters, vol. Vol. 12, no. 4, pp. 301–303, April 2008.

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 13:53:11 UTC from IEEE Xplore. Restrictions apply.

