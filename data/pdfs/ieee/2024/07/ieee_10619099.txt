2024 IEEE International Symposium on Information Theory (ISIT) | 979-8-3503-8284-6/24/$31.00 ©2024 IEEE | DOI: 10.1109/ISIT57864.2024.10619099

Generalized Gradient Flow Decoding and Its Tensor-Computability

Tadashi Wadayama Nagoya Institute of Technology
wadayama@nitech.ac.jp

Lantian Wei Nagoya Institute of Technology
wei.lantian@nitech.ac.jp

Abstract—This paper introduces an extension of Gradient Flow (GF) decoding for LDPC codes. GF decoding, a continuous-time methodology based on gradient flow, employs a potential energy function associated with bipolar codewords of LDPC codes. The original GF decoding was designed for AWGN channels but in this paper, we introduce the negative log-likelihood function of the channel for generalizing the original method. The proposed method is shown to be tensor-computable, which means that the gradient of the objective function can be evaluated with the combination of basic tensor computations. This characteristic is well-suited to emerging AI accelerators, potentially applicable in wireless signal processing. The paper assesses the decoding performance of the generalized GF decoding in LDPC-coded MIMO channels. Our numerical experiments reveal that this method’s decoding performance rivals that of established techniques such as MMSE + BP. A further benefit of the proposed method is its suitability for deep unfolding. This advantage stems from the fact that each component of the method is differentiable.
Index Terms—LDPC codes, ordinary differential equations, gradient descent, tensor computation friendly, MIMO channel, MIMO detection, joint detection-decoding
I. INTRODUCTION
Low-Density Parity-Check (LDPC) codes [1] have emerged as a cornerstone in the ongoing advancement of contemporary wireless communication technologies. Their role is becoming increasingly critical, and LDPC codes are expected to be necessary in ensuring reliable data transmission in the forthcoming 6G systems. The advent of 6G networks represents a substantial leap forward in communication technology, demanding levels of technical innovation. For example, the seamless integration of Artificial Intelligence (AI) with signal processing stands as a pivotal challenge in the realization of 6G networks [2]. This integration is essential not only for enhancing network efficiency and capability but also for unlocking new potentials in wireless communications.
The recent remarkable achievements of Large Language Models (LLMs), such as GPT-4 [3], have significantly influenced the technological landscape, particularly in the field of artificial intelligence. These successes have acted as a catalyst in the development of fast and power-efficient AI accelerators [4], including the next generation of Graphics Processing Units (GPUs). These advanced devices are specifically designed to cater to the demanding computational needs of AI models. They play a crucial role not only in accelerating the training
This work was supported by JSPS KAKENHI Grant Number JP22H00514.

process of these complex models but also in enhancing the efficiency of inference tasks.
In recent years, optical integrated programmable circuits based on the Mach-Zehnder Interferometers (MZI) have garnered significant interest from researchers [5], [6]. An MZI is an optical component that consists of phase shifters, which alter the phase of the input light beams, and beam splitters, which divide it. As an MZI has a two controllable parameters, it can be seen as a programmable optical component. Optical matrix-vector product (MVP) circuit implemented using MZIs has been actively studied [7]. The programmable MZI-based MVP circuit is a promising candidate of the next generation AI accelerators because of its significant computation speed and power efficiency. A recent example is a neural network implemented in the optical domain [8].
By offering significant enhancements in speed and energy efficiency, these next-generation AI accelerators could facilitate more extensive and practical applications across various fields, including wireless signal processing. Traditionally, a practical LDPC decoder is often implemented in an Application Specific Integrated Circuit (ASIC) for achieving sufficiently high throughput and energy efficiency. However, the advancements in AI accelerators may be paving the way for the practical implementation of LDPC decoders on AI accelerators in the near future because programability provided by AI accelerators yields significant flexibility in wireless signal processing tasks handling various demands such as rate, latency, code type, and signal format.
For this direction, a next-generation decoding algorithm needs to be tensor-friendly, i.e., each internal process of the decoding algorithm should be efficiently executable on an AI accelerator. This implies that each sub-process must be based on tensor computation, which is optimally managed by the AI accelerator. In other words, the core of the algorithm should comprise tensor-friendly computations to fully leverage the power of the AI accelerator. A tensor-friendly decoding algorithm facilitates mini-batch based decoding, allowing for the simultaneous handling of multiple codewords.
Gradient Flow (GF) decoding [10] represents a continuoustime, gradient flow-based methodology for decoding LDPC codes. This approach utilizes a potential energy function akin to the objective function employed in the Gradient Descent Bit Flipping (GDBF) algorithm [11]. It has been reported that the decoding performance of GF decoding is on par with that

979-8-3503-A8u2th8o4riz-6ed/2li4ce/n$s3e1d.u0s0e ©lim2ite0d2t4o:IYEoEnsEei Univ. Downloaded on3J3un5e624,2026 at 15:44:50 UTC from IEEE Xplore. Restrictions apply.

of the multi-bit mode of the GDBF algorithm [10]. The objective of this study is to devise a tensor-friendly
decoding algorithm for LDPC codes. To achieve the goal, we begin by broadening the scope of GF decoding to encompass general channels, achieved by incorporating the channel’s negative log-likelihood function. Subsequently, we concretely define the concept of tensor-computability, and then prove the tensor-computability of the generalized GF decoding. A significant attribute of the generalized GF decoding is its differentiability, facilitating seamless integration with other AI components, and it allows us to use deep unfolding [22] [23] [25] for improving the convergence speed. Additionally, we introduce a discretized version, equivalent to a gradient descent method, which is ideally suited for implementation on digital AI accelerators.

II. PRELIMINARIES
A. Related works
The method presented in this paper can be classified as part of the optimization-based decoding algorithms for LDPC codes. A number of works have been developed in this class. The seminal work on Linear Programming (LP) decoding by Feldman [13] clearly formulated the LDPC decoding problem as an LP problem. Applications of interior point methods for solving the LP problem have been discussed in [14] [15]. A gradient descent formulation of a non-convex objective function leads to the GDBF algorithm [11], which introduced optimization perspective into the bit-flipping decoding algorithms. Some variants of the GDBF algorithm, such as the noisy GDBF algorithm [16], have shown considerable improvement in decoding performance over a simple GDBF algorithm. Zhang et al. [17] and Barman et al. [18] applied Alternating Direction Method of Multipliers (ADMM) for solving the Feldman’s LP problem, which leads to ADMM decoding. ADMM decoding has been studied by many researchers and has become a decoding algorithm quite competitive with BP in both decoding performance and decoding complexity.

B. Notation

Assume that a binary parity check matrix H = {Hij} ∈

FC˜m 2(H×n)

is given. The binary linear code C˜(H) is ≡ {b ∈ Fn2 | Hb = 0}. The binary

defined by to bipolar

transform β : F2 → {1, −1} defined by β(0) ≡ 1 and

β(1) ≡ −1 transforms C˜(H) into the bipolar code defined

by C(H) ≡ {β(b) ∈ {1, −1}n | b ∈ C˜(H)}.

The index sets A(i) and B(j) are defined as

A(i) ≡ {j | j ∈ [n], Hi,j = 1}, i ∈ [m],

(1)

B(j) ≡ {i | i ∈ [m], Hi,j = 1}, j ∈ [n],

(2)

respectively. The notation [n] denotes the set of consecutive
integers {1, 2, . . . , n}. A function f : C → C can be applied to a vector a ∈ Cn as f (a) ≡ (f (a1), f (a2), . . . , f (an)) where a = (a1, . . . , an) ∈ Cn. Namely, a scalar function f
can be component-wisely applicable to a vector x. For a pair of vectors a = (a1, . . . , an) ∈ Cn, b ≡ (b1, . . . , bn) ∈ Cn, we

define the multiplication and division on two vectors by ab ≡ (a1b1, . . . , anbn) and a/b ≡ (a1/b1, . . . , an/bn), respectively.

C. Gradient flow dynamics

The gradient flow dynamics, also known as steepest descent dynamics, is a continuous-time dynamics defined by an Ordinary Differential Equation (ODE) [33]:

dx(t) dt

=

−∇F (x(t)),

(3)

where F : Rn → R is a potential energy function. The state of the system x : R → Rn evolves to reduce the potential energy F as the continuous time variable t increases. This continuous dynamical system can be viewed as the continuous counterpart of the gradient descent method. If the potential function F is strictly convex, the equilibrium point of the dynamics coincides with the minimum point of F . Therefore, the gradient flow dynamics can be seen as a continuoustime minimization process of the potential energy function. Recently, signal processing methods based on the gradient flow dynamics have been discussed in [30] [31].

D. Gradient flow decoding

The GF decoding for AWGN channel proposed in [10] is defined by an ODE:

dx dt

=

−(x

−

y

+

∇hα,β (x)),

(4)

where the initial condition is x(0) = x0 and y represents the received vector. The code potential energy function [32] [29]
for C(H) is a multivariate polynomial defined as

⎛⎛

n

m

hα,β(x) ≡ α (x2j − 1)2 + β ⎝⎝

⎞ ⎞2 xj⎠ − 1⎠ ,

j=1

i=1

j∈A(i)

(5)

where x = (x1, . . . , xn)T ∈ Rn. The parameters α ∈ R+ and β ∈ R+ control the relative strength of the first and second terms. The first term on the right-hand side of (5) represents

the bipolar constraint for x ∈ {+1, −1}n, and the second

term corresponds to the parity constraint induced by H, i.e.,

if x ∈ C(H), we have j∈A(i) xj −1 = 0 for any i ∈ [m].
The code potential energy hα,β(x) is inspired by the nonconvex parity constraint function used in the GDBF objective
function [11]. The sum-of-squares form of (5) directly implies
the most important property of hα,β(x), i.e., the inequality hα,β(x) ≥ 0 holds for any x ∈ Rn. The equality holds if and only if x ∈ C(H).

III. GENERALIZED GRADIENT FLOW DECODING
The GF decoding is defined only for AWGN channel in [10]. In this section, we will generalize GF decoding for a general vector channel defined by a conditional probability distribution p(y|x).

Authorized licensed use limited to: Yonsei Univ. Downloaded on3J3un5e724,2026 at 15:44:50 UTC from IEEE Xplore. Restrictions apply.

A. Approximate maximum a posteriori (MAP) decoding

We here briefly review the approximate MAP decoding
introduced in the paper on proximal decoding [12]. Assume
that a sender transmits a codeword of C(H) to a given
channel. The channel is defined by a probability density function (PDF), p(y|x)(x, y ∈ Rn). The negative log-likelihood function is defined as L(x; y) ≡ − ln p(y|x). The optimal
decoding rule for this channel is MAP decoding rule defined as xˆ ≡ argmaxx∈Rn p(y|x)p(x), where p(x) is the prior distribution. In general, MAP decoding is computationally
intractable and we thus need to consider an approximation
of MAP decoding for sufficiently long codes.
The ideal prior distribution is based on the code C(H) which is given by p(x) ≡ (1/|C(H)|) c∈C(H) δ(x − c), where δ is Dirac’s delta function. We here introduce a Gibbs prior distribution p˜(x) ≡ (1/Z) exp (−γhα,β(x)) , where Z is the normalizing constant and γ is a positive constant. The function hα,β(x) takes the value zero if and only if x ∈ C(H) and takes a positive value otherwise. This implies that we have

p˜(x)

=

1 Z

exp (−γhα,β(x))

→

1 |C (H )|

δ(x − c)

c∈C (H )

(6)

at the limit γ → ∞. This fact suggests the following approximation on the posterior PDF:

p(x|y) ∝ p(y|x)p(x) p(y|x)p˜(x)

= exp (−L(x; y) − γhα,β(x)) .

(7)

Hence, the approximate MAP rule considered here is given by

xˆ ≡ argminx∈Rn L(x; y) + γhα,β(x).

(8)

Note that we need to use a finite γ in a decoding process.

B. Generalized GF decoding

In the previous subsection, the concept of an approximate MAP rule was introduced. The objective function to be minimized is F (x) ≡ L(x; y) + γhα,β(x). There are many possibilities for solving the optimization problem. For example, proximal decoding aims to solve this continuous minimization problem by using a proximal gradient descent method [12]. In the subsequent argument, we will solve this minimization problem by using the gradient flow. The definition of the generalized GD decoding is given as follows.
Definition 1: Generalized GD decoding The ODE for the generalized GF decoding is given by

dx dt

=

−(∇L(x; y)

+

γ ∇hα,β (x))

(9)

where the initial condition is x(0) = x0.

For example, for AWGN channels, the objective function

has

the

form:

FAWGN(x)

≡

1 2

x−y

2 + γhα,β(x), where y

represents the received word. This means that the generalized

GF-ODE (9) becomes identical to the original GF-ODE (4).

The formulation of GF decoding is useful for continuous

time signal processing by an analog circuit. Figure 1 presents

a block diagram of the analog circuit realizing the continuoustime dynamics given by the generalized GF-ODE (9). The left box represents an analog circuit evaluating the value of −(∇L(x; y) + γ∇hα,β(x)). A similar optical circuit implementing an Ising machine with feedback loop was reported in [9]. In principle, an optical implementation of the analog circuit [26] [27] depicted in Fig.1 would be possible if the gradient part −(∇L(x; y) + γ∇hα,β(x)) can be implemented with an optical circuit [28].

−(∇L(x; y) + γ∇hα,β(x))

Integrator

Fig. 1. Analog circuit for generalized GF decoding corresponding to the generalized GF-ODE (9).
C. Discrete-time version of generalized GF decoding
The Euler method [33] is a well-known numerical method for solving ODEs. We can apply the Euler method to the generalized GF-ODE (9) for discretizing the minimization processes. We immediately have the recurrence formula for a discrete-time version of GF decoding:
s(k+1) = s(k) − η(∇L(s(k); y) + γ∇hα,β(s(k))), k = 0, 1, . . . , (10)
where s(0) = x0. The iterative process in (10) can be regarded as a gradient descent method for minimizing F (x). It might be considered a misnomer, but we also refer to the discretized decoding process (10) as GF decoding. GPUs and digital AI accelerators can be used to execute the discrete time iterative process defined in (10). In such cases, ∇L(s(k); y) + γ∇hα,β(s(k)) should be tensor-friendly, as these devices are designed for tensor-friendly computations.
D. Tensor-computability
Our target hardware includes AI accelerators, such as GPUs, and programmable optical circuits based on MZI. Such hardware is especially efficient for tensor-based computations. Here, we introduce the concept of tensor-computability.
Definition 2: The function f : Cn → Cn is said to be tensor-computable if the value of f can be evaluated by a combination of the following basic tensor operations:
• Tensor product, e.g., matrix-vector product. • Scalar multiplication, e.g., αx(α ∈ C, x ∈ Cn). • Component-wise function applications to a tensor • Vector addition and subtraction, e.g., a + b, a − b. A ’tensor-computable’ function can be naturally evaluated with parallel computation because its basic operations inherently has parallelism. This concept of tensor-computability could also be viewed as the essential criteria for basic, efficient operations that a future AI accelerator should support. While contemporary AI accelerators have the capacity to handle

Authorized licensed use limited to: Yonsei Univ. Downloaded on3J3un5e824,2026 at 15:44:50 UTC from IEEE Xplore. Restrictions apply.

more complex tensor operations, we restrict the permissible operations to maintain a straightforward and clear definition of tensor-computability.
In the realm of optical computing, MZI-based MVP circuits have already been implemented [6], and the computation of logarithmic and exponential functions in the optical domain has been explored [19]. While further advancements in optical circuit technology may be necessary to implement a GF decoder, the previously outlined definition of tensorcomputability seems appropriate for optical computation as well, with such basic operations likely becoming feasible in the near future.
If the righthand side of the generalized GF-ODE (9), ∇F (x) = ∇L(x; y) + γ∇hα,β(x), is tensor-computable, the whole decoding processes can be efficiently executed in an AI-accelerator. The following theorem clarifies the tensorcomputability of ∇F (x).
Theorem 1: If ∇L(x; y) is tensor-computable, then ∇F (x) is tensor-computable. (Proof) To prove the theorem’s claim, it is sufficient to show that ∇hα,β(x) is tensor-computable. Lemma 1 presented in the next subsection establishes the tensor-computability of the gradient of code potential energy function.

E. Gradient of code potential energy function

For implementing the generalized GF decoding, we need the gradient of the code potential energy function. The first order derivative of the potential energy function can be easily evaluated [10] as

∂ ∂xk hα,β(x)

= 4α(xk − 1⎛)(⎛xk + 1)xk⎞ ⎞ ⎛

⎞

+ 2β

⎝⎝

xj⎠ − 1⎠ ⎝

xj⎠ . (11)

i∈B(k)

j∈A(i)

j∈A(i)\{k}

The gradient ∇hα,β(x) is thus given by

∇hα,β(x) ≡

∂

∂

T

∂x1 hα,β(x), . . . , ∂xn hα,β(x) .

(12)

The following lemma indicates tensor-computability of the gradient of the code potential.
Lemma 1: The gradient ∇hα,β(x) can be evaluated by

∇hα,β(x) = 4α exp (z−1 + z + z+1) + 2β exp (w − z) , (13)

where z, z−1, z+1, w are defined by

z ≡ ln(x),

(14)

z−1 ≡ ln(x − 1),

(15)

z+1 ≡ ln(x + 1),

(16)

w ≡ ln(HT (exp(2Hz) − exp(Hz)))

(17)

if x = 0. This implies that ∇hα,β(x) is tensor-computable. It should be noted that the evaluation method presented
in Lemma 1 may not be optimal in terms of the computing

time. The computing time for evaluating ∇hα,β(x) could be improved depending on the basic tensor operations provided by an AI accelerator. For example, some AI accelerators have efficient sparse tensor operation which can make use of the sparseness of the parity check matrix H.

IV. GF DECODING FOR LDPC-CODED MIMO CHANNELS
In this section, we discuss an application of generalized GF decoding for LDPC-coded massive MIMO channels.

A. Generalized GF-ODE for LDPC-coded MIMO channels
Let A ∈ Rμ×n be a channel matrix. Suppose that a received word y ∈ Rμ is given by y = Ax + w, where w ∈ Rμ is a Gaussian noise vector, the components of which follow an i.i.d. Gaussian distribution. The channel input vector x is assumed to be a codeword of C(H), which implies that we assume BPSK modulation.
In this problem setting, the PDF representing the channel is given by p(y|x) ≡ a exp −b y − Ax 2 where a and b are real constants. Our objective function for the approximate MAP rule can be expressed as

FMIMO(x)

≡

1 2

y − Ax

2 + γhα,β(x).

(18)

The

gradient

of

the

first

term

∇

1 2

y − Ax

2 = AT (Ax − y)

is tensor-computable. By Theorem 1, we immediately have the

following corollary:

Corollary 1: ∇FMIMO(x) is tensor-computable. The generalized GF-ODE for LDPC-coded MIMO channels

thus becomes

dx dt

=

−(AT (Ax

−

y)

+

γ ∇hα,β (x)),

(19)

where the initial condition is given by x(0) = x0. In this case, the recursive formula of the discretized GF decoding is given by

s(k+1) = s(k) − η(AT (As(k) − y) + γ∇hα,β(s(k))), (20)

where s(0) = x0. The details of a decoding process is summarized in Algorithm 1.

B. Experimental conditions

In this subsection, we will explain the details of the numer-

ical experiments for an LDPC-coded MIMO model.

Let A ≡ {ai,j} ∈ CM×N be a channel matrix, where ai,j is the fading coefficient corresponding to the path between

the jth transmit antenna and the ith receive antenna. We

assume each entry of A independently follows the complex

Gaussian distribution with zero mean and unit variance. QPSK

modulation format is assumed for transmitted signals [21].

An equivalent real-valued MIMO model with BPSK modu-

lation [25] can be defined as y = Ax + w where A is given

by

A≡

Re(A ) −Im(A ) Im(A ) Re(A )

∈ Rμ×n,

where μ = 2M and n = 2N . The transmitted word x is randomly chosen from C(H) according to the uniform

Authorized licensed use limited to: Yonsei Univ. Downloaded on3J3un5e924,2026 at 15:44:50 UTC from IEEE Xplore. Restrictions apply.

Algorithm 1 Discretized GF (DGF) decoding for LDPC-coded MIMO channels
1: Sample sC0l � CN(O, a;,I)
2: fork:= 0 to K- 1 do 3: Set the following variables:
z := ln(s(kl),
z_l := ln(s(k)- 1), z+l := ln(s(k) + 1),
w - : ln(HT (exp(2Hz)-exp(Hz))), g := AT(As(k)-y)

With a maximum number of iterations of 100, our proposed method (DGF-100) achieves approximately a 1 .6dB advantage over MMSE+BP at BER= 10-4. Additionally, with DU, the adjustment of the step size for each iteration can expedite the convergence of DGF, further widening the BER performance gap between our proposed method and the MMSE+BP ap­ proach. Meanwhile, by comparing the results at the 50th and
lOOth iterations, we also found that the improvement of DU
on DGF is more obvious in fewer iterations.

4: 5:

shCk:=+l)4a:=exsp<k()z_117+(gz

I

+ z+1) rh)

+

28

exp

(w-z)

6: end for
7: x := sign(sCKl )

8: Output x

a:
� 10-41-t-- ---- - �----�----����--�--��4-- 1

distribution. Each component of the Gaussian noise vector w E IH.JL follows Gaussian distribution with zero mean and
variance a;)2. In this model, ;a u is related to the signal-to­ noise ratio SNR by a�u = (2N)/SNR.
ln the following experiment, we used the regular (3,6)­ LDPC code with n = 204 and m = 102 r34l For decoding, we employ DGF algorithm presented in Algorithm 1. The
parameters of the code potential were set to a = 1, f3 = 2. The hyper parameters were set to!= 10, 77 = 0.001, an= 10-4,
which were determined according to the results of the pre­ experiments. Furthermore, we implemented a deep-unfolded (DU) version of DGF decoding, called DU-DGF decoding, which is expected to speed up the convergence. In detail, we
set a specific step size 17(k l for each iteration instead of the previous common step size 17. Each step size 17(k) is trained
with incremental training [24].
For comparison, we exploited the MMSE detector defined
as x = AT(AAT+(a;,/2)I)-1y. Furthermore, as a baseline,
we employed the combination of the MMSE detector and BP decoding which is to he denoted hy MMSE + BP. The input of the BP decoder after the \1MSE detection was set to �;i;MMSE, where � is a positive scaling parameter, and XMMSE is a soft estimation vector by the MMSE detector. In the following experiments, we set � = 5, which was adjusted heuristically.
C. Numerical results
Fig. 2 shows the bit error rate (BER) performances of the proposed DGF and its DU version, DU-DGF, alongside baseline schemes. The terms "DGF-50" and "DU-DGF-50" represent the BER performance evaluated at the 50th iteration, while "DGF-100" and "DU-DGF-100" represent the BER performance at the 100th iteration. The maximum number of iterations for BP decoding was set to 100.
From the results, we can find that the BER performance of the plain M\1SE detector is far behind other methods that consider LDPC encoding (MMSE+BP, DGF, and DU-DGF).

10-5 -+- MMSE+BP ->E- DGF-50
10-6 ... DU-DGF-50 ->E- DGF-100 ... DU-DGF-100

5

6

7

8

SNR [dB]

9

10

Fig. 2. Bit error rate performances of DGF/DU-DGF decoding and baseline schemes (n = 204, m = 102).

V. CONCLUDING SUMMARY
This paper proposed the generalized GF decoding, which is tensor-friendly, making it well-suited for next-generation AI accelerators. The underlying principle of generalized GF decoding is remarkably universal, meaning it can be applied to a wide range of non-trivial channels, provided that the gradient of the negative log-likelihood function can be efficiently eval­ uated. Some examples of such channels have been discussed
in [32] [12]. Another significant advantage of our proposed
method is its suitability for deep unfolding. This is due to the ease of evaluating the gradient of the objective function, which can be efficiently handled using automatic differenti­ ation mechanisms or back-propagation because every part of the algorithm is differentiable. When considering the balance between decoding complexity and decoding performance, the generalized GF decoding shows considerable promise for LDPC-coded MlMO channels.
Traditionally, signal processing algorithms in a receiver
have been designed following a modular design principle.
Typically, components like a MIMO signal detector and an LDPC decoder are designed separately and then integrated. However, the principle of generalized GF decoding promotes
a joint design principle [20], potentially enhancing the overall
performance of the receiver. The methodology outlined in this paper may pave the way for novel strategies in developing jointly designed signal processing algorithms within a receiver.

Authorized licensed use limited to: Yonsei Univ. Downloaded on3J3un6e024,2026 at 15:44:50 UTC from IEEE Xplore. Restrictions apply.

REFERENCES
[1] R. G. Gallager, “Low density parity check codes,” MIT Press, 1963. [2] E. Peltonen et al. , “6G white paper on edge intelligence,” 6G Research
Visions, No. 8. University of Oulu, 2020. [3] OpenAI, “GPT-4 technical report, ” ArXiv:2303.08774v4, 2023. [4] A. Reuther, P. Michaleas, M. Jones, V. Gadepally, S. Samsi, J. Kepner,
“AI and ML accelerator survey and trends,” IEEE High Performance Extreme Computing Conference (HPEC), 2022. [5] J. Carolan et al.,“Universal linear optics,” Science, vol.349, no. 6249, pp.711-716, 2015. [6] J. Capmany and D. Pe´rez, Programmable Integrated Photonics. Oxford University Press, 2020. [7] X. Pengfei and Z. Zhou, “Silicon-based optoelectronics for generalpurpose matrix computation: a review,” Advanced Photonics, vol.4, no. 4, pp.044001-044001, 2022. [8] H. Zhang and M. Gu and X. D. Jiang and et al., “An optical neural chip for implementing complex-valued neural network,” Nature Commun., vol.12, 2021. [9] M.Prabhu et al., “Accelerating recurrent Ising machines in photonic integrated circuits,” Optica, no.7, pp.551-558, 2020. [10] T. Wadayama, K. Nakajima, and A. Nakai-Kasai, “Gradient flow decoding for LDPC codes,” International Symposium on Topics in Coding (ISTC2023), Brest, France, 2023. [11] T. Wadayama, K. Nakamura, M. Yagita, Y. Funahashi, S. Usami, I. Takumi, “Gradient descent bit flipping algorithms for decoding LDPC codes”, IEEE Trans. Comm., pp.1610-1614, vol.58, no.6, 2010. [12] T. Wadayama and S. Takabe, “Proximal decoding for LDPC codes,” IEICE Transactions on Fundamentals of Electronics, Communications and Computer Sciences, vol. E106-A, no. 3 pp. 359-367, 2023. [13] J. Feldman, “Decoding error-correcting codes via linear programming,” Massachusetts Institute of Technology, Ph. D. thesis, 2003. [14] P. O. Vontobel, “Interior-point algorithms for linear-programming decoding,” IEEE Information Theory and Applications Workshop, 2008. [15] T.Wadayama, “Interior point decoding for linear vector channels based on convex optimization,” IEEE Transactions on Information Theory, vol.56, no.10, pp.4905–4921, 2010. [16] G. Sundararajan and C. Winstead and E. Boutillon, “Noisy gradient descent bit-flip decoding for LDPC codes,” IEEE Transactions on Communications, vol.62, no.10, pp.3385–3400, 2014. [17] X. Zhang and P. H. Siegel, “Efficient iterative LP decoding of LDPC Codes with alternating direction method of multipliers,” IEEE International Symposium on Information Theory (ISIT), 2013. [18] S. Barman, X. Liu, and S. C. Draper, and B. Recht, “Decomposition methods for large scale LP decoding,” IEEE Transactions on Information Theory, vol.59, no.12, pp.7870-7886, 2013. [19] Y. Jiang, P. T. S. DeVore, A. Mahjoubfar, and B. Jalali, “Analog logarithmic computing primitives with silicon photonics,” in Conference on Lasers and Electro-Optics, OSA Technical Digest, 2016. [20] A.P. Worthen and W.E. Stark, “Unified design of iterative receivers using factor graphs,” IEEE Transactions on Information Theory, vol.47, pp.843–849, 2001. [21] E. Bjo¨rnson, J. Hoydis, and L. Sanguinetti, “Massive MIMO Networks: Spectral, Energy, and Hardware Efficiency,” Foundations and Trends® in Signal Processing, vol. 11, no. 3-4, pp. 154–655, 2017. [22] K. Gregor, and Y. LeCun, “Learning fast approximations of sparse coding,” Proc. 27th Int. Conf. Machine Learning, pp. 399-406, 2010. [23] A. Balatsoukas-Stimming and C. Studer, “Deep unfolding for communications systems: a survey and some new directions,” in Proc. 2019 IEEE International Workshop on Signal Processing Systems (SiPS), 2019, pp. 266–271. [24] D. Ito, S. Takabe, and T. Wadayama, “Trainable ISTA for sparse signal recovery,” in IEEE Transactions on Signal Processing, vol. 67, no. 12, pp. 3113–3125, 2019. [25] S. Takabe, M. Imanishi, T. Wadayama, R. Hayakawa, and K. Hayashi, “Trainable projected gradient detector for massive overloaded MIMO channels: data-driven tuning approach,” IEEE Access, 2019,vol. 7, pp. 93326–93338. [26] B. Ulmann, Analog and hybrid computer programming. Walter de Gruyter, 2020. [27] S. Ko¨ppel, B. Ulmann, L. Heimann, and D. Killat, “Using analog computers in today’s largest computational challenges,” Advances in Radio Science, vol. 19, pp. 105–116, 2021.

[28] L. Lu, J. Wu, T. Wang, and Y. SU, “Compact all-optical differentialequation solver based on silicon microring resonator,” Frontiers of Optoelectronics, vol. 5, pp. 99–106, 2012.
[29] T. Wadayama, “Fully analog noise-resilient dynamical systems storing binary sequence,” IEEE International Symposiumn on Information Theory (ISIT), 2022.
[30] A. Nakai-Kasai and T. Wadayama, “MMSE signal detection for MIMO systems based on ordinary differential equation,” IEEE Global Communications Conference (GLOBECOM), 2022.
[31] T. Wadayama and A. Nakai-Kasai, “Ordinary differential equation-based sparse signal recovery,” International Symposium on Information Theory and Its Applications (ISITA), 2022.
[32] T. Wadayama and S. Takabe, “Proximal decoding for LDPC-coded massive MIMO channels,” IEEE International Symposiumn on Information Theory (ISIT), 2021.
[33] D. F. Griffiths and D. J. Higham, Numerical Methods for Ordinary Differential Equations. Springer, 2010.
[34] D. J. C. MacKay, “Encyclopedia of sparse graph codes [online],” Available: http://www.inference.phy.cam.ac.uk/mackay/codes/data.html.

Authorized licensed use limited to: Yonsei Univ. Downloaded on3J3un6e124,2026 at 15:44:50 UTC from IEEE Xplore. Restrictions apply.

