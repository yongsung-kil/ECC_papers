2013 IEEE Wireless Communications and Networking Conference (WCNC): PHY

Locally-Optimized Reweighted Belief Propagation for Decoding Finite-Length LDPC codes

Jingjing Liu
Department of Electronics The University of York
Heslington, York, YO10 5DD, UK Email: jI622@ohm.york.ac.uk

Rodrigo C. de Lamare
Department of Electronics The University of York
Heslington, York, YOlO 5DD, UK Email: rcdI500@ohm.york.ac.uk

Henk Wymeersch
Department of Signals and Systems Chalmers University of Technology
41296 Gothenburg, Sweden Email: henkw@chalmers.se

Abstract-In practice, LDPC codes are decoded using message passing methods. These methods offer good performance hut tend to converge slowly and sometimes fail to converge and to decode the desired codewords correctly. Recently, tree-reweighted message passing methods have been modified to improve the convergence speed at little or no additional complexity cost. This paper extends this line of work and proposes a new class of locally optimized reweighting strategies, which are suitable for both regular and irregular LDPC codes. The proposed decoding algorithm first splits the factor graph into subgraphs and subsequently performs a local optimization of reweighting parameters. Simulations show that the proposed decoding algorithm significantly outperforms the standard message passing and existing reweighting techniques.
Index Terms-LDPC codes, belief propagation algorithms, decoding techniques.
I. INT RODUCTION
Low density parity check (LDPC) codes are among the most important capacity-approaching error-correcting codes [l]. They have a long history, deep theoretical underpinning, and have found practical application in multiple standards [2]. The main power of LDPC codes lies in their pseudo-random nature, assuring good properties for code, and the availability of low-complexity decoding algorithms. Recently, a great deal of research has been devoted to the design of LDPC codes with short to moderate block lengths, which correspond many application scenarios in wireless standards [2].
Decoding is commonly based on iterative message­ passing methods, allowing local parallel computations. While message-passing decoding leads to good performance in terms of bit error rate (BER), it suffers from a number of drawbacks: (i) convergence to a codeword can take many iterations, espe­ cially with low signal-to-noise ratios (SNR); (ii) convergence to a codeword is not guaranteed; (iii) LDPC code design is guided by the decoding algorithm, constraining codes to have large girths. In this context, the occurrence of short cycles and stopping sets have a significant impact on the performance of LDPC codes, and require the development of novel decoding strategies that mitigate these drawbacks.
Different approaches have been considered to deal with

these issues. The most prominent approach is linear pro­ gramming (LP) based decoding, which relaxes the decoding problem to an LP and has a maximum likelihood certificate property [3]. LP decoders suffer from high complexity (expo­ nential in the check node degree), unless further relaxations are employed [4]. Another line of investigation, which aims to improve performance while still maintaining the message passing nature of the decoder, is that of tree-reweighted message passing decoding. Based on tree-reweighted belief propagation [5], decoding reverts to a tractable convex op­ timization problem, iteratively computing beliefs and factor appearance probabilities (FAPs). These concepts were applied to LDPC decoding in [6], [7] where the FAPs were optimized in an offline procedure, subject to additional constraints: in [6], the FAPs were constrained to be constant, while in [7], the FAPs were constrained to take on two possible values. In both cases, gains with respect to standard message passing decoding were observed.
In this paper, we continue this latter line of research and explicitly optimize the FAPs off-line, without the constraints from [6], [7]. This allows more freedom in the decoding algorithm without additional online computational complexity. We propose a locally-optimized reweighting belief propagation (LOW-BP) decoding algorithm that first splits the factor graph corresponding to the code into subgraphs and then performs lo­ cal optimization of the reweighting parameters. The proposed LOW-BP algorithm can mitigate the effects of short cycles in factor graphs by applying a reweighting strategy per subgraph. The LOW-BP algorithm is evaluated for regular and irregular LDPC codes.
This paper is structured as follows. Section II briefly de­ scribes the LDPC system model. Section III reviews reweight­ ing strategies and existing algorithms. Section IV is dedicated to a detailed description of the proposed LOW-BP algorithm, whereas Section V presents and discusses the simulation results. Section VI draws conclusions from the work.
II. LDPC SYSTEM MODEL
We consider a rate K/N code, with parity check matrix H,
and a corresponding set of codewords C. Note that x E C if

978-1-4673-5939-9/13/$31.00 ©2013 IEEE

4311

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:24:07 UTC from IEEE Xplore. Restrictions apply.

and only if Hx= O. Assuming binary phase-shift keying and
transmission over an additive white Gaussian noise (AWGN) channel, the received data are described by

2 y= x -1 + n,

(1)

where n is a tributed (i.i.d.)

sequence of N independently
AWGN samples with variance

i(dJ"e2,ntaicnadllxy

dis­
EC

is the transmitted codeword. Given y, the aim of the iterative

decoder is to recover x in an iterative fashion until either

Hx = 0 or the maximum number of decoding iterations

is reached. Iterative decoding can be interpreted as message

passing on a suitable factor graph, and is often implemented

using belief propagation (BP), or a variation thereof.

The factor graph corresponding to our model, Q(V, E),

include the check and variable nodes V = Vc U Vs, as well as

a set of edges, E C;; Vc x Vs, such that an edge connecting the

check node Ci and the variable node Sj exists in the factor

graph only if the entry hij of the parity-check matrix H
equals 1. The decoding process can be interpreted as finding
x = argrnaxxp(xly). Using Bayes' rule, this a posteriori
distribution becomes

P(xIy)

-

p(ylx)p(x) p(y)

,

(2)

which then factorizes as

N

M

p(xly) ex II ¢n(xn) II 1/Jm(XCm),

(3)

n=l

rn=l

where the potential function ¢n(xn) corresponds to P(Yn IXn),
and the compatibility functionl/Jrn(xcm) corresponds to an
indicator :[ {2:nECm Xn = O} within a clique Cm, form = 1,2,... , Aif, corresponding to the m-th check [5], [13], with
M=N - K.

III. REWEIGHTING ST R ATEGIES AND VARIATIONS

When a factor graph contains cycles, the standard BP algo­

rithm may require a larger number of iterations and may still

fail to converge. To tackle the issue of non-convergence, Wain­

wright et al. [5] presented the tree-reweighted BP (TRW-BP)

algorithm for cyclic graphs, which involves a variational op­
timization problem involving the FAPs p = [PI,P2,...,PM]'
given by

N

M

F(b,p)= L H(bn) - L PmICm(bcm)

n=l

m=l

N

+ L L bn(xn) log ¢n(xn)

(4)

n=l Xn,

M

L L +

bc,Jxcm) log 1/Jm(xc",) ,

rn=l XC'nl

where b(·) denotes the so-called belief, H(bn) is the entropy of the belief of the n-th variable, ¢n(xn) and l/Jrn(xcm)
are the potential function and the compatibility function,
respectively, which are defined depending on the application.

TABLE I

LOW-BP for Decoding LDPC Codes

Offline: subgraphs formation
I: Given an expansion strategy (disjoint or RA) and dmax,
apply PEG expansion to generate T ::0- 1 subgraphs;

Pt Offline: optimization of

for the t-th subgraph

p�O) 2: Initialize

to an allowable value;

b(Xt) 3: For each subgraph, calculate the beliefs

and

the mutual information term It = [It.l, It,2, ..., It,Ll]

by using reweighted message passing rule (5)-(7);

b(xt) It 4: With

and obtained from step 3, update

p� P; r) to r+ 1) using the conditional gradient method

(detailed in the Appendix);

Pt 5: Repeat steps 3-4 until

converges for each subgraph;

P [PI, P2, ..., P!vI] Offline: choice of =

for decoding

PI, ..., pi, ..., PT. 6: For all T subgraphs, collect
In case of disagreement on a value pm for the rn-th check node, choose the value offering the best performance;

Online: real-time decoding

7: Use reweighted message passing decoding (5)-(7) with

p [PI, P2, ..., P!vI optimized =

1 during actual data transmission.

Notice that if we use Cm to denote the m-th node's cluster

(m = 1,2,... , M), then bCm

) denotes joint belief and

ICm(bcm) represents joint mutual information term [8]. The
optimization with respect to (b,p) starts with a fixed p(k), then solves this for the stationary points of F (b,p(k») via TRW-BP. Next, for a fixed belief vector b(k), minimizing the function F(b(k),p) with respect to p results in an updated p(k+I). This

algorithm keeps running recursively until the belief converges.

Observe that standard BP corresponds to the sub-optimal and
p generally invalid choice =1. The work reported in [5] is not

directly applicable to problems such as the decoding of LDPC

codes, and only derives message-passing rules for graphs with

pairwise interactions.

In [6], [9], the uniformly reweighted BP (URW-BP) algo­

rithm extends pairwise factorizations to higher order interac­

tions
pE

(aOn,dI]rMedutoceas

a series of globally optimized parameters
simple constant p" E (0,1]. Additionally,

the FAPs are generalized to edge appearance probabilities

(EAPs) so that the problem size is significantly reduced.

Another reweighting strategy is referred to as variable FAP
p BP (VFAP-BP) [7] that aims to select on the basis of the

cycle distribution of the graph. However, neither URW-BP nor
p VFAP-BP optimizes the values of explicitly.

4312
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:24:07 UTC from IEEE Xplore. Restrictions apply.

Algorithm 1 Progressive edge-growth (PEG) expansion for
the t-th subgraph (t = 1,2,... ,T)

1: while the complementary set Vi is not empty do

2:

- 1 N j for = 0 ---+

do

3:

1J)Sj - 1 for k = 0 ---+

do

4:

if k = 0 then

5:

add the first edge(ci' Sj) denoted by e�j to Sj,

such that Ci has the lowest degree under the

current subgraph

6:

else

7:

keep expanding the subgraph from root Sj and

remove dmax is

(Ci' Sj) from Vi
reached or Ni}

until
cf 0

the but

mNasxJ+dim1 u= m0level

8:

end if

9:

end for

10: end for

11: end while

IV. PROPOSED LOW-BP ALGORITHM FOR DECODING LDPC CODES
In this section, we describe LOW-BP, which explicitly opti­
mizes the reweighting parameter vector P = [P1,P2,...,P!VI]'
By allowing optimization over smaller subgraphs, LOW-BP is able to trade off complexity vs. performance. LOW-BP comprises an offline phase, during which, for a fixed SNR and
a fixed code, the best choice of P is determined, as outlined
in Table I. The online phase of LOW-BP occurs during real­
p time decoding, when optimized is used in the reweighted
message passing decoding algorithm.
A. Offline Phase of LOW-BP
In the offline phase, we transform the factor graph into a set
of T � 1 subgraphs and then locally optimize the reweighting parameter vector Pt for each subgraph, where t = 1,2,... ,T. Note that when T > 1 the dimension of Pt depends on
the size of the t-th subgraph. The optimization turns out to be significantly less complex when more subgraphs are considered, hence there is a need for a flexible method to decompose the original factor graph into subgraphs. We apply the progressive-edge growth (PEG) technique [10] to this end.
Construction of T Subgraphs: PEG expands Q(V, E) into T subgraphs Qt (Vi, Et ), where Vi and Et are subsets of V and E, respectively. This method is straightforward to use
if the LDPC code was designed by PEG, or its variations [11], [12], but is not limited to such designs. We consider a disjoint strategy or are-appearance (RA) strategy to apply PEG. The disjoint strategy prohibits duplicates of check nodes in all subgraphs, while the RA strategy allows check nodes to appear more than once over T subgraphs. In general, the number of subgraphs T depends on: (i) a pre-set maximum expansion level dmax, as a large dmax results in a small T but a high probability of the existence of very short cycles within subgraphs; (ii) given dmax, whether all the nodes of
V are included in the expanded subgraphs. Let us denote the
degree of a variable node Sj by 1J)Sj' and define NSjd as the

neighborhood of Sj at current expansion level d, as well as NSdj being the complement of Ns} d. To generate the t-th subgraph
Qt (Vi, Et ) based on Q(V, E), the PEG expansion is detailed in Algorithm 1. In the case of the RA strategy, Vi, the set of candidate nodes of Qt (Vi, Et ), is always initialized as V , for each of the T expansions. On the other hand, Vi is the complement set of Vi-1 if the disjoint strategy is applied, so that the size of subgraph Qt (Vi, Et ) decreases as t increases. Furthermore, some of the subgraphs, such as QT(VT' ET) or QT-1(VT-1, ET-d, may be a acyclic (i.e., a tree), with corresponding reweighting factors Pt = 1, which complies
with the observations in [5], [6]. Compared to the greedy

search algorithm in [10], [11], our PEG-based expansion stops
as soon as every member of Vi has been visited. The number
of edges incident to Sj might be less than 1J)Sj as some short
cycles are excluded from the subgraphs so as to guarantee
that the local girth of each subgraph is always larger than the
global girth of the original graph. Optimization of FAPs: After obtaining T subgraphs, we
introduce L = [L1' L2,...,LT]' where Lt is the number of
check nodes (possibly with duplicates) in the t-th subgraph.
L Note that Lt t = lv1 if a disjoint expansion is used, while Lt Lt > lv1 if the RA expansion is employed. Observe also 1, L that when T = then = Aif. With the t-th subgraph, we optimize the associated FAPs Pt = [Pt,l,Pt,2,... , Pt,LJ using
the optimization method for TRW-BP [5], modified to higher­
order interactions, with the corresponding message-passing
rules from [9]. The optimization problem is solved recursively

p�O) as follows, starting from the initial values

for each of the

T subgraphs:

p�r), 1) For all T subgraphs in parallel, for fixed

use

message-passing to calculate the beliefs b(Xt ) and the

mutual information term It = [It,l,It,2,""It,L, ], pro­ vided with Lt <:: lv1 check nodes in the t-th subgraph.

2) For all T subgraphs in parallel, given {Id[=l' use

the conditional gradient method (see the Appendix) to

update, for all t, p�r) to p�r+1), then go back to step 1).

When applying the RA strategy, it is possible for a check node to have a non-unique FAP. In that case, we choose the FAP that gives the best BER performance through simulations. For clarity, the two steps above are referred to as a recursion. The optimization of the FAPs runs for as many recursions as
are necessary until each Pt converges, in order to acquire the
optimal reweighting vector.

B. Online Phase of LOW-BP

Once the optimized values of P = [P1,P2,...,P!VI] are

found, actual data transmission can commence. For complete­

ness, we briefly review the message-passing rules applied here

(see [9] for more details). All messages are represented in log­

IvkaerliiahnocoedCJr2a,titohse

(LLRs). For an AW GN message from the n-th

channel variable

with node

noise Sn to

the m-th check node Cm is given by

wnm = Ach.n + L Pm,Am'n - (1 - Pm)Amn, (5)
m'EN(n)\m

4313
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:24:07 UTC from IEEE Xplore. Restrictions apply.

0.08

� Irregular codes

-- Regular codes

0.07

Z. 0.06 �.� 0.05

� 0.04
.0
2
D- 0.03

0.02

0.01

O"__�L---�----�-----L----��'"

0.6

0.65

0.7

0.75

0.8

0.85

0.9

Values ofp

10-6

<) LOW-BP with RA selection (T=25) --+- LOW-BP with disjoint selection (T=9)

_ LOW-BP (T=1)

10-7o' 0.5 - 2 2.5 -�"

Fig. 1. Histograms of the p values for the regular and irregular codes at an SNR of 2 dB. The p is derived by using LOW-BP optimization with disjoint selection and run until convergence.

Fig. 2. Comparison of decoding performance using the proposed LOW-BP algorithm with various numbers of subgraphs T for the regular code.

where ACh.n = log(P(Ynlxn = 1)/P(Ynlxn = 0)) = 2Yn/(J2,
rn' E N(n)\rn is the neighboring set of check nodes of Sn
except em. The quantity Arnn denotes messages sent from em to Sn in previous decoding iteration, then for check nodes em we update Arnn as:

(6)

mW0::

where fEE(-) denotes the standard BP message passing rule to
compute an LLR message from check node ern to variable node Sn. The function fEE(.) can be implemented by using
the well-known hyperbolic tangent expressions [7], or the
numerically more stable Jacobian logarithm [13], [14]. Upon
convergence, we have the belief Abelief,n with respect to Xn
given by

Abelief.n = Ach.n + 2..: PmArnn.

(7)

rnEN(n)

It should be noted that in (5)-(7), the standard BP algorithm
corresponds to Pm = 1, vrn. The receiver utilizes the above
message-passing rules and does not update p as long as the
channel conditions are unchanged.

V. SIMULATION RESULTS
In this section, we show the numerical results obtained from applying the proposed LOW-BP algorithm to the decoding of regular and irregular LDPC codes with short block lengths, over the additive white Gaussian noise (AWGN) channel. The
regular code has block length N = 500 and rate R = 1/2, 4 6. with constant column weight Ws = and row weight We =
The irregular code has the same block length and rate, but a
variable node degree distribution A(X) = 0.21 X X5 + 0.25 X x3 + 0.25 X x2 + 0.29 x x and a constant check node degree
of 5. For the sake of numerical stability and data storage, all messages are represented as LLRs, and the Jacobian logarithm [13], [14] is used to compute the messages passed from check

"" ..

-- Standard BP
...¢. LOW-BP with RA selection (T=30) --+- LOW-BP with disjoint selection (T=12)

.

---+ LOW-BP (T=1)

0.5

2.5

Fig. 3. Comparison of decoding performance using the proposed LOW-BP algorithm with various numbers of subgraphs T for the irregular code.

nodes to variable nodes. In the offline phase of LOW-BP,
1000 codewords known to the receiver are transmitted so as to optimize p = [Pl,P2,""PM]' We allowed up to 60 decoding
iterations in the online phase.
Fig. 1 illustrates the distribution of the reweighting parame­
ters for the regular code and the irregular code, at an SNR of 2
dB. It is clear that the optimized p of irregular codes is widely
distributed over the range of [0.6,0.9], while the p-distribution
for regular codes is more concentrated within a smaller range
[0.8,0.85]. This observation is congruent with the findings in
[5], [6], which state that for symmetric graphs, the optimal
reweighting parameters should be more or less constant.
In Fig. 2 and Fig. 3, the BER performance with ditlerent values of T, with RA and disjoint selection, are compared to
the standard BP algorithm, for the regular and the irregular
code, respectively. We observe a performance gain of up
to 0.4 dB over the standard BP algorithm by using the

4314
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:24:07 UTC from IEEE Xplore. Restrictions apply.

f(If]i 10-4

" ---B URW-BP
.. ---B- VFAP-BP _ LOW-BP (T=l)
-- Standard BP

A comparison with existing reweighted methods is shown in Fig. 4 and Fig. 5, for the regular and irregular code, respectively. The algorithms considered are URW-BP from [6], VFAP-BP from [7], and the proposed LOW-BP algorithm (with T = 1). For the regular code, we observe that URW­ BP and VFAP-BP outperform standard BP. LOW-BP is able to provide further improvements. For the irregular code, the optimal constant value of the FAP in URW-BP is p = 1, so that BP and URW-BP coincide. VFAP-BP provides a small performance gain, while LOW-BP again outperforms BP by
up to 0.4 dB. We clearly see that explicit optimization of P
leads to non-trivial performance gains.

10-7o' 0.5 - 1.5 2.5-�" Fig. 4. Comparison of the performance of BP, URW-BP, YFAP-BP and the proposed LOW-BP for decoding the regular code.
"(wI']

VI. CONCLUSION
We have proposed a locally-optimized reweighting belief propagation (LOW-BP) algorithm for decoding finite-length LDPC codes. The proposed algorithm has been compared to previously reported reweighted belief propagation algorithms and has demonstrated superior performance for the scenarios considered. LOW-BP comprises an offline and an online stage. The online stage relies on standard tree-reweighted belief propagation, while the offline stage involves an optimiza­ tion problem over subgraphs of the original factor graph. By increasing the number of subgraphs, the offline stage converges faster and exhibits less complexity. Reducing the number of subgraphs will lead to improved BER performance, albeit at an additional delay and complexity cost during the offline stage. LOW-BP is especially well suited to decoding of short to moderate LDPC codes and is a promising choice for applications that require a reduced number of decoding iterations. Future avenues of research include fast adaptation of the offline stage to time-varying channel conditions.

ApPENDIX: DETAILS OF CONDITIONAL GR ADIENT

Fig. 5. Comparison of the performance of BP (same results as URW-BP), URW-BP, YFAP-BP and the proposed LOW-BP for decoding the irregular code.
proposed LOW-BP method. For the regular code, all check nodes are visited once in T = 9 subgraphs with the disjoint selection, while with RA selection T = 25 subgraphs are generated (maximum 60 recursions), where some check nodes are revisited. For the irregular code, the disjoint selection generates T = 12, meanwhile the RA selection gives T = 30 (maximum 100 recursions). When using disjoint selection,
P converges to a set of stable values after a number of
recursions that varies from one subgraph to another. Notice that in both figures, T = 1 is a special case that corresponds to Wainwright et al. 's optimal solution from [5]. For T = 1,
to improve convergence in the offline phase, we initialized P
from URW-BP [6] for regular code and from VFAP-BP [7] for irregular code. Normally, around 800 recursions were required to converge for the regular code and 2700 recursions for the irregular code.

Our goal is to minimize (4) with respect to the column
vector Pt, for a specific subgraph �ft(Vt,Et). Dropping terms
that do not depend on Pt, we find the following optimization
problem with It = [It,1 It,2 • • • It,Lt lt , where (-)t denotes the
transpose:
p!It mllllffilze s.t. Pt E lI'(gt(Vt,Et)),
where lI'(gt(Vt,Et)) is the set of all valid FAPs over the subgraph gt(Vt,Et) and It,! is a mutual information term, which depends on p�r), the previous value of Pt. We will denote the objective function by !(Pt) = - p!It and use the
conditional gradient method to update Pt, similar to [5]. In the conditional gradient method, we first linearize the objective
around the current value p�r):
iwnitwhhriecshpe\c7tpt,oJP(pt,�rd)e)n= oti-ngItt.heSemcoinnidmlyiz, ewr ebymPin;imanidzez�!rJi+nl(P) = t )

4315
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:24:07 UTC from IEEE Xplore. Restrictions apply.

�r») p�r) max(flin(Pn, z 2 , where z = - 00 . Finally,

is updated

to p�r+l) as

P(tr+l) -_ P(tr) + a(P*t - P(tr»),

(9)

a in which is chosen as

arg min
aE[O,l]

f(p(t r)

+

a(p*t

_

pet r»)).

(10)

f(p�r») At every iteration,

is an upper bound on the optimized

objective, while z�r+l) is a lower bound.

ACKNOWLEDGMENT
This work was supported, in part, by the European Research Council, under grant No. 258418.

REFERENCES
[1] R.G. Gallager, "Low density parity-check codes," IRE Trans. Infi)rm. Theory, vol. IT-8, pp. 21-29, Jan. 1962.
[2] W. Ryan and S. Lin,"Channel Codes: Classical and Modem;' Cambridge University Press, 1st edition, Oct. 30, 2009.
[3] J. Feldman, M. Wainwright, and D. Karger, "Using linear programming to decode binary linear codes," IEEE Trans. Inform. Theory, vol. 51, no. 3, pp. 954-972, 2005.
[4] S. Barman, X. Liu, S. C. Draper, and B. Recht, "Decomposition methods for large scale LP decoding;' preprint arXiv:1204.0556.
[5] M. J. Wainwright, T. S. Jaakkola, and AS. Willsky, "A new class of upper bounds on the log partition function," IEEE Trans. Infi)rmation Theory, vol. 51, no. 7, pp. 2313 - 2335, July 2005.
[6] H. Wymeersch, F. Penna and Y. Savic, "Uniformly Reweighted Belief Propagation for Estimation and Detection in Wireless Networks," IEEE Trans. Wireless Communications, vol.11 PP, No.4, pp.1587-1595, Apr. 2012.
[7] J. Liu, R. C. de Lamare, "Low-Latency Reweighted Belief Propagation Decoding for LDPC Codes," IEEE Communications Leiters, vol. 16, no. 10, pp. 1660-1663, October 2012.
[8] T. M. Cover and J. A Thomas, "Elements of Information Theory," 2nd edition. Wiley-Interscience, 2006.
[9] H. Wymeersch, F. Penna and Y. Savic, "Uniformly reweighted belief propagation: A factor graph approach," Information Theory Proceedings (ISIT), 2011 IEEE International Symposium on Issue Date: July 31 201I-Aug. 5 2011 On pagers): 2000 - 2004.
[10] Y. Hu, E. Eleftheriou and D. M. Arnold, "Regular and irregular pro­ gressive edge-growth tanner graph," IEEE Transactions on Information Theory, vol. 51, no. I, pp. 386-398, Jan. 2005.
[11] A Uchoa, C. Healy, R. C. de Lamare and R. D. Souza, "Design of LDPC Codes Based on Progressive Edge Growth Techniques for Block Fading Channels," IEEE Communications Leiters, vol. IS, no. II, pp. 1221-1223, 2012.
[12] H. Xiao, A.H. Banihashemi, "Improved progressive-edge-growth (PEG) construction of irregular LDPC codes," IEEE Communications Leiters, vol.8, no.12, pp. 715- 717, Dec. 2004.
[13] H. Wymeersch, "Iterative Receiver Design," Cambridge University Press, Sep. 2007.
[14] P. Robertson, E. Villebrun, and P. Hoeher, "A comparison of optimal and sub-optimal MAP decoding algorithms operating in the log domain," in Pmc. IEEE International Conference on Communications, vol. 2, pp. 1009-1013, 1995.

4316
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:24:07 UTC from IEEE Xplore. Restrictions apply.

