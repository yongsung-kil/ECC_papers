1D-19

A High Performance LDPC Decoder for IEEE802.11n Standard
Wen Ji†, Yuta Abe, Takeshi Ikenaga and Satoshi Goto
Graduate School of Information, Production and Systems, Waseda University 2-7 Hibikino, Wakamatsu-ku, Kitakyushu, Fukuoka, 808-0135, Japan †jiwen@ruri.waseda.jp

Abstract—In this paper, we propose a partially-parallel irregular LDPC decoder for IEEE 802.11n standard. The design is based on a novel sum-delta message passing schedule to achieve high throughput and low area cost design. We further improve the design with pipeline structure and parallel computation. The synthesis result in TSMC 0.18 CMOS technology demonstrates that for (648,324) irregular LDPC code, our decoder achieves 7.5X improvement in throughput, which reaches 402 Mbps at the frequency of 200MHz, with 11% area reduction.
I. INTRODUCTION
A Low-Density Parity-Check (LDPC) code is an error correcting code first proposed by Gallager [1] in the early 1960’s, and re-discovered in 1996. Irregular LDPC codes are constructed enabling data transmission rates close to Shannon Limit. Communication standards, such as DVB-S2, WLAN(802.11n) and WiMAX(802.16e) have adopted or are considering the use of LDPC codes.
In this paper, a high throughput low area cost partiallyparallel irregular LDPC decoder is designed with the novelties as follows: (i)The decoder designed is based on a novel sumdelta message passing schedule well suitable for high throughput and low area design. (ii)The decoding process is further speeded up by a binary sorting structure for row operation and parallel computation for column operation. An efficient pipeline structure is utilized to boost message-passing throughput. (iii)The proposed sum-delta message passing schedule can effectively reduce the storage area for messages passing through computation units by storing only the frequently used messages, thus saving the total area.

II. PROPOSED DECODING SCHEDULE

LDPC code can be decoded iteratively using the Message

Passing (MP) algorithm described in [1]. Each iteration of the

algorithm is composed of four phases: row operation, column

operation, error correction and parity check. The row operation

updates message α using message β, after which, the column

operation updates message β using updated message α and ini-

tial message λ. The computations in row and column operation

are determined by the relationship between α and β messages,

which is represented by a matrix called parity check matrix.

Equation (1) and (2) illustrate the detailed calculation, where α(m, n) and β(m, n) denote the message in mth row and nth

column of the parity check matrix H, respectively. λ(n) denotes the initial message of nth column. And A(m) and B(n) are

defined as A(m) = {n|Hmn = 1} and B(n) = {m|Hmn = 1}.

α(m, n) = (

sign(β(m, n ))) × min |β(m, n )| (1)

n ∈A(m)\n

n ∈A(m)\n

β(m, n) = λ(n) +

α(m , n)

(2)

m ∈B(n)\m
We develop our sum-delta message passing (SDMP) schedule
to improve design throughput and save storage area. We first introduce the concept of a new message sum(n), which is the sum of λ(n) and all the α(m, n) values of the nth column. Compared with Equation (2), β(m, n) can be computed by subtracting sum(n) by α(m, n). We further simplify the computation of message sum by introducing Δα values.

sum (n) = sum(n) +

Δα(m, n)

(3)

m∈D(n)

In Equation (3), D(n) are those row numbers of which the message α has been updated since the last computation of α(m, n) in column n. The total number of possible addition is reduced from |B(n)| to |D(n)|. Message sum instead of message β is stored and passed through different computation
modules to form a new message passing scheme and save
computation cost. The corresponding storage is also reduced from at most 12 message β to a single message sum.

III. IMPLEMENTATION
The decoder we designed is composed of five parts: row operation, column operation, parity check, controller and storage parts (memory for α value, registers for sum value, Δα value), as shown in Fig. 1.

Input Data

Row Operation Modules

CFU_for _፾ in

CFU_for _8in

Memory

8

4

sum Register

Column Operation Modules

BFU_for _12in

BFU_for _3in

BFU_for _2in

3

10

11

Register

Parity Check

Controller

Output Data
Fig. 1. Block diagram of the decoder

We implement 12 CFUs and 24 BFUs in order to execute
the computation of the same row or column in each sub-
block in parallel. Because of the irregularity of the targeted
decoding matrix, two different row operation modules, and
three different column operation modules are designed for rows and columns with different |A(m)| and |B(n)|. The controller module generates the control signals for storage and operation
modules, while parity check module provides error correction and parity check at the end of each iteration. Message α is only used in the row operation and stored in memory with corresponding row address. Other messages like sum, which have multiple-access problem, are stored in registers.

Fig. 2. Operation modules: (a)Comparison scheme in row operation and (b) structure of column operation
The row operation module, according to Equation (1), mainly involves the computation of the minimum and second minimum β values, which turns out to be the most time consuming part in

127 978-1-4244-27A4u9th-9o/r0iz9e/d$2li5ce.0n0se©d2u0s0e9liImEitEeEd to: Yonsei Univ. Downloaded on June 24,2026 at 02:02:49 UTC from IEEE Xplore. Restrictions apply.

1D-19

the row operation. The detailed comparison procedure proposed

in this design is shown in Fig. 2(a). We implement a binary

sort scheme in this module where the upper tree and the lower tree sorts for the minimum and the second minimum β value

respectively. Compared with a total 9 clock cycles in [2], our

scheme can complete the comparison in only 2 clock cycles.

The column operation module in this design applies the SDMP schedule discussed in Section II, message sum is

updated by addition of its original value and at most two updated Δα values using Equation (3). The throughput is

further improved by utilizing a parallel column operation for all columns with updated α, as shown in Fig. 2(b). The resulting

parallel SDMP-based column operation can be computed in one

clock cycle.

The decoding throughput is further improved by a pipeline

dataflow, as shown in Fig. 3. The row operation is divided into

five stages and the column operation is divided into three stages

to speed up the decoding procedure.

mth read sum,D compute E compute D ' compute D ' write D '

(m+1)th read sum,D compute E compute D ' compute D ' write D ' (m+2)th read sum,D compute E compute D ' compute D '

write D '

Row operation

(m+3)th read sum,D compute E compute D ' compute D ' write D ' (m+4)th read sum,D compute E compute D ' compute D ' (m+5)th read sum,D compute E compute D '

Column operation

mth readsum,'D computesum' write sum'
(m+1)th readsum,'D computesum' write sum' (m+2)th readsum,'D computesum' write sum' (m+3)th readsum,'ÿD computesum'

Fig. 3. Timing schedule of the decoder

IV. MESUREMENT RESULTS
The decoding performance is measured by a C++ simulation program and the Bit Error Rate (BER) performance is shown in Fig. 4.

on the boundaries are for message α. The size of the chip core after layout is 13.69mm2(3.7mm × 3.7mm).
TABLE I COMPARISON OF AREA (μm2) FOR MESSAGES IN TSMC 0.18μm

β sum
α λ Δα
Total saving

Ref. [2]
1,949,487 0
1,949,487 1,301,845
0

Proposed
0 347,036 1,695,501
0 40,395

comparison
+1,949,487 -347,036 +253,986 +1,301,845 -40,395 +3,117,887

Fig. 5. Layout of the decoder

Design rule LDPC Code
LDPC decoder Throughput
Frequency Memory area(gates) Area w/o wiring(gates) Area w/. wiring(μm2)
Power (mW)
chip area (mm2)

TABLE II SYNTHESIS RESULT

Ref. [4] TI 0.11μm 8088bit rate 1/2
irregular
188Mbps (itr=25, SNR=NA)
212MHz 407K 742K N/A N/A
N/A

Ref. [3]* Ref. [2] Proposed

TSMC 0.18μm

802.11n 648bit rate 1/2 irregular

partially-parallel

54Mbps

54Mbps 404Mbps

( itr=5, SNR=3.0dB )

200MHz 200MHz 200MHz

708K

502K

170K

832K

611K

313K

13,090,549 9,004,366 8,012,999

765.85

486.44

712.38

( @200MHz, 1.6V )

N/A

N/A

13.69

Fig. 4. Bit error rate performance
The area comparison of storages between the proposed design and [2] is listed in Table I, where a significant area reduction in storage part is achieved. The synthesis results of the proposed decoder are listed in Table II. We compare our work with [2] and modified work of [3] ([3]* in the table) under the same LDPC code and design rule, and with typical partially-parallel decoder design [4] under different specifications and design rules. Because of the novel SDMP schedule along with the improved row and column operation modules, our proposed decoder requires only 32 clock cycles for a single iteration and five iterations for codeword correction under Signal-to-Noise Ratio (SNR) of 3.0dB and achieves the highest throughput. Because of the storage optimization, the proposed decoder achieves a gate count reduction of 39% and 11% compared with [3], [2] respectively.
The backend design of the proposed decoder is implemented by Synopsys Astro with ARM’s Artisan SAGE-X 0.18μm 1P6M stand-cell library for TSMC. The layout is provided in Fig. 5, where modules are labeled in the figure and memories

V. CONCLUSION
In this paper, a novel high throughput low area cost partiallyparallel irregular LDPC decoder is proposed. Row and column operations are speeded up by a binary sort scheme and SDMP schedule. Moreover a pipeline structure is utilized to further compact the procedure. Area cost is decreased because of the use of the proposed SDMP schedule. The synthesis result demonstrates that our decoder can achieve a much higher throughput with less area cost compared to other partiallyparallel irregular LDPC decoders.
VI. ACKNOWLEDGEMENT
This research is supported by CREST, JST and ”Ambient SoC Global COE Program of Waseda University” of the Ministry of Education, Culture, Sport, Science and Technology, Japan.
REFERENCES
[1] R. G. Gallager, Low-Density Parity-Check Codes, MIT Press, Cambridge, MA, 1963.
[2] X. Li, Y. Abe, K. Shimizu, Z. Qiu, T. Ikenaga, and S. Goto, “Cost-efficient parallel irregular LDPC decoder with message passing schedule,” in Int. Symp. Integrated Circuits, Sept. 2007, pp. 548–551.
[3] K. Shimizu, T. Ishikawa, N. Togawa, T. Ikenaga, and S. Goto, “Powerefficient LDPC decoder architecture based on accelerated message-passing schedule,” IEICE Trans. Fundamentals, vol. E89-A, no. 12, pp. 3602–3612, Dec. 2006.
[4] Y. Chen and D. Hocevar, “A FPGA and ASIC implementation of rate 1/2 8088-b irregular low density parity check decoder,” in IEEE Global Telecommunications Conf., Dec. 2003, pp. 113–117.

128 Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 02:02:49 UTC from IEEE Xplore. Restrictions apply.

