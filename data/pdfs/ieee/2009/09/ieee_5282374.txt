682

IEEE COMMUNICATIONS LETTERS, VOL. 13, NO. 9, SEPTEMBER 2009

Bilateral Exchange of Soft-Information for Iterative Reliability-Based Decoding with Adaptive Belief Propagation

Chung-Hsuan Wang, Yu-Min Hsieh, and Hsin-Chuan Kuo

Abstract—For linear block codes without a sparse graph representation, there exists an iterative decoding algorithm which combines the traditional reliability-based decoding (RBD) with adaptive belief propagation (ABP) to achieve a good tradeoff between the error performance and decoding complexity. However, in the original design of the iterative scheme, only a one-way flow of soft-information from the ABP-part to the RBD-part is available, hence limiting the performance of iterative processing. In this study, several low-complexity schemes are presented for the RBD-part to produce desirable soft-outputs such that decoded results can be bilaterally exchanged between both of the RBD and ABP parts. Simulation results also verify the superiority of the proposed idea over the conventional design.
Index Terms—Linear block codes, reliability-based decoding, adaptive belief propagation.
I. INTRODUCTION
G RAPH-BASED decoding with belief propagation has been extensively applied to low density parity-check codes and shown to achieve remarkable performance. However, for the traditional block codes, e.g., Reed-Solomon codes, such a decoding scheme may suffer severe error propagation due to the existence of numerous short cycles in their graph representations. In [1], Jing and Narayanan presented a variant of belief propagation, called the JN algorithm hereinafter, which adaptively tranforms the paritycheck matrix into a special systematic form at each iteration to alleviate the decoding failure because of bit errors in the least reliable positions. Nevertheless, such an algorithm suffers undesired performance degradation due to unremovable errors in the most reliable positions. In [2], a hybrid scheme which combines the above adaptive belief propagation with the traditional reliability-based decoding, e.g., the order statistic decoding (OSD) [3], capable of correcting errors in the most reliable positions was proposed for performance improvement. Verified by simulation results, the number of unremovable errors in both of the most and least reliable positions can be decreased owing to the iterative processing between the OSD-part and JN-part.
However, soft-information is passed only from the JNpart to the OSD-part in the hybird scheme. Since the OSD algorithm generates only hard-decision outputs, no further exchange of soft-outputs to the JN-part is available, hence limiting the attainable performance gain. In this study, several low-complexity schemes are presented for the OSD algorithm to produce desirable soft-outputs such that decoded results can then be bilaterally exchanged between the both parts to enhance the overall decoding performance.
Manuscript received December 4, 2008. The associate editor coordinating the review of this letter and approving it for publication was C. Charalambous.
The authors are with the Department of Communication Engineering, National Chiao Tung University, Hsinchu 30010, Taiwan (e-mail: chwang@mail.nctu.edu.tw).
Digital Object Identifier 10.1109/LCOMM.2009.082059

The rest of this paper is organized as follows. In Section II, we give a brief review of the OSD, JN, and hybird decoding schemes. The proposed schemes for bilateral exchange of softinformation between the OSD and JN algorithms are presented in Section III. Simulation results for performance verification are given in Section IV. Finally, Section V concludes this work.

II. REVIEW OF CONVENTIONAL RELIABILITY-BASED DECODING SCHEMES

Consider a binary (n, k, dmin) linear block code C with a generator matrix G and a parity check matrix H. Let c = (c0, c1, · · · , cn−1) be a codeword corresponding to the information vector u = (u0, u1, · · · , uk−1), i.e., c = uG. Denote by x = (x0, x1, · · · , xn−1) the modulated symbols after the binary phase-shift keying (BPSK) modulation, where
xi = +1 if ci = 0 and xi = −1 if ci = 1, ∀ 0 ≤ i < n. Suppose x is transmitted over an additive white Gaussian noise (AWGN) channel. The received vector y = (y0, y1, · · · , yn−1) is obtained by yi = xi + ni, ∀ 0 ≤ i < n, where ni is the Gaussian noise with zero mean and variance N0/2. The reliability of each coded bit is then initialized by the log-
likelihood ratio based on the associated channel output

L(ci) = ln

Pr{ci Pr{ci

= 0|yi} = 1|yi}

=

4 N0

yi,

∀

0≤

i < n.

(1)

A. The OSD Algorithm

The OSD algorithm [3] of order-w, denoted by OSD(w) ∀ 1 ≤ w ≤ k, first sorts |L(ci)|’s in a decreasing order. The first k most reliable independent positions (MRIP) i0, i1, · · · , ik−1 in the received vector are chosen, based on which G is transformed into a systematic form G by proper
row operations such that columns associated with the MRIP form a k × k permutation matrix∗. With respect to G , define
the hard-decision estimate of effective information vector uˆ = (uˆ0, uˆ1, · · · , uˆk−1) corresponding to y by

uˆl =

0, 1,

if L(cil ) ≥ 0 otherwise

, ∀ 0 ≤ l < k.

Consider all error patterns e = (e0, e1, · · · , ek−1) of Hamming weight at most w. For each e, a new information estimate
ue = uˆ + e is obtained and the corresponding codeword ce = ueG is constructed. Let xe = (xe,0, xe,1, · · · , xe,n−1) be the modulated sequence of ce. For each ce, its correlation discrepancy with respect to y is calculated by

λ(ce, y) =

|yi|

i∈Ω(ce ,y)

where Ω(ce, y) = {i|∀ 0 ≤ i < n, yixe,i < 0}. The ce∗

with the minimum correlation discrepancy is then chosen

∗A matrix is called a k × k permutation matrix if and only if it can be obtained from the k × k identity matrix with some column permutation.

1089-7798/09$25.00 c 2009 IEEE Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:15:54 UTC from IEEE Xplore. Restrictions apply.

WANG et al.: BILATERAL EXCHANGE OF SOFT-INFORMATION FOR ITERATIVE RELIABILITY-BASED DECODING . . .

683

as the decoded codeword. Moreover, define Ω¯ (ce∗ , y) as {0, 1, · · · , n − 1}\Ω(ce∗ , y). Let Θ(ce∗ , y) consist of the first dmin − |Ω(ce∗ , y)| least reliable positions in Ω¯ (ce∗, y) if dmin > |Ω(ce∗ , y)|; otherwise, let Θ(ce∗ , y) be the empty
set. The decoded codeword will also maximize the likelihood

if it satisfies

λ(ce∗ , y) ≤

|yi|.

(2)

i∈Θ(ce∗ ,y)

B. The JN Decoding Algorithm

Let L(l) = (L(l)(c0), L(l)(c1), · · · , L(l)(cn−1)) be the reliability estimates of coded bits at the lth iteration; L(0)(ci) is initialized as L(ci) in (1), ∀ 0 ≤ i < n. During each iteration |L(l)(ci)|’s are first sorted in an increasing order. Based on the first n − k least reliable independent positions (LRIP) i0, i1, · · · , in−k−1 in L(l), H is transformed into a systematic form H(l) by proper row operations such that columns associated

with the LRIP form an (n − k) × (n − k) permutation matrix.

Denote by h(jl,i) the (j, i)-entry in H(l), ∀ 0 ≤ j < n − k,

0 ≤ i < n. The extrinsic information of ci is calculated based

on H(l) by the sum-product ⎛algorithm [4] as shown b⎞elow:

Le(lx)t(ci) =

2 tanh−1 ⎝

tanh

j∈Φi

m∈Ψj,i

L(l)(cm) ⎠ 2

∀ 0 ≤ i < n, where Φi = {j|∀ 0 ≤ j < n − k, hj(l,i) = 1} and Ψj,i = {m|∀ 0 ≤ m < n, m = i, hj(l,m ) = 1}. The reliability estimates are then updated by

L(l+1)(ci) = L(l)(ci) + αLe(lx)t(ci), ∀ 0 ≤ i < n
where α denotes the damping coefficient with 0 < α ≤ 1. The above process is continued until the hard-decision estimate of codeword based on L(l) satisfies all parity-check equations or until the maximum number of iterations is reached.

C. The OSD-JN Decoding Algorithm
The OSD-JN algorithm [2] is an iterative decoding scheme with a direct combination of the original OSD and JN algorithms which aims at error correction in both of the most and least reliable positions. Let NJN be the maximum number of inner iterations for the JN-part and NOSD-JN be the maximum number of outer iterations for the hybrid decoding. The OSDJN algorithm proceeds as follows.
Step 1. Set l = 0 and initialize L(0)(ci) by the likelihood ratio in (1), ∀ 0 ≤ i < n.
Step 2. Conduct OSD(w) based on L(l) to generate a decoded codeword cˆ. If cˆ satisfies (2) or l = N , OSD-JN then declare cˆ as the final result and the algorithm stops; otherwise, go to the next step.
Step 3. Based on L(l), conduct the JN algorithm with at most NJN iterations and obtain a new update of reliabiliy estimates L(l+1). Increase l by 1 and go back to Step 2.

gains no additional improvement, since the OSD algorithm generates only hard-decision outputs and no further exchange of soft-information to the JN-part is available. However, besides hard-outputs, we note that many candidate codewords are generated during the execution of the OSD algorithm. Suppose those candidates can be properly utilized to produce soft-outputs; without extra search efforts, the resulting softinformation can then be passed from the OSD-part to the JNpart to enhance the overall decoding performance.
Recall the notations described in Section II. Also, denote by C0,i and C1,i the sets consisting of all candidate codewords whose ith coded bit is of binary value 0 and 1, respectively, ∀ 0 ≤ i < n. To generate the desirable soft-outputs for the OSD algorithm, three schemes with various trade-offs between the complexity and performance are proposed as follows.

Scheme 1 for Updating L(l):

Pre-determine a reliability threshold Δ with Δ > 0. In this scheme, L(l) is updated based on all possible candidate

codewords
L(l) (ci

by⎧
⎨⎪⎪⎪⎪⎪⎪ ) =⎪⎪⎩⎪⎪⎪⎪

L(l) L(l)

(ci (ci

) )

+ if −

min ln
c∈C0,i
min ln

c∈C0,i Pr{c|y} c∈C1,i Pr{c|y}

,Δ

,

Pr{cc∈∈cCC|y10},,ii>PPrr{{ccc∈||yyC}}1,i,PΔr{c,|y}

(3)

otherwise

∀ 0 ≤ i < n. The resulting L(l) is then fed to the JN algorithm

for further processing.

Scheme 2 for Updating L(l):

For transmission over AWGN channels with BPSK modu-

lation, we have

2

Pr{c|y} = Pr{x|y} ∝ exp

x·y

N0

where x · y denotes the inner product of x and y. Let cm,i

be the codeword in Cm,i which has the greatest a posteriori probability and denote by xm,i the corresponding modulated sequence, ∀ m = 0, 1, 0 ≤ i < n. With the approximation of

ln i exp(Ai) maxi Ai for real numbers Ai’s, (3) can be further simplified as the following to reduce the computation

complexity⎧:
⎪⎪⎨L(l)(ci) + min L(l)(ci) = ⎪⎩⎪L(l)(ci) − min

2 N0

|(x0,i

−

x1,i)

· y| , Δ

,

if Pr{c0,i|y} > Pr{c1,i|y} (4)

2 N0

|(x0,i

−

x1,i)

· y| , Δ

, otherwise

Scheme 3 for Updating L(l): Suppose only the cˆ (= (cˆ0, cˆ1, · · · , cˆn−1)) with the mini-
mum discrepancy is considered for computation in (3); the
soft-outputs can be obtained with a great amount of complexity reduction. By such an approximation, L(l)(ci)’s just need to be updated in the following simple way:

L(l)(ci) =

L(l)(ci) + Δ, L(l)(ci) − Δ,

if cˆi = 0 if cˆi = 1

, ∀ 0 ≤ i < n.

(5)

III. BILATERAL EXCHANGE OF SOFT-INFORMATIOM FOR

IV. SIMULATION RESULTS

THE OSD-JN ALGORITHM

In this section, the (31, 25), (63, 55), and (127, 121) Reed-

In the original OSD-JN algorithm, the OSD-part is observed Solomon (RS) codes are simulated for performance verifica-

to achieve better decoding performance than the pure OSD tion. The nonbinary generator and parity-check matrices are

algorithm owing to the extra supplement of soft-information first transformed into the equivalent binary image expansions

from the JN algorithm. On the other hand, the JN algorithm based on the concept of subfield subcodes [5], and coded

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:15:54 UTC from IEEE Xplore. Restrictions apply.

684

IEEE COMMUNICATIONS LETTERS, VOL. 13, NO. 9, SEPTEMBER 2009

Fig. 1. BER curves of various decoding schemes for the (31, 25) RS code with α=0.05, w=1, Δ=2, NOSD-JN=20.

Fig. 3. BER curves of various decoding schemes for the (127, 121) RS code with α=0.05, w=1, Δ=2, NOSD-JN=20.

Fig. 2. BER curves of various decoding schemes for the (63, 55) RS code with α=0.05, w=1, Δ=2, NOSD-JN=20.

Fig. 4. Average number of erroneous bits after each decoding iteration for
the (31, 25) RS code by the original OSD(1)-JN and proposed algorithms with NJN = 1 and Scheme 3 for reliability updating at SNR 4.5 dB.

bits are transmitted over an AWGN channel with BPSK

modulation. Figs. 1, 2, and 3 show that our design with

even the simplest Scheme 3 for reliability updating in Section the proposed idea of bilateral exchange of soft-information

III achieves at least 0.4 dB, 0.3 dB, and 0.15 dB signal-to- and reliability updating schemes are also applicable to other

noise ratio (SNR) gains over the original OSD-JN algorithm reliability based decoding algorithms in [6] for further perforfor the three codes, respectively. (The α and Δ used for mance enhancement.

simulation are chosen to optimize the decoding performances

by exhaustive search.) From Figs. 1, 2, and 3, we also observe that the JN-part with more inner iterations, i.e., NJN > 1, provides almost no extra performance gain during the iterative process. Owing to the bilateral exchange of soft-information, the proposed scheme can decrease the number of error bits rapidly in the first several iterations and hence experiences a faster convergence of decoding than the original OSD-JN algorithm as revealed in Fig. 4.
V. CONCLUSIONS
In this study, the original OSD-JN algorithm is modified with a bilateral exchange of soft-information between the OSD-part and JN-part to improve the decoding performance for linear block codes. Without extra search efforts, lowcomplexity schemes are presented to produce desirable soft-

REFERENCES
[1] J. Jiang and K. R. Narayanan, “Iterative soft decision decoding of ReedSolomon codes based on adaptive parity check matrices,” in Proc. 2004 IEEE Int. Symp. Inform. Theory, Chicago, IL, June 2004, p. 261.
[2] A. Kothiyal, O. Y. Takeshita, W. Jin, and M. P. C. Fossorier, “Iterative reliability-based decoding of linear block codes with adaptive belief propagation,” IEEE Commun. Lett., vol. 9, pp. 1067-1069, Dec. 2005.
[3] M. P. C. Fossorier and S. Lin, “Soft-decision decoding of linear block codes based on order statistics,” IEEE Trans. Inform. Theory, vol. 41, pp. 1379-1396, Sept. 1995.
[4] J. Hagenauer, E. Offer, and L. Papke, “Iterative decoding of binary block and convolutional codes,” IEEE Trans. Inform. Theory, vol. 42, pp. 425-449, Mar. 1996.
[5] A. Vardy and Y. Be´ery, “Bit-level soft-decision decoding of ReedSolomon codes,” IEEE Trans. Commun., vol. 39, pp. 93-111, Jan./Feb. 1989.
[6] W. Jin and M. P. C. Fossorier, “Reliability-based soft-decision decoding with multiple biases,” IEEE Trans. Inform. Theory, vol. 53, pp. 105-120, Jan. 2007.

outputs based on only the candidate codewords generated

during the execution of OSD. Besides the OSD algorithm,

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 24,2026 at 07:15:54 UTC from IEEE Xplore. Restrictions apply.

