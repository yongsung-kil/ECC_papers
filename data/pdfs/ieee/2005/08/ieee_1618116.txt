2005 IEEE International Symposium on Microwave, Antenna, Propagation and EMC Technologes for Wireless Communications Proceedings

Fast Decoding Algorithm for Low-Density Parity-Check Codes

WANG Dan*, LI Ping, HU Xiao Yu and WANG Xin Mei State Key Lab of ISN, Xidian University, Xi'an, Shaanxi, 710071, China
E-mail: wangdan39Asohu.com

A fast decoding algorithm for low-density parity-check codes is presented based on graph decomposition and two-way message passing schedule. Simulations show that the convergence speed of the proposed algorithm is about twice as fast as that of the conventional belief propagation algorithm.
Introdcdtion: The belief propagation algorithm (BPA) is considered to be a standard decoding technique for low-density parity-check (LDPC) codes [1-4]. The conventional BPA is based on the flooding schedule [3] (we will refer to this type of BPA as the flood algorithm (FA) for convenience in this paper), in which all the variable nodes and, successively, all the check nodes pass new messages to their neighbors in a decoding iteration. In this algorithm, the messages from each node only affect the state of its immediate neighbors, which can lead to a slow convergence speed when serial processors are involved.
In order to improve the convergence speed, a turbo-type two-way algorithm (TTWA) is presented here for decoding a general LDPC code. This new algorithm is developed from a two-way schedule technique for local decoding combined with a turbo mechanism for global decoding. A pre-processing stage is involved to decompose the general LDPC code into several tree-structured component codes so

that the LDPC code can then be viewed as a concatenated tree (CT) code [5], TTWA can then be used to accomplish the decoding task. The proposed method is particularly suited to the numerical evaluation of LDPC codes since modem computers are still mainly based on seral processors.
Two-Way Schedule: This technique is suitable for decoding a tree-structured code (tree code), that is, a code whose Tanner graph contains no loops. In this schedule, messages are only passed once in each direction along each edge in the tree during the whole decoding process to achieve optimal performance [3]. The two-way schedule is more efficient than the flooding schedule when decoding a tree code using a serial processor (the detailed discussions can be found in [3]). However it cannot be directly applied to a code containing loops.
CT codes and Turbo-7jpe decoder: CT codes are
formed from the parallel concatenation of several tree codes [5], and can be decoded by a serial Turbo-type algorithm. Suppose that the number of local decoders is M and each local decoder is responsible for one component tree code. The main decoding process is:
1) At the first iteration, an initial a priori LLR
vector for code bits is fed into the first local decoder. 2) The local decoders run successively on the turbo principle [5]. LetL(m) denote the a posteriori

0-7803-9128-4/05/$20.00 © 2005 IEEE.

1115

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:19:42 UTC from IEEE Xplore. Restrictions apply.

LLR vector of the mth component code and
E ra-rtin the extrinsic LLR vector generated
in the last iteration. At the first iteration,
E(l-) is initialized to 0 for all m.
WithLJr) = LMt'- E15m=-,,C,I as its input, the mth
local decoder performs a posteriori probability decoding and then outputs L(m) for the next decoder. Finally, the Mth (last) local decoder feeds L(X) into the 1st (first) local decoder for the next iteration. 3) The process iterates until some termination condition is satisfied.

without any check nodes). Next, we test each check
node ck k c {L2,---,M} one by one. If adding ck into T, results in loops, it will not be acceptable;
otherwise, we can add Ckinto T1. After testing all the
check nodes, the process of constructing T, is
completed. In the same way, we construct T from
the check nodes that were not added to T,. The
process is repeated to build sub trees T3,T4 * until
no check nodes are left. The above decomposing
process is illustrated by Fig. 1, where Fig. l.a is a Tanner graph with loops while Figs.l.b and l.c give its two sub trees. After the decomposition, the LDPC
code can be viewed as a CT code, and so the TTWA can be used for decoding.

With this algorithm, CT codes combine the advantages of LDPC codes and Turbo codes and can achieve good performance with low decoding cost. This mechanism has been used in decoding concatenation tree codes in [5]. In the following, we will show that it can also be applied to general LDPC
codes to improve convergence speed.
Twrbo-13pe Two-Way Algorithm (TJVA) and Graph Decomposition: TTWA, as the name indicates, uses the turbo mechanism in the global decoder and the two-way schedule in the local decoders. If properly used, this algorithm can provide fast
convergence when a serial processor is involved. Before it can be applied to a general LDPC code, we need to decompose the code into several trees by graph decomposition. Many methods can be employed for this purpose. One approach is as follows. Given a Tanner graph T with variable
nodes vl,v2, ,v, and check nodes cl,c2, ,cM (N
and M are the number of variable nodes and check nodes respectively), we initially construct the first sub tree T, by taking all ofthe variable nodes (and

VI

CO

O

v V3 V4 (3 V5 V60 -

C1 V2

C2 V3

V4

C4 V5

C4

6-

a

b

vO
v
v \0

V3

C

V4 0-

,

V6
c

Fig.I Process of decomposmg a Tanner graph containing loops into a group oftrees
a Tanner graph of LDPC code b Tree-structuredgraphofcomponentcode-l c Tree-structured graph of component code-2

1116
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:19:42 UTC from IEEE Xplore. Restrictions apply.

Complexity: The proposed TIWA and the

conventional FA involve almost the same number of

operations at each iteration, i.e. about 6NA

multiplications and 5NA additions each iteration,

1D-2

where A is the average variable node degree.

NFA=Z2
N2

Numerical Resuflt: Fig.2 and Fig.3 show the X 10

performance of two irregular LDPC codes (code

NW=16.32

length=1008 and 5000 respectively) decoded using the

14

NFA=32

TTWA and FA methods. In the graphs, solid lines mark

- TTWA

the TTWA results and dashed lines the FAperformance;

\--FAA

the iteration number can also be read from the figures.

1 -1

1.5

2

25

3

The LDPC codes used in the simulation are constructed

Eb/NO,d

by the PEG algorithm [6] with the following degree

distributions:

Fig.2 Comparison of convergence speed of TTWA
and FA (Code length =1008, Rate=1/2)

A(x) = 0.238687x1 + 0.209597x2 +

0.035803x3 +0.135505x4 + 0.380408x4

10'

---------

--

~~~~~~~NFA4

p(x) = 0.017401X6 + 0.982599x7.

L
0'2-

4
NIWA= 4

Evidently, the TTWA converges faster than the FA. This is due to the fact that the ITWA can achieve locally optimal solution for a component tree code within one iteration (this property does not hold for the FA). Experimentally, we observed that different decompositions of an LDPC code make little difference to tie convergence speed and performance. We also observed that FA requires about twice the
number of iterations to achieve tie same BER as
TTWA.
Conclusion: We have presented a TTWA algorithm for decoding general LDPC codes using a serial processor. Simulation results show that the new algorithm converges twice as fast as the conventional FA algorithm with almost the same computational complexity and can achieve the nearly same decoding performance. Thus it can explore f advantage of the Turbo mechanism and two-way
schedule.

NT 8

a

NFA 16 -3

i

TTWA

- - - -FA

o
05

NFnA=
Eb/NO d

32
A=16
32
1.\5
1

Fig.3 Comparison of convergence speed of TTWA and FA (Code length =5000, Rate=1/2)

Acknowledgements: This work was supported by
CERG under grand N_CityU N_101/01 and NSFC
under Grant 60131160742

IR1eEGfaeTlrleaangsceer,s nR.G.:h'oLyow-d9en2sityop.aIri-ty,-pch.ec2k1c2odes',
IErrs n:Ter,16,o.I-,p 12

1117
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:19:42 UTC from IEEE Xplore. Restrictions apply.

2 Mackay, D.J.C., and Neal, RI: 'Near Shannon limit performance of low density parity check codes', Electron. Lett., 1996, Vol. 32, pp. 1645-1646 3 Kschischang, F.R., and Frey, B.J.: 'Iterative decoding of compound codes by probability propagation in graphical models', IEEE J. Selected Areas Comnun., 1998, Vol. 16, pp. 219-230 4 Kschischang, F.R., Frey, B.J., and Loeliger, H.A.: "Factor graph and the sum-product algorithm', IEEE Trans. Inf: Theory, 2001, Vol. 47, pp.498&518 5 LI Ping, and Wu, K.Y: 'Concatenated tree codes: a low complexity, high performance approach', IEEE Trans. Inf: Theory, 2001, Vol.47, pp.791-799 6 Hu, X.Y, Eleffheriou, E., and Amold, D.M: 'Regular and irregular progressive edge-growth
1118
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:19:42 UTC from IEEE Xplore. Restrictions apply.

