A Memory Efficient Partially Parallel Decoder Architecture for QC-LDPC Codes

Zhongfeng Wang and Zhiqiang Cui
School of Electrical Engineering and Computer Science Oregon State University, Corvallis, OR 97333, USA {zwang, cuizh}@eecs.oregonstate.edu

Abstract—In this paper, a memory efficient partially parallel decoder architecture suited for high rate Quasi-Cyclic LowDensity Parity-Check codes using (modified) Min-Sum decoding algorithm is proposed. In general, more than thirty percent of memory can be saved over conventional partially parallel decoder architectures. To reduce the computation delay of Check-node Processing Unit, an efficient architecture based on variants of rank order filter is presented. The optimized partially parallel decoder architecture can linearly increase the decoding throughput with small hardware overhead. Consequently, the proposed approach facilitates the applications of LDPC codes in area/power sensitive high speed communication systems.
I. INTRODUCTION
Low-Density Parity-Check (LDPC) codes are linear block codes specified by very sparse parity-check matrix H [1]. LDPC codes have attracted considerable attention due to their near Shannon limit performance and inherently parallelizable decoding scheme. Quasi-Cyclic LDPC (QCLDPC) codes are well suited for hardware implementation because of the regularity in their parity check matrices. Recently, several classes of QC-LDPC codes [2-5] have been proposed that can achieve comparable performance with equivalent random LDPC codes. Among various LDPC codes decoding algorithms, the Sum Product (SP) algorithm has the best decoding performance. The modified Min-Sum algorithm [6], which doesn't require any knowledge about the channel parameters and offers comparable decoding performance to SP algorithm, is preferred in low complexity hardware implementation.
In general, LDPC codes achieve outstanding performance only with large code word lengths (e.g., N t 2000 bits). Thus, the memory part normally dominates the overall hardware of a LDPC codec. A memory efficient serial decoder was presented in [7]. The decoding throughput is less than 5.5Mbps per tile. Partially parallel decoder architectures, which can achieve a good trade-off between hardware complexity and decoding throughput, are preferred in practice. In this paper, a memory efficient partially parallel decoder architecture for high rate QC-LDPC codes is proposed, which exploits the data redundancy of soft messages in the Min-Sum decoding algorithm. Typically, over 30% memory can be reduced.

In this paper, a rearranged Min-Sum LDPC decoding procedure and the associated partially parallel decoder architecture are proposed to reduce the required memory for storing the extrinsic soft messages. To reduce the complexity of Check-node Processing Unit (CPU), an optimized Pseudo Rank Order Filter (PROF) is proposed. A low complexity data scheduling structure is developed to enable parallel processing. The required memory can be further reduced by replacing the dual-port memory with single-port memory. In this case, the simultaneous memory read and write operations are performed at different memory segments while employing memory partitioning and data arbitration techniques [10].
The structure of this paper is as follows. In Section II, The rearranged Min-Sum decoding procedure is discussed. Section III presents the partially parallel decoder architecture. Various optimizations to further reduce the hardware complexity are addressed in Section IV. The conclusions are drawn in Section V.

II. REFORMULATED MODIFIED MIN-SUM DECODING ALGORITHM
In the modified Min-Sum decoding algorithm, two classes of computation units, the variables nodes and the check nodes, iteratively exchange soft messages with each other through the edges in the Tanner graph. In the check-tovariable message updating phase, each check node c computes the check-to-variable messages Rcv with received variable-to-check messages Lcv as the following:

R (k ) cv

Du



sgn(

L(k 1) cn

)

u

min

|

L(k 1) cn

|,

(1)

nN (c)\v

nN (c)\v

where D is a scaling factor. The superscript (k) is used to indicate that the data is generated in the k-th iteration.

In the variable-to-check message updating phase, each variable node v computes the variable-to-check messages Lcv using the check-to-variable messages Rcv and the intrinsic messages Iv with the following equation.

L(k ) cv

¦

R (k) mv



R (k) cv



Iv.

(2)

mM (v )

1­4244­0132­1/05/$20.00 ©2005 IEEE

729

Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:52:30 UTC from IEEE Xplore. Restrictions apply.

In the conventional decoding procedure, two classes of extrinsic messages in the individual form are needed. On the other hand, it can be observed from (1) that the magnitudes of the check-to-variable messages corresponding to one row of H matrix have only two possible values. By extending the idea presented in [11], a rearranged decoding procedure of the Min-Sum algorithm is developed to reduce the memory requirement for extrinsic messages. The computation of
variable-to-check messages Lcv shown in (2) is completed within two decoding phases. The new decoding procedure is expressed as the following.

L(ckn)

S

(k n



1)



R

(k cn

1)



In

(3)

Rc(vk ) D u  sgn( L(ckn) ) u min | L(ckn) | (4)

nN (c)\v

nN (c)\v

S

( v

k

)

¦

R

(k mv

)

(5)

m M (v)

In the check-to-variable message updating phase, the variable-to-check messages Lcv are not directly retrieved from memory. Instead, they are computed using (3) and transferred to CPU immediately. In the variable-to-check message updating phase, Sv is stored in memory after (5) is performed. Based on the rearranged decoding procedure, optimized low complexity decoding architecture is proposed in Section III.

III. THE MEMORY EFFICIENT DECODER ARCHITECTURER
In this design, the check-to-variable messages and variable-to-check messages are stored in separated memories. At first glance, the architecture is not memory efficient. However, optimizations can be applied to achieve a memory efficient architecture. First, all check-to-variable messages Rcv corresponding to one row in H matrix can be stored in a compressed form to significantly reduce memory requirement. Second, the variable-to-check messages are not stored individually. Instead, only the summation of check-tovariable messages computed from (5) are stored.
A. The Decoder Architecture for H Matrix Consisting of Circulant Permutation Matrices
The block diagram of the proposed architecture for circulant permutation matrices based LDPC codes is shown in Fig. 1. The R-memory module is used to store the checkto-variable messages. The messages corresponding to one row of H matrix are stored into one entry of R-memory in compressed form, which is composed of four elements, i.e., 1) the smallest magnitude, 2) the second smallest magnitude, 3) the index of the second smallest magnitude, and 4) the signs of all messages. To recover the individual check-to-variable messages from their compressed form, the data distributor is introduced as shown in Fig. 2. Each S-memory and I-memory module is used to respectively store the summation of check-to-variable messages and the

Figure 1. The memory efficient decoder architecture.

intrinsic messages corresponding to a block column of the H matrix.

The CPU performs the computation expressed in (4) in the check-to-variable message updating phase. The Variablenode Processing Unit (VPU) works in the both decoding phases. It performs the computation listed in (3) in the check-to-variable message updating phase while performs (5) in the variable-to-check message updating phases. The summation of check-to-variable messages in one column of H matrix is serially accumulated. The decision and parity equation check unit is introduced for tentative decision and parity check computation.

Assuming that the H matrix is a J u K block matrix and

each submatrix is a circulant permutation matrix with a size

of t u t , the message updating procedure in the kth iteration

is as follows. In check-to-variable message updating phase,

for each row c , three kind of messages

Rc(vk 1) ,

S

(k v

1)

and

Iv , where v  N (c) , are sent to the v-th VPU via the path

indicated by the dashed line to compute L(ckv) . Then, all of

the intermediate results calculated by the K VPUs are sent to

CPU. Finally, the compressed check-to-variable messages

for the K block columns are generated and stored into one

entry of the R-memory. In the variable-to-check message

updating phase, the summation of check-to-variables for

each column of H matrix is accumulated. For each row c, the

compressed check-to-variable messages are read out from

R-memory module. Next, the individual messages Rc(vk)

recovered by the data distributor are sent to K VPUs to

accumulate

S

(k v

)

via the path indicated by the solid line. At

the end of this phase, the final summations of check-to-

variable messages corresponding to all columns of the H

matrix are accumulated in the K S-memory modules.

Fig. 2 shows the structure of data distributor which is introduced to convert the check-to-variable messages corresponding to a row of H matrix into their true values. The data read from an R-memory entry is composed of four segments as mentioned before. On the output side, the

730
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:52:30 UTC from IEEE Xplore. Restrictions apply.

more memory efficient compared to other overlapped decoder architectures.

Figure 2. The structure of data distributor.
relative location of m2 is determined by the index decoder. m1 is distributed to other locations. Because the order of the sign bits in signs is the same as that of the check-to-variable messages corresponding to a row of H matrix, the sign bits can be simply distributed. It is convenient to use two's complement data representation in variable-to-check message computation. Therefore, the sign-magnitude to two's-complement conversion units, denoted as SM-2'S, are employed to perform the data format conversion.
For a quantitative comparison of the needed memory between the proposed architecture and the conventional designs [8-9], let us consider a (4608, 4096) (4, 36) regular circulant permutation matrices based LDPC code. In the conventional designs, the two classes of messages in their true values are alternately stored into the common extrinsic memory modules. Assuming that 6-bit quantization is used, the required memories are summarized in the Table I and II, respectively. It can be seen that nearly 37% of memory is reduced in this case.

TABLE I.

MEMORY NEEDED BY THE PROPOSED ARCHITECTURE

Memory Component The R-memory modules (DP) The S-memory modules (DP) The I-memory modules (SP)
The decoded bits (DP)

Memory Size (bits) 128x4x2x(5+5+36+6)=53,248
128x36x2x(6+2)=73,728 128x36x1x6=27,648 128x36x2x1=9,216 Total=163,840

B. The Partially Parallel Decoder Architecture
The proposed partially parallel decoder architecture, which linearly increases the decoding throughput with small hardware overhead, is shown in Fig 3. To facilitate the multiple data accesses per clock cycle in a p -parallel
architecture, the data corresponding to p adjacent rows or
columns of H matrix are stored into one memory entry. In the R-memory modules, check-to-variable messages corresponding to p adjacent rows are stored in one memory
entry. Similarly, in one entry of S-memory (I-memory) modules, summations of the check-to-variable message (intrinsic messages) corresponding to p adjacent columns
are stored. In Fig. 4, for clarity, each adder or multiplexer symbol is used to represent p components for parallel processing case. Because the data stored in the R-memory are in the row order while the data stored in the S-memory and I-memory are in the column order, data scheduling and reverse scheduling units are needed for resolving the data access conflicts. In general, they can be implemented using barrel shifter. An optimized data scheduling (reverse scheduling) unit is proposed in the Section IV.B. Because one data distributor can only distribute the compressed check-to-variable messages onto the position corresponding to one row of H matrix, p data distributors are needed.
IV. OPTIMIZATION ON THE PARTIALLY PARALLEL DECODER ARCHITECTURE
A. The Optimized CPU
The critical task of CPU is to find the two smallest magnitudes from all input data and identify the relative position of the input data with the smallest magnitude. In this section, an optimized 2 u 2 Pseudo Rank Order Filter (PROF) is proposed. Then, an efficient CPU based on the PROF is presented to minimize the hardware complexity. The 2 u 2 PROF sorts two presorted sequence and indicates which sequence containing the smallest data with respect to

TABLE II.

MEMORY NEEDED BY THE TRADITIONAL DESIGNS

Memory Component
The extrinsic memory modules (DP)
The intrinsic memory modules (SP)
The decoded bits (DP)

Memory Size (bits) 128x36x4x2x6=221,184
128x36x1x1x6=27,648 128x36x2x1=9,216 Total=258,048

It should be pointed out that the throughput of the proposed architecture can be significantly increased through overlapping the two decoding phases. In this case, the hardware complexity will be increased. However, it is still

Figure 3. The Partially parallel decoder architecture.

731
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:52:30 UTC from IEEE Xplore. Restrictions apply.

their magnitudes. Only the smallest and the second smallest magnitude are sent out. Fig. 4 shows the architecture of the PROF. Input [a2 a1] and [b2 b1] are two presorted sequences such that a2 t a1 and b2 t b1 . All the elements in the two input sequences are non-negative number. m1 and m2 stand for the smallest and the second smallest input data, respectively. index is used to indicate which sequence contains the smallest input data. Its value is 0 if the smallest input is in the sequence [a2 a1], otherwise, it is 1. It can be observed that the smallest magnitude must be either a1 or b1. The second smallest magnitude can be selected from a2, b2, and the intermediate comparison result of a1 and b1. Therefore, only one stage compare-and-swap unit plus a very simple combinational logic is used to perform the task of the PROF.
The architecture of a CPU with eight inputs is shown in Fig. 5. Input data are variable-to-check messages and represented in sign-magnitude format. On the output side, the scaled smallest magnitude and second smallest magnitude, the relative position of the second smallest magnitude, and the signs of all data computed in (4) are denoted as m1, m2, index, and signs, respectively. In Fig. 5, the part above the dash-dotted line performs data sorting task. The compareand-swap unit is used to compare two input data. If the larger magnitude is in the upper position, the comparison result indicated by the dashed line is 1, otherwise, it is 0. In the same time, the larger magnitude is placed at upper output position. In the optimized CPU, the bits of index are aggregated stage by stage. To illustrate the aggregation procedure, let we assume that the input data with the smallest magnitude has an index of “101”. In the first stage, the comparison result of the third compare-and-swap unit (from top to bottom) is 1. In the second stage, the index bit of the second PROF is 0. In the same time, it is used to select the index bit generated by the previous stages. Therefore, the aggregation result after the second stage is “10”. In the final stage, the index bit of the PROF is 1. This bit is used to select the “10” generated from the previous stages. In this way, the final value of index is “101”. The part below the dash-dotted line is for computing the sign bit of each check-to-variable
Figure 4. The structure of pseudo rank order filter.

Figure 5. The architecture of the optimized CPU.
message as in the conventional design. The relative position of each sign is not changed when they are grouped together.
B. The Optimized Data Scheduling (Reverse Scheduling) Unit
The data scheduling (reverse scheduling) unit plays the key role in the partially parallel processing architecture. In [12], a component performing a similar task was implemented with a combination of data concatenation unit and cyclic shifter, which significantly increases the hardware cost and computation latency. In this paper, an efficient data scheduling unit is proposed by exploiting the structure of LDPC codes. For QC-LDPC codes, the number of non-zero matrices in one block column of H matrix is very limited, typically around four. It implies that the number of data cyclic shifting patterns in a block column is limited. Hence, a very simple switching block can be employed for resolving the data access conflicts for each circulant matrix. If there are W non-zero matrices in a block column of H matrix, W switching blocks are needed for the block column. Thus, a W:1 multiplexer is used to select a switching block for a circulant permutation matrix.
Fig. 6 shows a scheduling unit for 4 parallel processing targeting at a circulant permutation matrix (shifting offset 7, dimension (4k  1) u (4k  1) , where k is an arbitrary
integer). For the considered matrix size and parallelism level, each S-memory module has k+1 entries. When assessing the messages corresponding to the considered circulant matrix, the sel signal only changes three times, i.e., sel = “01” for the last entry, sel = “11” for the second last entry and sel = “00” for all of other entries. The scheduling unit introduces one clock delay from input to output. The data reverse scheduling unit can be designed in a similar way. In the proposed decoder architecture, the two message updating phases are not overlapped. Due to the similarity in the structure of data scheduling unit and reverse scheduling unit, the two components can be combined into one unit. It works alternately in the two message updating phases.

732
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:52:30 UTC from IEEE Xplore. Restrictions apply.

Figure 6. The structure of the data scheduling unit.
V. CONCLUSION
In this paper, a memory efficient partially parallel decoder architecture suited for Min-Sum decoding algorithm for QC-LDPC codes is proposed. By rearranging the decoding procedure of the Min-Sum algorithm and exploiting the data redundancy of extrinsic messages, generally over 30% memory can be reduced compared to the traditional designs. To minimize the computation delay, a low complexity CPU, which is based on PROF proposed in the paper, is developed. The approach discussed in this paper facilitates the applications of high variable degree and/or high rate LDPC codes in area/power sensitive high speed communication systems.
REFERENCES
[1] R. G. Gallager, “Low-density parity-check codes,” IRE Transactions on Information Theory, vol. IT-8, pp. 21-28, Jan. 1962.
[2] M. P. C. Fossorier, “Quasi-cyclic low-density parity-check codes from circulant permutation matrices,” IEEE Trans. on Info. Theory, vol. 50, pp. 1788 – 1793, Aug. 2004.
[3] Z. Li and B. V. K. V. Kumar, “A class of good quasi-cyclic lowdensity parity check codes based on progressive edge growth graph,” Thirty-Eighth Asilomar Conference on Signals, Systems and Computers, vol. 2, pp. 1990-1994, 2004.
[4] L. Chen, J. Xu, I. Djurdjevic and S. Lin, “Near-Shannon-Limit QuasiCyclic Low-Density Parity-Check Codes,” IEEE Transactions on Communications, vol. 52, pp. 1038-1042, Jul. 2004.
[5] “LDPC coding for OFDMA PHY,” http: /www. ieee802.org/16/tge/ [6] J. Chen and M. Fossorier, ``Near optimum universal belief
propagation based decoding of low-density parity check codes,” IEEE Trans. Commun., vol. 50, pp. 406-414, Mar. 2002. [7] M. Cocco, J. Dielissen, M. Heijligers, A. Hekstra, J. Huisken, “A scalable architecture for LDPC decoding,” Automation and Test in Europe Conference and Exhibition, vol. 3, pp. 88-93, Feb. 2004. [8] T. Zhang and K. K. Parhi, “Joint (3, k)-Regular LDPC Code and Decoder/Encoder Design,” IEEE Transactions on Signal Processing, vol. 52, pp. 1065-1079, Apr. 2004. [9] Z. Wang, Y. Chen and K. Parhi, “Area-efficient quasi-cyclic LDPC code decoder architecture,” in proc. of ICASSP 2004, vol. 5, pp. 4952, May 2004. [10] Z. Wang and Q. Jia, “Low complexity, high speed decoder architecture for quasi-cyclic LDPC codes,” in proc. of IEEE ISCAS’05, May 2005. [11] Y. Chen and D. Hocevar, “A FPGA and ASIC implementation of rate-1/2, 8088-b irregular low density parity check decoder,” IEEE GLOBECOM '03, vol. 1, pp. 113-117, Dec. 2003.
733
Authorized licensed use limited to: Yonsei Univ. Downloaded on June 23,2026 at 12:52:30 UTC from IEEE Xplore. Restrictions apply.

